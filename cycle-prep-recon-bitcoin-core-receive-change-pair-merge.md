# cycle-prep recon — 2026-07-06 — bitcoin-core-receive-change-pair-merge

**Origin/master SHA at recon time:** `d9063523`
**Local branch:** `master`
**Sync state:** `up-to-date (0 ahead / 0 behind)`
**Untracked:** cycle-prep-recon-*.md banks + design/{SPEC,PLAN,agent-reports}/* from prior cycles; no in-flight work, no open worktrees of ours.

Slug verified: `bitcoin-core-receive-change-pair-merge`. Expectation: citations largely ACCURATE (entry is fresh, filed 2026-07-06 during Cycle A split-out) — but one funds-critical **structural imprecision** in the negative-control fixture reference, plus material omissions (two reject-tests to flip, a wrong manual prose claim) the SPEC must absorb.

---

## Per-slug verification

### `bitcoin-core-receive-change-pair-merge`

- **WHAT (from FOLLOWUPS.md `### bitcoin-core-receive-change-pair-merge`, line 34-37):** Bitcoin Core `listdescriptors` exports receive (`/0/*`, `internal:false`) + change (`/1/*`, `internal:true`) as SEPARATE single-path descriptors; md1 can't hold a single fixed use-site step, so since v0.76.0 a standard Core import HARD-FAILS. Add a pre-pass in `bitcoin_core.rs::parse` that pairs same-key receive/change entries into one `<0;1>/*` multipath descriptor (order by `internal`; actual step values; never across accounts/keys), recompute the BIP-380 checksum, and thread the `internal` metadata through as nullable. Restores standard Core import. Funds-safety-sensitive, cross-repo (paired GUI PR).

- **Citations:**
  - `crates/mnemonic-toolkit/src/wallet_import/bitcoin_core.rs::parse` — pre-pass "before the per-entry loop" — **ACCURATE.** `fn parse` at `bitcoin_core.rs:142`; the per-entry `ParsedImport`-building loop is `bitcoin_core.rs:207-211` (`for (i, entry) in descriptors.iter().enumerate() { out.push(parse_entry(...)) }`). NOTE there are TWO loops in `parse`: a dropped-fields aggregate loop at `:185-197` and the ParsedImport loop at `:207`. The merge pre-pass belongs after `descriptors`/`wallet_name` are extracted (`:163-177`) and before `:207` — most cleanly on the `descriptors` JSON `Vec<Value>` (so `parse_entry` sees the already-merged `<0;1>/*` string).
  - `CoreSourceMetadata.internal: bool → Option<bool>` — **ACCURATE.** `struct CoreSourceMetadata` at `wallet_import/mod.rs:353-364`; `pub(crate) internal: bool` at `:355`. It is `pub(crate)`, not public API. Test builder `sample_core_metadata()` at `mod.rs:534-537` constructs it (ripples).
  - `apply_select_descriptor` receive/change arms — **ACCURATE (symbol + semantics).** `pub(crate) fn apply_select_descriptor` at `wallet_import/mod.rs:394`; the `SelectDescriptor::ActiveReceive` arm filters `active && !internal` and `ActiveChange` filters `active && internal` (documented `mod.rs:383-386`). Flipping `internal` to `Option<bool>` forces these two arms to decide what a merged (`internal:null`) entry means for `active-receive`/`active-change` — a merged `<0;1>` entry IS both chains, so it must satisfy BOTH filters (SPEC decision point, funds-visible).
  - `import_wallet.rs:1859` (`--json` wire site) — **ACCURATE.** Line 1859 = `"internal": meta.internal,` inside the `source_metadata` JSON envelope (`if let Some(meta) = p.source_metadata()` at `:1858`).
  - `import_wallet.rs:2265` (text-summary site) — **ACCURATE.** Line 2265 = `writeln!(stdout, "bundles[{i}].internal={}", m.internal)` (guarded by `if let Some(m) = b.source_metadata()` at `:2263`).
  - "RECOMPUTE the BIP-380 checksum on the merged descriptor" — **ACCURATE (need real; primitive exists but NOT here).** A working `recompute_descriptor_checksum` (via `miniscript::descriptor::checksum::Engine`) exists as a **private per-parser copy** in `wallet_import/descriptor.rs:246` and `wallet_import/electrum.rs:1027`. `bitcoin_core.rs` currently only *validates* (`verify_checksum` in `parse_entry`, `bitcoin_core.rs:254` region) — it has NO recompute helper. SPEC decision: add a third copy in `bitcoin_core.rs` (matches existing per-parser duplication idiom) or factor a shared helper (cleaner; broader blast radius). Merged string must be re-checksummed BEFORE `parse_entry` re-validates it, else the synthesized `<0;1>/*` body fails its own `verify_checksum`.
  - Merge-NEGATIVE-control fixture `core_fixture_file_multipath_receive_change_pair_parses` "(distinct keys, must NOT merge)" — **STRUCTURALLY-IMPRECISE (funds-critical).** That test (`cli_import_wallet_bitcoin_core.rs:952`) consumes `core-multipath-receive-change-pair.json`, whose two entries are **already `<0;1>/*` MULTIPATH** with distinct keys/scripts (`MAINNET_FP_A` wpkh BIP-84 + `MAINNET_FP_B` sh(wpkh) BIP-49) — it asserts the parser does not CONFLATE two distinct already-multipath entries (`bundles=2`). It is NOT the negative control the merge pre-pass actually needs: **two FIXED-single-step entries (`/0/*`+`/1/*`) with DISTINCT keys/origins**, which must NOT silently merge into a wrong `<0;1>` wallet (fund-loss class). That true negative control does **not exist today** → SPEC MUST ADD it. The cited test is a valid *adjacent* guard to keep green, but the FOLLOWUP mislabels it as the merge discriminator.
  - Merge-INPUT fixture `core-mainnet-receive-change-pair.json` (KEPT at v0.76.0) — **ACCURATE.** Present at `tests/fixtures/wallet_import/core-mainnet-receive-change-pair.json`; two entries with IDENTICAL key `[b8688df1/84'/0'/0']xpub6FQya…8mX` differing only in final step (`/0/*` `internal:false` vs `/1/*` `internal:true`). Exactly the positive merge case → `<0;1>/*`.
  - Protocol claim "Core exports SEPARATE non-multipath receive+change; multipath is import-only (bitcoin/bitcoin PR #22838)" — **ACCURATE / CONFIRMED vs primary source.** bitcoin/bitcoin `doc/descriptors.md` (master) + Bitcoin Optech + achow101 0.21-wallets all confirm: a `<0;1>` multipath descriptor is **expanded at import into two separate single-path descriptors stored independently**; `listdescriptors` lists the separately-stored (split) descriptors, so it re-emits `/0/*`+`/1/*`, never a combined `<0;1>`. PR #22838 = the import-only multipath feature. **Caveat (scope, not error):** ongoing Core work trends toward native-multipath storage (doc PR #34100 rewrites descriptors.md to multipath examples); the merge pre-pass is robust either way — an already-`<0;1>` entry never enters the merge (it has no fixed final step) and is handled by the existing multipath parse path (`core_multipath_split_to_receive_change`, `:369`).

- **Action for brainstorm spec:** Cite source SHA `d9063523`. (1) Live line numbers above are all current — use them. (2) **Add the missing distinct-key fixed-step negative control** (`/0/*` under FP_A + `/1/*` under FP_B → must NOT merge; the funds-critical discriminator) as the first oracle; keep `core_fixture_file_multipath_receive_change_pair_parses` green as a secondary guard. (3) Enumerate the two CURRENT reject-asserting tests that must FLIP reject→merge-accept (below — the FOLLOWUP omits them). (4) Decide the `apply_select_descriptor` merged-entry semantics (merged `internal:null` satisfies both `active-receive` AND `active-change`). (5) Decide checksum-helper placement (third private copy vs shared factor). (6) Correct the wrong manual prose (below) in lockstep. (7) Specify the full merge-guard matrix: pair IFF identical `(fp, full origin path, xpub)` AND differ in EXACTLY the final step AND `internal` flags disagree (one false / one true) AND exactly two such entries — reject/skip-merge on any deviation; use ACTUAL step values in the emitted `<a;b>`, never hardcoded `0`/`1`.

---

## Cross-cutting observations

1. **Funds-safety is the whole cycle.** The merged descriptor IS what gets bundled/engraved/imported. A wrong merge (across distinct keys or non-adjacent accounts, or pairing two receives) = a different wallet = fund loss, and (like Cycle A's C1) `verify-bundle` could false-pass on the merged output. This demands the oracle-guarded, funds-reviewed pipeline the FOLLOWUP calls for, and the added distinct-key negative control is the linchpin oracle.

2. **Two reject-tests must FLIP (FOLLOWUP omits them).** Both currently assert exit-2 reject for the same-key split pair and must become merge-accept: `core_fixture_file_mainnet_receive_change_pair_parses` (`:926`, file-driven, uses the KEPT input fixture) and `core_receive_change_pair_rejected_with_workaround` (`:1108`, inline-blob twin, same `MAINNET_FP_A` `/0/*`+`/1/*` shape). Their current assertions (`code==2`, "multipath"/"<0;1>" remedy text) invert under this cycle.

3. **A manual prose claim is factually WRONG and in-scope (this is Continuity anchor #5).** `docs/manual/src/40-cli-reference/41-mnemonic.md:1402-1404` states "Bitcoin Core 25+ emits `listdescriptors` output with the `<0;1>/*` multipath shape on the canonical receive/change pair" — which **contradicts** the verified split-export fact and the toolkit's own v0.76.0 behavior. The workaround docs in `docs/manual/src/45-foreign-formats.md` (hand-combine + `--format descriptor`) also become stale once merge lands. Manual lockstep here is **PROSE + possibly a `verify-examples` transcript** (if a Core-import worked example changes) — NOT the flag-coverage lint, since no clap flag/subcommand/dropdown changes.

4. **schema_mirror is NOT triggered.** The merge is automatic parse behavior; there is no clap flag/option/subcommand/dropdown-value add/remove/rename. Therefore `mnemonic-gui/src/schema/mnemonic.rs` needs no update. The GUI impact is the **`import-wallet --json` wire-shape** (`source_metadata.internal` becomes nullable + merged entries appear where two used to), which is the **un-gated wire-shape** class — a paired `mnemonic-gui` PR by the manual paired-PR rule (matches the FOLLOWUP's "PAIRED mnemonic-gui PR (--json wire-shape change)"). No automated drift gate fires; discipline is the paired PR.

5. **BIP-380 checksum helper is triplicated-by-design in this codebase.** `recompute_descriptor_checksum` already lives privately in `descriptor.rs` and `electrum.rs`; `bitcoin_core.rs` validates but never recomputes. Consistent with the repo's per-parser-copy idiom — a shared factor is optional cleanup, not required.

6. **No DRIFTED-by-N line findings.** Every cited line number is exact against `d9063523` (the entry was filed at this HEAD's immediate predecessor and nothing has touched these files since). The only defects are the one structural fixture-label imprecision (#7 above) and the FOLLOWUP's omission of the flip-tests + wrong-prose ripple — content gaps, not stale citations.

---

## Recommended brainstorm-session scope

- **Single cycle, toolkit + paired GUI.** Slug is self-contained; do NOT bundle with `bip389-double-star-shorthand-support` (`/**`) or `concrete-nonranged-xpub-implied-wildcard` — those are separable toolkit-only cycles (continuity queue #2/#3). Keep this one tight and funds-reviewed.
- **SemVer: MINOR → `mnemonic-toolkit-v0.77.0`.** Restores a capability (standard Core import) + changes the `import-wallet --json` wire-shape (nullable `internal`) + flips a previously-erroring input from reject→accept. Pre-1.0 additive-plus-wire-change ⇒ MINOR. md/ms/mk codecs: **NO-BUMP** (no codec change — merge is pure toolkit parse-layer). GUI: paired MINOR bump for the wire-shape consumer.
- **Rough sizing:** ~150-250 LOC in `wallet_import/bitcoin_core.rs` (merge pre-pass + checksum recompute) + the `bool→Option<bool>` ripple across `mod.rs` (struct + `apply_select_descriptor` arms + `sample_core_metadata`) + `import_wallet.rs` two emit sites; ~6-10 new/flipped test cells (add distinct-key negative control, flip the two reject-tests, add a positive merge round-trip asserting the engraved/verify-bundle path, add multi-entry and non-adjacent-step guards); manual prose fix (2 files) + GUI schema-consumer PR.
- **Lockstep flags:** (a) manual PROSE lockstep — `41-mnemonic.md:1402-1404` (correct the wrong claim) + `45-foreign-formats.md` (retire the workaround) + regen any affected `verify-examples` transcript with the real new binary (Cycle-A gotcha: prose-only is insufficient; run `make -C docs/manual lint` with the new-behavior binary before tag). (b) `.examples-build/` corpus is a version site — `examples.yml` re-runs `gen.sh` on crates/Cargo/install.sh changes (Cycle-A gotcha). (c) paired `mnemonic-gui` `--json` wire-shape PR (no schema_mirror). (d) sibling-codec companions: NONE needed (no codec change).
- **Ordering / dependencies:** No inter-slug dependency — this slug is unblocked (Cycle A's use-site-collapse floor already shipped, which is what makes the split-import hard-fail today). Proceed straight to the mandatory R0-gated brainstorm/SPEC once scope is confirmed with the user.

---

## Reminder — mandatory R0 gate before implementation

This recon is recon only. The brainstorm SPEC and the IMPLEMENTATION_PLAN for this slug each MUST pass an opus architect **R0 review to 0 Critical / 0 Important BEFORE any code** — fold → persist verbatim to `design/agent-reports/` → re-dispatch until GREEN. No implementer dispatch, phase advance, tag, or ship with any open Critical/Important. Given the funds-safety class, weight the R0 toward the merge-guard matrix and the `verify-bundle`-on-merged-output oracle.
