% mk1-regular paper-computer wheel (BCH(93,80,8)).
% Black-and-white, letter-size, two pages: top disc + bottom disc.
% Cell data is helper-generated; see Makefile target `pdf`.

\input{preamble.tex}

% --- Per-wheel parameters (reviewer-facing literals; constants live in the
% Python helper + md-codec/mk-codec, never in the wheel .tex) ---
\newcommand{\WheelFormat}{mk1-regular}
\newcommand{\WheelTargetResidue}{svfp4lyg897ju}
\newcommand{\WheelCanonicalInput}{mk1qprsqhpp0f30mtxzd65mvwcur9usdatwuqvq6z70r9nwrgk6xn6l8gy6nwa2n977sw6zh34rma0nh}
\newcommand{\WheelEndpointAngle}{180.0}

% Bech32 alphabet, in 5-bit symbol order. Indexed 0..31 around the ring.
\newcommand{\WheelAlphabet}{q,p,z,r,y,9,x,8,g,f,2,t,v,d,w,0,s,3,j,n,5,4,k,h,c,e,6,m,u,a,7,l}

\begin{document}

% =========================================================================
% Page 1 — top disc (rotating overlay).
% =========================================================================
\begin{center}
% Top disc: scale=0.93 (v0.1.5 re-alignment). The v0.1.4 bottom-disc grid-
% shift moved row 13 to 1.86" printed; this scale puts the viewing-window
% center (unscaled radius 2.0") at exactly 1.86" printed so the windows
% expose rows 10–16 of the bottom disc (matching the pre-v0.1.4 row band).
% Pointer at 3.85 × 0.93 = 3.58" fits within 4.15" usable (margin=0.1in).
\begin{tikzpicture}[rotate=-90, scale=0.93]
  % 32 input-symbol labels at radius 3.5", 11.25° per segment, 10pt.
  \foreach \sym [count=\i from 0] in \WheelAlphabet {%
    \node at ({\i*11.25}:3.5in) [font=\sffamily\fontsize{10}{12}\selectfont,
      rotate={90 - \i*11.25}] {\sym};%
    % Segment divider (thin radial tick).
    \draw[line width=0.2pt] ({(\i+0.5)*11.25}:3.4in)
      -- ({(\i+0.5)*11.25}:3.6in);%
  }
  % Outer ring of input symbols.
  \draw[line width=0.4pt] (0,0) circle (3.4in);
  \draw[line width=0.4pt] (0,0) circle (3.6in);

  % 32 viewing-window outlined rectangles between radius 1.5" and 2.5",
  % one per 11.25° segment. The user scissors these out after printing.
  \foreach \i in {0,...,31} {%
    \draw[line width=0.3pt]
      ({(\i-0.4)*11.25}:1.5in) -- ({(\i-0.4)*11.25}:2.5in)
      -- ({(\i+0.4)*11.25}:2.5in) -- ({(\i+0.4)*11.25}:1.5in) -- cycle;%
  }

  % Index pointer: small filled triangle at 12 o'clock pointing inward.
  % After the rotate=-90 frame, "12 o'clock" is angle 0.
  \fill (0:3.65in) -- (-2.5:3.85in) -- (2.5:3.85in) -- cycle;

  % Format-specific endpoint mark.
  \volvelleendpoint{\WheelEndpointAngle}{\WheelFormat\ endpoint}

  % License footer.
  \node at (0,-4.65in) [font=\sffamily\fontsize{7}{8}\selectfont, rotate=90]
    {\copyright\ 2026 Brian Goss --- MIT License \quad \WheelFormat\ top disc};
\end{tikzpicture}
\end{center}

\newpage

% =========================================================================
% Page 2 — bottom disc (fixed). 32×32 cell grid + registration target +
% self-test legend.
% =========================================================================
\begin{center}
% Bottom disc: helper-emitted cells span 0.7"–11.55" after the v0.1.4 row-
% shift (helper places row r at radius 0.35"·(r+2) instead of 0.35"·(r+1)).
% Row 0's arc-length per cell jumps from 0.069" → 0.137" — 4× more readable;
% innermost-ring crowding eliminated. scale=0.355 + 11.7" outer boundary
% gives 11.7 × 0.355 = 4.15" → 8.30" diameter (within 8.3" usable at
% margin=0.1in). Inner boundary circle removed: at 0.3" it overlapped the
% new row-0 cells at 0.7"; the registration target's center cross-hair
% suffices for cut-and-pivot alignment.
\begin{tikzpicture}[rotate=-90, scale=0.355]
  \draw[line width=0.3pt] (0,0) circle (11.7in);

  \input{mk-regular.cells.tex}

  \registrationtarget
\end{tikzpicture}

\vspace{0.15in}
\selftestlegend{\WheelFormat}{\WheelTargetResidue}{\WheelCanonicalInput}

\vspace{0.05in}
{\sffamily\fontsize{7}{8}\selectfont\copyright\ 2026 Brian Goss --- MIT
License \quad \WheelFormat\ bottom disc}
\end{center}

\end{document}
