# IMPLEMENTATION PLAN — the visual/screenshot track

**Spec:** `SPEC_gui_visual_screenshot_track.md` (R0-GREEN, 2 rounds). **Recon:** `agent-reports/visual-track-recon-2026-07-01.md`. **Status:** plan-R0 GREEN (round 3, 2026-07-01). r1 RED (6I/6m) + r2 RED (1I/4m — fold-drift I7) both folded; trail at `agent-reports/visual-track-plan-r0-round-{1,2,3}.md`.
**Two legs, ordered:** mnemonic-gui (P0 spike → P1 suite + corpus + CI job → **tag `mnemonic-gui-v0.54.0`**, also carrying the merged PR-#26 help fix); then manual-gui (P2 figures + byte-gate). Each phase: per-phase R0 to 0C/0I; a post-impl whole-diff per leg.
**GOTCHAS:** GUI clippy `--all-targets -D warnings` gated; NO fmt gate; `cargo test --jobs 2`; version bump + CHANGELOG + README self-pin **+ Cargo.lock** (m4) ride the release commit; manual-side local gates need `env RUSTUP_TOOLCHAIN=stable`; trailer `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>` + `Claude-Session: https://claude.ai/code/session_01AXCxehvWYFoCE2TDpcV2Db`.

## LEG 1 — mnemonic-gui (branch `feat/gui-form-snapshots`)

### P0 — the spike on REAL runners (spec §3; NO further implementation before its persisted result)
**Trigger mechanics (I2/ruling-1):** a branch-only `workflow_dispatch` is NOT dispatchable (GitHub: the workflow file must exist on the default branch). The spike therefore runs via the **`pull_request` event** — open a DRAFT PR from the branch; the added workflow runs for that PR. The **≥3 distinct runner instances** come from **"Re-run all jobs"** (fresh runner per re-run) or empty-commit pushes — NOT dispatch. `workflow_dispatch` in the spike file is inert pre-merge (default-branch rule) and DIES WITH THE FILE in P1 (I7) — the ladder's dispatch-regen workflow stays DEFERRED-DOCUMENTED.
**Do:**
1. Dev-dep: `egui_kittest = { version = "0.31", features = ["wgpu", "snapshot"] }` (Cargo.lock: + dify/colored/getopts/rayon, dev-graph only). **MSRV live check (I4):** `cargo +1.88.0 check --locked --all-targets` (or `+1.88.0 test --no-run`) — plain `cargo check` never compiles dev-deps, so `--all-targets` is the ONLY thing that compiles dify at 1.88. Budget note: this compiles the whole existing test tree at the MSRV floor for the first time — a pre-existing dev-dep floor violation is a SPIKE FINDING (report + decide), not an implicit fix obligation. The CI msrv job's lib/bins scope stays unchanged (its charter is the shipped-binary floor).
2. Prototype `tests/spike_form_snapshots.rs`: compute + print the tallest-form ranking from the schema, then **render ALL 61 forms** (I6 — the spec-§3 M2 measurements are mandated on the full set: write + size + time all 61; the cross-runner threshold comparison may anchor on 3 representative forms — a small one, `mnemonic inspect` (secret-bearing), and the tallest — or extend to all 61 for free, which also strengthens the (iii) fleet evidence). `fit_contents()` AND a fixed-size variant on the tallest (the I4-bar measurement); `pixels_per_point 2.0`.
3. The CI workflow (PR-event, per above): **Plan A** `apt-get install mesa-vulkan-drivers` + `WGPU_BACKEND=vulkan`; **Plan B** (if A fails) wgpu-CI's pinned Mesa tarball + `VK_DRIVER_FILES`; **Plan C** (if B fails) `WGPU_BACKEND=gl LIBGL_ALWAYS_SOFTWARE=1`. Prove spec-§3 (i)/(ii)/(iii via re-runs on ≥3 instances); print the adapter string; upload PNGs + hashes as artifacts; measure full-61 render + job wall-time (cold vs rust-cached) + actual corpus size vs the ~3.4 MB estimate.
**Gate:** the spike report persisted to **`mnemonic-toolkit/docs/manual-gui/design/agent-reports/visual-track-p0-spike.md`** (m5 — full path; Leg 1 executes in mnemonic-gui but the trail lives toolkit-side) with adapter/backend/plan-letter, hashes, threshold results across runners, tallest-form no-clipping evidence, all M2 measurements → **per-phase R0 reviews it AND RATIFIES the sizing choice** (fit_contents vs per-form measured height, bound by the no-clipping bar). STOP if A+B+C all fail (spec §3).

### P1 — the full suite + corpus + the permanent CI job (after P0-R0 GREEN)
**Files:**
- **Promotion refactor (I1 — the extended render path is currently PRIVATE to another test binary):** promote `render_one_positional` / `render_positionals` / `render_action_bar` / `render_extended_form_harness` from `tests/gui_render_faithfulness.rs` (where they live as private fns) into `tests/ui_harness/mod.rs` as `pub`, and re-point `gui_render_faithfulness.rs` at the shared copies. Behavior-identical move — but it edits a shipped GREEN test file, so it is PLANNED here and covered by the P1 R0 (the faithfulness test must stay green, byte-for-byte same assertions).
- `tests/gui_form_snapshots.rs`: enumerate all 61 forms (the PR-#24 enumerator + `render_fixture` + the now-shared extended render path); the P0-ratified sizing; `GUI_SNAPSHOTS=1` EARLY-RETURN-SKIP with an `eprintln!` marker; one `snapshot_options(...)` per form → `tests/snapshots/forms/<tab>-<sub>.png` at default threshold 0.6; census 61; **the I3 hygiene assertion** `flag.secret ⇒ default_value.is_none()` over the static schema tables (its own always-run test, NOT env-gated).
- The corpus: 61 PNGs generated (locally, any software rasterizer — CI arbitrates) + committed plain (no LFS; `.gitignore` today is only `/target`+`/.worktrees`, nothing swallows them). `.gitignore` += `tests/snapshots/**/*.new.png`, `*.diff.png`, `*.old.png`.
- `build.yml`: the new `snapshots` job — NO path filter; the existing triggers already fire on PR + master push + tag push (verified `build.yml:3-8`); P0's proven rasterizer recipe; `GUI_SNAPSHOTS=1 cargo test --test gui_form_snapshots`; then the **ran-at-all census** `find tests/snapshots/forms -name '*.new.png' | wc -l` == 61 (`.new.png` is written on PASSING runs — verified in kittest source); upload `.diff.png` artifacts on failure; `Swatinem/rust-cache` in-pattern.
- **Spike-artifact removal (I7):** delete `tests/spike_form_snapshots.rs` + the P0 spike workflow file in P1 (the rasterizer recipe is ABSORBED into `build.yml`'s `snapshots` job); nothing spike-named reaches the merge.
- **Red-corpus mitigation (I2 fold — the old "regenerate via workflow_dispatch" was mechanically impossible pre-merge):** if the PR's snapshots job REDs on the locally-generated corpus, push a TEMPORARY commit flipping the PR's own job to `UPDATE_SNAPSHOTS=1` **and adding a corpus-upload-artifact step** (m7 — the failure-path `.diff.png` upload never fires on a passing UPDATE run), download the regenerated corpus artifact, commit it, revert the flip. Never merge red. **The spec-§5 ladder's `workflow_dispatch` regen workflow is DEFERRED-DOCUMENTED** — post-merge-only by construction (default-branch rule), created only if systemic drift materializes (spec rung 4); it is NOT a P1 deliverable and nothing in P1 cites it as existing.
- **Branch-protection ship-step (I3 — master is UNPROTECTED, verified live; a workflow file alone cannot make a check "required"):** a one-time admin repo-settings action — create a branch-protection rule (or ruleset) on `mnemonic-gui` master naming the `snapshots` check context as required. Out-of-diff, executed at ship alongside the merge. **Scope: per the user's call (2026-07-01), require ONLY the `snapshots` context** — minimal footprint satisfying the spec; the other checks remain green-before-merge by the repo's standing convention; the broader-scope question is FILED as `gui-branch-protection-scope` in `mnemonic-gui/FOLLOWUPS.md` (open, governance — pushed at `a522a69`) for a later user discussion. Pin the exact context name to the job's declared `name:`.
**Gate:** the snapshots job GREEN on the PR (61/61 at 0.6 on the pinned rasterizer + the census); existing suite unaffected (622/0/4 + the new tests, incl. the re-pointed faithfulness test); clippy both configs; the hygiene assertion green; no-clipping spot-verified by OPENING the 3 tallest PNGs; count the real number of PR checks in-phase (m3 — likely 12, don't pin it in gate text). Per-phase R0 → **leg post-impl whole-diff** → PR → CI green → merge → the branch-protection ship-step → release commit (0.53.0→0.54.0: Cargo.toml + **Cargo.lock** + CHANGELOG + README self-pin; **m10: once the required-`snapshots` rule is live, a protected master rejects direct pushes of unchecked commits — the release commit rides the standing PR flow or an explicit admin bypass, decided at ship**) → **tag `mnemonic-gui-v0.54.0`** → **verify the tag-push snapshots run is GREEN** (the provenance anchor).

## LEG 2 — manual-gui (branch `feat/manual-gui-visual-figures`; AFTER the tag)

### P2 — figures + embed + byte-gate
**Step 0 (m2 — concrete provenance verification, not a prose intention):** `gh api repos/bg002h/mnemonic-gui/commits/$(git ls-remote https://github.com/bg002h/mnemonic-gui mnemonic-gui-v0.54.0 | cut -f1)/check-runs --jq '.check_runs[] | select(.name=="snapshots") | .conclusion'` == `success` (m8 — EXPLICIT remote URL: Leg 2 executes in the toolkit repo where `origin` is mnemonic-toolkit; fail-closed either way — empty SHA → API error ≠ success; GUI tags are lightweight today, add `^{}` if v0.54.0 is ever annotated), recorded in the P2 report. Only then pin-bump.
**Files:**
- `pinned-upstream.toml`: GUI pin `v0.53.0 → v0.54.0`. The 4 implied CLI pins re-mirrored — EXPECTED IDENTICAL (verify against the tag's own pinned-upstream.toml, don't assume). `manual-gui.yml` verify-examples tags: NO-OP expected. `gui-schema-coverage`: EXPECTED ZERO anchor delta (v0.53.0→v0.54.0 = help text + tests/CI; VERIFY by running the gate; budget a mini-catch-up only if it REDs).
- `figures/gui/<tab>-<sub>.png` ×61: copied from the pinned clone (`$MANUAL_GUI_UPSTREAM_ROOT/tests/snapshots/forms/`); committed.
- The 4 gallery chapters: each form section gains its image ABOVE the structural render. **Image-path mechanics (I5 — the corrected constraint map; "fails loudly" is FALSE for HTML):** no bare path form satisfies all 5 consumers — lychee resolves image links FILE-relative (verified: it DOES check md image links), pandoc resolves CWD/resource-path-relative, xelatex runs from `build/`. Resolution space (m9 — BOTH branches need a consumer-config alignment; no bare form works alone): source md uses EITHER the lychee/markdownlint-clean file-relative form (`../../figures/gui/x.png` — verify lychee passes it) + an alignment for pandoc-HTML/xelatex, OR the manual-root-relative form + an alignment: `--resource-path` for pandoc-HTML, **`\graphicspath{{../}}`** (NOT `{{../figures/gui/}}` — graphicspath prepends to the path AS WRITTEN) or copy-figures-into-`build/` for xelatex, following the repo's own mermaid absolute-path-via-env precedent if a Lua rewrite is cleanest. Decide in-phase INSIDE this constraint map. **The HTML gate is a POSITIVE EMBED CENSUS** — `grep -c 'img src="data:' build/m-format-gui-manual.html` == 61 (pandoc `--self-contained` on a missing image is WARNING + exit 0 — empirically verified — so a green `make html` proves nothing without the census).
- `750-overview.md`: the Part-intro paragraph — dark theme (user-acked), 2×, egui-content-only (no OS titlebar), **and (m1) what the PNG frame includes/excludes: flags + positionals + Run; the bespoke sub-surface PLACEHOLDER lines that appear in the structural render are not depicted in the PNG** — generated + gated from the pinned GUI.
- **`verify-figures-gui`**: new lint phase (banners `N/8`→`N/9`) — byte-compare `figures/gui/*.png` vs `$MANUAL_GUI_UPSTREAM_ROOT/tests/snapshots/forms/*.png`, census 61 BOTH directions, fail-closed. Reuses the existing clone/env (`lint.sh` already hard-requires `MANUAL_GUI_UPSTREAM_ROOT`); thread the figures dir as an explicit lint.sh arg or derive `$SRC_DIR/../figures/gui` (m6 — pick in-phase, mirror the `TRANSCRIPTS_GUI` precedent).
- CHANGELOG `[Unreleased]` + FOLLOWUP `manual-gui-visual-screenshot-track` RESOLVED in the shipping commit + the GUI-side companion entry ADDED to `mnemonic-gui/FOLLOWUPS.md` (none exists yet — the plan creates it, satisfying the both-ways convention).
**Gate:** `make lint` 9/9 (incl. the new phase; gui-form-xref + gui-schema-coverage unchanged); `verify-examples-gui` STILL 61/61 byte-identical (untouched); `make md`/`html`/`pdf` build + **the HTML embed census == 61** + open the PDF and spot 3 figures incl. the tallest form (no clipping in the embedded image); manual-gui.yml green. Per-phase R0 → leg post-impl → PR → merge.

## Risk / sequencing
- **P0 is the go/no-go** — nothing past it until its R0 is GREEN; STOP honored (spec §3).
- **The corpus is locally generated, CI-arbitrated** — the I2-folded red-corpus path (temp `UPDATE_SNAPSHOTS` flip on the PR's own job) is the ONLY pre-merge in-CI regeneration mechanism; never merge red.
- **Leg-2's byte-gate inherits meaning from the tag-push snapshots run** — P2 step 0 verifies it mechanically.
- **Secret hygiene:** the I3 assertion ships in P1, always-run; the P1 R0 verifies no PNG shows a plausible secret (channel-side + widget-side, spec §4).
- **Image-path mechanics (P2) is the fiddly bit** — 5 consumers, a solvable-but-nonobvious constraint map (I5), and the HTML failure mode is SILENT — the embed census is the gate, not the build exit code.
