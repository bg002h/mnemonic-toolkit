# SPEC — `gui_example.pdf`: a generated + gated GUI tutorial mirroring Examples.pdf's wallet journeys

- **Status:** DRAFT — R0-GREEN (round 2, 2026-07-05; r1 RED 0C/3I+4M folded) — cleared for the P0 spike (MANDATORY gate; no implementation before GREEN 0C/0I).
- **Cycle:** `gui_example_tutorial` (cross-repo: mnemonic-gui GUI leg + mnemonic-toolkit book leg).
- **Author:** RECON + SPEC session, 2026-07-02.
- **Source SHAs (all citations verified at write time against these):**
  - toolkit `master@4c401b16` (v0.75.0 tier; manual-gui pinned to `mnemonic-gui-v0.55.0`)
  - mnemonic-gui `master@0d4429d` (v0.55.0; pins `mnemonic-toolkit-v0.74.0` + md/ms/mk-cli v0.11.0/v0.13.0/v0.11.0)
- **Recon provenance (persisted verbatim, this dir `agent-reports/`):**
  - `gui-example-tutorial-recon-A-app-shell.md` — app-shell extraction map (verdict: MODERATE)
  - `gui-example-tutorial-recon-B-runner-pane.md` — runner + populated-pane (verdict: FEASIBLE-WITH-NORMALIZATION; note: its "recipe A BLOCKED" is unblocked by the A-extraction)
  - `gui-example-tutorial-recon-C-fixtures-journeys.md` — journeys/fixtures/masking (all journeys GUI-expressible; secrets = 3 published test phrases only)
  - `gui-example-tutorial-recon-DE-corpus-book.md` — corpus conventions + book build/gates (reusable-vs-new inventory)
- **R0 reviews (persisted verbatim, this dir `agent-reports/`):**
  - `gui-example-spec-r0-round-1.md` — RED 0C/3I (+4M); I1–I3 + M1–M4 folded into this
    revision.
- **Cite-nit record (R0 M4b — recon reports preserved verbatim; this spec's cites are
  the verified ones):** recon DE cites `gen.sh:25` for the Examples version assert —
  the correct line is `gen.sh:22` (the spec's cites were already correct and are
  kept); recon A's modal-Run cite `main.rs:1108` is `:~1110` at HEAD.
- **Dispatch note:** recon B's first final-report emission was lost to an API stream
  timeout; the agent was resumed and re-emitted from intact context. Flagged per the
  dispatch-failure convention; no silent inline substitution occurred.

---

## 1. Goal

A standalone, **generated + gated** GUI tutorial book — `gui_example.pdf`, sitting
beside `docs/Examples.pdf` — that walks the same wallet journeys as Examples.pdf but
as **GUI walkthroughs**: each tutorial step shows the real mnemonic-gui application
window with the form filled in, then the same window after clicking **Run**, with the
output panel genuinely populated by a real pinned-CLI execution. Every screenshot is
machine-generated by the house snapshot machinery and CI-gated; every full CLI output
is machine-captured and byte-gated (standing rule: all CLI output in all docs is
binary-generated).

### 1.1 USER-LOCKED design decisions (2026-07-02 — NOT re-openable in R0)

1. **Scope: ALL of Examples.pdf's journeys, GUI-style** — single-sig card set →
   conventional 2-of-3 multisig → the pathological 4-tier degrading wsh vault → its
   taproot twin → watch-only export (source `.examples-build/Examples.md`; built book
   `docs/Examples.pdf`).
2. **Visuals: filled screenshots, generated + threshold/byte-gated** (house pattern),
   and **WHOLE-WINDOW** — tab bar + subcommand selector + the filled form + the output
   panel, not the current form-only crops. No OS titlebar — kittest renders egui
   content only; documented caveat (see §10 non-goals and the reader-facing note).
3. **Post-Run shots show the output panel GENUINELY POPULATED by a real pinned-CLI
   execution in the harness** — two shots per step (filled-form, post-Run), the I4 +
   snapshot machineries combined.

Recon fidelity note on decision 2: in the current shell the "subcommand selector" is a
`ComboBox` dropdown inside the CentralPanel (`mnemonic-gui src/main.rs:525`), not a
persistent side list. Whole-window shots therefore show: top tab strip
(`main.rs:424-453`), central panel with pinned-version line + subcommand combo +
`?` help + the scrollable form (`main.rs:512-889`) + action bar with `Run`
(`main.rs:975-1026`), bottom output panel (`main.rs:456-509`). That IS the whole
window as a user sees it; the spec does not invent a side list.

---

## 2. Recon summary — the four load-bearing answers

**(A) App-shell extraction — MODERATE, well-mapped.** `MnemonicGuiApp` is bin-private
in `src/main.rs` (struct `:98-151`, `impl eframe::App` `:383`); the `update()` body
**never touches `eframe::Frame`** (`_frame` unused, verified) and contains no
viewport/repaint/file-dialog/clipboard entanglement. All OS side-effects live in
`main()` + `new(cc, …)`: window-capture protection (`:166-174`), 1 Hz keepalive thread
(`:195-202`), SIGINT/SIGTERM + ctrlc handlers (`:217-256`), and `AppState::detect_all()`
$PATH probing (`:263`). egui_kittest 0.31.1 natively steps Context-closures with real
panels (`Harness::build_state`, `egui_kittest-0.31.1/src/builder.rs:113`). Minimal
extraction: lift the app into a `#[cfg(feature="gui")]` lib module, split the
constructor (pure init + injectable `AppState`, side-effects stay in the `cc` wrapper),
extract `fn ui(&mut self, ctx)` from `update()`. No egui_kittest `eframe` feature
needed.

**(B) Populated pane — FEASIBLE, and simpler than feared.** The runner is **fully
synchronous**: `spawn_and_capture` (`main.rs:1220`) calls
`runner::run_with_stdin` inline **inside `update()`**; completion lands in
`app.last_run` the *same frame* as the Run/confirm click. No channels, no polling, no
spinner, no timestamps/durations/PIDs anywhere in the pane — the pane is timing-free
(recon B §2). The production argv echo uses bare `schema.cli_name` as argv[0]
(`form/invocation.rs:115-116`) → deterministic. The one conditional between click and
spawn is the "Confirm secret-bearing run" modal (`main.rs:1081-1124`), which a driver
clicks through (two-click drive for secret forms) — itself a tutorial teaching moment.
CI already knows how to put the four pinned CLIs on `$PATH` under bare names
(`schema-mirror.yml:59-133`).

**(C) Fixtures + flows — everything is GUI-expressible; secrets are demo-only.**
Every journey subcommand has a GUI form (schema cites in recon C §3). The J3 vault
feeds `--spec policy.json` through the form's `Path` field (`schema/mnemonic.rs:3921`,
`stdio_sentinel:true`) — the guided-builder **refusal** teaching moment survives; the
vault itself rides `--descriptor-file` Path fields, exactly like the CLI journey. The
only secret-bearing inputs anywhere are the **three world-known BIP-39 test phrases**
(S0/S1/S2), all printed verbatim in Examples.md; J3/J4/J5 are entirely watch-only.
Masking for the exact journey entry paths (SlotEditor `@N.phrase` rows; `convert
--from phrase=` composite) is complete — via the `SECRET_SLOT_SUBKEYS` /
`SECRET_NODE_TYPES_ARGV` taxonomies, **not** the I3 64-flag census (precision matters:
see §7). Shell-only glue (python hashlock helpers, jq/sed md1 hops, `$(cat …)`)
all has enumerated adaptations (recon C §5).

**(D) Sizing — fixed window is new territory; scrolling has a guaranteed fallback.**
The existing corpus is auto-sized (`fit_contents()`); a fixed whole-window shot must
budget for the tab strip + output panel and the tallest journey forms
(`mnemonic-restore` 1169 px, `mnemonic-bundle` 912 px physical @ ppp 2.0), so
**in-viewport scrolling is unavoidable for some steps**. kittest scroll control has
zero in-repo precedent (UNVERIFIED → spike), but a guaranteed fallback exists: a
scroll-offset seam (`ScrollArea::vertical_scroll_offset`) on the extracted app.
Conventions to inherit unchanged: ppp 2.0, dark theme, embedded fonts, dify 0.6
threshold, lavapipe CI recipe, CI-compares-never-regenerates.

**(E) Book — follow the manual-gui gated model, not the un-CI'd Examples model.**
`docs/Examples.pdf` is generated by `.examples-build/gen.sh` (real-binary capture,
hard-pinned `mnemonic 0.55.3`, `gen.sh:22`) but has **no CI gate at all**. The
manual-gui stack (pandoc→xelatex Makefile, 9-phase lint, verify-figures byte-census,
pinned-clone plumbing, release job) is fully reusable and fully gated. The tutorial
book therefore lives inside `docs/manual-gui/` as a **sibling book** sharing that
stack (decision + rationale in §5.2).

**Version-skew consequence (spec decision):** Examples.pdf is branded `v0.55.3`
throughout; the current tier is toolkit v0.75.0. The GUI tutorial pins its **own**
tier (the GUI repo's pinned toolkit tag at capture time) and **explicitly disclaims
byte-parity with Examples.pdf's printed output**. Journey *shape* (commands, flags,
teaching moments) mirrors Examples.pdf; output *bytes* are regenerated at the
tutorial's pin. A P1 task re-runs each journey's commands against the current tier and
flags any behavioral divergence (e.g. changed refusal wording) for the chapter prose.

---

## 3. Architecture overview

Two legs, same split-gate pattern as the visual-screenshot track
(`SPEC_gui_visual_screenshot_track.md:9-15`): the GUI repo owns rendering + the
pixel threshold; the toolkit repo owns byte-copies + censuses (no rasterizer in docs
CI).

```
mnemonic-gui (GUI leg)                          mnemonic-toolkit (book leg)
─────────────────────────                       ───────────────────────────
P0 spike (whole-window drive)                   docs/manual-gui/tutorial/       (chapters)
src: app-shell extraction                       docs/manual-gui/figures/tutorial/   (byte-copied PNGs)
tests/gui_tutorial_snapshots.rs (harness)       docs/manual-gui/transcripts/tutorial/ (byte-copied outputs)
tests/tutorial/ (manifest + fixtures)           Makefile: gui-example targets → gui_example.pdf
tests/snapshots/tutorial/<stem>.png             tests/lint.sh: new tutorial phases
tests/snapshots/tutorial/<stem>.stdout.txt      .github/workflows/manual-gui.yml: extended jobs
CI: tutorial-snapshots job                      docs/gui_example.pdf (committed deliverable)
tag mnemonic-gui-v0.56.0            ──pin──►    pinned-upstream.toml bump
```

### 3.1 GUI leg (mnemonic-gui)

**(a) App-shell extraction (the enabling refactor — zero behavior change).**
Per recon A §5:
1. Lift `MnemonicGuiApp` (struct + impls + `render_exit_badge` + `spawn_and_capture`
   + `AUTOSAVE_INTERVAL`; `main.rs:98-1260`) into a new `#[cfg(feature = "gui")]`
   library module (working name `src/app_window.rs`), re-exported; `main.rs` shrinks
   to `fn main()` + the eframe bootstrap.
2. Split the constructor: `MnemonicGuiApp::new_headless(app_state: AppState, loaded:
   Option<PersistedState>, state_path: Option<PathBuf>)` = pure field init only
   (today's `main.rs:258-335` minus side-effects). The existing `new(cc, …)` becomes a
   thin wrapper: run the three `cc`-dependent OS effects (window-capture, keepalive
   thread, signal handlers), call `AppState::detect_all()`, then `new_headless`.
3. Extract `fn ui(&mut self, ctx: &egui::Context)` = today's `update()` body verbatim
   (already `Frame`-free); `impl eframe::App::update` becomes `self.ui(ctx)`.

Invariants: the production binary's behavior is byte-identical (pure relocation);
`cargo build --no-default-features` still builds (the lifted module is gui-gated —
this class of edge bit the form-renders cycle twice pre-code, so it is an explicit
per-phase check); `gui-render` bin and the existing 61-form snapshot suite are
untouched; **no clap-surface change anywhere in the cycle → no toolkit schema-mirror
update needed** (the paired-PR rule is satisfied vacuously — state this in the PR).

**(b) Tutorial capture harness** — new env-gated integration test
`tests/gui_tutorial_snapshots.rs` (`GUI_TUTORIAL_SNAPSHOTS=1`, early-return-skip like
`gui_form_snapshots.rs:109-116`):
- Builds the real app: `Harness::builder().with_size(WINDOW_SIZE)
  .with_pixels_per_point(2.0).build_state(|ctx, app| app.ui(ctx), app)` where `app =
  MnemonicGuiApp::new_headless(fixed_appstate_all_found(), None, None)`. **Fixed
  window size** (spike-ratified, §4) — no `fit_contents()`.
- **`pinned-tier-version-gate` (HARD, named — the house's own `gen.sh:22` pattern
  adopted; R0 I1).** Before ANY step renders, the harness probes `<cli> --version`
  for every CLI the manifest will spawn and hard-fails on any mismatch with the
  tutorial's pinned tier (expected strings derived from `pinned-upstream.toml` /
  the schema `pinned_version` constant, `schema/mnemonic.rs:4620`). Rationale: the
  window's `Pinned:` line renders a schema CONSTANT (`main.rs:518`), not a probed
  value, and `spawn_and_capture` resolves bare argv[0] against the **real `$PATH`**
  at click time (`path_detect::detect`, `main.rs:~1232`) — so without this gate a
  wrong-tier local regen (`UPDATE_SNAPSHOTS` flow) would render honest-looking
  panes + transcripts from the wrong binary, detectable only as an undiagnosed
  downstream byte/pixel diff (or not at all when tiers agree). The gate makes a
  wrong-tier render impossible and machine-guards the `Pinned:` label's honesty in
  every shot. Also pinned in the determinism contract (§6 item 4).
- Executes the **step manifest** (one `#[test]` fn, steps strictly sequential — this
  permits a single `set_current_dir` to the fixture dir so Path fields hold short
  display names like `policy.desc`, and permits intra-journey chaining):
  per step: select tab + subcommand (real combo drive), fill fields via the existing
  drive-dispatch primitives (`tests/ui_harness/mod.rs:282-334`, extended to drive
  widgets *inside the real window* by AccessKit node lookup), set scroll position,
  `harness.run()` to quiescence, snapshot `<stem>-form.png`; click `Run` (+ modal
  `Run` for secret steps, snapshotting `<stem>-modal.png` where the manifest says so);
  the subprocess completes **in-frame** (recon B §1); `harness.run()`; snapshot
  `<stem>-run.png`.
- **`SAME-FRAME-COMPLETION` assertion (named per-step tripwire — R0 I2).**
  Immediately after the Run-click frame (modal-Run frame for secret steps) and its
  single `harness.run()` settle — BEFORE any further frames or the `-run.png`
  snapshot — the harness asserts `app.last_run.is_some()` (or `last_run_error` for
  a detect-fail step) with a failure message naming the invariant: *"runner must
  complete in the Run-click frame — populated-pane contract, SPEC §6.5; any
  async-runner change is a USER-decision downgrade"*. A future async runner
  (spinner/cancel-button UX evolution) then surfaces as ONE named invariant breach,
  not a confusing 25-file pixel diff — and the seeded-`last_run` "fix" someone
  might reach for is precisely the downgrade the user reserved for themselves
  (§4 STOP menu; weakening the populated-pane contract is a USER decision, never an
  implementation choice). P1 also leaves a pointer comment at `spawn_and_capture`
  citing this contract so a future refactorer finds it from the code side.
- **No-shot transcript steps (`shots: 0`) — production path (R0 M1).** Steps marked
  prose + transcript only in §5.3 (J2 devices-1/2 converts, the J4 NUMS
  bundle/restore) and the `--json` bundle runs the J3/J4 restore chains consume are
  ORDINARY manifest steps executed through the exact same GUI-driven Run path —
  driven form, real click(s), real pinned-CLI execution, RunResult persisted and
  byte-gated as `<stem>.stdout.txt`/`.stderr.txt`/`.exit.txt` — with a manifest
  `shots: 0` (no PNG captured). Every gated transcript therefore satisfies the
  "populated by a real pinned-CLI execution in the harness" contract, and the
  transcript census stays manifest-derived (run count ≥ shot-bearing step count).
- After each Run: persist the **full RunResult text** as corpus files
  `tests/snapshots/tutorial/<stem>.stdout.txt` (+ `.stderr.txt` when non-empty, +
  exit code in a one-line `.exit.txt`), byte-gated exactly like the PNGs
  (regenerate-and-diff in the same job). This guarantees pane-pixels ↔ transcript
  identity from a single execution, and satisfies the standing binary-generated-output
  rule without re-running commands in toolkit CI.
- Intra-journey chaining replaces the jq/sed glue: e.g. J5's restore step parses the
  md1 chunks out of the *previous step's* captured `RunResult.stdout` (`--json` run)
  and types them into the repeating `--md1` rows — deterministic, no derived-fixture
  drift, and mirrors what a real user does (copy chunks from the output pane).
- Assertions per step (beyond snapshots): `exit_code` matches the manifest's expected
  value (refusal steps expect non-zero — J3 build-descriptor, J4 depth-2 + BSMS
  steps); chained values parse; secret-hygiene assertions (§7).

**(c) Fixtures** — `tests/tutorial/fixtures/`: byte-copies of the four tracked
primitives from toolkit `.examples-build/` under their tutorial display names
(`policy.json` gets the same `opensessame`-digest rewrite `gen.sh:33-37` applied
**once at fixture-authoring time**, with a provenance header comment in the fixture
dir's README noting source repo + SHA + transform). Seed phrases live as the S0/S1/S2
literals in the manifest (already published in Examples.md — §7). Everything else is
produced by chaining.

**(d) CI job** `tutorial-snapshots` in `mnemonic-gui/.github/workflows/build.yml`:
the `snapshots` job recipe (lavapipe, `WGPU_BACKEND=vulkan`, CPU-adapter guard,
`.new.png` census, `.diff.png` artifacts; `build.yml:87-147`) **plus** the
schema-mirror job's pinned-CLI install recipe (`schema-mirror.yml:59-88`) so the
pinned `mnemonic` (and if any step needs them, `md`/`ms`/`mk`) are on `$PATH` as bare
names. Census: `.new.png` count == manifest shot count AND `.stdout.txt` regen count
== manifest run count. Runs on every PR/master/tag push (no path filter — same
rationale as the snapshots job: the tag run is the book leg's provenance anchor).

**(e) Pin bump + tag.** Bump `mnemonic-gui/pinned-upstream.toml` toolkit tier
v0.74.0 → latest toolkit tag (v0.75.0) *early in P1* — this fires the `schema_mirror`
drift gate by design (lagging-indicator discipline); resolve the delta before capture
so the tutorial renders the current tier. **Expected delta — verified at R0 round 1:
ZERO flags.** Between the v0.74.0 and v0.75.0 tags the only
`crates/mnemonic-toolkit/src` changes are `cmd/inspect.rs` + `error.rs` (the md1
`template:` line + INSPECT_SCHEMA_VERSION 1→2): no clap-surface change, so the
schema_mirror flag-name delta is zero and the sibling mirrors
(`archetype_schema_mirror`, `spec_nodes_mirror` [pins spec_schema_version==1,
untouched], `xpub_search_schema_mirror`, …) are unaffected; the GUI's real-CLI cells
drive `decode-address`, not `inspect --json`, so the wire-shape bump is inert for GUI
tests. The mandatory catch-up is exactly two edits: the pin line + the
`pinned_version: "mnemonic 0.74.0"` schema string (`schema/mnemonic.rs:4620`) — a
budgeted edit, not an open risk. That string renders ONLY in the real window
(`main.rs:518`), NOT in the 61-form corpus or `.gui` renders, so the bump does not
invalidate the existing corpora (§3.2(e)'s one-pin-anchors-three-corpora claim
holds). Ship as `mnemonic-gui-v0.56.0` (GUI = PR + CI-before-tag per house release
ritual).

### 3.2 Toolkit leg (book — zero `src/` changes)

**(a) Sources** at `docs/manual-gui/tutorial/` (gfm chapters, one per journey +
front/back matter). Chapters embed, per step: prose (adapted from Examples.md's
narrative, GUI-ified per recon C §5's adaptation list), the `-form`/`-modal`/`-run`
figures, and the full output transcript via the existing `include-transcript.lua`
fenced-include mechanism pointed at `transcripts/tutorial/<stem>.stdout.txt`.

**(b) Corpus copies:** `docs/manual-gui/figures/tutorial/<stem>.png` and
`docs/manual-gui/transcripts/tutorial/<stem>.{stdout,stderr,exit}.txt` — byte-copies
from the pinned GUI clone, committed.

**(c) Build:** extend `docs/manual-gui/Makefile` with `gui-example-pdf` /
`gui-example-html` targets (same pandoc→xelatex recipe, own title page, resource
paths adding `figures/tutorial` + `transcripts/tutorial`). Deliverable **committed at
`docs/gui_example.pdf`** — the locked brief says "a `gui_example.pdf` artifact beside
`docs/Examples.pdf`", and Examples.pdf is the committed-PDF precedent. Gates run on
sources/figures/transcripts, never on PDF bytes (xelatex embeds timestamps). The PDF
is also attached by the manual-gui release job, which is **tag-triggered on
`manual-gui-v*`** (latest precedent: `manual-gui-v1.1.0`) — the attach stays dormant
until P4 cuts the next `manual-gui-v*` tag (named in §9 P4; R0 I3). Documentation
note, not a gate gap (R0 M4c): `manual-gui.yml`'s path filter does not include
`docs/gui_example.pdf`, so PDF-only touches don't trigger the workflow — gates never
gate PDF bytes anyway; P3 either adds the path to the filter or records this note
where the workflow is wired.

**(d) Gates** (new lint phases in `docs/manual-gui/tests/lint.sh`, wired into
`manual-gui.yml`): see §8.

**(e) Pin:** `docs/manual-gui/pinned-upstream.toml` bumps to `mnemonic-gui-v0.56.0`.
One pin now anchors THREE corpora (form gallery 61 PNGs + `.gui` renders + tutorial
corpus) — a single coherent bump, which is exactly why the tutorial lives inside
manual-gui rather than a parallel book dir (see §5.2).

---

## 4. P0 spike — whole-window harness drive + populated-pane determinism

**This is the cycle's P0-class risk and runs before ANY corpus or book work.**
Executed in a mnemonic-gui worktree on top of a minimal throwaway version of the §3.1(a)
extraction; report persists to `agent-reports/gui-example-tutorial-p0-spike.md`;
its ratifications fold into the implementation plan (which then goes through its own
R0).

Spike questions (each with a measurable exit):

- **S1 — whole-window render determinism.** Extracted app under
  `Harness::build_state` at a fixed size, panels + combo + populated pane rendered.
  Exit: two same-env runs byte-identical; Vulkan↔GL cross-backend diff ≤ 0.6 dify
  threshold on all pilot shots (same bar the visual-track spike met,
  `visual-track-p0-spike.md:26-40`).
- **S2 — live Run-click populated pane.** Two pilot steps through the REAL `ui()`
  loop with the pinned CLI on `$PATH`: (i) non-secret (`export-wallet` with
  `--descriptor-file`) — one click; (ii) secret (J1 `bundle` with one `@0.phrase`
  slot) — click Run → assert modal appears with masked argv → snapshot → click modal
  Run. Exit: pane populated same-frame; double-run byte-identical; argv echo shows
  bare `mnemonic` argv[0]; stdout slice + exit badge as expected; **and the ComboBox
  POPUP drive is an explicit exit criterion** — the subcommand-selection popup
  renders on a separate egui layer, every tutorial step depends on selecting through
  it in the whole-window context, so S2 must demonstrate the popup open + option
  click explicitly, not implicitly (R0 M2i).
- **S3 — scroll positioning.** Try in order: (i) kittest/AccessKit scroll actions,
  (ii) injected `egui::Event::MouseWheel`/scroll raw input, (iii) the guaranteed
  fallback — a scroll-offset seam on the extracted app (test-set field applied via
  `ScrollArea::vertical_scroll_offset`). Exit: pixel-reproducible scroll states for
  the tallest journey form (`mnemonic-restore`). (iii) cannot fail, so S3 ratifies a
  mechanism rather than gating feasibility.
- **S4 — window-size ratification.** Measure the widest/tallest journey states
  (filled `restore` with ~10 `--md1` rows; populated pane at max_height) at the two
  candidates: **920×720 logical** (production default seed, `main.rs:52`) and
  **1280×900 logical**. Ratify ONE global size for the whole corpus (single size =
  visual consistency + one determinism surface), plus the per-shot corpus byte
  budget (§6.3).
- **S5 — drive-in-window.** Confirm the existing drive-dispatch primitives can
  target widgets inside the full window via AccessKit node lookup (label collisions
  across panels are the risk; ratify a lookup discipline, e.g. scoping queries to the
  central panel's subtree). Includes one repeating-row drive (`--md1`) and one
  SlotEditor row drive — the latter **starting from the fresh-app DEMO SEED**
  (`main.rs:300-316`: a fresh app pre-fills `mnemonic:bundle` with
  `--network=mainnet`/`--template=bip84`/`--account=0` plus one **empty Xpub slot
  row**): the drive must flip that seeded row's subkey to `phrase` (the J1 path),
  ratifying that the harness controls/accounts for the seeded baseline rather than
  assuming empty forms — the same baseline the Chapter-0 orientation shot renders
  (R0 M2ii). The ComboBox popup selection is likewise an S5 exit criterion (shared
  with S2).

**STOP condition.** If S1 or S2 cannot reach cross-env sub-threshold determinism
(e.g. paneled-window rendering artifacts under lavapipe, or in-frame subprocess
execution misbehaving under kittest), **STOP the cycle and report to the user** with
the evidence and the fallback menu — (a) pane-mirror harness (recon B recipe B) with a
new pane-faithfulness guard, (b) seeded-`last_run` variant (RunResult still produced
by a real pinned-CLI `runner::run` execution inside the same test, assigned rather
than click-routed; Run-click wiring separately asserted non-pixel), (c) form-only +
transcript downgrade. **None of these may be adopted silently — the whole-window +
real-click contract is user-locked, so any downgrade is a user decision.** S3/S4/S5
cannot hard-fail (guaranteed fallbacks exist); they ratify mechanisms/values.

---

## 5. The book: placement, structure, naming

### 5.1 Naming scheme

Step stem: `tut-<j>-<nn>-<slug>` (e.g. `tut-j2-06-bundle-all-seeds`). Artifacts per
step: `<stem>-form.png` (one or more: `-form`, `-form2` for multi-scroll steps),
optional `<stem>-modal.png`, `<stem>-run.png`, `<stem>.stdout.txt` (+ `.stderr.txt`,
`.exit.txt`). Same stem across GUI corpus, toolkit figures/, transcripts/, and the
chapter anchor `{#tut-<j>-<nn>-<slug>}` — the xref gate keys on it (§8). The manifest
(in the GUI repo) is the single source of truth for the stem list; both censuses
derive their expected counts from it (a committed `manifest-stems.txt` emitted by the
harness, so the toolkit gate needs no Rust).

### 5.2 Placement decision

**Inside `docs/manual-gui/` as a sibling book** (chapters `tutorial/`, figures
`figures/tutorial/`, transcripts `transcripts/tutorial/`, shared Makefile + lint.sh +
workflow + **one** `pinned-upstream.toml`).

Rationale: (i) reuses the 9-phase lint + pinned-clone plumbing + release job wholesale;
(ii) one pin bump moves the form gallery, `.gui` renders, and tutorial corpus in
lockstep — two books pinning the same upstream at different tags would be a standing
incoherence generator; (iii) avoids duplicating the Dockerfile.build/pandoc stack.
Rejected alternative: standalone `docs/gui-examples/` dir (duplicate pin + lint + CI;
the `.examples-build` model additionally has **no CI gating at all** — recon E5 — and
inheriting that gap would violate the standing generated-output rule).

### 5.3 Chapter plan + shot budget

Steps and shots (from recon C's per-journey tables; "shots" = form + run + modal where
marked; multi-scroll `-form2` shots excluded from this count, spike may add a few):

| Ch | Journey (Examples.md src) | Steps | Shots |
|---|---|---|---|
| 0 | Orientation: the app window, tabs, combo, output panel, "(no run yet)" | 1 | 1 |
| 1 | J1 single-sig card set (§2) — bundle bip84, typed masked phrase | 1 | 3 (form, **modal**, run) |
| 2 | J2 2-of-3 multisig (§3, §3.4) — convert fp, convert xpub, canonicalise, BSMS, watch-only bundle, all-seeds bundle (3 slots), restore | 7 | 15 (all-seeds keeps its **modal** shot) |
| 3 | J3 4-tier degrading vault (§5) — build-descriptor **refusal**, canonicalise, BSMS first addr, engrave bundle, restore round-trip | 5 | 10 |
| 4 | J4 taproot twin (§6) — depth-2 **refusal**, canonicalise, bundle, restore, core export, BSMS **unsupported error**, compare-cost, NUMS multisig export | 8 | 16 |
| 5 | J5 watch-only export (§4) — bundle --json, restore descriptor, restore bitcoin-core | 3 | 6 |
| — | **Total** | **25** | **51** |

Convert steps for devices 1/2 (J2) and the NUMS bundle/restore (J4) are prose +
transcript only (no shots) to keep the corpus lean — the interaction is identical to
the shot-bearing sibling step. These (and the `--json` chaining runs the J3/J4
restores consume) are ordinary `shots: 0` manifest steps executed through the same
GUI-driven Run path (§3.1b): their transcripts are gated real-harness executions
like every other. External `bitcoin-cli` steps and Appendix B are prose
callouts (§10).

Corpus size estimate: 51 PNGs at the ratified fixed size (1840×1440–2560×1800
physical, dark flat UI) ≈ 100–300 KiB each → **~5–15 MiB** per repo (GUI corpus +
toolkit copies), vs 2.12 MiB for the 61-form corpus. Accepted; S4 measures the real
number and the plan flags if it exceeds ~20 MiB (mitigation: drop to one
representative modal shot, prune `-form2` shots).

### 5.4 Scrolling / clipping contract (explicit, per the locked no-clipping question)

- Tutorial shots are **viewport-faithful**: fixed window, the form scrolls inside the
  CentralPanel `ScrollArea` exactly as production (`main.rs:593`). A visible scrollbar
  is correct, not a defect.
- Each `-form` shot is captured at a manifest-recorded scroll offset chosen so **every
  field the step fills is visible**; when driven fields span more than one viewport,
  the step emits additional `-form2…` shots at recorded offsets. No step may ship a
  shot where a driven field is clipped out of every form shot (machine-checkable: the
  driver asserts each driven widget's rect intersects the viewport in at least one
  captured offset).
- The **no-clipping canonical reference for full form geometry remains the existing
  form-only gallery** (`figures/gui/<tab>-<sub>.png`); every tutorial step cross-links
  its gallery figure. The tutorial's contract is "what the user sees at this moment,"
  not "the whole form at once."
- Post-Run shots: the pane's stdout `ScrollArea` clips at `max_height(180.0)`
  (`main.rs:490-495`) — deterministic top-slice. The **full** output always accompanies
  the shot as the gated transcript block. Chapter 0 explains both conventions to the
  reader, plus the no-OS-titlebar caveat.

---

## 6. Determinism contract

The corpus is deterministic given:
1. **Pinned renderer env** (unchanged conventions): egui_kittest 0.31.1 wgpu+snapshot,
   ppp 2.0, dark theme, embedded fonts, lavapipe CPU adapter with backend guard, dify
   0.6 threshold, CI compares/never regenerates, remediation ladder per
   `SPEC_gui_visual_screenshot_track.md:31`.
2. **Fixed window size** (S4-ratified), fixed manifest scroll offsets, single
   sequential `#[test]` (stable frame sequence; no parallel-test cwd races).
3. **Injected `AppState`** (all four CLIs `Found`) — no `$PATH`-dependent tab
   grey-out; `state_path = None` — no persisted-toggle interference (fresh-app
   defaults show argv+exit+stdout+stderr, `main.rs:271`). The fresh-app baseline
   INCLUDES the deterministic **demo seed** (`main.rs:300-316`: `mnemonic:bundle`
   pre-filled `--network=mainnet` + `--template=bip84` + `--account=0` + one empty
   Xpub slot row) — rendered as-is by the Chapter-0 orientation shot and mutated by
   J1; any future change to the demo seed moves tutorial pixels and is understood as
   a corpus-regenerating change (R0 M3).
4. **Pinned CLI tier** on `$PATH` as bare names (tutorial-snapshots job installs the
   `pinned-upstream.toml` tags), **HARD-asserted by the `pinned-tier-version-gate`**
   (§3.1b): before any render the harness probes each spawned CLI's `--version` and
   hard-fails on tier mismatch (the `gen.sh:22` pattern) — a wrong-tier local regen
   can never produce honest-looking corpus bytes, and the window's `Pinned:` label is
   machine-guarded in every shot. Pane argv echo is bare-`cli_name` argv[0] by
   production construction (`invocation.rs:115-116`) — no path normalization needed on
   the click path. (The I4-style argv[0]-overwrite pattern is NOT used for shots.)
5. **Deterministic commands only**: every journey command is a pure function of its
   fixed inputs (bundle/convert/export-wallet/restore/build-descriptor/compare-cost on
   the demo fixtures — no RNG, no timestamps; recon B §2 verified the pane itself is
   timing-free; refusal steps pin exact non-zero exits). Runner env is inherited +
   `MNEMONIC_FORCE_TTY=1` (`runner.rs:199`) — output is captured in TTY mode,
   consistently for pane and transcript since both come from the same RunResult.
   **Synchronous in-frame runner completion is a PINNED INVARIANT of the tutorial
   corpus** — enforced per step by the named `SAME-FRAME-COMPLETION` assertion
   (§3.1b) and cross-referenced by a pointer comment at `spawn_and_capture`;
   weakening it (async runner, spinner, seeded `last_run`) is a USER-decision
   downgrade of the populated-pane contract, never an implementation choice (R0 I2).
6. **cwd pinned** to the fixture dir for the whole test (short relative display names
   in Path fields and argv echoes; runner inherits cwd, sets none itself — recon B §6).

---

## 7. Secret hygiene (first-class bar)

- **Demo-data-only rule (machine-enforced).** The only secret-class values the harness
  may ever drive are the three published BIP-39 vectors S0/S1/S2 (printed in
  Examples.md:209/215, 279/312, 284/329, 1491-1493; fingerprints
  `73c5da0a`/`b8688df1`/`28645006`). The driver asserts every value routed to a
  secret-classified widget (per the taxonomies below) is a member of that allowlist —
  fail loud otherwise. No fixture in either repo contains any secret not already
  printed in Examples.md (recon C's verdict: none exists; J3/J4/J5 are xpub-only).
- **Masked-by-construction, correctly attributed.** The journeys' secret entry paths
  ride the **`SECRET_SLOT_SUBKEYS`** (SlotEditor rows — `slot_editor.rs:46-52`,
  persist-drop `persistence.rs:111-121`) and **`SECRET_NODE_TYPES_ARGV`** (composite
  `phrase=` nodes — `widget.rs:604-606`) taxonomies, **not** the I3 64-flag census
  (they are a slot-subkey and a node-dynamic composite, not classified flags). The
  spec/tests must cite the sibling taxonomies; claiming 64-list coverage would be
  false precision. Their masking is already pinned by dedicated suites
  (`slot_secret_mask_v0_38_0.rs`, `argv_assembler_slot.rs`, i3 composite cells).
- **Pixel-channel + text-channel guards in the tutorial harness:** for every secret
  step, assert (a) the masked argv string (`render_copy_command_masked`) contains no
  allowlisted phrase substring (mask sentinel `••••`, `invocation.rs:137`); (b) the
  confirm-modal shot is taken only after the same no-plaintext assertion over the
  modal's masked token list; (c) `<stem>.stdout.txt` transcripts MAY contain the
  published demo phrases/derived material exactly as Examples.pdf already prints them
  — allowed by the demo-data-only rule, and stated in the book's front matter.
- **No persistence:** `state_path = None` (nothing is written); RunResults
  zeroize-on-drop (`runner.rs:48-61`); fixtures contain no secrets to leak.
- The reader-facing chapters teach the GUI's own hygiene surfaces as content: masked
  fields, the paste warning, the confirm modal, argv masking in the output pane.

---

## 8. Gates (reusable vs new)

**Reused unchanged (GUI leg):** snapshots-job recipe (lavapipe/backend/adapter guard,
`.new.png` census pattern, `.diff.png` artifacts, `UPDATE_SNAPSHOTS` regen flow); dify
0.6 + remediation ladder; secret `default_value` assertion pattern
(`gui_form_snapshots.rs:188-229`).

**New (GUI leg):**
- `tutorial-snapshots` CI job = snapshots recipe + pinned-CLI install (§3.1d).
- `pinned-tier-version-gate`: hard `--version` probe of every spawned CLI against the
  pinned tier before any render (§3.1b, §6 item 4) — runs in local regen AND CI.
- Dual census: shots (`.new.png` == manifest count 51) AND transcripts (regenerated
  `.stdout.txt` byte-diff + count == run-step count, incl. `shots: 0` steps).
- Per-step exit-code + chaining + secret-allowlist + driven-field-visibility +
  `SAME-FRAME-COMPLETION` assertions (§3.1b, §5.4, §7) — these run inside the same
  gated test.

**Reused unchanged (book leg):** markdownlint, cspell, lychee --offline, pandoc
build + `src="data:image/png"` embed-census pattern, `include-transcript.lua`,
pinned-clone plumbing (`manual-gui.yml:41-63`), release/gh-pages job shape.

**New (book leg), as lint.sh phases 10–12 + workflow wiring:**
- Phase 10 `verify-tutorial-figures`: byte-compare `figures/tutorial/*.png` against
  the pinned clone's `tests/snapshots/tutorial/*.png`; bidirectional census against
  `manifest-stems.txt` (orphan / gap / drift = fail) — the `verify-figures-gui`
  pattern (`lint.sh:204-247`) with manifest-derived expected counts (no hardcoded 51).
- Phase 11 `verify-tutorial-transcripts`: same byte + census discipline for
  `transcripts/tutorial/*` against the pinned clone's corpus.
- Phase 12 `tutorial-xref`: every manifest stem's figures + transcript are each
  embedded exactly once in `tutorial/*.md`, and every embed resolves to a manifest
  stem (the `check_gui_form_xref.py` pattern).
- Build job: `make gui-example-pdf gui-example-html` + embed census keyed to the
  manifest count; release job attaches `gui_example.pdf`; committed
  `docs/gui_example.pdf` refreshed in the shipping PR.
- Existing phases 1–3 extended to glob the new `tutorial/` sources (verify globs —
  markdownlint/cspell/lychee configs may be dir-scoped today).

---

## 9. Phases (each R0-gated; post-impl adversarial review per phase + whole-diff)

- **P0 (GUI, worktree):** spike per §4. Deliverable: spike report + ratified
  {window size, scroll mechanism, drive discipline, corpus budget} + GO/STOP.
- **P1 (GUI):** app-shell extraction (§3.1a) + invariants (byte-identical production
  behavior; `--no-default-features` builds; full `cargo test` suite green — house
  rule: full-package suite, not targeted); toolkit pin bump v0.74.0→v0.75.0 +
  the pre-scoped zero-flag delta catch-up (§3.1e); harness skeleton incl. the
  `pinned-tier-version-gate` + the `SAME-FRAME-COMPLETION` assertion + the
  `spawn_and_capture` pointer comment (§3.1b) + Chapter 0/J1 pilot steps (5
  shots) + `tutorial-snapshots` CI job live.
- **P2 (GUI):** full manifest (25 steps / 51 shots + transcripts), chaining,
  secret-hygiene + visibility assertions, corpus committed, censuses green;
  **`mnemonic-gui/CHANGELOG.md` entry** for the v0.56.0 leg (incl. the scroll-seam
  test-only-surface doc if S3 ratifies fallback iii — §11) →
  release + tag `mnemonic-gui-v0.56.0` (PR + CI-before-tag).
- **P3 (toolkit):** pin bump to v0.56.0; figures/transcripts byte-copies; chapters
  (prose adapted per recon C §5, version-skew callout, Chapter-0 conventions incl.
  no-titlebar caveat); Makefile targets; lint phases 10–12; workflow wiring (incl.
  the path-filter decision for `docs/gui_example.pdf`, §3.2c); start the
  **`docs/manual-gui/CHANGELOG.md` entry**; local full-gate run.
- **P4 (toolkit):** committed `docs/gui_example.pdf` + release-attach wiring; **cut
  the `manual-gui-v*` tag that ships the attach** (`manual-gui-v1.2.0`;
  `manual-gui.yml` is tag-triggered on `manual-gui-v*`, latest precedent
  `manual-gui-v1.1.0`) — OR explicitly defer the attach to the next manual-gui tag
  as a stated decision in the shipping PR, never an omission (R0 I3); finalize the
  `docs/manual-gui/CHANGELOG.md` entry (BOTH CHANGELOG sites now covered — GUI-side
  landed in P2); README pointers (both READMEs per release ritual if they reference
  doc artifacts); FOLLOWUP filings/flips (§11); ship.

Cross-repo ordering is strict: GUI tag before toolkit pin bump (checked-in-corpus +
pin flow, recon E7). Toolkit leg has **zero `src/` changes** (no version-site ritual
beyond docs; no vendor re-vendor; no ToolkitError ordering concerns).

---

## 10. Non-goals

- OS titlebar / window decorations (kittest renders egui content only — locked,
  documented reader caveat). Light theme; alternate ppp; font variations.
- Animated capture (GIF/video) or per-frame interaction sequences.
- `bitcoin-cli` / Bitcoin Core external steps as *gated* content (prose callouts with
  clearly-marked pre-captured external output, exempt from verify gates and so marked
  in the chapter, like the manual's existing external-output convention).
- Examples.pdf Appendix B (`mnemonic-depth2` experimental build) — prose pointer only.
- Replacing or regenerating `docs/Examples.pdf` (it remains the canonical CLI book at
  its own pin); byte-parity between the tutorial's outputs and Examples.pdf's printed
  v0.55.3 outputs.
- Re-basing the existing 61-form gallery onto the whole-window path, or adding a
  pane-faithfulness guard for the OLD mirror helpers (filed as FOLLOWUPs, §11).
- Extending schema_mirror to `--json` wire-shapes (existing FOLLOWUP, untouched).
- UX/layout critique of the shell (the harness renders what exists).

---

## 11. FOLLOWUP bookkeeping

File at cycle start (toolkit `design/FOLLOWUPS.md` + `docs/manual-gui/FOLLOWUPS.md` +
mnemonic-gui `FOLLOWUPS.md` with cross-citing `Companion:` lines per house rule):

- `gui-example-tutorial-book` — the cycle's tracking entry; flips to RESOLVED in the
  shipping commit (status-flip discipline).
- `examples-pdf-un-ci-gated` (NEW, surfaced by recon E5): `docs/Examples.pdf` is
  generated but has zero CI gating and a hard-stale `mnemonic 0.55.3` pin
  (`gen.sh:22`) while the tier is v0.75.0 — candidate for a Cycle-C-FULL-style
  verify gate + regen. Out of scope here; catalog only.
- `ui-harness-mirror-vs-real-window` (NEW, GUI repo): after the P1 extraction, the
  existing form-only suites still render via hand-copied mirrors
  (`ui_harness/mod.rs:419-536`, incl. the dead `[Run]` button and no pane mirror);
  evaluate re-basing them on the extracted real `ui()` (would retire the
  mirror-drift class). Catalog only; not this cycle.
- If the spike ratifies the scroll-offset seam (S3 fallback iii): note the seam as a
  documented test-only surface in the GUI CHANGELOG entry (not a FOLLOWUP; just
  lockstep docs).

---

## 12. Acceptance criteria (cycle-level)

1. `gui_example.pdf` builds from committed, gated sources; every figure and every
   output block is machine-generated, censused, and byte-anchored to the pinned GUI
   tag; lychee/markdownlint/cspell green over the new book.
2. All 25 steps / 51 shots render the REAL application window (post-extraction `ui()`
   loop) at one fixed size; every post-Run shot's pane was populated by a real pinned
   CLI execution in the same test — tier-verified up front by the
   `pinned-tier-version-gate` and pinned per step by the `SAME-FRAME-COMPLETION`
   assertion (§3.1b); refusal steps show real non-zero exit badges.
3. Secret material in the entire pipeline ∈ {S0, S1, S2} (machine-asserted); every
   secret entry renders masked; argv echoes and modal shots contain `••••`, never
   plaintext.
4. Production GUI behavior unchanged (extraction is pure relocation; full suite +
   snapshots + schema_mirror + `--no-default-features` all green); no clap-surface
   change → schema-mirror lockstep satisfied vacuously.
5. R0 GREEN before code; per-phase and whole-diff adversarial reviews GREEN before
   ship; all agent reports persisted to `agent-reports/`.
