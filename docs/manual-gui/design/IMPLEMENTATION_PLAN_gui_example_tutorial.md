# IMPLEMENTATION PLAN — `gui_example.pdf`: the GUI tutorial book (cycle `gui_example_tutorial`)

**Spec:** `SPEC_gui_example_tutorial.md` (R0-GREEN ×2; §3 architecture, §5 manifest/book, §6 determinism, §7 hygiene, §8 gates, §9 phase skeleton; §3.2c AMENDED 2026-07-05 by USER decision — `gui_example.pdf` release-attach-only, pointer-in-repo).
**P0:** `agent-reports/gui-example-p0-spike.md` (GO; 5-rule lookup discipline; measurements) + `agent-reports/gui-example-p0-r0-review.md` (GREEN 0C/0I; **4 RATIFICATIONS + the F1/F2 scope RULING are BINDING on this plan**; 6 Minors m1–m6 folded herein).
**Status:** plan-R0 r1 (RED 0C/2I/5m) folded 2026-07-05 (incl. the USER release-attach-only decision); awaiting scoped round-2 (house gate: NO implementation before GREEN 0C/0I).
**Source SHAs (cites grep-verified at write time, 2026-07-05):** toolkit `master@aa3d61bb`; mnemonic-gui `master@0d4429d` (v0.55.0); spike branch `spike/gui-example-p0@80f7eb8` (extraction commit `29777ee`); toolkit `docs/manual-gui` pin = `mnemonic-gui-v0.55.0`, lint = 9 phases (`lint.sh:78-194`).
**Phase map vs spec §9 (renumbering only — no scope moved across a leg boundary):** spec-P0 = DONE (spike + P0-R0 GREEN). Spec-P1+P2 (GUI) = **plan-P1** (one leg, one branch, one PR, one tag). Spec-P3 (toolkit) = **plan-P2 + plan-P3** (infra/gates split from editorial completion so each phase ends locally GREEN). Spec-P4 = **plan-P4**. Every spec-§9 deliverable appears exactly once below.

## GOTCHAS (read before every dispatch; repeat in every implementer brief)

- **GUI test runs:** `cargo test --jobs 2` ALWAYS (local linker-OOM; not a CI issue). Per-phase reviews run the FULL package suite, never targeted `--test` lists.
- **Clippy BOTH configs** on the GUI: `cargo clippy --all-targets -- -D warnings` AND the `--no-default-features` build/check (the headless CI job). `cargo build --no-default-features` must stay green after every GUI-src-touching step (this class of edge bit the form-renders cycle twice).
- **NO fmt, either repo.** Never `cargo fmt --all` (toolkit mlock.rs g6 exemption); do not fmt the GUI at all (no fmt gate; the extraction relocation stays verbatim).
- **Commit trailer (all commits, both repos):** `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>` + `Claude-Session: https://claude.ai/code/session_01AXCxehvWYFoCE2TDpcV2Db`. Stage paths explicitly — no `git add -A`.
- **mnemonic-gui master branch-protection: VERIFY AT SHIP, do not assert.** The visual-track plan-R0 round-1 verified master UNPROTECTED; the required-`snapshots` rule was added later — the current state is checked at ship time (plan-R0 r1 m1). The dual-path handling is safe either way: release/version commits ride the PR flow or an explicit admin bypass (decided at ship — visual-track m10 precedent). `gh pr merge` local-cleanup may fail under a worktree while the server merge succeeds — verify via `gh pr view --json state,mergeCommit`.
- **Local manual-gui gates:** `RUSTUP_TOOLCHAIN=stable` + `MANUAL_GUI_UPSTREAM_ROOT=<FRESH clone of the pinned tag>`. The sibling-checkout default (`../mnemonic-gui`) is the **`.new.png` false-RED trap** — a dirty/wrong-ref sibling makes verify-figures-gui/verify-examples-gui compare against the wrong corpus and litters `.new.png`; always clone the tag fresh into scratch space.
- **NO background watchers/monitors in any agent brief.** No `run_in_background` loops, no polling agents. Poll git/CI ground truth inline between steps.
- **Review dispatches = opus.** Every R0/post-impl review in this plan is an opus-tier adversarial dispatch; reports persist VERBATIM to `agent-reports/` BEFORE the fold-and-commit step. Post-impl folds RE-ENTER the review loop (re-dispatch a scoped convergence review; never self-verify and ship).
- **Cross-repo ordering is strict:** GUI tag `mnemonic-gui-v0.56.0` BEFORE the toolkit pin bump (checked-in-corpus + pin flow). Toolkit leg has ZERO `src/` changes — any pressure to touch toolkit `src/` (or `cmd/gui_schema.rs` projections) is a STOP (see the ledger).
- Single implementer subagent per phase in a worktree; TDD; no parallel re-implementations.

## BINDING RULINGS carried from the P0-R0 (the plan's fixed points)

1. **GO ratified** on the user-locked contract (whole-window, real `ui()`, real Run clicks, populated panes, masked-everywhere).
2. **Scroll = injected `PointerMoved` + `Event::MouseWheel{unit: Point}` + `run()`**, deltas manifest-recorded; `with_max_steps(64)`; the `vertical_scroll_offset` seam stays an UNUSED fallback (NOT added to production; the §11 CHANGELOG test-only-surface doc is therefore NOT triggered — state this in the P1 CHANGELOG entry).
3. **Window size = 920×720 logical / 1840×1440 physical @ ppp 2.0** (production default seed, `main.rs:46`), single global size.
4. **Corpus budget = HARD ≤ 20 MiB, target ≤ 15 MiB**, re-measured at the full 51-shot manifest; pre-committed trim levers (prune `-form2` to the driven-field-visibility minimum; ONE representative modal shot). A named budget assertion in the harness census — not a footnote.
5. **F1 = variant (a):** GUI-render-scoped `--template` unset/"(none)" affordance for **export-wallet only** — a NEW R0-gated scope item (P1.2). Variant (b) (conditional carve-out) REJECTED (would drift from the toolkit's projected rules and force a paired toolkit `src/` change).
6. **F2 = route-around, NO src change:** J2/J3 engrave-shape bundle steps ride `--descriptor` TEXT (chaining-proven); a GUI-only `has_descriptor_file → --template Disabled` mutex arm is REJECTED (toolkit-projection-divergent). Any hardening is a separate paired cross-repo item — never folded silently.
7. **F3/F6/F7 dispositions:** F3 = prose (Chapter 0 + divergence pass cover materialized-default argv tokens); F6 = sequencing (pin + `pinned_version` string BEFORE any capture; the version gate is the machine backstop); F7 = accept ballooned descriptor-text panes as viewport-faithful + a named 920×720 legibility check.
8. **PR #29:** reuse extraction commit `29777ee` (cherry-pick onto the post-pin-bump base); DISCARD the spike test/workflow/PNGs/fixtures (never cherry-picked — they live only in `1a1a826`/`80f7eb8`); author the real harness FRESH inheriting the spike's proven mechanics in intent; do NOT merge #29 — CLOSE it with a pointer to the P1 PR.
9. **Click semantics (spec-round-2 m1, spike-proven):** every Run and modal-Run click = queue click → assert `last_run.is_none()` (non-vacuity) → exactly ONE `harness.step()` → assert `last_run.is_some()` (or `last_run_error` for detect-fail steps) BEFORE any further stepping → THEN `harness.run()` to settle → snapshot. Failure message cites the populated-pane contract, SPEC §6.5.
10. **Counting language (spec-round-2 m2):** "25 SHOT-BEARING steps / 51 shots"; the manifest contains MORE total steps (`shots: 0` transcript runs — J2 devices-1/2 converts, J4 NUMS pair, J3/J4 `--json` chain feeds). All censuses derive from the manifest; nothing hardcodes 25 or 51.

---

## LEG 1 — mnemonic-gui (plan-P1; branch `feat/gui-tutorial-corpus` off `master@0d4429d`)

### P1.1 — pin bump v0.74.0 → v0.75.0 (FIRST commit; F6 pin-before-capture) + FOLLOWUP filings

**Files (the full site list — the spec's "two edits" phrasing undercounts; these are the verified sites):**
- `Cargo.toml:76` — git-dep tag `mnemonic-toolkit-v0.74.0` → `mnemonic-toolkit-v0.75.0` (+ `Cargo.lock` refresh, dep-graph only).
- `pinned-upstream.toml:22` — `[mnemonic].tag` (documentary-only cross-cite per the file's own header — the md/ms/mk tags in that file are the load-bearing ones for the pinned-CLI install; this bump is parity/documentation, nothing reads it as load-bearing — plan-R0 r1 m4).
- `src/schema/mnemonic.rs:4620` — `pinned_version: "mnemonic 0.74.0"` → `"mnemonic 0.75.0"` (the F6/I1 label-honesty edit; renders ONLY in the real window `main.rs:517-518`, NOT in the 61-form corpus or `.gui` renders → zero corpus impact from this commit).
- `FOLLOWUPS.md` (GUI repo): file `ui-harness-mirror-vs-real-window` (catalog-only, spec §11) + the cycle's `gui-example-tutorial-book` companion entry, cross-citing the toolkit entries (filed in P2.1). The forward-reference to not-yet-filed toolkit entries is inherent to cross-repo lockstep (each repo's FOLLOWUPS.md is only editable from its own leg) and is resolved by the Companion-line convention — acknowledged, not a defect (plan-R0 r1 m3).
**Tests-first:** none new — this commit's gates are the EXISTING suites. **Gates:** full suite `--jobs 2` green; `schema-mirror` job GREEN (expected ZERO-flag delta — verified between the tags at spec-R0 round 1: only `cmd/inspect.rs` + `error.rs` changed; the gate run is the empirical confirmation, budgeted mini-catch-up ONLY if it REDs); `msrv` job green (toolkit 0.75.0 dep at the 1.88 floor); `snapshots` 61/61 unchanged; clippy both configs.
**STOP note:** if schema-mirror reveals a NON-zero flag delta, stop and re-scope — the "zero-flag" premise is load-bearing for the vacuous paired-PR claim (spec §3.1a) and the P2 anchor-delta expectations.

### P1.2 — app-shell extraction: cherry-pick `29777ee` (pure relocation)

**Files:** `src/app_window.rs` (new, `#[cfg(feature="gui")]`; `MnemonicGuiApp` struct+impls+`render_exit_badge`+`spawn_and_capture`+`AUTOSAVE_INTERVAL`; `new(cc,…)` OS-effects wrapper / `new_headless(app_state, loaded, state_path)` pure ctor / `ui(&mut self, ctx)` with `update()` delegating); `src/main.rs` → 82-line bootstrap; the two source-scan tripwires (`paste_warn_wiring_v0_40_0`, `r7_no_auto_repair_removal`) extend scans to `app_window.rs`, `main.rs` retained — all INSIDE the cherry-picked commit.
**Tests-first:** N/A (relocation; the existing suite is the oracle). NOT fmt'd; NOT re-authored — a parallel re-implementation is prohibited (single-implementer rule; the commit is CI-proven on PR #29: build+snapshots+schema-mirror+headless all green).
**Gates:** full suite `--jobs 2` (0 fail); `cargo build --no-default-features`; clippy both configs; `snapshots` 61/61 byte-green; production behavior byte-identical (pure relocation — the P1 post-impl review verifies no semantic hunk). Add the **`spawn_and_capture` pointer comment** citing the populated-pane contract (SPEC §6.5) — the spec-mandated code-side breadcrumb (do it here, where the fn now lives).

### P1.3 — F1: export-wallet `--template` unset affordance (OWN R0-gated sub-scope; the cycle's ONLY src-behavior change)

**Pre-impl mini-R0 (MANDATORY, opus):** a short design note (in the P1 worktree, persisted as `agent-reports/gui-example-p1-f1-design-r0-round-N.md`) ratifying the implementation shape BEFORE code. Recommended shape for the note (plan's default, minimal blast radius): keep the `opts[0]` materialization for the virgin form (closed dropdown still shows `bip44`; virgin pixels unchanged) and add a selectable unset entry rendered "(none)" via the existing `display_or("(none)", …)` machinery (`widget.rs:528-535`, the archetype `--preset`/`--spec` pattern); selecting it clears/empties `--template` in `state.values` → `has_value` false (`schema/mod.rs:388`) → `conditional::export_wallet` (`conditional.rs:596-601`) leaves `--descriptor` enabled. The tutorial then DRIVES the unset as a step action (an honest teaching moment: "clear Template to unlock Descriptor").
**The mini-R0 MUST CHOOSE between the two self-consistent sub-shapes (plan-R0 r1 I1) — not pre-declare the structural-render gates inert — and record the book-fidelity consequence of the choice:**
- **A1 (archetype-style; the plan's default): the unset sentinel is enumerated in the per-flag `opts`.** `render_emit::render_form` enumerates a dropdown's `opts` inline (`render_emit.rs:678` `dropdown[{}]`; `:698-702` maps `""`→`(none)`) — the archetype already proves the effect (`gui_render_emit.rs:58` renders `dropdown[(none),decaying-multisig,…]`). Under A1, export-wallet's structural render moves `dropdown[bip44,…]` → `dropdown[(none),bip44,…]`, re-pinning per the artifact-surface census below. The sentinel MUST live in a per-flag list distinct from the shared `TEMPLATES` const, and the mini-R0 must CONFIRM the per-flag `choices` gate stays inert against the toolkit projection (else it forces a forbidden paired toolkit change). **Book fidelity: the structural render DEPICTS the taught "(none)" unlock** — the emit is honest about the affordance the tutorial drives.
- **A2 (opts = `TEMPLATES` unchanged; a separate per-flag allow-unset the widget honors):** every listed gate stays inert INCLUDING `gui_render_emit` — **but the structural emit then does NOT depict the unset affordance the tutorial teaches** ("clear Template to unlock Descriptor"): the structural render / gallery cross-reference the book uses under-reports the widget, and `gui_render_faithfulness` will NOT catch the divergence (its axes are per-flag presence / disabled / control-class / secret-masking, `gui_render_faithfulness.rs:1-30`; option-list contents are explicitly OUT of that gate) — a silent doc-honesty gap. Choosing A2 requires NAMING and ACCEPTING that gap explicitly in the design note.
**Hint-text interaction: NULL (plan-R0 r1 I1(d)).** Export-wallet `--template` is a `Dropdown` with `default_value: None`; `hint_text_defaults` covers *Text* flags with schema defaults (e.g. compare-cost `--feerate <hint:1.0>`, `gui_render_emit.rs:132-135`) and is correctly inert here regardless of A1/A2.
**BINDING constraints (P0-R0 RATIFICATION 4 — the mini-R0 may refine within these, never past them):**
- Scoped to export-wallet's `--template` rendering/materialization. **NO `""` added to the shared `TEMPLATES` const** (`schema/mnemonic.rs:69` — feeds bundle/verify-bundle/export-wallet + the `SINGLE_SIG_TEMPLATES`/`MULTISIG_TEMPLATES` partition gate `schema_mirror.rs:599-715` + toolkit `dropdown_value_in` predicates).
- The emitted conditional RULES are unchanged (template set ⇒ `--descriptor` Disabled, exactly the toolkit projection; unset ⇒ enabled because `has_value` is false — not because a rule changed).
- Clap-flag-name set unchanged; mirrored/projected template value-set untouched.
**Tests-first (TDD; all in the always-run suite, not env-gated):**
1. **Reachability:** drive a virgin export-wallet form to the `--descriptor` TEXT arm (select "(none)", type a descriptor, assert `--descriptor` enabled + argv carries it + no `--template` token) — the F1 regression test.
2. **Conditional-drift regression:** `gui_schema_conditional_drift` stays GREEN UNCHANGED (assert zero rule-set delta) — the variant-(b) tripwire.
3. **Materialization/`has_value` unit tests:** unset path yields no `--template` in assembled argv; re-selecting a real value re-disables `--descriptor`.
4. **Hint-text/(none) display:** the sentinel renders "(none)", never leaks `""` into the argv echo or the copy-command string.
**Adjacent-gate assessment (EXPLICIT, per-surface statuses — the P0-R0's mandated list, statuses corrected per plan-R0 r1 I1):**
- **Expected INERT, each verified by RUNNING it:** flag-name `schema_mirror` (`schema_mirror.rs:52-124`); `gui_schema_conditional_drift`; template-partition drift (`schema_mirror.rs:599-715`); per-flag `choices` (under A1: CONFIRM inert vs the toolkit projection — a binding mini-R0 check, see above); `archetype_schema_mirror`; the 61-form snapshot suite (closed-form gallery — see census).
- **`hint_text_defaults` — inert, interaction NULL:** Dropdown, not a hinted Text flag (see the mini-R0 brief).
- **`gui_render_emit` — NOT "verified inert": expected RE-PIN under A1.** The export-wallet structural render is an exact-ASCII `assert_eq!` pin (`gui_render_emit.rs:95`, test body `:93-115`); under A1 it moves to `dropdown[(none),bip44,…]` — a NAMED, EXPECTED P1.3 GUI-repo test-source edit, not a ripple. Under A2 it stays green, at the doc-honesty cost recorded above.
- **`gui_render_faithfulness` — expected green under BOTH shapes, but NOT an oracle for this change:** its axes exclude option-list contents (`gui_render_faithfulness.rs:1-30`), so it moves only if both projections move together and it cannot catch an A2 emit/widget divergence on the option set.
**Re-pin blast radius (empirical census PER ARTIFACT-SURFACE — plan-R0 r1 I1; the round-1 "zero to two 61-form PNGs / ≤2 of 61" phrasing measured the wrong surface):** run the FULL suite; enumerate every pinned artifact that moves. Expected movement under A1:

| Artifact surface | Expected under A1 |
|---|---|
| 61-form gallery PNGs (`gui_form_snapshots`; toolkit `figures/gui/mnemonic-export-wallet.png`) | **≈0 — inert.** The gallery renders the CLOSED form; popup contents (the added "(none)" entry) are not rendered, and virgin `opts[0]` materialization keeps the closed render at `bip44`. |
| `gui_render_emit.rs:95` exact-ASCII export-wallet pin (GUI-repo **test source**) | **RE-PIN in P1.3** — `dropdown[bip44,…]` → `dropdown[(none),bip44,…]`; a named P1.3 source edit. |
| `gui_render_faithfulness` | Green (option-list not an axis) — not a census signal either way. |
| toolkit `transcripts/gui/mnemonic-export-wallet.gui` | **RE-PIN on the toolkit leg**, regenerated by `verify-examples-gui` from the new pin (toolkit `Makefile:267-276`) at the P2.1 "F1 ripple lands HERE" intake step. (The round-1 text's "deferred to P2.2" label is corrected to P2.1 — the pin bump + `verify-examples-gui` re-run are P2.1 steps; no ordering change.) |
| `expected_gui_schema_inventory.json` anchors | **ZERO** iff the `opts`/projection surface is untouched by the chosen A1 mechanics — VERIFY by running, mini-catch-up only if RED. |

Under A2: every surface inert (including the emit) — at the named doc-honesty gap. **STOP tripwire (per-surface, replacing "N of 61 forms"):** ANY movement outside this table — any OTHER form's gallery PNG, any OTHER subcommand's emit pin or `.gui` transcript, any unexplained anchor delta — = scope smell → back to the mini-R0.
**Gates:** the 4 TDD tests green; adjacent-gate list all green; full suite; clippy both configs; `--no-default-features`. **Scoped post-impl review** (opus) on the F1 diff → GREEN before P1.4 starts.
**STOP note:** if the affordance cannot stay inert on `gui_schema_conditional_drift` or the partition gate under ANY compliant shape, STOP — the alternative is a paired toolkit change (a USER decision; it breaks the zero-toolkit-src invariant and the tag ordering).

### P1.4 — the REAL tutorial harness + manifest + fixtures + pilot steps (Ch 0 + J1)

**Files:**
- `tests/gui_tutorial_snapshots.rs` — env-gated (`GUI_TUTORIAL_SNAPSHOTS=1`, early-return-skip with an `eprintln!` marker, the `gui_form_snapshots.rs:109-116` pattern); ONE sequential `#[test]` (stable frames; single `set_current_dir` to the fixture dir — spec §6.6); `Harness::builder().with_size([920.0,720.0]).with_pixels_per_point(2.0).with_max_steps(64).build_state(|ctx, app| app.ui(ctx), app)` over `MnemonicGuiApp::new_headless(fixed_appstate_all_found(), None, None)` (demo-seed baseline asserted, not assumed — spec §6.3).
- `tests/tutorial/` — the step manifest (Rust module: typed step table = stem, tab, subcommand, drives, wheel-delta scroll offsets, `shots` count, expected `exit_code`, secret-allowlist flags, chain-parse spec) + `manifest-stems.txt` EMITTER (committed file listing every corpus artifact basename; a harness assertion regenerates + diffs it — the single derivation source both repos' censuses read).
- `tests/tutorial/fixtures/` — byte-copies of the four tracked `.examples-build/` primitives under tutorial display names; `policy.json` gets the `opensessame`-digest rewrite (`gen.sh:33-37`) ONCE at authoring; fixture README records source repo + SHA + transform (spec §3.1c). NO secrets beyond the S0/S1/S2 published phrases (manifest literals).
- **Named gates, fail-closed, inside the test:**
  - `pinned-tier-version-gate` — BEFORE any render or spawn: probe `<cli> --version` for every manifest-spawned CLI vs the pinned tier (schema `pinned_version` + `pinned-upstream.toml`); `panic!` on mismatch. Runs in local regen AND CI (it lives in the test). Include a unit-level negative (fake wrong-tier string ⇒ the gate fires) so the BITE is suite-pinned, not just spike-history.
  - `SAME-FRAME-COMPLETION` — per click, the ruling-9 single-`step()` semantics, at EVERY Run/modal-Run site including `shots: 0` steps.
  - Driven-field visibility — each driven widget's rect intersects the viewport in ≥1 captured offset (spec §5.4; spike-prototyped).
  - Secret-allowlist — every value routed to a `SECRET_SLOT_SUBKEYS`/`SECRET_NODE_TYPES_ARGV`-classified widget ∈ {S0,S1,S2}; whole-tree no-plaintext assertion (every AccessKit label+value) at the spike's four checkpoints for secret steps; masked argv/modal (`••••`) asserts.
  - **Corpus-budget assertion** — after all steps: total tutorial PNG bytes HARD ≤ 20 MiB (panic), report-line if > 15 MiB target.
- **Drive mechanics = the spike's 5-rule lookup discipline, verbatim in intent:** (1) unique-label direct query (subcommand combo `by().role(ComboBox).label("subcommand")`; buttons by exact label); (2) row-anchored geometric lookup for unlabelled inputs (exact-label match kills `--descriptor`/`--descriptor-file` prefix collisions); (3) block-bounded repeating-row targeting (md1 rows between headers; one `+ add` per band); (4) Window-subtree scoping for modal-vs-action-bar `Run` (assert the 2-button collision, then scope); (5) **Escape after every popup option click** (F4 — AccessKit clicks never fire `clicked_elsewhere()`). Scroll = ruling 2. Drives are pointer-free and work off-viewport — only SHOTS need scroll.
- Per-step artifacts: `tests/snapshots/tutorial/<stem>-form.png` (+`-form2…` at recorded offsets), optional `<stem>-modal.png`, `<stem>-run.png`, `<stem>.stdout.txt` (+ `.stderr.txt` when non-empty, `.exit.txt`) — pane pixels ↔ transcript from the SAME RunResult. `shots: 0` steps run the identical GUI-driven path, PNG capture skipped.
- Pilot steps in this sub-phase: **Chapter 0 orientation (demo-seed baseline) + J1 (form/modal/run)** — 4 shots — proving the full pipeline before the corpus fan-out. (4 follows the authoritative spec-§5.3 table; spec-§9's "5 shots" is a loose parenthetical — §5.3 governs. Plan-R0 r1 m2.)
**Tests-first bullets:** the named gates + their negatives land BEFORE the pilot steps; manifest-stems emitter unit test (uniqueness, ordering, regen-diff); allowlist checker unit test.
**Gates:** pilots byte-deterministic across two local runs; all named gates demonstrated (version-gate negative, SAME-FRAME at both click classes, no-plaintext checkpoints); full suite still green; `.gitignore` ALREADY covers `tests/snapshots/tutorial/**` — the existing `tests/snapshots/**/*.new.png` / `*.diff.png` / `*.old.png` globs are recursive, not forms-scoped (verified at plan-R0 r1 m5); confirmed covered, no extension needed.

### P1.5 — full manifest (25 shot-bearing steps / 51 shots + the `shots: 0` runs) + corpus commit

**Files:** manifest completion per spec §5.3 (Ch 0–5; J2 ×7 steps/15 shots incl. the all-seeds modal; J3 ×5/10 incl. the build-descriptor REFUSAL; J4 ×8/16 incl. depth-2 + BSMS refusals + compare-cost + NUMS export; J5 ×3/6; `shots: 0`: J2 devices-1/2 converts, J4 NUMS bundle/restore, J3/J4 `--json` chain feeds). Intra-journey chaining parses md1 chunks/descriptors out of the PREVIOUS step's captured `RunResult.stdout` and types them (J5 restore rows; J3/J4 restore chains) — no derived fixtures. **F2 routing:** engrave-shape bundle steps ride `--descriptor` TEXT (ruling 6). Refusal steps pin exact non-zero exits. **F7 named check:** open the ballooned descriptor-text `-run` shots and verify legibility at 920×720; record in the phase report (a growing/scrolling pane is viewport-faithful and CORRECT — do not resize, do not re-route; if genuinely illegible, STOP: the size is a P0 ratification, re-opening it is corpus-wide).
**Budget:** re-measure the REAL corpus total (ruling 4). If > 15 MiB: apply the pre-committed trims (prune `-form2` beyond the visibility-gate minimum; keep ONE representative modal shot — J1's — dropping J2-all-seeds') and record before/after. If still > 20 MiB: STOP (locked ALL-journeys scope vs budget = user decision).
**Gates:** 51 (or post-trim count) PNGs + transcripts committed; manifest-stems.txt committed + regen-diff green; two full local runs byte-identical (aggregate sha256 over `.new.png` + transcripts); budget assertion green; full suite `--jobs 2`; clippy both configs.

### P1.6 — `tutorial-snapshots` CI job + ship (PR → merge → release → tag → tag-run verification)

**Files:** `.github/workflows/build.yml` — new `tutorial-snapshots` job = the `snapshots` recipe (`build.yml:87-147`: lavapipe `mesa-vulkan-drivers`, `WGPU_BACKEND=vulkan`, CPU-adapter guard, `.diff.png` artifacts, rust-cache) **plus** the schema-mirror pinned-CLI install recipe (`schema-mirror.yml:59-133`; bare names on `$PATH`; cacheable `cargo install`); `GUI_TUTORIAL_SNAPSHOTS=1 cargo test --test gui_tutorial_snapshots`; **dual census** — `.new.png` count == manifest shot count AND regenerated transcript count == manifest run count AND byte-diff of regenerated transcripts vs committed; no path filter (existing triggers fire on PR + master + tag push — the tag run is Leg 2's provenance anchor). Red-corpus mitigation if the PR REDs on locally-generated bytes: the visual-track temp-`UPDATE_SNAPSHOTS`-flip + corpus-artifact-upload pattern; never merge red.
**Branch protection (named decision, not an omission):** do NOT add `tutorial-snapshots` as a required context this cycle — the standing user call (visual-track P1, 2026-07-01) is minimal footprint (`snapshots` only); the broader question is already FILED as `gui-branch-protection-scope`. The job still gates by green-before-merge convention + the tag-run verification.
**Ship sequence:** P1 post-impl phase review (opus, full-suite) → **Leg-1 whole-diff adversarial review** (opus) → GREEN → PR (state the vacuous paired-PR satisfaction: no clap-surface change; describe F1 as render-scoped) → CI green (all jobs incl. `snapshots` 61/61 + `tutorial-snapshots` censuses) → merge → **CLOSE PR #29** with a pointer to the merged PR (+ delete `spike/gui-example-p0`) → release commit 0.55.0→0.56.0 (`Cargo.toml` + `Cargo.lock` + `CHANGELOG.md` [incl. the scroll-seam-NOT-added note, ruling 2; the F1 affordance; the corpus + gates] + README self-pin; via PR or admin bypass per GOTCHAS) → **tag `mnemonic-gui-v0.56.0`** → **verify the tag-push run**: `snapshots` + `tutorial-snapshots` conclusions == `success` via `gh api repos/bg002h/mnemonic-gui/commits/<tag-sha>/check-runs` (EXPLICIT remote URL — Leg 2 executes where `origin` is the toolkit; fail-closed), recorded in the P1 report.

---

## LEG 2 — mnemonic-toolkit (plan-P2 + plan-P3; branch `feat/manual-gui-tutorial-book`; AFTER the tag)

### P2.1 — provenance + pin bump + corpus intake

**Step 0 (fail-closed):** the tag-run check-run verification above == `success` for BOTH shot jobs; only then pin-bump.
**Files:** `docs/manual-gui/pinned-upstream.toml` — `[mnemonic-gui].tag` v0.55.0 → v0.56.0; `toolkit-tag-implied` → `mnemonic-toolkit-v0.75.0`; md/ms/mk implied fields EXPECTED unchanged (P1 bumps only the toolkit pin) — VERIFY against the tag's own `pinned-upstream.toml`, don't assume. Refresh `MANUAL_GUI_UPSTREAM_ROOT` to a fresh v0.56.0 clone (GOTCHAS trap). `docs/manual-gui/figures/tutorial/<stem>*.png` + `docs/manual-gui/transcripts/tutorial/<stem>.{stdout,stderr,exit}.txt` — byte-copies from the pinned clone (`tests/snapshots/tutorial/`). The censuses read `manifest-stems.txt` DIRECTLY from the pinned clone (single source of truth; no toolkit copy to drift — decide the exact read path in-phase and document in lint.sh).
**F1 ripple lands HERE (per the P1.3 artifact-surface census, plan-R0 r1 I1):** re-run `verify-figures-gui` + `verify-examples-gui` + `gui-schema-coverage` against v0.56.0 — re-pin exactly the artifacts the P1.3 census moved. Expected under A1: **ZERO** `figures/gui/` PNGs (closed-form gallery inert) + exactly ONE `.gui` transcript (`transcripts/gui/mnemonic-export-wallet.gui`, regenerated by `verify-examples-gui` from the new pin — the toolkit-side counterpart of the P1.3 `gui_render_emit.rs:95` source re-pin); under A2: zero movement. `expected_gui_schema_inventory.json` anchor delta EXPECTED ZERO either way — verify by running, mini-catch-up only if RED. Any ripple beyond the P1.3 census table = STOP (unexplained drift).
**FOLLOWUP filings (toolkit side, cross-citing Companions):** `design/FOLLOWUPS.md` + `docs/manual-gui/FOLLOWUPS.md`: `gui-example-tutorial-book` (tracking; flips P4) + `examples-pdf-un-ci-gated` (catalog-only, spec §11).

### P2.2 — build plumbing + the three lint phases + chapter scaffolds (all gates GREEN at phase end)

**Files:**
- `docs/manual-gui/Makefile`: `gui-example-pdf` / `gui-example-html` targets (same pandoc→xelatex recipe, own title page; resource paths += `figures/tutorial` + `transcripts/tutorial` — follow the visual-track image-path constraint map already solved for `figures/gui`; HTML gate = POSITIVE embed census, `grep -c 'img src="data:'` == manifest shot count).
- `docs/manual-gui/tests/lint.sh`: phases **10 `verify-tutorial-figures`** (byte-compare `figures/tutorial/*.png` vs the pinned clone; bidirectional census vs manifest-stems — orphan/gap/drift = fail; the `verify-figures-gui` pattern `lint.sh:194-…`, counts manifest-derived, NO hardcoded 51), **11 `verify-tutorial-transcripts`** (same discipline for `transcripts/tutorial/*`), **12 `tutorial-xref`** (every stem's figures + transcript embedded exactly once in `tutorial/*.md`; every embed resolves to a stem — the `check_gui_form_xref.py` pattern). Renumber banners `N/9` → `N/12`. Extend phases 1–3 globs to `tutorial/` sources (VERIFY dir-scoping of markdownlint/cspell/lychee configs).
- `.github/workflows/manual-gui.yml`: lint job inherits the new phases via lint.sh; build job runs the gui-example targets + embed census (the CI build IS the gated PDF — see the P3 release-attach-only decision); release job attaches `gui_example.pdf` on `manual-gui-v*` tags. The former spec-§3.2c **path-filter decision is MOOT** under the 2026-07-05 USER release-attach-only decision: no `docs/gui_example.pdf` is committed, so there is no PDF path to filter on (tag pushes bypass `paths` regardless).
- `docs/manual-gui/tutorial/` chapter SCAFFOLDS: front/back matter + Ch 0–5 files embedding EVERY manifest stem (figures + `include-transcript.lua` fenced includes) with skeleton prose — so phases 10–12 + build + embed census go GREEN at P2 end (the split's purpose: mechanical completeness first, isolating census/xref wiring from prose).
**Tests-first bullets:** each new lint phase demonstrated FAIL-CLOSED before green (temporarily orphan one figure / drop one embed / perturb one transcript byte → phase REDs → restore).
**Gates:** `make lint` 12/12; `verify-examples-gui` + `verify-figures-gui` green at the new pin; `make gui-example-pdf gui-example-html` + embed census == manifest count; EXISTING book targets + gates untouched-green; `manual-gui.yml` green on the PR branch.

### P3 — editorial completion + `gui_example.pdf` (CI-built, RELEASE-ATTACH ONLY) + CHANGELOG

**Files:** full chapter prose — Examples.md narrative adapted per recon-C §5's enumerated adaptations; Chapter 0 conventions (no-OS-titlebar caveat; scrolling/viewport-faithful contract incl. the pane 180 px top-slice + full-transcript convention; the demo-seed baseline; **F3**: GUI argv echoes carry materialized defaults — the app's REAL argv, not Examples.pdf's minimal lines); the version-skew callout (own tier; explicit no-byte-parity disclaimer); cross-links to the form-gallery figures (§5.4 canonical-geometry reference); external `bitcoin-cli` steps as clearly-marked prose callouts (gate-exempt, house convention); **divergence pass** (spec §2E + F3): a transcript-vs-Examples.md table (refusal wording + argv-token divergence) in the phase report, callouts folded into prose. **`gui_example.pdf` is RELEASE-ATTACH ONLY — NOT committed (USER DECISION 2026-07-05, plan-R0 r1 I2; spec §3.2c amended):** the PDF is built + gated locally and in CI (`make gui-example-pdf` + the embed census) and its distribution channel is the `manual-gui-v*` tag workflow's release attach (P4); the repo carries a committed README/docs POINTER beside `docs/Examples.pdf` instead of the binary — the README pointers (toolkit README + `docs/manual-gui/README.md`) authored here link the latest `manual-gui-v*` release asset and are what satisfies "beside Examples.pdf". `docs/manual-gui/CHANGELOG.md` entry STARTED. cspell dictionary additions as needed.
**Gates:** `make lint` 12/12; both gui-example targets + embed census; open the PDF and spot-check ≥3 figures (the J1 modal, one scrolled `-form2`, one ballooned descriptor-text `-run` — the F7 print-legibility check) — no clipping, legible; `manual-gui.yml` green.
**Ship:** P2+P3 per-phase post-impl reviews (opus) → **Leg-2 whole-diff review** → GREEN → PR (note: toolkit leg has ZERO `src/` changes — no version-site ritual, no vendor, no ToolkitError concerns) → CI green → merge.

### P4 — the attach tag + FOLLOWUP flips (mechanical; no code)

1. Small master commit (toolkit direct-FF is house-permitted): finalize the `docs/manual-gui/CHANGELOG.md` entry (dated); flip `gui-example-tutorial-book` → RESOLVED in this shipping commit (status-flip discipline; verify `examples-pdf-un-ci-gated` + `ui-harness-mirror-vs-real-window` entries + Companion lines exist from P1.1/P2.1).
2. **Cut `manual-gui-v1.2.0`** (precedent form: `manual-gui-v1.1.0`; the workflow is tag-triggered on `manual-gui-v*`, paths bypassed on tags) → verify the release job attaches `gui_example.pdf` (+ the existing manual PDF) to the GitHub release — **the release attach is now the SOLE distribution channel for the PDF** (release-attach-only, USER DECISION 2026-07-05): a deferral-to-next-tag leaves the book unpublished as a PDF until the next tag, so deferral remains allowed ONLY as an explicitly recorded decision in the shipping PR, never an omission (spec R0 I3) — and the P3 README pointers must then say so. Known gotcha: gh-release TOCTOU (`|| true` pattern exists in-house); if the attach fails, fix-forward + re-tag per house precedent.
3. Ship report to the user (per-cycle history + FOLLOWUP burndown offer per standing preference).

---

## Review cadence (all opus; all persisted verbatim BEFORE folds)

| Gate | Artifact |
|---|---|
| THIS plan | `agent-reports/gui-example-plan-r0-round-N.md` → GREEN 0C/0I before ANY implementation |
| F1 design (pre-impl) | `gui-example-p1-f1-design-r0-round-N.md` |
| F1 scoped post-impl | `gui-example-p1-f1-postimpl-round-N.md` |
| P1 phase post-impl | `gui-example-p1-postimpl-round-N.md` (FULL suite run) |
| Leg-1 whole-diff | `gui-example-leg1-whole-diff-review.md` (pre-merge) |
| P2 / P3 post-impl | `gui-example-p2-postimpl-round-N.md` / `gui-example-p3-postimpl-round-N.md` |
| Leg-2 whole-diff | `gui-example-leg2-whole-diff-review.md` (pre-merge) |

Folds re-enter the loop (scoped convergence re-dispatch); if Agent-API dispatch fails mid-session, flag explicitly and defer the review to API recovery — never substitute inline self-review.

## RISK / SEQUENCING

- **F1 is the cycle's ONLY src-behavior change.** Blast radius: `form/`-side rendering/materialization for ONE flag on ONE subcommand; variant (a) keeps every drift gate clean BY CONSTRUCTION — the conditional rule-set is untouched (unset ⇒ `has_value` false ⇒ the projected mutex simply doesn't trigger; set ⇒ Disabled exactly as the toolkit projects), the shared `TEMPLATES` const is untouched (partition + choices + archetype mirrors inert), and the clap flag-name set is untouched (`schema_mirror` inert). The failure mode to watch is scope creep (a "general" materialization change — F3 explicitly does NOT license one) and unexplained pinned-artifact movement beyond export-wallet(±bundle): both are STOPs back into the F1 mini-R0.
- **Corpus ceiling:** 16.4 MiB/51 is a PROJECTION from an 11-shot mean; populated-pane card shots run heavier. The budget is a HARD in-test assertion (≤20 MiB) with pre-committed trims at >15 MiB — measured at P1.5, not trusted. The ~16 MiB PDF weight question is **RESOLVED by the 2026-07-05 USER DECISION (plan-R0 r1 I2): `gui_example.pdf` is RELEASE-ATTACH ONLY, never committed** — the toolkit repo carries only the corpus copies + a README pointer, so no per-corpus-change PDF re-commit ever enters git history (spec §3.2c amended accordingly). (Lossless PNG re-compression is REJECTED: it would break byte-identity between committed corpus and kittest-regenerated `.new.png`.)
- **Pin-bump schema ripple:** verified zero-flag between the tags at spec-R0 (only `inspect.rs`/`error.rs` moved); the P1.1 `schema-mirror` run is the empirical confirmation — the plan budgets a mini-catch-up ONLY on a RED, and a non-zero delta is a STOP (re-scope), not an in-flight absorb.
- **Determinism:** the spike proved same-env byte-identity + Vulkan↔GL ≤0.6 on the whole-window shot class (15/15 on PR #29, 2 runners, Mesa-delta included) — but the FULL corpus adds shot states the pilots didn't (BSMS forms, compare-cost, deep-scroll `-form2`). The remediation ladder + `.diff.png` artifacts + the red-corpus flip pattern are the tools; systemic cross-backend drift on a new state class = STOP with evidence (spec §4 menu — user decision).
- **Ordering:** pin-bump (P1.1) precedes ALL capture (F6); GUI tag precedes toolkit pin (recon E7); the toolkit leg starts only after the tag-run verification. The `shots: 0` runs are inside the version gate + transcript census by construction (manifest-derived).

## STOP / ESCALATION LEDGER (locked-contract wobbles — user decisions, never implementer choices)

1. `SAME-FRAME-COMPLETION` fires anywhere (async-runner behavior surfaced) → STOP; the seeded-`last_run` "fix" is the reserved downgrade (spec §4/§6.5).
2. Any toolkit `src/` edit pressure (variant (b), F2 mutex arm, `cmd/gui_schema.rs` projection changes) → STOP (breaks §12.4 + tag ordering; paired cross-repo item requires user sign-off).
3. Corpus > 20 MiB after both trim levers → STOP (ALL-journeys lock vs budget).
   - **RESOLVED 2026-07-05 (USER DECISION at the P1.5 budget STOP):** corpus measured 27.1 MiB (all 51 shots, 24 populated-pane shots); user chose RAISE THE CEILING and keep all 51 shots (over trim / GUI-only-fetch). The 20 MiB rationale (committed-PDF era) was already resolved by the release-attach-only decision. `BUDGET_HARD_MIB` = **32 MiB** (regen-drift headroom over 27.1). The trim levers stay documented but unused.
4. F1 cannot satisfy the binding constraints (drift-gate ripple under every compliant shape) → STOP.
5. Ballooned descriptor-text steps genuinely illegible at 920×720 → STOP (size is a P0 ratification; resizing is corpus-wide re-ratification).
6. Cross-backend > 0.6 on a shot class after the remediation ladder → STOP with evidence + the spec-§4 fallback menu.
7. Wrong-tier version-gate failure in CI → fix the pins; NEVER weaken or bypass the gate.
