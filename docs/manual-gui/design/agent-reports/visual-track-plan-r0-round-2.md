# R0 PLAN review — GUI visual screenshot track — round 2 (2026-07-01)

**Artifact:** `docs/manual-gui/design/IMPLEMENTATION_PLAN_gui_visual_screenshot_track.md` (post-fold of round-1's 0C/6I/6m).
**Round 1:** `agent-reports/visual-track-plan-r0-round-1.md`. **Spec:** `SPEC_gui_visual_screenshot_track.md` (R0-GREEN, re-read in full this round). **Charter:** confirm fold fidelity + hunt fold-introduced drift; settled points (P0-as-go/no-go, split-gate architecture, round-1's verified-clean list) not re-litigated.
**Verified live this round:** `mnemonic-gui` HEAD/origin-master = `a522a69` (the FOLLOWUP commit — pushed, HEAD == origin/master); `mnemonic-gui/FOLLOWUPS.md:998` (`gui-branch-protection-scope` — open, governance, content matches the plan's characterization: require-only-`snapshots` decided 2026-07-01, broader ~11-context scope deferred); the four extended-render fns still private in `tests/gui_render_faithfulness.rs` (`render_one_positional` :74, `render_positionals` :109, `render_action_bar` :121, `render_extended_form_harness` :127; `mod ui_harness;` :65); toolkit-side FOLLOWUP `manual-gui-visual-screenshot-track` live at `docs/manual-gui/FOLLOWUPS.md:23`; GUI tags are LIGHTWEIGHT (`git cat-file -t mnemonic-gui-v0.53.0` = `commit`, no `^{}` peel line on ls-remote).

## Verdict: RED — 0 Critical / 1 Important / 4 Minor

Five of the six Importants and all six minors folded faithfully — each checked against its round-1 prescription below, all clean. But the I2 fold, in rewriting the P0 trigger paragraph, **dropped the spike-artifact disposition**: the pre-fold plan called the P0 workflow a "throwaway" (round-1 quotes it verbatim); the revised text deletes that word, adds "`workflow_dispatch` may sit in the file as a post-merge convenience" (which points toward merging a throwaway), and no step anywhere removes `tests/spike_form_snapshots.rs` or the spike workflow before the P1 merge — on a single-branch/single-PR leg where they are merge-bound by construction. That is fold-introduced drift of exactly the class this round exists to catch (I7 below). It is a one-sentence + one-clause fold.

---

## Fold verification (I1–I6, m1–m6) — ALL FAITHFUL

- **I1 ✓** — P1's file list now opens with the promotion refactor: the four named fns from `gui_render_faithfulness.rs` → `tests/ui_harness/mod.rs` as `pub`, faithfulness re-pointed, explicitly "behavior-identical move — but it edits a shipped GREEN test file, so it is PLANNED here and covered by the P1 R0 (the faithfulness test must stay green, byte-for-byte same assertions)". Names/locations re-verified against source this round. Matches the prescription exactly, including the R0-coverage clause.
- **I2 ✓ (with the I7 residue)** — P0 trigger mechanics: `pull_request` event on a DRAFT PR; ≥3 runner instances via "Re-run all jobs"/empty commits — NOT dispatch; `workflow_dispatch` carries no pre-merge weight. P1 red-corpus mitigation is the ruled sequence: temp commit flipping the PR's own job to `UPDATE_SNAPSHOTS=1` → regenerated-corpus artifact → commit corpus → revert flip → (job re-runs in compare mode against the committed corpus) → never merge red. Mechanically sound. The ladder-disposition sentence is unambiguous: "DEFERRED-DOCUMENTED — post-merge-only by construction (default-branch rule), created only if systemic drift materializes (spec rung 4); it is NOT a P1 deliverable and nothing in P1 cites it as existing" — spec §5 rung 4 confirmed as the dispatch-regen rung. What the fold dropped is the spike file/workflow fate — see I7.
- **I3 ✓** — Explicit out-of-diff admin ship-step in P1's gate chain ("merge → the branch-protection ship-step → release commit → tag"), scope = require ONLY `snapshots` per the user's 2026-07-01 call, context name pinned to the job's declared `name:`. The FOLLOWUP reference is accurate and live: `gui-branch-protection-scope` at `mnemonic-gui/FOLLOWUPS.md:998`, commit `a522a69` pushed to origin/master; entry content (minimal-footprint decision + deferred broader-scope question, governance/ci tier, open) matches the plan's citation.
- **I4 ✓** — The live check is `cargo +1.88.0 check --locked --all-targets` (or `+1.88.0 test --no-run`), with the correct rationale ("plain `cargo check` never compiles dev-deps"), floor violations = SPIKE FINDING not fix obligation, CI msrv job scope explicitly unchanged. Verbatim-faithful.
- **I5 ✓** — P2 carries the corrected constraint map: lychee resolves image links FILE-relative and DOES check them; pandoc `--self-contained` on a missing image = WARNING + exit 0 (stated as empirically verified); xelatex runs from `build/`; `\graphicspath{{../}}` NOT `{{../figures/gui/}}` with the prepends-as-written rationale; mermaid absolute-path-via-env precedent named for a Lua rewrite; "no bare path form satisfies all 5 consumers" stated up front; decide-in-phase INSIDE the map. The embed census is stated as THE gate, twice — "The HTML gate is a POSITIVE EMBED CENSUS" in the file list and "the embed census is the gate, not the build exit code" in Risk — with the exact command (`grep -c 'img src="data:' build/m-format-gui-manual.html` == 61) in the P2 gate line. Not an addon. (One grammar nit on the OR-phrasing → m9.)
- **I6 ✓** — P0 renders ALL 61 ("the spec-§3 M2 measurements are mandated on the full set: write + size + time all 61"), threshold comparison anchored on 3 representative forms (small/secret-bearing/tallest) or extended for free, and step 3 + the gate list carry full-61 render + job wall-time cold-vs-cached + actual corpus size vs ~3.4 MB. Matches spec §3 M2 word-for-word.
- **m1 ✓** (750-overview discloses the PNG frame: flags + positionals + Run; sub-surface placeholder lines not depicted) · **m2 ✓** (concrete `gh api …/check-runs --jq` command as P2 step 0, recorded in the P2 report — one command-detail nit → m8) · **m3 ✓** ("likely 12, don't pin it in gate text", counted in-phase) · **m4 ✓** (Cargo.lock in BOTH the GOTCHAS ritual line and the P1 release-commit list) · **m5 ✓** (spike report at the full toolkit-side path) · **m6 ✓** (explicit lint.sh arg or `$SRC_DIR/../figures/gui`, TRANSCRIPTS_GUI precedent named, pick in-phase).

## Drift hunt (charter 2)

- **Draft-PR vs P1 "PR" — coherent by construction.** Leg 1 declares ONE branch (`feat/gui-form-snapshots`); P0's draft PR is opened from it; P1's "→ PR → CI green → merge" can only be that same PR (GitHub permits one open PR per branch/base pair) marked ready. Readable without a ruling; no contradiction.
- **Release ritual — complete and sanely ordered.** merge → branch-protection ship-step → release commit (Cargo.toml + Cargo.lock + CHANGELOG + README self-pin) → tag `mnemonic-gui-v0.54.0` → verify the tag-push snapshots run GREEN (the provenance anchor P2 step 0 then consumes). All round-1 sites present. (One ordering footnote → m10.)
- **Spike-artifact disposition — DROPPED in the fold.** → I7.
- No other new contradiction found: v0.53.0→v0.54.0 pin arithmetic matches live repo state (0.53.0 is HEAD's release; the tag will carry merged PR #26 as line 4 says); the hygiene assertion stays always-run; Plans A/B/C still verbatim spec §3; census placement unchanged; P2 expected-no-ops still verify-don't-assume.

---

## Critical

None.

## Important

### I7 — The spike-artifact absorb/remove step vanished in the I2 fold: `tests/spike_form_snapshots.rs` and the spike workflow are merge-bound with no planned disposition, and the "post-merge convenience" clause now points the wrong way.

Pre-fold, the P0 workflow was a "workflow_dispatch+PR-triggered **throwaway** on the feature branch" (round-1 I2, quoting the draft). The revised plan: (a) deletes "throwaway" — P0 now just says "The CI workflow (PR-event, per above)"; (b) keeps "`workflow_dispatch` may sit in the file as a post-merge convenience" — a clause that only means anything if the spike workflow SURVIVES the merge; (c) P1 absorbs "P0's proven rasterizer recipe" into `build.yml` (the absorb half) but no step anywhere deletes the spike test or the spike workflow (the remove half). Grep-verified: the only "spike" mentions are lines 4, 9, 10, 13, 15 — none is a removal.

Since P0 and P1 share one branch and one PR (verified above), both spike artifacts merge to master unless removed — and every path from here requires improvisation: an un-env-gated 61-form wgpu spike test REDs the headless CI job and every dev `cargo test` (loud, pre-merge, but forces an unplanned fix); a surviving spike workflow on master becomes a dispatchable render workflow that either references a deleted test (dead) or duplicates the `snapshots` job — and its now-live `workflow_dispatch` is exactly the thing a future reader will mistake for the ladder's DEFERRED dispatch-regen workflow, re-creating the ambiguity the I2 fold was written to kill. This is the I1 class (a forced unplanned edit to merge-bound content) plus a self-contradiction with the plan's own ladder-disposition sentence.

**Fold (two edits, no architecture change):** (1) P1 file list += "delete `tests/spike_form_snapshots.rs` + the spike workflow file (the recipe is absorbed into `build.yml`'s `snapshots` job); nothing spike-named reaches the merge." (2) P0 line 10: replace "`workflow_dispatch` may sit in the file as a post-merge convenience but carries NO pre-merge weight" with "`workflow_dispatch` in the spike file is inert pre-merge (default-branch rule) and dies with the file in P1 — the ladder's dispatch-regen workflow stays DEFERRED-DOCUMENTED."

## Minor / Nit (non-blocking)

- **m7 — Flip-commit artifact upload is implicit.** The red-corpus sequence says "download the regenerated corpus artifact" — under `UPDATE_SNAPSHOTS=1` the job PASSES, so the failure-path `.diff.png` upload never fires; the temp commit must itself add an upload-artifact step for `tests/snapshots/forms/*.png`. Executable as written (you can't download what you didn't upload — the implementer will notice), but one clause ("flip + add a corpus-upload step") makes it mechanical.
- **m8 — P2 step-0 command: wrong-remote `origin`.** Leg 2 executes in the toolkit repo, where `origin` is mnemonic-toolkit — `git ls-remote origin mnemonic-gui-v0.54.0` returns empty there. Name the remote explicitly (`git ls-remote https://github.com/bg002h/mnemonic-gui …`) or run inside the pinned clone. Fail-closed either way (empty SHA → API error ≠ `success` — cannot false-pass). Peel is a non-issue today (GUI tags verified lightweight); if v0.54.0 is ever created annotated, add `^{}`.
- **m9 — I5 OR-phrasing grammar.** "source md uses the file-relative form … OR manual-root-relative + a consumer-config alignment" reads as if the file-relative branch needs NO alignment — round-1's fact 2 showed BOTH forms need one (file-relative fails pandoc-HTML silently + xelatex). The surrounding sentences ("no bare path form satisfies all 5 consumers"; "decide in-phase INSIDE this constraint map") and the census gate make the intent unambiguous and the failure caught, so this is wording only: "+ a consumer-config alignment" should distribute over both branches.
- **m10 — Release-commit transport once protection is live.** The ship chain enables required-`snapshots` BEFORE the release commit; a protected branch with required status checks rejects direct pushes of unchecked commits (admin bypass applies under classic protection defaults; a ruleset needs an explicit bypass entry). The repo's standing GUI convention (PR + CI before tag) already satisfies this — one clause noting "release commit rides the standing PR flow (or admin bypass)" prevents a surprise rejection at ship.

---

## Gate disposition

**RED at 0C/1I.** Round-1's six Importants and six minors are all faithfully folded — verified against prescriptions, live repo state, and source. The single blocker is fold-introduced, surgical, and text-only: restore the spike-artifact remove-half (I7's two edits). The four minors may ride the same fold or be carried into their phases. Per the reviewer-loop rule: fold I7 → re-dispatch a scoped round 3 (fold-fidelity check on the two edited sentences only — fast). Do NOT start P0 until round 3 returns GREEN.
