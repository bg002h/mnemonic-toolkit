# Generated artifacts — never commit.
build/
src/99-build-banner.md
mermaid-filter.err
# LaTeX byproducts inside build/ are covered by `build/`. Don't ignore
# `*.out` globally — it would also ignore `transcripts/*.out` (the
# committed worked-example transcripts) — though here `transcripts/`
# is a symlink so the rule only matters if a future change inlines them.
