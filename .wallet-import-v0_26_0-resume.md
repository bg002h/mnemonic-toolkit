# Wallet-import v0.26.0 cycle — resume continuity doc

**Date saved:** 2026-05-18 (refreshed post Phase 0 + Phase 1 first-commit)
**Cycle state:** **Phase 0 SHIPPED (GREEN-after-fold) + Phase 1 first-commit SHIPPED (§7.0 amendments).** Next: Phase 1 §1.1 code work (env-var sentinel resolver).

**Worktree:** `.claude/worktrees/wallet-import-export-multiformat-brainstorm/` on branch `worktree-wallet-import-export-multiformat-brainstorm`.

**Predecessor:** toolkit master `7c1f874` (v0.25.1 SHIPPED).

**Recent worktree commits (ahead of `e6b54fd`):**
- `97ee868` design: plan-doc §7.1 anchor fixup — §7 → §6.11.a (spec-compliance Minor fold)
- `82d849d` design: pre-cycle SPEC + BRAINSTORM amendments for wallet-import v0.26.0 (§7.0.a-e + architect-I2 expansion + D16/D17 cleanup)
- `dcf0523` phase 0: wallet-import v0.26.0 recon close (GREEN-after-fold; 2 Important folded: I1 + I2)
- `e6b54fd` design: implementation plan + resume continuity for wallet-import v0.26.0
- `fc6efa1` design: brainstorm + SPEC for v0.26.0 wallet-import cycle

## What's done

- **`design/BRAINSTORM_wallet_import_v0_26_0.md`** — 280-line brainstorm capturing 9 user-clarifying questions Q1-Q9 + 17 locked design decisions D1-D17 + reviewer-loop fold history. Committed at `fc6efa1`.
- **`design/SPEC_wallet_import_v0_26_0.md`** — 450-line normative SPEC covering CLI surface, error catalog, env-var sentinel, BSMS Round-2 + Bitcoin Core parsers, round-trip discipline, GUI lockstep, test corpus. Committed at `fc6efa1`.
- **`design/IMPLEMENTATION_PLAN_wallet_import_v0_26_0.md`** — 8-phase implementation plan (Phase 0 recon, Phase 1 env-var sentinel, Phase 2 BSMS, Phase 3 Bitcoin Core, Phase 4 round-trip, Phase 5 sniff+seed-overlay, Phase 6 GUI lockstep, Phase 7 cycle close). Mirror of plan-mode artifact at `~/.claude/plans/floofy-brewing-diffie.md`. Committed at this resume commit.
- **Reviewer-loop:** 6 opus architect dispatches (R0 + R1 convergent on Section 3; R0 on Sections 4, 5; holistic on BRAINSTORM+SPEC+PLAN; convergent post-fold). All findings folded.

## Phase 0 SHIPPED 2026-05-18 (commit `dcf0523`)

8 gates executed (GREEN-after-fold). GO/NO-GO verdict: **GO** — user's flagship BSMS blob parses end-to-end through rust-miniscript v13.0 + full `parse_descriptor` pipeline (I1 fold test `parse_descriptor::tests::phase0_i1_bsms_decaying_multisig_walks_end_to_end` passes). Reports: `design/agent-reports/phase-0-recon.md`, `design/agent-reports/phase-0-r0-review.md`.

Architect R0 (opus, 0 Critical / 2 Important / 3 Minor):
- **I1 folded:** end-to-end walker test for `wsh(thresh(2, pkh, s:pk, sln:older))` AST shape added at `parse_descriptor.rs::tests::phase0_i1_bsms_decaying_multisig_walks_end_to_end`.
- **I2 folded:** §0.7 decision locked to option (b) — extract network from BIP-48 coin-type — with explicit signet/regtest collapse-to-testnet rule + cosigner heterogeneity → exit 2 rule. Captured in SPEC §7.0.a normative text.

Plan-doc inline corrections per recon R1/R2/R3 (also committed in `dcf0523`):
- §0.3 `gui-schema --classify-flags` → bare `gui-schema`
- §0.6 + §0.6.a `convert --from descriptor=` → `export-wallet --descriptor`
- §0.1 expectation: byte-identical → outputs DIFFER (toolkit preserves declaration order)
- §0.2 TlvSection field list completed (added `origin_path_overrides`, `unknown`)

## Phase 1 first-commit SHIPPED 2026-05-18 (commit `82d849d` + Minor fold `97ee868`)

All 5 §7.0 amendments landed (documentation-only, no code touched):
1. **§7.0.a** SHIPPED — SPEC §4.2 step 8 rewritten to BIP-48 coin-type rule + I2 expansion (signet collapse + cosigner heterogeneity).
2. **§7.0.b** SHIPPED — `§7 (NEW)` → `§6.11.a (NEW)` in SPEC §3.4 + BRAINSTORM §7 third bullet + plan-doc §7.1 anchor list (the last via the `97ee868` Minor fold).
3. **§7.0.c** SHIPPED — SPEC §8.1 `Vec<CosignerKeyInfo>` → `Vec<ResolvedSlot>`; BRAINSTORM D12 mirrored; backward-compat alias retained.
4. **§7.0.d** SHIPPED — SPEC §3.2 explicit-scope rule landed.
5. **§7.0.e** SHIPPED — SPEC §3.1 row 1 `bip85` → `derive-child (covers BIP-85 path)`.

Plus D16/D17 cleanup (`ImportWalletEnvVarMissing` → cross-cutting `EnvVarMissing`) in BRAINSTORM.

Spec-compliance review (opus): **COMPLIANT**. 2 Minor — one folded (plan-doc §7.1 anchor); one left to §3.4 textual heaviness (within scope; no action).

## What's been locked in design (highlights)

- **Formats:** BSMS Round-2 + Bitcoin Core `listdescriptors` (xpub-only). xprv-bearing Core blobs → exit 2 with FOLLOWUP for future cycle.
- **CLI:** `mnemonic import-wallet --blob <FILE|-> [--format <bsms|bitcoin-core>] [--ms1 ...] [--slot @N.phrase=...] [--select-descriptor <N|active-receive|active-change|all>] [--json] [--no-auto-repair]`.
- **Output:** engraving cards on stdout (default; `\n;\n` separator for N>1); `--json` envelope array with per-bundle `roundtrip: {byte_exact, semantic_match, diff}` field.
- **Env-var sentinel `@env:VAR`:** cross-cutting across 6 inline-value secret-flag surfaces (`--passphrase`, `--bip38-passphrase`, `--ms1`, `--share`, `--slot @N.phrase=`, `--slot @N.ms1=`).
- **BSMS rounds:** Round-2 only this cycle; Round-1 + verify-signatures filed as FOLLOWUPs.
- **BSMS leniency:** 2-line excerpts accepted with stderr WARNING; 6-line full Round-2 also accepted.
- **GUI lockstep:** v0.11.0 ships CLI + STATIC schema mirror form (`mnemonic` tab combobox entry); v0.12.0 deferred for dynamic per-cosigner widget + argv-redaction.
- **GUI seed channel:** `@env:VAR` sentinel obviates argv-redaction need; GUI sets `MNEMONIC_MS1_<i>` env-vars on subprocess spawn; argv contains sentinels only.
- **Round-trip discipline:** bundle struct equality + semantic blob canonicalize + stderr unified-diff (or `--json` envelope diff field) when byte-exact fails.
- **Cell budget target:** 70-94 cells. v0.25.1 baseline 1153 → v0.26.0 target ~1230-1250.

## 13 FOLLOWUPs filed at cycle close

(All listed in BRAINSTORM §6 + IMPLEMENTATION_PLAN §5 — to be added to `design/FOLLOWUPS.md` in Phase 7.)

## Resume command (next session)

```
Resume wallet-import v0.26.0 cycle from worktree `.claude/worktrees/wallet-import-export-multiformat-brainstorm`. Read `.wallet-import-v0_26_0-resume.md` FIRST. Phase 0 + Phase 1 first-commit are SHIPPED (commits dcf0523 + 82d849d + 97ee868). Execute Phase 1 §1.1 code work (env-var sentinel resolver in secrets.rs + ToolkitError::EnvVarMissing variant + 12-18 cells in tests/cli_env_var_sentinel.rs) per IMPLEMENTATION_PLAN §1. Use superpowers:subagent-driven-development for per-phase opus-architect-reviewed execution.
```

## Notes for the resuming agent

1. **Worktree discipline:** Stay in `.claude/worktrees/wallet-import-export-multiformat-brainstorm`. Parallel Claude instances may be working in master / sibling worktrees; do NOT `cd` out.
2. **Phase 1's first commit is documentation-only** per §7.0. Do not start Phase 1 code work until the SPEC + BRAINSTORM amendments are committed.
3. **Phase 0 GO/NO-GO** at §0.6: if rust-miniscript can't parse the user's seed-case, pause and file FOLLOWUP — do not proceed to Phase 2.
4. **Per-phase opus architect review** at R0/R1+ until 0 Critical / 0 Important (project convention `[[feedback-opus-primary-review-agent]]`).
5. **GUI lockstep at Phase 6** requires the `mnemonic-gui` repo (`/scratch/code/shibboleth/mnemonic-gui`). Toolkit v0.26.0 tag MUST land BEFORE GUI v0.11.0 Cargo.toml pin.

## Memory note (correction from this session)

Memory entry `project_v0_3_0_mnemonic_gui_closed.md` (the `gui-run-confirm-modal-secret-redaction-manual-companion` FOLLOWUP citation) was found to be **incorrect** during architect-review of Section 4: the mnemonic-gui run-confirm modal at `main.rs:686-688` **renders argv VERBATIM** with no redaction. Verified by Explore agent grep against `mnemonic-gui/src/` (0 hits for `redact|argv_for_display|mask`). The mistake was in MEMORY index wording, not in the underlying FOLLOWUP. Resuming agent should add `feedback_run_confirm_modal_renders_argv_verbatim_2026_05_18.md` if not already present.

---

**End of resume continuity doc.**
