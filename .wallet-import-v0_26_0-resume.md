# Wallet-import v0.26.0 cycle — resume continuity doc

**Date saved:** 2026-05-18 (refreshed at Phase 7 cycle-close-in-flight)
**Cycle state:** **Phases 0-6 SHIPPED + R0 GREEN-after-fold; Phase 7 cycle-close docs landing; PR creation imminent.** Holistic architect review dispatched in background; awaiting findings before PR open.

**Worktree:** `.claude/worktrees/wallet-import-export-multiformat-brainstorm/` on branch `worktree-wallet-import-export-multiformat-brainstorm`.

**Predecessor:** toolkit master `7c1f874` (v0.25.1 SHIPPED).

## Commit history (ahead of `7c1f874`)

```
b8cd8c0 phase 3: R0 architect-review folds (C1 + I1 + I2 + M2)   ← HEAD
120e6b4 phase 4: round-trip discipline — canonicalize + similar + 50 cells  [UNREVIEWED]
e7a19f9 phase 3: Bitcoin Core listdescriptors parser + xprv refusal + 14 cells
340f734 phase 2: R0 architect-review folds (I1 + I2)
7689cbc phase 2: BSMS Round-2 parser + watch-only invariant + 16 cells
0c66d58 phase 1: R0 architect-review folds (I1 + I2)
6b0588f phase 1: cross-cutting @env:VAR sentinel resolver
fb70b89 docs: refresh resume continuity doc for Phase 0 + Phase 1 first-commit close
97ee868 design: plan-doc §7.1 anchor fixup
82d849d design: pre-cycle SPEC + BRAINSTORM amendments
dcf0523 phase 0: recon close (GREEN-after-fold)
e6b54fd design: implementation plan + resume continuity
fc6efa1 design: brainstorm + SPEC for v0.26.0 cycle
```

**89 test binaries pass; clippy clean.** Architect reports persisted at `design/agent-reports/phase-{0,1,2,3}-r0-review.md`.

## Phase-by-phase status

### Phase 0 — recon — SHIPPED (GREEN-after-fold)

8 gates run empirically. GO/NO-GO: GO. I1 fold (end-to-end walker test for user's BSMS AST) + I2 fold (network detection via BIP-48 coin-type; testnet collapses signet/regtest). See `design/agent-reports/phase-0-{recon,r0-review}.md`.

### Phase 1 — env-var sentinel — SHIPPED (GREEN-after-fold)

`crate::env_sentinel::resolve_env_var_sentinel` helper + `ToolkitError::EnvVarMissing` variant + 7-callsite wiring + 34 cells. I1 fold (advisory suppression on `@env:`-bearing flags) + I2 fold (SPEC §3.1 rows 5+6 amended to composite-form classes).

### Phase 2 — BSMS Round-2 parser — SHIPPED (GREEN-after-fold)

`wallet_import/{mod.rs,bsms.rs,pipeline.rs}` + watch-only invariant + 6 new ToolkitError variants + thin `cmd/import_wallet.rs` scaffold + 16 cells. I1 fold (first-address verification deferred to v0.27+) + I2 fold (4 new FOLLOWUPs filed).

### Phase 3 — Bitcoin Core listdescriptors parser — SHIPPED (GREEN-after-fold)

`wallet_import/bitcoin_core.rs` + `SelectDescriptor` enum + `apply_select_descriptor` + 15 cells. C1+I1+I2+M2 folds (b8cd8c0):
- C1: xprv refusal extended to `tprv`/SLIP-132 private-key prefixes via regex
- I1: strip `#<csum>` before substring scan (BIP-380 false-positive guard)
- I2: extract `wallet_name` from envelope into `CoreSourceMetadata`
- M2: `aggregate_dropped.join(", ")` clean stderr (no Debug format)

### Phase 4 — round-trip discipline — SHIPPED + R0 GREEN-after-fold

`wallet_import/roundtrip.rs` (canonicalize_bsms + canonicalize_bitcoin_core + unified_diff) + `similar = "2"` dep + 8 BSMS (post-fold) + 4 Core fixtures + 51+ cells.

**Architect R0 verdict (2026-05-18, `design/agent-reports/phase-4-r0-review.md`):** GREEN — 0 Critical, 2 Important, 4 Minor. All Important + Minor folded inline:
- **I1** (fixture corpus gap) → SPEC §10.1 amended to lock v0.26.0-shipped subset; FOLLOWUP `wallet-import-fixture-corpus-expansion` filed (v0.27+).
- **I2** (`#[allow(dead_code)]` Phase 5 wiring contract) → propagated into Phase 5 R0 brief (see Step 3 below). The `#[allow(dead_code)]` attributes at `roundtrip.rs:39,117,211,229` MUST be removed in the same commit that wires the helpers; Phase 5 R0 grep is load-bearing.
- **M1, M2** (comment fixes in `roundtrip.rs:56-58, 80-81`) → folded inline.
- **M3** (no `multi(...)` declaration-order assertion fixture) → new fixture `bsms-2line-multi-2of3.txt` + assertion cell.
- **M4** (SPEC §4.1 6-line shape deviation from BIP-129) → SPEC §4.1 note acknowledging the toolkit-specific lenient consolidation.

**Phase 5 deferrals documented in commit 120e6b4:**
- BSMS bundle round-trip blocked (no `export-wallet --format bsms` emitter exists — FOLLOWUP `wallet-export-bsms-emitter` filed)
- `--json` envelope `roundtrip` cell deferred to Phase 5

## Next steps — RESUME HERE

### Step 1: Phase 4 architect review (NOT yet dispatched)

Dispatch opus `feature-dev:code-reviewer` against commit `120e6b4` per the architect-review template used in Phases 0-3. Scope: `wallet_import/roundtrip.rs` correctness against SPEC §6.11.a / §7.3 + fixture corpus completeness + canonicalize semantics + cell coverage.

Persist review to `design/agent-reports/phase-4-r0-review.md`.

### Step 2: Phase 4 folds (if any)

Apply Important/Critical folds inline. File FOLLOWUP `wallet-export-bsms-emitter` per Phase 4 implementer's Phase 5 deferrals note.

### Step 3: Phase 5 implementer

Per IMPLEMENTATION_PLAN §5 (lines 470-509). Surface:
- `wallet_import/sniff.rs` — auto-detect dispatcher
- `apply_seed_overlay` logic
- Full clap CLI surface for `mnemonic import-wallet` (`--blob`, `--format`, `--ms1`, `--slot`, `--select-descriptor`, `--json`, `--no-auto-repair`)
- `--json` envelope `roundtrip` field wiring (Phase 4 helper available)
- `tests/cli_import_wallet_sniff.rs` + `tests/cli_import_wallet_seed_overlay.rs` (8-12 cells)

### Step 4: Phase 5 architect review

### Step 5: Phase 6 implementer + review (GUI + manual)

`mnemonic-gui` repo at `/scratch/code/shibboleth/mnemonic-gui` — `SubcommandSchema` entry + `runner.rs` env-var seed channel + kittest cells. Toolkit-side manual updates: `docs/manual/src/40-cli-reference/41-mnemonic.md` (mirror invariant) + `docs/manual/src/45-foreign-formats.md` (new chapter) + `docs/manual-gui/src/20-feature-walkthroughs/24-import-wallet.md`.

### Step 6 (Phase 7, deferred): cycle close — OUTPUT PULL REQUEST, NOT MERGE

User direction: "the last phase will be modified to make final work output a pull request as there are other worktrees in progress." So Phase 7 = `gh pr create` against master, NOT a merge. Open the PR from worktree branch `worktree-wallet-import-export-multiformat-brainstorm`.

## Resume command (next session)

```
Resume wallet-import v0.26.0 cycle from worktree `.claude/worktrees/wallet-import-export-multiformat-brainstorm`. Read `.wallet-import-v0_26_0-resume.md` FIRST. Phase 0-3 GREEN-after-fold; Phase 4 SHIPPED at 120e6b4 but UNREVIEWED. Dispatch Phase 4 opus architect R0 review of commit `120e6b4`, apply folds, then proceed through Phases 5 + 6 using superpowers:subagent-driven-development with per-phase opus-architect-reviewed execution. Final phase (7) outputs a `gh pr create` PR against master (NOT a merge — parallel worktrees in progress).
```

## Operational notes for the resuming agent

- **Parallel worktree discipline:** Do NOT merge to master. Do NOT push to origin without explicit user direction. The cycle outputs a PR at Phase 7.
- **Architect-review skill:** project convention `[[feedback-opus-primary-review-agent]]` — every phase dispatches opus `feature-dev:code-reviewer` (or `code-architect`) for R0 review; iterate R0 → R1 until 0 Critical / 0 Important; persist each review to `design/agent-reports/phase-N-r0-review.md`.
- **Fold-discipline:** per `[[feedback-r0-must-read-source-off-by-n]]` + `[[feedback-architect-must-run-prose-commands]]`, R0 architect must cite file:line; folds land inline as a follow-on commit per phase.
- **Parallel-fan-out is OK** when subagents have non-overlapping file scopes. Review agents (read-only) parallelize freely with implementers. Implementers parallelize only when file scopes are disjoint.
- **Phase 5 may need to extend Phase 3's `ParsedImport` / `CoreSourceMetadata` carrier** for sniff-format + JSON envelope; Phase 4's `roundtrip` field name expectation is `{byte_exact, semantic_match, diff}` per SPEC §7.4.
- **FOLLOWUPs filed during this cycle so far (in `design/FOLLOWUPS.md`):**
  - `bsms-first-address-verify` (v0.27+; Phase 2 I1)
  - `wallet-import-signet-regtest-disambiguation` (v0.27+; Phase 0 I2)
  - `wallet-import-bsms-checksum-delegation-note` (v0.26.0-cycle-close; SPEC §4.4 wording)
  - `bsms-verify-signatures` (v0.27+; planned)
  - Pending file at Phase 4 review or cycle close: `wallet-export-bsms-emitter` (per Phase 4 implementer)
- **SPEC + BRAINSTORM amendments** to apply at Phase 7 cycle close, in `SPEC_mnemonic_toolkit_v0_5.md`:
  - Add §5.11 (CLI value-source sentinels)
  - Add §6.11 (import-wallet CLI grammar)
  - Add §6.11.a (round-trip discipline) — per §7.0.b amendment, NOT new top-level §7

---

**End of resume continuity doc.**
