# manual-gui v1.0.0 — cycle-close handoff

**Status as of 2026-05-15:** v1.0 cycle code-complete; M-P2.5 + P3
LOCKed. PE.1 (CHANGELOG), PE.2 (FOLLOWUPS), PE.8 (this handoff)
all landed in commit. Outstanding human-side steps:

- **PE.3 / PE.4** — open PRs against `master` in both repos
  (requires push to origin).
- **PE.5** — enable GitHub Pages on `bg002h/mnemonic-toolkit`
  (UI action; source = `gh-pages` branch, root = `/`).
- **PE.6** — push `manual-gui-v1.0.0` tag from the toolkit
  merge-commit after both PRs merge. Triggers gh-pages deploy.
- **PE.7** — `gh release create` runs from CI on tag push; no
  manual step unless CI fails.

**Supersedes:** `.manual-gui-P2.4-batch6-shipped-handoff.md` (kept
on disk for historical reference; P1 + P0 handoffs also retained).

## Cycle artifact summary

| Track | Branch | HEAD | LOC delta | Status |
|---|---|---|---|---|
| M (manual) | `manual-gui-v1` in `bg002h/mnemonic-toolkit` | (commit after this file) | ~3,400 LOC across 51 source files; 223-page PDF; 562K HTML | LOCK pending PR + merge |
| G (GUI affordances) | `manual-gui-help-icons` in `bg002h/mnemonic-gui` | `8e04e2f` | Help-icon URL helper + widget integration + kittest + manual-anchor-coverage Rust test + 2 lockstep comment touchups | LOCK pending PR + merge |

## All 10 batches LOCKed (M-P2.4 close)

| Batch | Toolkit commit | LOC / files | R0→R1 verdict |
|---|---|---|---|
| 1 — 00 + 10-foundations | (prior) | — | LOCK |
| 2 — 20-install | (prior) | — | LOCK |
| 3 — 30-tour | (prior) | — | LOCK |
| 4 — 40-mnemonic family | 5 sub-batches | — | LOCK each |
| 5 — 40-mnemonic completions | 5 sub-batches | — | LOCK each |
| 6 — 50-md | 3 sub-batches | — | LOCK each |
| 7 — 60-ms | `8ed2ac6` | 717 LOC / 6 files | ITERATE 3C → LOCK |
| 8 — 70-mk | `ebaa094` + lockstep `8e04e2f` (GUI) | 624 LOC / 6 files | ITERATE 1C → LOCK |
| 9 — 80-troubleshooting | `b4e7e56` | 297 LOC / 4 files | ITERATE 3C+2I → LOCK |
| 10 — 90-appendices | `55e922d` | 852 LOC / 5 files | ITERATE 5C → LOCK |

## Cycle phases

| Phase | Toolkit commit | Notes |
|---|---|---|
| M-P0 (scaffold) | (prior) | — |
| M-P1 (RED tests) | (prior) | — |
| M-P2.4 (manual content) | `55e922d` | All 10 batches |
| M-P2.5 (CI workflow) | `63397ef` | 3-job pipeline; ITERATE 1C → LOCK |
| P3 (cycle-LOCK) | (this commit) | G1-G7 all PASS dynamically |
| PE.1 + PE.2 + PE.8 (rollup) | (this commit) | CHANGELOG, FOLLOWUPS, this handoff |
| PE.3 + PE.4 | TODO | Open PRs |
| PE.5 | TODO (UI action) | Enable Pages BEFORE tag push |
| PE.6 | TODO | Tag push after both PRs merge |
| PE.7 | TODO (CI-driven) | Release create + asset upload |

## G1-G7 dynamic verification (2026-05-15 against HEAD)

- **G1 (gui-schema-coverage)**: ✅ 459 schema anchors / 28 subcommands all present in HTML.
- **G2 (outline-coverage)**: ✅ 59 outlines all present with correct bullet count.
- **G3 (markdownlint + cspell + lychee + glossary)**: ✅ all 4 sub-phases green.
- **G4 (gui-anchor-coverage)**: ✅ `cargo test --test manual_anchor_coverage -- --include-ignored` 6/6 tests pass.
- **G5 (build artifacts)**: ✅ PDF (223 pages — over the 140-180 plan target by ~25%; appendices drove the page count higher than the original LOC estimate; not blocking) + HTML build cleanly.
- **G6 (CI workflow + gh-pages deploy)**: ✅ workflow exists with 3 correct jobs; gh-pages deploy logic verified static (will be exercised at PE.6 tag push).
- **G7 (GUI help-icon smoke)**: ✅ `cargo test --test widget_help_icon` 2/2 tests pass.

## Cycle-side FOLLOWUPS filed (status: open)

In `mnemonic-toolkit/design/FOLLOWUPS.md`:

- `gui-manual-cross-refs-to-cli-manual` — bidirectional links to the CLI manual (v1.1).
- `cli-manual-html-target` — HTML render of the CLI manual (v1.1).
- `gui-manual-localization` — non-English translations (v1.1+).
- `mk-vectors-pretty-out-help-mismatch` — cross-repo help-text drift (filed this cycle).

In `mnemonic-gui/FOLLOWUPS.md`:

- `gui-help-icon-per-flag-affordance` — per-flag `?` buttons (v0.4).
- `gui-manual-base-url-runtime-override` — `--manual-base-url` runtime flag (v0.4).
- `gui-run-confirm-modal-secret-redaction` — argv redaction (v0.4, cross-repo).
- `gui-bundle-multisig-flags-conditional` — conditional disable under single-sig (v0.4).

## Key memories that apply (load relevant ones at fresh-session start)

- `[[feedback-manual-gui-lockstep]]` — two-PR lockstep; bidirectional mirror invariant.
- `[[feedback-opus-primary-review-agent]]` — per-phase opus reviewer-loops.
- `[[feedback-r0-must-read-source-off-by-n]]` — source-grep every claim; batch 10 was the most severe instance.
- `[[feedback-architect-must-run-prose-commands]]` — run the prose's commands end-to-end.
- `[[feedback-r2-blocking-vs-cosmetic-gate]]` — parse-failing tests are Important regardless.

## Working tree paths

- Manual: `/scratch/code/shibboleth/mnemonic-toolkit/docs/manual-gui/`
- GUI source: `/scratch/code/shibboleth/mnemonic-gui/`
- Plan: `/home/bcg/.claude/plans/eager-giggling-castle.md`
- Prior handoffs:
  `.manual-gui-P0-shipped-handoff.md`,
  `.manual-gui-P1-shipped-handoff.md`,
  `.manual-gui-P2.4-batch3-shipped-handoff.md`,
  `.manual-gui-P2.4-batch6-shipped-handoff.md`.

## Resume command (for the human-side PE.3 → PE.7 sequence)

```text
Continue manual-gui v1.0 cycle PE rollup. PE.1 (CHANGELOG), PE.2
(FOLLOWUPS), PE.8 (handoff) are landed at toolkit
`manual-gui-v1` (HEAD pre-PR). Read
.manual-gui-v1.0.0-shipped-handoff.md, then:
  1. PE.3 — git push origin manual-gui-v1; gh pr create against
     master.
  2. PE.4 — in mnemonic-gui: git push origin manual-gui-help-icons;
     gh pr create against master. Cross-cite the toolkit PR URL.
  3. PE.5 — open repo settings on bg002h/mnemonic-toolkit; enable
     GitHub Pages with source = gh-pages branch + root /. (One-time
     bootstrap before tag push.)
  4. After both PRs go green and merge: PE.6 git tag
     manual-gui-v1.0.0 <merge-sha>; git push --tags. (Toolkit tag
     first; the GUI lockstep tag follows if its own cycle tags
     anything.)
  5. PE.7 — the manual-gui.yml `release` job auto-creates the
     GitHub release + uploads PDF + publishes HTML to gh-pages.
     Verify the deployed URL resolves and the GUI's help-icon deep
     links land on the right anchors.
```
