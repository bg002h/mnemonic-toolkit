# PLAN — audit minors M1+M2+M3 (+M13 fold-in): self-check xpub binding, localize narrowing, inspect/repair ms1 argv advisory, orphaned-goldens deletion

**Cycle:** toolkit v0.53.2 (PATCH — see SemVer below) · **Source SHA:** `b822fb9` (master; = v0.53.1 `87c33c5` + one docs commit) · **Recon:** `cycle-prep-recon-minors-m1-m2-m3.md`
**Resolves (audit-2026-06-10-backlog index lines):** `self-check-no-mk1-xpub-binding` (M1), `localize-broad-error-collapse` (M2), `inspect-repair-no-argv-advisory` (M3), + the open `### orphaned-v0_2-md1-vectors-no-harness` entry (`design/FOLLOWUPS.md:157`) = audit M13.

## Part 1 — M1: `self_check_bundle` binds each mk1 xpub to the descriptor (defense-in-depth)

- **Verified state (recon):** `self_check_bundle` at `bundle.rs:2063-2197` (drifted +3 since the audit; Single branch `:2089-2114`, Multi `:2115-2141`) validates md1 reassembly, the 4-byte stub linkage, mk1 fingerprint presence, per-slot ms1 round-trip — but NEVER compares the decoded mk1 card's xpub bytes to the descriptor's pubkeys, in EITHER branch (the audit named Multi; **Single has the same gap** — fix both).
- **Fix (SLOT-EXACT, R0-r1 I-1):** for each decoded mk1 card at slot `i`, assert `desc.tlv.pubkeys` contains the exact pair `(i as u8, xpub_to_65(&card_xpub))` (helper: `synthesize.rs:115`, `pub fn xpub_to_65(&Xpub) -> [u8; 65]`); Single branch = `(0, …)`. Slot-exact is sound because every `self_check_bundle` call site reaches it via `synthesize_descriptor`/`synthesize_unified`, which build `tlv.pubkeys` AND the per-cosigner cards from the SAME slots vector in the same order (verified `synthesize.rs:590-607,816-833`; import-json re-synthesizes md1 — `bundle.rs:1714-1716`). Order-agnostic membership would MISS a cross-slot card swap (passes membership + the stub-equality check). Treat `pubkeys: None` as a self-check failure with a distinct message (zero emission sites produce it — defensive only). Precedent to mirror: `verify_bundle.rs:1630-1659` (the `:1654` `XpubNotInPolicy` arm). New failure message names the slot + truncated xpub (public material).
- **TDD (v0.47.4 pattern — synthesize-then-mutate; R0-r1 m-5):** RED cells live in the BIN crate (`mod` tests inside `cmd/bundle.rs`, precedent `:2300` — red-run via `cargo test --bin mnemonic`, NOT `--lib`). Synthesize a bundle, swap its mk1 card for one re-encoded with a DIFFERENT xpub but the same `chunk_set_id` (`mk_codec::encode_with_chunk_set_id` is exposed — 7 toolkit call sites), expect the new self-check error; cover Single AND Multi, plus a Multi CROSS-SLOT SWAP cell (the case slot-exactness exists for). Guards: cite the existing `cli_self_check.rs` happy-path + wif-slot cells as the discriminating greens (v0.47.4 G-A/G-B lesson). Error variant: reuse/extend `ToolkitError` — alphabetical placement if new.

## Part 2 — M3: inspect/repair fire the secret-argv advisory for inline ms1

- **Verified state (recon):** `cmd/inspect.rs:88-156` + `cmd/repair.rs:101-218` accept `--ms1 <inline>` (and HRP-autodetected ms1 in the positional `extra_strings`) without ever calling the advisory; **19** other call sites use the advisory helper (audit said "14+"); both support `--ms1 -` (stdin) so the advisory's recommendation is real; the lint Route rows for inspect/repair `--ms1` already exist (`lint_argv_secret_flags.rs:92-93`) — no lint lockstep.
- **Fix placement (recon wrinkle, better than the audit's):** the shared `src/repair.rs::resolve_groups` (`:262`, both subcommands' sole intake) — fire `secret_in_argv_warning(stderr, flag, alternative)` (`secret_advisory.rs:40-45`) **per-occurrence** (manual precedent `:1501`; R0-r1 m-3) on RAW pre-expansion values (BEFORE the `-` expansion at `:339-341` — post-expansion, stdin chunks are indistinguishable from inline → would false-fire on `--ms1 -`; R0-r1 m-1). Signature gains a stderr writer (both callers have `stderr: &mut E` in scope — `inspect.rs:88-105`, `repair.rs:101-111`; mechanical threading). Firing rule (R0-r1 m-2): **unconditionally on every non-`-` `--ms1` flag value** (the flag declares the kind — an indel-corrupted `s10…` value under `--max-indel ≥ 1` won't HRP-classify but IS secret; gate at `repair.rs:284`); **HRP-probe only the positionals** (mixed mk1/md1/ms1 intake — only ms1-classified ones fire, label `positional ms1`, `alternative` = `--ms1 -` for BOTH the flag and positional cases (positionals cannot be `-`; r2 m-D)). mk1/md1 values stay advisory-free. Lint hardening (m-4): append `"secret_in_argv_warning"` to the inspect/repair Route rows' evidence (`lint_argv_secret_flags.rs:92-93`) — the anchor lands in the rows' `source_file` (`src/repair.rs`).
- **TDD:** RED cells = `inspect --ms1 <inline>` and `repair --ms1 <inline>` stderr CONTAINS the advisory (+ a per-occurrence cell MIXING sources — `--ms1 <inline>` + a positional ms1, since `--ms1` is single-occurrence clap-side (r2 m-B), + an indel-relaxed corrupted-`--ms1` cell); guard cells = `--ms1 -` (stdin), positional mk1/md1-only, and stdin-expanded chunks DON'T fire. R0 verified there are NO exact/empty-stderr assertions anywhere in tests/ → no existing test breaks.

## Part 3 — M2: narrow `localize()`'s error collapse + enforce the invariant

- **Verified state (recon):** `gate.rs:438-453` (`localize`) maps every `Err(_)` from per-subtree `from_str_ext(insane)` to `None`; the narrowed twin `localize_parse_failure` is `:405-418`; `miniscript::Error::NonTopLevel(String)` EXISTS in the pinned miniscript (rev 95fdd1c, lib.rs:445); an `INVARIANT (M3)` comment already documents the reliance (`:446-451`). **The audit's proposed fix is a behavioral NO-OP** — the sole caller (`gate.rs:353`) already root-falls-back on `None`.
- **Fix (minimal, no release-behavior change):** narrow the match to `Err(miniscript::Error::NonTopLevel(_)) => None` (the invariant-sanctioned case) and, for any OTHER error, `debug_assert!(false, …)` naming the unexpected variant THEN return `None` (release stays fail-closed + root-fallback; dev/test builds catch an invariant break loudly the moment step-2 is ever relaxed). Update the `:446-451` comment to describe the enforcement. NOT a public-API change.
- **TDD:** unit cell asserting `localize` returns `None` on a NonTopLevel subtree (pins the sanctioned arm) — placed in gate.rs's existing `#[cfg(test)]` module (`:617`, the file's sole `#[cfg(test)]`; `localize` is private — R0-r1 m-7 / r2 m-A). The debug_assert arm is intentionally hard to reach (that's the point) — no contortion to force it; the cell documents it. Existing sanity-localization tests route only into the NonTopLevel case (verified) so the debug_assert cannot fire on the current suite.

## Part 4 — M13: delete the 25 orphaned v0_2 multisig goldens

- **Verified state (recon):** `tests/vectors/v0_2/` holds 26 fixtures; only `bip84-mainnet-0-false-true.txt` is read (`cli_self_check.rs:13`). The open entry is `### orphaned-v0_2-md1-vectors-no-harness` (`FOLLOWUPS.md:157`). Decision input: v0.48.0 (NUMS) + v0.53.0 (slot-unique csi) changed multisig wire bytes — the 25 goldens are now **stale shapes, not dead-but-correct**: a harness wired today would FAIL against them or pin outdated wire. **Delete the 25** (keep the read bip84 one); resolve the entry recording the rationale (the v0.48.0 orphaned-vectors lesson: goldens no test reads are false-green liabilities). **Also fix `cli_self_check.rs:5` (R0-r1 I-2):** its module doc names `wsh-sortedmulti-mainnet-0-false-true.txt` — in the delete set; post-deletion the doc would reference a non-existent file. Correct it to the bip84-only truth — this ALSO resolves the `[obs] self-check-header-claims-unread-vector` index line for free.
- Stage deletions explicitly (no `git add -A`).

## Ritual / Phase plan

1. **Phase 1 (RED):** Part-1 + Part-2 cells red (Part-3 cell green-by-construction on the narrowed arm — its value is the debug_assert enforcement; note in the test comment).
2. **Phase 2 (GREEN):** implement Parts 1-3 → full `cargo test --workspace` + clippy `-D warnings`.
3. **Phase 3:** Part 4 deletions + **manual prose (R0-r1 I-3):** add the secret-argv advisory row/prose to the `mnemonic inspect` (`docs/manual/src/40-cli-reference/41-mnemonic.md`, section at `:3001+`, advisory table `:3109`) and `mnemonic repair` (`:2731+`) sections, matching the per-occurrence wording precedent (`:1501`); run the **FULL manual lint** (`make -C docs/manual lint` with all four `*_BIN`s — the v0.50.0 cspell ship-detour lesson), not just flag-coverage + FOLLOWUPS (**R0-r1 I-4:** promote the worked minors per the backlog convention — ONE combined `### audit-minors-m1-m2-m3-m13` entry (precedent: I10+n1-vs-nge2 shared an entry) with per-part records; mark FIVE index lines resolved: the 3 minors + `orphaned-v0_2-multisig-goldens` (resolved-by-reference to `orphaned-v0_2-md1-vectors-no-harness`) + the `[obs] self-check-header-claims-unread-vector` line (Part 4); resolve the `### orphaned-v0_2-md1-vectors-no-harness` entry) + the toolkit `secrets.rs` module-doc "token-for-token" reword (the recorded errand) + the M14 companion entry in descriptor-mnemonic's FOLLOWUPS (docs errand) + CHANGELOG `[0.53.2]` + version bump (Cargo.toml + Cargo.lock + ONE marker in EACH of root `README.md:13` and `crates/mnemonic-toolkit/README.md` (m-6) + **`scripts/install.sh:32` self-pin** — the v0.53.1 lesson) → commit (explicit paths) → push → master CI green → advisor SemVer sanity-check → tag `mnemonic-toolkit-v0.53.2` → **verify ALL THREE tag-gated workflows** (changelog-check + install-pin-check + sibling-pin-check).

## SemVer — PATCH (advisor confirm at tag time)

No flag/subcommand/value surface change (no schema_mirror / manual impact — recon verified zero clap changes); no card-byte change. Deltas: a stricter self-check (validation tightening, precedent v0.47.4 ms1 self-check = PATCH), a new stderr advisory line on existing invocations (advisory additions have shipped in PATCHes), a dev-only debug_assert, test-fixture deletions.

## Non-goals

M4-M12 code changes (remain index-line FOLLOWUPs); M14 md-codec code (only its descriptor-mnemonic FOLLOWUP entry is filed in Phase 3). The secrets.rs doc reword + M14 entry are IN Phase 3 (docs errands).
