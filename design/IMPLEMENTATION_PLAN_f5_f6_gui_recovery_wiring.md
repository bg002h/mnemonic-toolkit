# IMPLEMENTATION PLAN — F5+F6 GUI recovery-form wiring

**SPEC:** `design/SPEC_f5_f6_gui_recovery_wiring.md` (✅ R0-GREEN @ round 2, Fable — `design/agent-reports/f5f6-spec-r0-round-{1,2}.md`; M-4 folded). The SPEC carries the exact fix per part (F5 split helper, F6 `_INFER` consts, I-3 persistence migration, #13 defaults-drift test), the complete test-update list, the snapshot regen, and the release/CI surface; this plan is the phase split + guard-rails + release. Subject to its own Fable **plan-R0** to 0C/0I BEFORE implementation.

**Plan-R0 status:** round 1 (Fable) = NOT GREEN → 2 Important + 3 Minor, ALL FOLDED (round 2 re-dispatch pending). Review `design/agent-reports/f5f6-plan-r0-round-1.md`. I-A: the snapshot-regen invocation was a silent no-op (needs `GUI_SNAPSHOTS=1 WGPU_BACKEND=gl LIBGL_ALWAYS_SOFTWARE=1` — no lavapipe locally, llvmpipe-GL route confirmed working); I-B: missed the ungated `README.md:47` install `--tag` site; M-1 (unconditional CHANGELOG), M-2 (defaults-drift binary-resolution/skip), M-3 (worktree merge gotcha). Phase split + I-3-into-P1 + no-toolkit-pin + branch-protection all verified GREEN.

**Source SHA:** GUI `mnemonic-gui` `master f5cb11f` (v0.57.0). **Target:** `mnemonic-gui` MINOR `v0.58.0`. **GUI-only — NO toolkit change, NO toolkit-pin bump** (Cargo pin `mnemonic-toolkit-v0.75.0` + `pinned-upstream.toml` UNCHANGED; `schema_mirror` names-only stays green). **Worktree:** one `mnemonic-gui` worktree (branch `feature/f5-f6-recovery-wiring`). Single implementer, **Opus** (funds-safety false-negative + a persistence migration + kittest — complex). TDD. F5+F6 ship in ONE PR (eval directive — fixing F5 alone activates F6's silent wrong-verdict).

## Phase P0 — F5 nested-argv split
- **Impl** (`src/form/invocation.rs`): add `const NESTED_PARENTS: &[&str] = &["seed-xor","seedqr","slip39","ms-shares","xpub-search"]` + `fn subcommand_argv_tokens(name) -> Vec<&str>` (prefix-set split, `*p` per M-2); replace the single push at `:161-162` with a push of the nested token sequence (one `mask.push(false)` per token). Update the stale assembler doc-comment invariant "argv[1] = subcommand.name" (`:116`) → nested token sequence. Fixes Run AND Copy (single assembler `:152`).
- **Tests:** a `subcommand_argv_tokens` unit tripwire (the 5 parents split; flat `import-wallet`/`export-wallet`/`verify-bundle` pass through unsplit — a future 6th nested parent must be added to `NESTED_PARENTS`, the tripwire fires). Per-nested-subcommand kittest (12 forms): `assemble_argv` → `argv[1..3]` == the nested pair; `argv[1]` is a bare parent. A `render_copy_command` cell: Copy starts `mnemonic seed-xor combine …`.
- **UPDATE (I-1, the COMPLETE set):** `tests/xpub_search_widgets.rs:78,154,224,306` + `tests/widget_interaction.rs:287,335,381,432` — flattened-token `contains` → nested-pair `windows(2)`.

## Phase P1 — F6 inference sentinel + I-3 persistence migration (fold together; funds-critical)
- **Impl (consts):** two GUI-only choice consts with `""` PREPENDED — `XPUB_SEARCH_ADDRESS_TYPES_INFER = ["", "p2pkh","p2sh-p2wpkh","p2wpkh","p2tr"]`, `NETWORKS_INFER = ["", "mainnet","testnet","signet","regtest"]`; point the `address-of-xpub` `--address-type` (`schema/mnemonic.rs:3243`) + `--network` (`:3256`) at them (keep `default_value:None`). Do NOT mutate the shared `NETWORKS`/`XPUB_SEARCH_ADDRESS_TYPES`.
- **Impl (I-3 migration):** extend `normalize_loaded_form_values` (`persistence.rs:342-401`) with a Dropdown branch (INDEPENDENT of the `default_value else return true` gate `:389-391`, since both flags are `None`-default) that, for the persisted key **`"mnemonic:xpub-search-address-of-xpub"`**, drops/`Dropdown("")`s the `--address-type`/`--network` values when they equal `"p2pkh"`/`"mainnet"` → reset to inference. Idempotent (post-fix `""` ≠ the literals).
- **Tests:** F6 virgin-form kittest (mirror `tests/restore_template_none.rs`): a virgin `address-of-xpub` form → `assemble_argv` carries NO `--address-type`/`--network`; masked copy (both flavors) carries neither + no `""` artifact; inverse (select `p2wpkh` → DOES emit). A PREPEND census (`_INFER[0]==""` for both; opposite of restore's APPEND pin). The I-3 migration unit test (fixture keyed EXACTLY `"mnemonic:xpub-search-address-of-xpub"` through `load()` → post-load no `p2pkh`/`mainnet` for the 2 flags; a persisted `p2tr` preserved; a fixture under any other key = no-op). Optional I4 real-CLI cell: virgin address-of-xpub vs the BIP-84 vector → exit 0 match.
- **Snapshot regen (I-2, REQUIRED gate — I-A CORRECTED invocation):** the harness SKIPS unless `GUI_SNAPSHOTS=1`, and this machine has NO lavapipe (only Intel hardware Vulkan), so the sanctioned local path is the llvmpipe-GL route (`gui_form_snapshots.rs:34-37` header): `GUI_SNAPSHOTS=1 WGPU_BACKEND=gl LIBGL_ALWAYS_SOFTWARE=1 UPDATE_SNAPSHOTS=1 cargo test --test gui_form_snapshots` (bare `UPDATE_SNAPSHOTS=1 cargo test …` is a silent GREEN NO-OP — it skips + exits 0 + empty diff). `UPDATE_SNAPSHOTS` rewrites ONLY snapshots whose dify diff > 0.6, so cross-rasterizer sub-threshold drift on the other 60 PNGs does NOT rewrite. **BACKSTOP:** verify the git diff is EXACTLY `tests/snapshots/forms/mnemonic-xpub-search-address-of-xpub.png` (the two dropdown labels now read `(none)`); `.new/.old/.diff.png` are gitignored. Commit that one PNG. F5 churns no PNG. (CI's lavapipe `snapshots` job arbitrates the llvmpipe-produced baseline at the 0.6 threshold.)

## Phase P2 — eval §2 #13 defaults-drift test (couple if clean; else FOLLOWUP)
- Extend `schema_check` with `json_flag_defaults`/`json_flag_choices` (mirror `json_flag_names`); add `tests/schema_mirror_defaults_drift.rs` asserting hand-mirror `default_value`/`choices` == live pinned `gui-schema` JSON. **Strip `""` sentinels + scope to NON-NULL-`choices` flags** (the `--separator` text-kind divergence; M-1). **M-2: follow the existing binary-resolution + skip convention** (`tests/schema_mirror.rs:606-614`: `MNEMONIC_BIN` → PATH fallback → LOUD SKIP when absent) — the required `schema-mirror gate` job installs the pinned v0.75.0 binary (fine), but a dev-machine PATH fallback picks the installed `mnemonic` (here v0.84.0, 9 minors past the pin) → spurious drift; the SKIP-when-absent path avoids a false red. If the pinned v0.75.0 JSON mismatches the current mirror on unrelated fields or the allowlist balloons → STOP, file `gui-schema-defaults-drift-gate` as a FOLLOWUP with the reason, do NOT block F5/F6.

## Post-implementation (mandatory) — Fable whole-diff
Fresh Fable over the whole diff: F5 split correct + no flat-sub corruption + Run/Copy both fixed; F6 virgin emits nothing + only address-of-xpub's 2 dropdowns changed + the I-3 migration resets the exact key/flags/values (persisted `p2tr` preserved); the snapshot diff is exactly the one PNG; kittest green; clippy default + `--no-default-features` clean; no toolkit change. Persist `f5f6-postimpl-whole-diff-review.md`.

## Release ritual (only after whole-diff GREEN) — mnemonic-gui v0.58.0
- Version sites (0.57.0→0.58.0): `mnemonic-gui/Cargo.toml:3` + Cargo.lock self-entry (`:2351`) + **`README.md:47` install one-liner `--tag mnemonic-gui-v0.57.0`→`v0.58.0`** (I-B — de-facto per-release site, UNGATED, bumped every prior release) + **the `CHANGELOG.md` v0.58.0 entry (M-1 — the repo keeps one; ungated)**. NO toolkit-pin bump. NO changelog-check / fmt gate on GUI.
- **FOLLOWUPs:** file `gui-dropdown-none-opts0-materialization-audit` (top target `verify-bundle --template`, G6) in BOTH repos with cross-cites; if #13 deferred, file `gui-schema-defaults-drift-gate`. Note eval-F5+F6 closed.
- **NO `cargo fmt`** (GUI project rule). Ship via PR + CI (or admin bypass push, `enforce_admins:false`) → tag `mnemonic-gui-v0.58.0`. Verify the 5 required contexts: `snapshots` (regen'd PNG), `clippy`, `headless (no-default-features)`, `schema-mirror gate`, `x86_64-unknown-linux-gnu` — plus `tutorial-snapshots` (unaffected). **USE `git commit -F`.** **M-3:** if merging via `gh pr merge` under a worktree, its local-branch cleanup may fail though the server merge succeeds — verify via `gh pr view --json state,mergeCommit`, do NOT re-merge.

## Guard-rails
- **G-A** F5 = closed 5-parent-prefix split (complete-token + `-`); flat subs pass unsplit (unit tripwire). Single assembler → Run+Copy; mask parallel.
- **G-B** F6 `""` PREPENDED (opts[0], inference); only address-of-xpub's 2 dropdowns; shared consts unchanged; virgin emits neither flag.
- **G-C** I-3 migration gated on the EXACT flattened key `"mnemonic:xpub-search-address-of-xpub"` + the 2 flags + old literals `p2pkh`/`mainnet`; deliberate `p2tr` preserved; idempotent; unit-tested through `load()`.
- **G-D** GUI-only; no toolkit change/pin; schema_mirror green; MINOR v0.58.0; snapshot single-PNG regen'd via the FULL env combo `GUI_SNAPSHOTS=1 WGPU_BACKEND=gl LIBGL_ALWAYS_SOFTWARE=1 UPDATE_SNAPSHOTS=1` (bare `UPDATE_SNAPSHOTS` is a no-op — I-A) + git-diff-is-exactly-one-PNG backstop (required gate).
- **G-E** F5+F6 in ONE PR (fixing F5 alone activates F6's silent wrong-verdict).
- **G-F** clippy `-D warnings` BOTH default + `--no-default-features`; no `cargo fmt`; test-only helpers behind `#![allow(dead_code)]`.
