# PLAN — HRP probes go case-insensitive (audit M11): uppercase cards route correctly; codecs stay the case authority

**Cycle:** toolkit v0.53.3 (PATCH) · **Source SHA:** `38db912` (= v0.53.2) · **Recon (THE detailed contract — this plan ratifies its §Recommended scope):** `cycle-prep-recon-hrp-case-insensitive.md`
**Resolves:** `hrp-classifier-rejects-valid-uppercase-cards` (audit M11; FOLLOWUPS index line). **Recon overturned the audit's blanket codec claim:** ms-codec 0.4.0's envelope layer is case-sensitive past codex32 (`envelope.rs:100/:112`) → uppercase ms1 cannot decode until an ms-codec companion ships; mk-codec/md-codec confirmed tolerant (primary-source, pinned crates.io versions).

## Ratified decisions (the three the recon left open)

1. **`validate_flag_hrp`'s deliberate case-mismatch rejection (repair.rs:175-187) is RELAXED** to accept consistent-case values: the uniform rule becomes "**the codecs are the authority on case**" (they reject mixed; they accept-or-reject uppercase per their own wire rules). Rationale: a probe-only fix would ship an inconsistent surface (positional `MK1…` accepted, `--mk1 MK1…` rejected), and the surface is ALREADY inconsistent today (`restore --md1`/`convert` accept uppercase). The I5-fold's "lowercase canonical per BIP-93" stays true for EMISSION (untouched); intake defers to the codecs. BIP-173 uppercase-QR is the real-world reason uppercase cards exist.
2. **`UnknownHrp` echo truncation rider (error.rs:794-800) — YES:** `got` is truncated to the FIRST 12 CHARS + `…` (normative rule — R0-r4 M1-r4) so a near-miss secret-ish positional never echoes in full. (Today an uppercase ms1 hits exactly this — the FULL master secret on stderr.)
3. **Cosmetic `seed_intake.rs:204-213` display-classifier — YES** (one-liner, message quality only).

## Contract = recon §Recommended scope, verbatim

- **Fix shape:** probe-only lowercasing; pass the ORIGINAL string to the codecs. NEVER normalize-at-intake (case-laundering would defeat the codecs' deliberate mixed-case rejection: mk `MixedCase`, ms/codex32 `InvalidCase`).
- **Sites:** `classify_hrp_prefix` (repair.rs:106-119 — one change also fixes the M3 positional advisory at :312 + seed_intake:129), restore.rs:1026, target_intake.rs:24, address_of_xpub.rs:178 (+ comment :179-180), descriptor_intake.rs:155, cmd/silent_payment.rs:134 (NOT src/silent_payment.rs — colliding basename, R0-r3 M1-r3), the decision-1 relaxation, the decision-2 rider, decision-3 cosmetic. Doc-comment rewrites: repair.rs:103-105, :121-124 ("strict per-flag HRP validation" framing), :143-150 (the three-case enumeration — case-mismatch row dies), :169-170, address_of_xpub.rs:179-180 (R0-r1 M2).
- **Tests (TDD red-first):** uppercase MK1 through inspect + restore `--cosigner` + `--target-xpub`; uppercase MD1 through inspect + xpub-search `--descriptor`; uppercase MS1 positional → **advisory FIRES** + ms-codec-attributed error with **NO full-string echo**; a verify-bundle uppercase-positional cell (R0-r1 M5 — verify_bundle.rs:1242 rides the shared fn); **a decision-2 RIDER cell (R0-r3 I1-r3):** a LONG unknown-HRP positional (51-char `xs1…` secret-shaped) → stderr does NOT contain the full string, DOES contain the truncated head + `…`, still contains "does not begin with a recognized HRP prefix", exit 2 (red today — error.rs:796 formats the full `{got}`; green only via the truncation; the 3 existing 10-char `abc1xxxxxx` UnknownHrp cells stay green under a 12-char width); `silent-payment --secret MS1…` → ms-codec-attributed error replaces the generic refusal (R0-r1 I2); mixed-case ms1/mk1 → clean codec-attributed errors; md1 mixed-case ACCEPTED (characterization of md-codec leniency — note, don't fix); `--mk1 MK1…` typed flag accepted post-relaxation (+ `--ms1 MS1…` reaches ms-codec). **INVERT the existing I5 pin (R0-r1 I1, oracle pinned by R0-r2 I1-r2 option (a)):** `validate_flag_hrp_case_mismatch_distinguishable` (`tests/cli_positional_hrp_autodetect.rs:403-427`, asserts `contains("case mismatch")`) keeps its `MS1XXX` fixture and flips to: NO `"case mismatch"` in stderr + the parse-step marker `"parse failed before correction could run"` (the typed-flag repair path pre-gates through the toolkit's own `parse_chunk` at repair.rs:785 — lowercases + length-gates — so the 6-char fixture NEVER reaches ms-codec; asserting codec attribution there would be unsatisfiable) + exit 2. Drop/retarget the third assert (:423-426 `contains("--ms1")` — post-relaxation it passes VACUOUSLY via the M3 advisory line, not error attribution; assert the advisory and the error independently — R0-r2 M1-r2). Rewrite its I5-history doc-comment (:396-402). The positional-MS1 "ms-codec-attributed" cell goes **through inspect** (a true decode path) and pins the literal friendly marker `ms1 wrong HRP: got "MS", expected "ms"` (R0-r2 M2-r2) — **fixture = uppercased `VALID_MS1` (repair.rs:1483), FULL-LENGTH (R0-r3 M2-r3):** a short `MS1XXX` dies at ms-codec's own rule-9 length gate (UnexpectedStringLength) before the envelope HRP check (R0-r4 M2-r4), never the WrongHrp marker. The `--ms1 <UPPER full-length>` relaxation-reaches-ms-codec cell uses the same fixture and pins the REPAIR-path marker `repair: chunk 0 HRP mismatch — expected 'ms', found 'MS'` (the toolkit RepairError::HrpMismatch translation at repair.rs:859-863 — NOT the friendly decode-path string).
- **Docs:** optional one-line case-tolerance note in the manual's inspect/repair prose (full manual lint after); CHANGELOG `[0.53.3]`; version sites (Cargo.toml + Cargo.lock + 2 README markers + **scripts/install.sh:32**); FOLLOWUPS: resolve the index line via a promoted entry recording the overturned-claim + companion split; **companion FOLLOWUPs mirrored both repos:** `mnemonic-secret::ms1-envelope-uppercase-bip173` (envelope case-normalize; then toolkit pin bump flips the ms1 leg) + optional md-codec observation `md-codec-accepts-mixed-case-bip173-leniency` + (R0-r1 M1) `friendly-ms1-invalidchecksum-echoes-full-input` — friendly.rs:79 Debug-prints codex32 InvalidChecksum{string} = the FULL near-secret on stderr for an uncorrectable lowercase ms1; out of this cycle, the rider does not close it.
- **SemVer PATCH** (acceptance/error-path only; no clap/schema/manual-flag lockstep — recon-verified). Advisor confirm at tag; watch all THREE tag-gated workflows.

## Non-goals

Making uppercase ms1 decode toolkit-side (impossible without the companion); emission case changes; normalize-at-intake; the md-codec mixed-leniency "fix".
