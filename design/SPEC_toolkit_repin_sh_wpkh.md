# SPEC — toolkit md-codec 0.40→0.41 re-pin (sh(wpkh) canonical flip + F-A8 variant) — Track B

**Status:** formal SPEC, in R0 loop (SPEC review = **Opus** per user 2026-07-11). Round 1 = OPEN 1C/1I+3M (`design/agent-reports/toolkit-repin-sh-wpkh-spec-r0-round-1.md`) → **C1+M4 REJECTED (contamination: reviewer diffed the P0-dirtied working tree; published 0.41.0 = ONE new variant, git-verified against `md-codec-v0.41.0` tag); I1 FOLDED (S3.5 6-consumer audit + restore.rs:1645 guard test); M2/M3 FOLDED; cross-track `EmptyOriginOverride` arm folded into the partial-decode plan P2.** Round 2 convergence (opus) = C1/M4 rejection UPHELD (sound, tag-verified) + OPEN 1I+3M → FOLDED (I1: 8-consumer audit now complete incl. `restore.rs:319`/`verify_bundle.rs:391` + verify-side guard; M-a F-A9, M-b F-A2 count, M-c S5). Round 3 convergence (opus) = GREEN (0C/0I) — 8-consumer audit complete, folds drift-free. **ERRATA (plan-R0 C1, 2026-07-11):** the plan R0 (a downstream review that traced the toolkit EMIT path) found S3.1/Acceptance-#2/migration-note codified the WRONG flip destination — flip #1 is **empty-origin/master (wpkh-parity), NOT `49'`** (the emit path early-returns without filling the origin; 49' is only the md-codec policy-id/template-completion value). Corrected + I1 (`--slot` refuse scoped to `[Phrase,Path]`). Combined re-converge (opus, `toolkit-repin-sh-wpkh-plan-r0-round-1.md` + empirical binary check) = the empty/master correction FACTUALLY VERIFIED (no-origin `wpkh(@0)` emits master/`m/`, byte-identical to explicit-master; 49' never on the emit path) + OPEN(1I/1M) = the §Testing echo still said 49' + a notice-cite nit → BOTH FOLDED (line 63 → empty/master; `emit_default_path_notice` def ~:2434/call ~:1791), grep-confirmed no stale 49'-emit-default remains. **SPEC+plan GREEN (reviewer conditional pre-bless: "fix I-1 → sound to begin implementation B0-first").** **Toolkit-only cycle** (md-codec 0.41.0 already published — no codec release). Prerequisite for the partial-decode P2 leg (task #12) — must ship first.
**Source:** `cycle-prep-recon-toolkit-repin-sh-wpkh.md`. SHAs: mnemonic-toolkit `7ffce786`, descriptor-mnemonic `a39c9d9f`. **SemVer: v0.86.0 → v0.87.0 (MINOR** — behavior-visible flip + new decode-reject class).
**Slugs resolved:** `toolkit-repin-sh-wpkh-canonical-flip` + `canonical-origin-sh-wpkh-toolkit-mirror-divergence`.

## Why now / what it is
The toolkit pins `md-codec = "0.40.0"` (`crates/mnemonic-toolkit/Cargo.toml:34`); the published md-codec is `0.41.0`. Bumping consumes THREE 0.41.0 deltas (per md-codec CHANGELOG): **F-A1** (`canonical_origin(sh(wpkh(@N)))` `None`→`Some(m/49'/0'/0')`), **F-A2** (`decode_md1_string` chunked auto-dispatch), **F-A8** (new `Error::MalformedPayloadPadding`). This is a DEFERRED funds-labeled re-pin (from the BIP-alignment cycle); it ships as its own R0-gated cycle so its "same-command-different-wallet" flip is attributed + tested + documented separately from partial-decode.

**Delta verified against the `md-codec-v0.41.0` TAG, NOT the descriptor-mnemonic working tree (R0-R1 C1/M4 rejection).** The published 0.41.0 adds exactly **ONE** new `Error` variant — `MalformedPayloadPadding` (git-verified: `git show HEAD:crates/md-codec/src/error.rs` at the committed 0.41.0 = `MalformedPayloadPadding`×1, `EmptyOriginOverride`×0). The R0-R1 reviewer diffed the WORKING TREE, which carries the CONCURRENT, uncommitted partial-decode P0 (same repo) — so `EmptyOriginOverride` + `DecodeOpts`/`*_with_opts`/`unresolved_origin_indices` are Track-A 0.42.0 work, NOT in published 0.41.0. Track B (→ published 0.41.0) needs ONLY the `MalformedPayloadPadding` arm. (The `EmptyOriginOverride` toolkit arm is a partial-decode-P2 concern at the 0.41→0.42 bump — folded into that plan.) When implementing, RE-VENDOR from the `md-codec-v0.41.0` tag / crates.io, not the descriptor-mnemonic working tree.

## Scope

### S1 — pin bump + re-vendor
Bump `crates/mnemonic-toolkit/Cargo.toml:34` `md-codec` 0.40.0 → 0.41.0; update root + `fuzz/Cargo.lock` (via `cargo check`, not sed); **re-vendor** `vendor/md-codec` → 0.41.0 (vendor-freshness gate). install.sh SELF-pin bumps to v0.87.0; **md/ms/mk sibling pins stay FROZEN** (install.sh:35 `md-cli-v0.11.2` — the compiled md-codec dep is orthogonal to the installed sibling CLI; bumping the sibling pin broke `sibling-pin-check` at v0.75.0).

### S2 — F-A8 `MalformedPayloadPadding` exhaustive-match fix (COMPILE-BLOCKING — recon §1)
`md_codec::Error` is NOT `#[non_exhaustive]` (md-codec `error.rs:20`); the toolkit has TWO hand-exhaustive matches that WILL NOT COMPILE at 0.41.0 without a new arm:
- `md_codec_exit_code()` (`error.rs:505-569`) — add `MalformedPayloadPadding { bits } => 2` (the decode-reject class every sibling TLV/chunk-shape variant uses).
- `friendly_md_codec()` (`friendly.rs:216-406`) — add a friendly message.
- **R0 CALL (placement):** the existing blocks are grouped by exit-code-class/chronology, NOT alphabetical (CLAUDE.md's alphabetical rule is for FROM-SCRATCH exhaustive blocks; this is an addition to a pre-existing non-alpha block) — R0 to confirm grouped-placement is correct here, not a forced alpha-sort.
- Add a pinning test `md_codec_v0_41_new_variant_routing` (mirror `error.rs:1222-1269` `md_codec_v0_30_new_variant_routing`) asserting `MalformedPayloadPadding`'s exit code + `kind()`.
- **repair.rs funds-adjacent (recon §1, NOT compile-mandatory; R0-R1 M2):** `reject_non_zero_pad` (new in 0.41.0, `descriptor-mnemonic tlv.rs:308-334`) runs in the shared TLV parser hit by EVERY decode path (incl. `decode_with_correction`), so a non-zero-pad payload now REJECTS where it previously silently passed (`break;`). **Test as a DIRECT decode-path cell (M2 — a BCH-corrected-onto-non-zero-pad codeword is NOT deterministically constructible):** feed a hand-forged non-zero-pad payload to `reassemble`/`decode_payload` → assert `MalformedPayloadPadding`/exit 2. `repair.rs:1710`'s wildcard (`Err(other) => PostCorrectionDecodeFailed{..}`) degrades a mid-repair `MalformedPayloadPadding` to a generic message — assert that exact degraded outcome. (md-codec already unit-tests `reject_non_zero_pad` directly `tlv.rs non_zero_trailing_pad_*`; the toolkit cell only proves the toolkit SURFACES it.)

### S3 — sh(wpkh) canonical flip (F-A1, 4 behaviors — recon `toolkit-repin-sh-wpkh-canonical-flip`)
`canonical_origin(sh(wpkh(@N)))` flips `None`→`Some(m/49'/0'/0')`. The toolkit's descriptor-mode probes consume `is_non_canonical = canonical_origin(tree).is_none()` directly → FOUR observable flips (all funds-visible, currently ZERO test coverage):
1. **(plan-R0 C1 — CORRECTED; NOT 49')** `bundle --descriptor "sh(wpkh(...))"` NO-ORIGIN default: pre-repin `is_non_canonical=true` → default-inference `m/48'/0'/0'/1'` **+ a notice**; **post-repin `is_non_canonical=false` (bundle.rs:1416-1418) → `bind_descriptor_mode_paths` EARLY-RETURNS (no default-inference, bundle.rs:2262-2266) → the origin stays EMPTY → derives at the MASTER key (`m/`), notice ABSENT** (wpkh-parity — identical to `wpkh(@0)`/`pkh(@0)`/`tr(@0)` no-origin TODAY). md-codec's `49'` table value is used ONLY for policy-id hashing (`identity.rs`) + the n≥2 template-completion `canonical_fallback` — NEVER on the toolkit's bundle EMIT default-origin path. **SAME COMMAND, DIFFERENT WALLET** (`48'/0'/0'/1'`+notice → empty/master). ASSERT: mk1 `origin_path: m/`, master xpub, `emit_default_path_notice` (def ~:2434, called ~:1791; plan-R0 M-a) ABSENT.
2. `bundle --descriptor sh(wpkh) --account≠0` succeed→REFUSE (bundle.rs:1421-1427 `ModeViolation`).
3. `bundle --descriptor sh(wpkh) --slot @N.path=` override succeed→REFUSE — **BUT ONLY for a SECRET-BEARING slot set `[Phrase|Seedqr|Ms1, Path]` (bundle.rs:1429-1453, §6.6 row 4); plan-R0 I1.** An `[Xpub, Path]` set does NOT refuse (succeeds + honors the path, no pre/post delta) — pin `[Xpub, Path]`-still-succeeds as a companion assertion or note out-of-scope.
4. A pre-repin elided-sh(wpkh) bundle — a HARDCODED fixture captured PRE-bump (post-bump the command can't emit it; plan-R0 M4) → fails verify post-repin — fail-LOUD, NOT a silent false-pass. **Mechanism (R0-R1 M3):** `verify_bundle.rs:1408-1414` is the canonicity PROBE, not the refusal site; the actual refusal is a downstream md1 BYTE-MISMATCH (post-repin re-derives an EMPTY-origin md1 that can't match the stored explicit-48' card). Assert the byte-mismatch, not a probe-level error.
**Tests (S3):** author the 4-flip coverage (each with several assertions) — the flip is the funds-load-bearing behavior change. Confirm the NEW behavior for flip #1 is **empty-origin/master (wpkh-parity), NOT 49'** (the old `48'/0'/0'/1'` default came from `bip48_script_type_for_root_tag(Sh)→1'`, a BIP-48 MULTISIG script-type nonsensical for single-key sh(wpkh); the new canonical behavior elides the origin like every other canonical shape).

### S3.5 — `canonical_origin` consumer audit (R0-R1 I1, funds-adjacent completeness)
The flip must disposition EVERY `canonical_origin()` consumer in toolkit `src/`, not just the 2 probes:
| Site | Use | Post-flip (sh(wpkh)) | Disposition |
|---|---|---|---|
| `bundle.rs:1416-1418` | `is_non_canonical` probe | flips → the 4 S3 behaviors | S3 (tested) |
| `verify_bundle.rs:1408-1414` | `is_non_canonical` probe | flips → verify byte-mismatch | S3 (tested; M3) |
| `restore.rs:1645` | `canonical_origin(&tree).ok_or_else(...)` — **EXTRACTS origin as a DERIVATION path** in the general/multisig `canonical_fallback` | pre `None`→err, post `Some(49')`→**derives** | **SAFE but load-bearing → GUARD TEST REQUIRED.** Unreachable for sh(wpkh): `is_singlesig_template` (`restore.rs:317-320`) requires `cli_template_from_tree(...).is_some()` = FALSE for sh(wpkh) (no-`Sh`-arm mirror), AND this is the n≥2 general path while sh(wpkh) is n==1. Test: an n==1 sh(wpkh) keyless-template md1 still refuses and NEVER reaches `canonical_fallback` with a 49' derivation. |
| `synthesize.rs:1201` | `if canonical_origin(...).is_some()` elides template origins | gated | SAFE — behind template-emission refusal (no-`Sh`-arm gate); confirm no sh(wpkh) template is emitted |
| `bundle.rs:1199` | `if ...is_none()` gates a "GENERAL POLICY" stderr warning | warning suppressed for sh(wpkh) | SAFE (warning-only, benign; if any test asserts that stderr line, update it) |
| `restore.rs:319` | `canonical_origin(...).is_some()` conjunct in `is_singlesig_template` (RESTORE route) | flips None→Some | SAFE — neutralized by the `&& cli_template_from_tree(&tree).is_some()` conjunct (no-`Sh`-arm → FALSE for sh(wpkh)); `is_singlesig_template` stays FALSE regardless of the flip (this is the block the `restore.rs:1645` reachability arg cites) — convergence R0 I1 |
| `verify_bundle.rs:391` | `canonical_origin(...).is_some()` conjunct in `is_singlesig_template` (VERIFY route, mirror of restore.rs:319) | flips None→Some | SAFE — same `cli_template_from_tree` neutralization. **VERIFY-SIDE GUARD ASSERTION REQUIRED** (a verify false-pass is a funds risk): mirror the restore guard — an n==1 sh(wpkh) card never routes into the single-sig-template verify path via this gate post-flip — convergence R0 I1 |
| `gui_schema.rs:1320` | `--classify-descriptor` verdict | `non-canonical`→`canonical` | S4 (verdict test cell) |
(8 total consumers.) The `restore.rs:1645` guard + the `verify_bundle.rs:391` symmetric guard are the funds-load-bearing cells — the only two that (absent the `cli_template_from_tree` neutralization) could silently derive/verify at a DIFFERENT path.

### S4 — 6 comment sites: intentional-divergence (companion slug — recon `canonical-origin-sh-wpkh-toolkit-mirror-divergence`)
Post-repin, `canonical_origin(&tree).is_some()` becomes NECESSARY-BUT-NOT-SUFFICIENT for template-admissibility (the real gate is `cli_template_from_tree`, which has NO `Sh` arm). Update the 6 stale comments to state the divergence is now INTENTIONAL: `synthesize.rs:349-368` (cli_template_from_tree "Mirrors canonical_origin"), `synthesize.rs:50-52` (Md1Form doc), `synthesize.rs:1092-1112` (template_admissible bip49-refused rationale), `restore.rs:303` (3-clause comment vs 4-clause code :317-320), `tests/cli_bundle_md1_template_form.rs:239` (inline "canonical_origin returns None" rationale), `error.rs:343-349` (`TemplateFormUnsupportedShape` doc "e.g. bip49" — becomes actively wrong). **DO NOT FIX THE MIRROR** — `cli_template_from_tree` (`synthesize.rs:359-368`) stays no-`Sh`-arm; the sh(wpkh) template refusal is UNCHANGED (only the comment's *reasoning* was wrong). `gui_schema.rs:1317-1320` `--classify-descriptor` verdict flips `non-canonical`→`canonical` for sh(wpkh) — add a test cell pinning the new verdict.

### S5 — manual mirror (MANDATORY same-PR — recon §4)
`docs/manual/src/40-cli-reference/41-mnemonic.md`: line **:418** (`### Non-canonical descriptor mode`) hard-codes "**five** canonical wrapper shapes" → **six**; add `sh(wpkh(@N))` + adjust the "anything else is non-canonical" framing. This is the ONE mandatory five→six edit (M-c). The `--classify-descriptor` flag table + worked examples at ~2989-3005 have NO "five" string / no shape enumeration — OPTIONALLY add a `sh(wpkh)→canonical` worked example there (pairs with the S4 gui-schema verdict cell), NOT a five→six edit. (NOT the `m/49'/0'/0'` cites in 31-singlesig-steel.md:163 / 63-bip32-primer.md:49 — `--template bip49` entropy-mode, a separate path, stay correct.)

### S6 — GUI companion FOLLOWUP (FILE, no code — recon §5)
File a new entry in `mnemonic-gui/design/FOLLOWUPS.md` (mirror `canonical-origin-sh-wpkh-toolkit-mirror-divergence`): `conditional.rs:58-101` `classify_descriptor_canonicity()` reimplements the canonical table as 5 regexes with its own lagging gate `tests/canonicity_drift.rs`. No drift YET (GUI toolkit pin frozen v0.75.0, far behind). To be picked up whenever the GUI next bumps its toolkit pin past this release — NOT this cycle (GUI pin bump = separate release event). Paper trail per the paired-PR discipline.

### Dispositions (name explicitly so R0/reviewers don't hunt)
- **F-A2** (decode_md1_string chunked auto-dispatch): LIKELY no-op for the toolkit (production decode uses `chunk::reassemble` directly at restore.rs:316/verify_bundle.rs:388; the **THREE** `decode_md1_string` calls are ALL NON-CHUNKED test fixtures — `prop_repair_never_wrong.rs:483`, `tests/cli_inspect.rs:234`, `tests/cli_repair_md1_non_chunked.rs:126` [M-b: not 1]; and md-codec's single-payload versions {4,8,12} are all-even ⇒ first-symbol LSB=0 ⇒ never diverts). CONFIRM via the FULL suite (not targeted), per the standing convention that decode-adjacent re-pins ripple.
- **F-A9** (M-a — 0.41.0 also changes `md_codec::Error::TooManyErrors` Display message, a "comment/message correction"): NO-OP for the toolkit — `friendly_md_codec` re-derives its own message from the `chunk_index`/`bound` fields (`friendly.rs:376-377`), `bound` unchanged, and no toolkit test pins md-codec's Display. Named so reviewers don't hunt; covered by the full-suite gate. (`chunk.rs`/`bch.rs` tag deltas are comment-only; `test_vectors.rs` is a corpus the toolkit doesn't consume.)
- **F-A3/F-A4**: md-CLI-only (separate CHANGELOG entry `md-cli [0.12.0]`), NOT md-codec — the toolkit never links md-cli as a library (frozen sibling binary). Zero toolkit compiled surface. No action.
- **Migration note (CHANGELOG):** the sibling-pin semantic-inconsistency window — post-repin `mnemonic gui-schema --classify-descriptor 'sh(wpkh(...))'` says `canonical` while the installed `md` sibling (frozen md-cli-v0.11.2 = md-codec 0.40.0) still treats it as needing explicit origin. Window closes only when a future separate release bumps the sibling pin to md-cli-v0.12.0+ (orthogonal, later). Not a reason to break the frozen-pin discipline.

## Testing
- S3 4-flip coverage (bundle default-origin **empty/master — wpkh-parity, NOT 49'** (plan-R0 C1); --account≠0 refuse; --slot refuse for `[Phrase,Path]` only (I1); pre-repin elided-sh(wpkh) verify-fail byte-mismatch) — the funds-load-bearing cells.
- S2 `md_codec_v0_41_new_variant_routing` (exit 2 + kind) + the repair non-zero-pad-rejected funds cell.
- S4 gui_schema `--classify-descriptor sh(wpkh)` → `canonical` verdict cell.
- Full `cargo test -p mnemonic-toolkit` (confirms F-A2 no-op + no other 0.41.0 ripple); clippy `-D warnings`; NEVER `cargo fmt --all` (per-package; mlock.rs diff empty); `make -C docs/manual lint` + verify-examples (S5 manual + any `md`/`bundle` transcript drift — regen with the REAL binary if drifted).
- Vendor-freshness green (re-vendor to 0.41.0).

## Release ritual (MINOR v0.86.0→v0.87.0)
Version at Cargo.toml:3, both READMEs (`toolkit-version` markers), root + fuzz Cargo.lock (via cargo check), install.sh SELF-pin (sibling FROZEN), `.examples-build` gen.sh pins + regen (confirm no sh(wpkh)/bundle example drift), Examples.pdf if drifted, CHANGELOG (LOUD sh(wpkh) migration note — **no-origin `sh(wpkh)` now emits an ELIDED-origin/master-key card (wpkh-parity): `48'/0'/0'/1'`+notice → empty/master; to reproduce a pre-repin wallet pass the explicit `[fp/48'/0'/0'/1']` origin. Do NOT say the new default is 49' — the emit path elides the origin (plan-R0 C1)** — + the F-A8 new-reject-class note + the sibling-pin window). re-vendor md-codec 0.41.0. No GUI schema-mirror (no flag delta). Manual mirror same-PR (S5). Tag `mnemonic-toolkit-v0.87.0` on CI-green.

## Acceptance
1. Compiles at md-codec 0.41.0 (S2 `MalformedPayloadPadding` arm in BOTH exhaustive matches; routing-pin test green).
2. sh(wpkh) 4 flips tested + correct-new-behavior (**flip #1 = empty-origin/master, wpkh-parity, NOT 49' — plan-R0 C1**); refusal-classes fire (flip #3 scoped to `[Phrase,Path]`; `[Xpub,Path]` still succeeds — I1); no silent false-pass; flip-4 asserts the byte-mismatch mechanism (M3). **S3.5 8-consumer `canonical_origin` audit complete + the `restore.rs:1645` guard test + the `verify_bundle.rs:391` VERIFY-side guard assertion** (n==1 sh(wpkh) never reaches `canonical_fallback` with a 49' derivation on restore, and never routes into the single-sig-template verify path — the two funds-load-bearing consumers).
3. 6 comments updated (intentional-divergence framing); mirror NOT fixed (`cli_template_from_tree` no-`Sh`-arm); gui-schema verdict cell.
4. Manual 5→6 (S5, same PR); GUI companion FOLLOWUP filed (S6).
5. repair non-zero-pad funds cell; F-A2 confirmed no-op by full suite.
6. Full suite green; clippy; manual lint + verify-examples; vendor-freshness; no `cargo fmt --all`.
7. v0.87.0 release ritual; both slugs RESOLVED (toolkit design/FOLLOWUPS.md); migration note.
8. Per-phase + post-impl R0 (Fable). The sh(wpkh) flip + F-A8 routing + repair non-zero-pad cell are the funds-load-bearing pieces.

## Phasing
Single implementer, one toolkit cycle: (1) pin+re-vendor + S2 F-A8 arm (unblocks compile) → (2) S3 flip tests + S4 comments + gui-schema cell → (3) S5 manual + S6 GUI FOLLOWUP + release ritual. Per CLAUDE.md R0 before impl; per-phase R0; post-impl whole-diff before tag. Ships BEFORE partial-decode P2.
