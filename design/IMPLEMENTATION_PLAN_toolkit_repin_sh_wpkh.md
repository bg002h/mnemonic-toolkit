# IMPLEMENTATION PLAN — toolkit md-codec 0.40→0.41 re-pin (sh(wpkh) flip) — Track B

**Status:** plan-doc, pre-R0. Feeds the mandatory PLAN R0 gate (**Fable** per user 2026-07-11) → 0C/0I before ANY code. Source: `design/SPEC_toolkit_repin_sh_wpkh.md` (R0-GREEN). **Toolkit-only, v0.86.0 → v0.87.0.** Prerequisite for partial-decode P2. SHAs: toolkit `7ffce786`, published md-codec = `md-codec-v0.41.0` tag (`546346c1`).
**Single implementer, per-phase TDD (RED first), per-phase R0 (full `cargo test -p mnemonic-toolkit`), post-impl whole-diff R0, tag on CI-green.**

## CRITICAL implementation note
**Re-vendor from the `md-codec-v0.41.0` TAG / crates.io 0.41.0, NOT the descriptor-mnemonic working tree** (which carries the concurrent Track-A partial-decode P0 = the unpublished 0.42.0 `EmptyOriginOverride`/`DecodeOpts` etc.). Published 0.41.0 adds exactly ONE new `Error` variant: `MalformedPayloadPadding`.

## PHASE B0 — pin bump + F-A8 arm (COMPILE-BLOCKING; do first)
- **B0.1 (S1):** `crates/mnemonic-toolkit/Cargo.toml:34` `md-codec` 0.40.0 → 0.41.0; `cargo check` to update root + `fuzz/Cargo.lock` (not sed); **re-vendor** `vendor/md-codec` → 0.41.0 (from the tag/crates.io). install.sh SELF-pin → v0.87.0; **md/ms/mk sibling pins FROZEN** (install.sh:35 `md-cli-v0.11.2` untouched).
- **B0.2 (S2, compile-blocking):** add `MalformedPayloadPadding { bits } => 2` to BOTH exhaustive matches — `md_codec_exit_code()` (`error.rs:505-569`) + `friendly_md_codec()` (`friendly.rs:216-405`); grouped placement (not alpha — additions to a pre-existing grouped block). Add pinning test `md_codec_v0_41_new_variant_routing` (mirror `error.rs:1222-1269`) → exit 2 + `kind()`.
- **B0.3 (S2 repair cell, M2):** direct decode-path cell — hand-forged non-zero-pad payload → `reassemble`/`decode_payload` → `MalformedPayloadPadding`/exit 2; assert `repair.rs:1710` wildcard degrades a mid-repair occurrence to `PostCorrectionDecodeFailed`.
- **B0 gate:** compiles; `cargo test -p mnemonic-toolkit` green; clippy. Per-phase R0.

## PHASE B1 — sh(wpkh) flip tests + consumer audit + comments
- **B1.1 (S3, funds-load-bearing):** 4-flip test coverage (each several assertions): (1) **[plan-R0 C1 — NOT 49']** `bundle --descriptor "sh(wpkh(...))"` no-origin default: post-repin `is_non_canonical=false` → `bind_descriptor_mode_paths` EARLY-RETURNS → EMPTY-origin/MASTER-key card (wpkh-parity) — assert mk1 `origin_path: m/`, master xpub, `emit_default_path_notice` (def ~:2434, called ~:1791; plan-R0 M-a) ABSENT (was `48'/0'/0'/1'`+notice); (2) `--account≠0` → `ModeViolation` refuse; (3) `--slot @N.path=` → refuse **ONLY for a SECRET-BEARING `[Phrase|Seedqr|Ms1, Path]` slot set (`bundle.rs:1429-1453`); pin `[Xpub, Path]`-STILL-SUCCEEDS as a companion (I1)**; (4) a HARDCODED pre-bump elided-sh(wpkh) fixture → verify fails post-repin as a **md1 BYTE-MISMATCH** (empty-vs-48', M3 — assert the mismatch, not the probe).
- **B1.2 (S3.5 audit guards):** `restore.rs:1645` guard test (n==1 sh(wpkh) keyless-template md1 still refuses, never reaches `canonical_fallback` with a 49' derivation) + `verify_bundle.rs:391` VERIFY-side guard assertion (n==1 sh(wpkh) never routes into the single-sig-template verify path post-flip). These are the 2 funds-load-bearing consumers; the other 6 need no test (SAFE-by-inspection per the S3.5 table).
- **B1.3 (S4):** update the 6 stale comments to intentional-divergence framing (`canonical_origin.is_some()` = necessary-not-sufficient) at `synthesize.rs:349-368/:50-52/:1092-1112`, `restore.rs:303`, `tests/cli_bundle_md1_template_form.rs:239`, `error.rs:343-349`. **DO NOT FIX THE MIRROR** (`cli_template_from_tree` stays no-`Sh`-arm). gui-schema verdict cell: `mnemonic gui-schema --classify-descriptor 'sh(wpkh(...))'` → `canonical` (was `non-canonical`).
- **B1 gate:** full suite green (incl. any pre-existing sh(wpkh) test whose expectation flips — update it); clippy. Per-phase R0.

## PHASE B2 — manual + release ritual
- **B2.1 (S5, mandatory same-PR):** `docs/manual/src/40-cli-reference/41-mnemonic.md:418` "five" → "six" canonical wrapper shapes + add `sh(wpkh(@N))` + adjust the "anything else is non-canonical" framing. (Optionally a `sh(wpkh)→canonical` worked example near ~2989-3005 — NOT a five→six edit.)
- **B2.2 (S6):** file `mnemonic-gui/design/FOLLOWUPS.md` companion entry for `conditional.rs:58-101` `classify_descriptor_canonicity()` regex table (no code — GUI pin frozen v0.75.0; picked up at the next GUI pin bump).
- **B2.3 dispositions in-doc:** name F-A2 (3 non-chunked calls, no-op) + F-A9 (`TooManyErrors` Display no-op) + F-A3/F-A4 (md-cli-only) so the whole-diff reviewer doesn't hunt.
- **B2.4 release ritual (MINOR v0.86.0→v0.87.0):** version at Cargo.toml:3, both READMEs (`toolkit-version` markers), root + fuzz Cargo.lock, `scripts/install.sh` SELF-pin (~:32 `mnemonic-toolkit-v0.86.0`→v0.87.0; md sibling pin ~:36 in `component_info()` `descriptor-mnemonic-md-cli-v0.11.2` stays FROZEN — plan-R0 M1), `.examples-build` gen.sh pins + regen (confirm no sh(wpkh)/bundle example drift — recon: no sh(wpkh) in the corpus/manual transcripts → none expected; regen with the REAL binary), Examples.pdf if drifted, **CHANGELOG (LOUD sh(wpkh) migration note — no-origin `sh(wpkh)` now emits an ELIDED-origin/master-key card [wpkh-parity]: `48'/0'/0'/1'`+notice → empty/master; TO REPRODUCE A PRE-REPIN WALLET PASS explicit `[fp/48'/0'/0'/1']`; do NOT say 49' — plan-R0 C1)** + F-A8 new-reject-class note + the sibling-pin semantic-inconsistency window note). re-vendor md-codec 0.41.0 from the **`md-codec-v0.41.0` TAG** (vendor-freshness). Manual mirror same-PR (B2.1). No GUI schema-mirror (no flag delta). Resolve BOTH slugs (`toolkit-repin-sh-wpkh-canonical-flip` + `canonical-origin-sh-wpkh-toolkit-mirror-divergence`) in the shipping commit.
- **B2 gate:** full suite + clippy + `make -C docs/manual lint` + verify-examples + vendor-freshness green. **Post-impl whole-diff R0 (Opus)** before tag. Tag `mnemonic-toolkit-v0.87.0` on CI-green (codec/toolkit = direct-FF + tag).

## Acceptance = SPEC §Acceptance 1-8. Gate at every phase: 0C/0I R0 before advancing. Ships BEFORE partial-decode P2.
