# SPEC — Cycle I: test-hardening — make suites gate merges (#1) + wire wc-codec into CI (#2)

**Source SHA:** toolkit `e6b87323` (master; v0.83.0). Sibling repos at their current HEADs (recon 2026-07-10).
**Finding:** constellation-eval 2026-07-06 §2 cross-cutting **#1** (no repo's suite gates merges) + **#2** (wc-codec engine + fuzzers run in NO CI). `design/agent-reports/constellation-eval-2026-07-06.md:207-222`. Eval calls #1 "the single highest-leverage change."
**SemVer:** #2 = toolkit CI-only change, **NO version bump** (no crate/CLI/wire change — a NO-BUMP master commit, precedent: the g4a follow-up `293a887a`). #1 = `gh api` branch-protection config on 5 repos, no commit.

**R0 status:** SPEC-R0 round 1 (Fable) = NOT GREEN → 2 Critical + 2 Important + 3 Minor, ALL FOLDED (round 2 re-dispatch pending). Review `design/agent-reports/cycleI-spec-r0-round-1.md`. Headline catch: **path-filtered `pull_request` triggers** on toolkit + ms rust.yml would wedge every docs-only PR at "Expected — waiting for status" if their contexts were required (the repo's own `examples.yml` ruling) — #2 now also removes those PR path filters. Plus `--workspace` (anti-recurrence) over `-p`, fuzz-smoke paths, and the `checks[]`+`app_id` PUT shape.

## 0. Two independent workstreams
- **#2 (code/config, toolkit repo):** wc-codec's 100 tests + 2 fuzzers currently run in NO workflow (`cargo test -p mnemonic-toolkit` is package-scoped; `wc-codec` is a workspace member but never `-p`'d for tests; fuzz-smoke only builds `descriptor_parse`). Clippy IS workspace-wide (`clippy --all-targets`, rust.yml:200) so it already covers wc-codec — tests + fuzz are the gap.
- **#1 (branch-protection config, 5 repos):** md/mk/ms have ZERO protection; toolkit requires only `[examples]`; gui only `[snapshots]`. Add the reliable test/clippy contexts to `required_status_checks` on each default branch, `enforce_admins: false` (preserve the direct-FF release ritual + survive infra flakes like the macOS rustc ICE seen on v0.83.0), `strict: false`.

**Ordering:** land #2 FIRST — the toolkit CI edit (wc-codec into `--workspace` test + fuzz + **remove the PR path filter, C1**) AND the ms CI edit (**remove the PR path filter, C2** — §1b), each pushed + verified green — THEN apply #1 (require the now-broader, no-longer-path-wedged contexts). A required context whose workflow can't fire on a docs-only PR wedges it; the #2 path-filter removals are the precondition for a safe toolkit/ms PUT.

## 1. #2 — wire wc-codec tests + fuzzers into toolkit CI
Files: `.github/workflows/rust.yml`, `.github/workflows/fuzz-smoke.yml`.

**Tests (rust.yml `test` job, ~:105-112):** the main build+test steps run `cargo build --tests -p mnemonic-toolkit` (:105) + `cargo test -p mnemonic-toolkit` (:112). Change BOTH to **`--workspace`** (`cargo build --tests --workspace` / `cargo test --workspace`) — the **anti-recurrence** choice (R0-I1): the nested fuzz sub-crate `crates/wc-codec/fuzz/` is its OWN `[workspace]` (own Cargo.lock) and is INVISIBLE to the root `--workspace` (verified: `cargo metadata` workspace_members = exactly `mnemonic-toolkit` + `wc-codec`), so `--workspace` picks up wc-codec's tests WITHOUT the fuzz sub-crate, and a future 3rd workspace member is auto-covered (eval #2 happened precisely because wc-codec was a member never `-p`'d). The mlock-specific `-p mnemonic-toolkit --lib mlock::…` steps (:126/:138/:153/:174/:191) stay unchanged (mlock lives only in mnemonic-toolkit). The context/job name `test (ubuntu-latest)` is unchanged (job name, not step) — so #1 requires the same context name and it now gates wc-codec's 100 tests. Verify locally: `cargo test --workspace` green (mnemonic-toolkit suite + wc-codec's ~100 tests incl. the forced-cross-codeword-miscorrection-refused KATs).

**C1 (R0-Critical) — remove the toolkit rust.yml `pull_request` path filter (the wedge fix, land in the SAME #2 commit).** rust.yml (:14-27) filters BOTH `push` and `pull_request` on `crates/**`/`Cargo.toml`/`Cargo.lock`/`.github/workflows/rust.yml`. A required `test (ubuntu-latest)`/`clippy` from a path-filtered workflow WEDGES every docs-only PR forever at "Expected — waiting for status" (the repo's own `examples.yml:9-14` ruling). Fix: DELETE the `paths:` list under `pull_request:` (keep the `push:` `branches`+`paths` filter — admin direct pushes bypass required checks anyway, so docs-only direct pushes are unaffected). rust.yml then runs on EVERY PR → reports the contexts → green on a docs-only PR (code unchanged → tests pass). (The `examples.yml`-style born-green early-exit guard to skip the heavy matrix on docs-only PRs is an OPTIONAL efficiency follow-up — NOT required for correctness; removing the PR path filter alone closes the wedge. Defer the guard to a separate cycle if CI-minutes on the rare docs PR matter.)

**Fuzzers (fuzz-smoke.yml):** wc-codec's fuzz dir is `crates/wc-codec/fuzz/` (targets `wc_roundtrip`, `wc_decode_never_panics`), SEPARATE from the toolkit `fuzz/` (`descriptor_parse`). The build job (:51-73) + smoke-run job (:75-104) currently operate on the top-level `fuzz/`. Add the wc-codec fuzz dir via `--fuzz-dir crates/wc-codec/fuzz` (a real cargo-fuzz flag; verified `cargo +nightly-2026-04-27 fuzz build --fuzz-dir crates/wc-codec/fuzz --target x86_64-unknown-linux-gnu` → exit 0, both targets built; the nested workspace pins the same nightly and cannot collide with top-level `fuzz/`). Preferred shape: extend the smoke matrix to include the 2 wc-codec targets with a per-target `--fuzz-dir` (M3: matrixing renames the smoke check-run to `cargo fuzz run (60s smoke) (<target>)` — harmless, never a required context; md/ms already use the matrixed shape). Mirror the existing pins (`nightly-2026-04-27`, `--target x86_64-unknown-linux-gnu`, 60s smoke, crash-artifact upload).

**I2 (R0-Important) — extend fuzz-smoke.yml `paths:` to cover wc-codec.** The push+PR `paths:` (:29-41) are `fuzz/**`, `crates/mnemonic-toolkit/src/parse_descriptor.rs`, `…/lib.rs`, the workflow file — so the new wc-codec fuzz build would NOT fire on wc-codec changes (only the daily cron), defeating the build job's early-drift purpose. Add `crates/wc-codec/fuzz/**` + `crates/wc-codec/src/**` to BOTH the `push` and `pull_request` paths lists. (fuzz-smoke is NOT a required context → no wedge from its path filter; this is coverage completeness, not a gating fix.)

**Guard:** the #2 change adds coverage only — it must not change the `test (ubuntu-latest)` / fuzz job NAMES (else #1's required contexts drift). Confirm the job-name contexts are stable after the edit.

## 1b. C2 (R0-Critical) — remove the ms rust.yml `pull_request` path filter (separate ms-repo NO-BUMP commit)
`mnemonic-secret/.github/workflows/rust.yml` (`ffc9d71`, :19-35) filters BOTH `push` and `pull_request` on `crates/ms-cli/**`/`crates/ms-codec/**`/`Cargo.toml`/`Cargo.lock`/the workflow file. Requiring `test (ubuntu-latest)`/`clippy`/`test (ms-codec)`/`clippy (ms-codec)` would wedge every docs-only ms PR (e.g. a `design/FOLLOWUPS.md` companion — routine per the cross-repo convention). Fix (mirror C1): a small **ms-repo CI-only NO-BUMP commit** DELETING the `paths:` under `pull_request:` (keep the `push:` filter), pushed to ms `master`, verified green, BEFORE the ms branch-protection PUT. This SUPERSEDES the old §3 "md/mk/ms code untouched" (that line is corrected below). If deferring the ms CI edit, DROP the ms row from the §2 matrix this cycle instead (do NOT PUT ms contexts against a path-filtered workflow). md/mk/gui need no CI edit (no path filters — verified reporting on docs-only commits).

## 2. #1 — branch-protection required-context matrix (exact, verified contexts)
For EACH repo, PUT `required_status_checks` with the listed contexts, `enforce_admins: false`, `strict: false`, and NO required PR reviews (the direct-FF/admin model is preserved; contexts gate PRs + non-admins, admin bypasses with a recorded bypass). Every listed context was verified (recon 2026-07-10) to (a) exist as a job, (b) run on push to the default branch, (c) be currently green. **`test (macos-latest)` / `cargo test (macos-latest)` / windows are DELIBERATELY EXCLUDED** (macOS just ICE'd on vendored build scripts on v0.83.0 — a required flaky context would wedge non-admin merges; ubuntu is the reliable canonical).

| Repo | Branch | Add contexts (keep existing) |
|---|---|---|
| `bg002h/mnemonic-toolkit` | master | keep `examples`; add `test (ubuntu-latest)`, `clippy` |
| `bg002h/descriptor-mnemonic` (md) | main | create: `cargo test (ubuntu-latest)`, `cargo clippy` |
| `bg002h/mnemonic-secret` (ms) | master | create: `test (ubuntu-latest)`, `clippy`, `test (ms-codec)`, `clippy (ms-codec)` |
| `bg002h/mnemonic-key` (mk) | main | create: `build (stable on ubuntu-latest)` (test+clippy are STEPS inside this job — verified ci.yml:52/55/58) |
| `bg002h/mnemonic-gui` | master | keep `snapshots`; add `clippy`, `headless (no-default-features)`, `schema-mirror gate`, `x86_64-unknown-linux-gnu` |

**Application method (per repo):** `gh api -X PUT repos/<repo>/branches/<branch>/protection` with a JSON body: `required_status_checks: {strict:false, checks:[{"context":"<ctx>","app_id":15368}, …]}` (M1: the `checks[]` form with `app_id:15368` = GitHub Actions, matching the existing `[examples]`/`[snapshots]` entries' app binding, vs the legacy `contexts:[]` array which records `app_id:null` = any-app-satisfies), `enforce_admins:false`, `required_pull_request_reviews:null`, `restrictions:null` (all four top-level keys required by the API). For md/mk/ms (no protection), this CREATES it. After each PUT, GET the protection back and assert the `checks` contexts + `enforce_admins:false` round-trip (fail-loud on drift).

## 3. Out of scope (separate eval items / cycles)
- Test items §2 #3-#15 (property/oracle harnesses, wire goldens) — future cycles; #1+#2 land first so those new tests actually gate.
- Do NOT set `enforce_admins:true` anywhere (would break the direct-FF release ritual + block admin flake-bypass). Do NOT add required PR reviews. Do NOT set `strict:true` (would force branch-up-to-date on every push).
- **CORRECTED (was "md/mk/ms/gui code untouched"):** #2 now edits toolkit rust.yml + fuzz-smoke.yml AND ms rust.yml (the C2 PR-path-filter removal — CI-only, NO-BUMP, no crate/version change). md/mk/gui remain untouched by #2 (no path filters). #1 is config only.

## 4. Guard-rails
- **G1** Require ONLY contexts verified to run on push to the default branch + currently green + reliable (ubuntu, not macOS/windows). A tag-only or never-running required context wedges every push (for non-admins) — the primary hazard.
- **G2** `enforce_admins:false` on ALL five (preserve direct-FF + admin bypass; the release ritual's admin-bypass pushes keep working, now with a recorded bypass for the new contexts).
- **G3** Land #2 (wc-codec into `test (ubuntu-latest)`) + verify GREEN in CI BEFORE requiring `test (ubuntu-latest)` on toolkit — so the newly-required context is green on application.
- **G4** After each protection PUT, GET-verify the contexts + `enforce_admins:false` round-trip; if a PUT drifts, revert. Reversible: `gh api -X DELETE .../protection` (md/mk/ms) or PUT back the prior `[examples]`/`[snapshots]` set (toolkit/gui).
- **G5** #2 must not rename the `test (ubuntu-latest)` / fuzz job-name contexts (would desync #1's required set).
- **G6** No version bump for #2 (CI-only); no codec/GUI code change; no sibling-pin/re-vendor.
