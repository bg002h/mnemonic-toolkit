# SPEC — Test-hardening T5 (GUI): #14 canonicity h-notation + #15 bundle→restore independent oracle

**Repo:** `mnemonic-gui` ONLY. SHA at authoring (recon 2026-07-10): gui `5d88286`; recon = `design/RECON_T5_gui_test_gaps.md` (in the toolkit repo). Eval items §2 #13/#14/#15.
**Origin:** constellation-eval §2 `### GUI` cluster. **#13 (`schema_mirror_defaults_drift.rs`) is ALREADY SHIPPED** (gui `3c5c7dd`, PR #32, v0.58.0; CI-gated required check `schema-mirror gate`) — **NO new work; out of scope** except the optional S4 addendum. T5's real work = #14 (partially open) + #15 (fully open).
**Nature:** additive TEST code only, `mnemonic-gui/tests/`. No `src/` change, no CLI-surface change, no toolkit change, no pin bump. Each new cell RED under a named mutation the existing suite survives. If a test surfaces a real bug → STOP + report.
**Parallelism:** T5 has **zero dependency on T2/T3/T4** — the GUI's toolkit dep is **tag-pinned** at `mnemonic-toolkit-v0.75.0` (`Cargo.toml:76`, `pinned-upstream.toml:22`); NO-BUMP test-only toolkit changes produce no new tag, so nothing moves the pin. Both T5 surfaces (`gui-schema --classify-descriptor` since toolkit v0.20.0; `bundle`/`restore --md1` since earliest releases) long pre-exist the pin. **T5 may implement in parallel with the entire T2/T3/T4 pipeline.**

## GUI gate mechanics (differ from the codecs — load-bearing for the implementer)
- **PR + CI-before-tag** (NOT direct-FF). Required contexts already live (`schema-mirror gate`, `clippy`, `headless (no-default-features)`, `snapshots`, `x86_64-unknown-linux-gnu`; `enforce_admins:false`) — the new tests gate merges from the moment they land.
- **MSRV 1.88** (`Cargo.toml:8`) — both cells are subprocess/regex only, MSRV-insensitive.
- **NO fmt gate — do NOT `cargo fmt` the GUI** (standing preference; no fmt step in any workflow).
- **clippy `-D warnings`** (`build.yml`, default + `--no-default-features`) is required — new test code must be clippy-clean.
- **NO lavapipe/kittest dependency** — both cells are pure subprocess (`runner::run` = stdlib `Command` wrapper, `runner.rs:147`; `run_with_stdin` `:172`); they belong in the plain `cargo-test-full-suite` step, same tier as `ui_harness_i4_realcli.rs`/`canonicity_drift.rs`, NOT the GPU `snapshots` job.

## T5-S1 (#14a) — h-notation fixture grid in `tests/canonicity_drift.rs`
**Gap:** the differential harness (`canonicity_drift.rs`, 223 lines — shells `MNEMONIC_BIN gui-schema --classify-descriptor <STR>`, asserts 4 invariants per fixture against a 19-row `FIXTURES` table) pre-dates the eval and is sound — but **every fixture uses apostrophe notation** (`44'`/`84'`/`86'`/`48'`; grep-confirmed zero `h`-notation rows). The GUI classifier regex (`src/form/conditional.rs:99-129`) structurally claims h-support (`(?:/\d+'?h?)*`, `/\*+'?h?`) but **no test exercises that claim against the toolkit's real parser**.
**Fix (test):** extend the `FIXTURES` const with an h-notation shape grid mirroring the existing apostrophe rows, reusing the SAME `Expect` enum + 4-invariant machinery UNCHANGED. **Capture each new row's `Expect` empirically** (run `MNEMONIC_BIN gui-schema --classify-descriptor <STR>` against the pinned binary — R0-verified verdicts: pkh/wpkh/tr h-origins, mixed `44'/0h/0'`, suffix-origin-h, use-site `/*h`, wsh(multi h-origins) → all `canonical`; `sh(multi(…45h…))` → `non-canonical`; double-marker `44'h` → exit 2 ParseFails "invalid child number format"). Rows: origin-fingerprint h (`pkh([deadbeef/44h/0h/0h]@0/<0;1>/*)`), use-site h, `tr(@0)` h-origin, ≥1 mixed apostrophe+h, the `44'h`→ParseFails row (pins where the GUI's `'?h?` structurally over-claims; safe — ParseFails rows never assert the GUI verdict), AND a `wsh(multi(2,@0,@1,@2))` h-origin row.
**Mutation-teeth (R0 m2):** the S1 RED mutation bites ONLY the pkh/wpkh/tr(+use-site) rows (GUI regexes 1-3 carry `'?h?`, `conditional.rs:99-122`). The **wsh(multi) row is wrapper-prefix-matched** (regexes 4-5, `:116-118`, no `'?h?`) → it stays green under the mutation; KEEP it (pins toolkit-side h-acceptance) but EXEMPT it from the RED-proof requirement (toolkit-differential coverage only — see Acceptance §1).
**Named RED mutation:** swap `conditional.rs`'s `'?h?` → bare `'?` (drop h-support). The existing 19 apostrophe fixtures stay 100% green (none contain `h`) → a GUI that misclassifies h-notation descriptors as NonCanonical ships silently; the **pkh/wpkh/tr/use-site** h-rows RED immediately (the wsh(multi) row does NOT — see Mutation-teeth). (Toolkit side already normalizes h/apostrophe identically at `gui-schema --classify-descriptor` — this purely tests the GUI regex against it.)

## T5-S2 (#14b) — verdict-vs-consequence assertion (extend the EXISTING account-pin suite, BOTH directions)
**Gap:** a fixture checking only the raw `Canonicity` verdict does not prove the *consuming* `--account → PinValue(0)` pin-lift logic agrees on h-input. NOTE (R0 I2): `is_descriptor_non_canonical` (`conditional.rs:139`) is a **private `fn`** (no `pub`) — NOT callable from a `tests/` integration test; do NOT plan a direct call. But `tests/non_canonical_descriptor_account_pin.rs` (8 tests) ALREADY is the verdict-vs-consequence gate: it asserts `bundle(&state)` → `--account` `PinValue(0)` FIRES (canonical, :27-95) / LIFTS (non-canonical, :98-128) via the consumer chain `conditional.rs:241-248`. It is apostrophe-only.
**Fix (test):** extend `tests/non_canonical_descriptor_account_pin.rs` (its existing idiom — no new machinery) with h-notation cases in **BOTH directions**:
- (i) an h-notation **Canonical** descriptor ⇒ `--account` pin FIRES. This is the S1-regex-mutation's consequence symptom: a canonical h-descriptor misclassified NonCanonical → pin LIFTED → user `--account N` reaches the toolkit → hard error on valid input (the L12 bug class, `:44-63`).
- (ii) an h-notation **NonCanonical** descriptor ⇒ pin LIFTS.
**Named RED mutation:** the S1 regex `'?h?`→`'?` drop → the h-Canonical case's pin fails to fire (mis-lifted) → RED (this is the funds-relevant direction); an h-specific classifier flip → the h-NonCanonical case REDs.

## T5-S3 (#15) — funds-core `bundle`→`restore` with a spec-independent oracle
**Gap:** the only end-to-end GUI bundle coverage is a **snapshot-of-own-pipeline** pin. Confirmed (recon §4): `wire_shape_snapshot.rs` pins `--json` key-SHAPE for xpub-search/import-wallet only (never invokes `bundle`/`restore`); `ui_harness_i4_realcli.rs`'s 4 cells are decode-only (decode-address/md/ms/mk, no bundle→restore); `tests/tutorial/*` drives bundle→restore end-to-end but byte-gates against a **committed golden it re-captures** — the eval's exact "snapshot-of-own-pipeline" characterization. A symmetric bug shifting both encode+expected-golden in lockstep stays green forever.
**Fix (test):** one new cell (new file or appended to `ui_harness_i4_realcli.rs`), modeled on I4's `assemble_argv`/`runner::run` idiom against the pinned `MNEMONIC_BIN`:
1. Build the `bundle` `FormState` via the **SLOT idiom** (R0 I1 — bundle at v0.75.0 has NO `--phrase`/`--phrase-stdin`; seed source = `--slot @N.<subkey>=<value>`; boolean `*-stdin` toggles are never emitted by `assemble_argv`, `invocation.rs:325-354`): `FormState::with_slots(SlotState{rows:[SlotRow{index:0, subkey: Phrase, value:"abandon abandon abandon abandon abandon abandon abandon abandon abandon abandon abandon about"}]})`, **Template=`bip84`** (NOT `--descriptor` mode; scope guard), no passphrase, network=mainnet → `assemble_argv` emits `--slot "@0.phrase=…"` (exact shape pinned `tests/argv_assembler_slot.rs` cell_1) → plain `runner::run` (NO stdin — pipes `Stdio::null`); parse `--json`.
2. Assert bundle's emitted **`ms1[0]` == `"ms10entrsqqqqqqqqqqqqqqqqqqqqqqqqqqqqcj9sxraq34v7f"`** (published all-zero `ms vectors` constant, external oracle; = `ui_harness_i4_realcli.rs:63` `MS1_ALL_ZERO`) AND bundle's `master_fingerprint == "73c5da0a"` (R0-verified literal).
3. Feed the emitted `ms1` **and the emitted `md1` chunk ARRAY** (~3 chunks; restore takes **repeating `--md1`**) into a `restore --from ms1=<emitted> --md1 <c1> --md1 <c2> … --json` `FormState`, assembled/run the same way; assert restore's **`wallets[].first_addresses[0]` (receive) == `bc1qcr8te4kr609gcawutmrza0j4xv80jy8z306fyu`** (published **BIP-84** m/84'/0'/0'/0/0 address, external oracle) AND the `verification` cross-check passes.
   - **DO NOT assert recovered-entropy-hex from `restore`** — v0.75.0 `restore --json` emits NO entropy field (R0 C1, empirically confirmed). (Optional extra independence: feed the emitted `ms1` → `ms decode --json` (MS_BIN) asserting `entropy_hex == "00000000000000000000000000000000"`.)
**EXTERNAL-PROTOCOL-FACT (CLAUDE.md) — SATISFIED:** the BIP-84 address `bc1qcr8te4kr609gcawutmrza0j4xv80jy8z306fyu` is **triple-verified** (authoritative bip-0084.mediawiki + an independent stdlib-only derivation + the pinned binary's own bundle→restore all agree; master fp `73c5da0a`). The `ms1` constant is a published `ms vectors` value. Both may be pinned as-is (still add a provenance comment on each).
**Named RED mutation:** corrupt a BIP-84 path-derivation constant shared by `bundle`'s encode + `restore`'s decode (or the GUI template-mode default wiring) — the tutorial golden + `wire_shape_snapshot` regenerate self-consistently and stay green; only the hardcoded `ms1` constant / BIP-84 address REDs.
**Scope guard:** scope S3 to **`--template bip84`** (matches the eval's literal text). A `--descriptor`-mode variant would exercise Cycle-A's C1 use-site-collapse fix end-to-end but needs the GUI pin bumped to ≥v0.76.0 (currently v0.75.0, pre-C1) — **out of scope for NO-BUMP-parallel T5**; file as a follow-on FOLLOWUP contingent on the next GUI pin-bump cycle (`gui-descriptor-mode-bundle-restore-independent-oracle`).

## T5-S4 (OPTIONAL — #13 addendum, include only if R0 judges cheap-in-scope)
Extend the already-shipped `schema_mirror_defaults_drift.rs` from `mnemonic`-only to also cover `md`/`ms`/`mk` default_value/choices (the original author deliberately deferred this as a "bounded add"; docstring `:28-31`). No FOLLOWUP currently tracks it. Genuinely optional — if excluded, file `schema-mirror-defaults-drift-md-ms-mk-extension` as a FOLLOWUP instead.

## Acceptance
1. S1+S2+S3 (S4 optional); each new fixture/cell RED-proven under its named mutation (report mutation + failure) — EXCEPT the S1 `wsh(multi)` h-row, which is wrapper-prefix-matched and cannot RED under `'?h?`→`'?` (toolkit-differential coverage only); the existing suite survives that mutation (proves the gap).
2. S3's oracles are external-verified: the BIP-84 address triple-verified (R0), the `ms1` constant a published `ms vectors` value. (Restore-entropy is NOT an oracle — v0.75.0 does not emit it.)
3. Oracles independent of the code under test: S1/S2 = the toolkit's `gui-schema --classify-descriptor` (external to the GUI regex under test) + the account-pin consequence; S3 = the published `ms1` constant + the published BIP-84 address.
4. Full `cargo test --workspace` green (with `MNEMONIC_BIN` = the pinned v0.75.0 binary); clippy `-D warnings` (default + `--no-default-features`) clean. **NO `cargo fmt`.** No `snapshots`/lavapipe dependency introduced.
5. **NO-BUMP / no pin change** — tests only; the GUI ships via PR+CI (a version bump to cut a GUI release is a separate ritual decision at ship time, NOT gated by these test additions; confirm at ship whether to tag a GUI release or leave the tests to ride the next functional release).
6. **#13 SCOPE GUARD:** do NOT reimplement #13 (shipped). **#15 SCOPE GUARD:** `--template bip84` only, not `--descriptor` mode.

## Phasing
Single repo, three cells: S1+S2 (both in `canonicity_drift.rs`, one implementer) + S3 (separate file/cell — parallel-safe with S1/S2, disjoint file). Per-leg + post-impl R0 per CLAUDE.md. RED-under-mutation first. Ships as a `mnemonic-gui` PR (CI-before-tag); merges once required contexts green.
