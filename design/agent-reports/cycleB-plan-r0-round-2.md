# PLAN R0 review — bitcoin-core-receive-change-pair-merge — round 2

**Verdict:** GREEN (0C / 0I)
**Reviewer:** opus architect, SHA d9063523
**Dispatched:** 2026-07-06 (Cycle B, IMPLEMENTATION_PLAN R0 loop round 2 — convergence on plan rev-2 + SPEC rev-5 reconciliation). Persisted verbatim per CLAUDE.md.

## Critical (must fix before implementer dispatch)
- none

## Important (must fix before implementer dispatch)
- none

The sole round-1 Important (I1-plan) is fully resolved, and its cross-doc reconciliation (SPEC rev-5 §4.1/§4.3) is internally consistent with both the plan and the rest of the SPEC — no residual mechanism contradiction. All four Minors are folded. The plan is cleared for the single-implementer TDD execution.

## Fold-by-fold verification

- **I1-plan (extraction re-pin) → PLAN P1 step 2 / SPEC §4.1: RESOLVED.** Extraction now parses the checksum-validated body via `Descriptor::<DescriptorPublicKey>::from_str(body)` and reads per-key `origin`(fp+path), bare xpub, `derivation_path()` (the final use-site step), `wildcard`, template kind + threshold, and `Tr::tap_tree()`. Both docs explicitly state `lex_placeholders`/`concrete_keys_to_placeholders` CANNOT be used for the final step (they are the v0.76.0 floor that rejects it) and that `extract_origin_components` MAY still supply only the `(fp, origin, xpub)` tuple. `Tr::tap_tree()` `None`/`Some` cleanly delivers carry-forward-1 (key-path vs script-path `tr`). The API is cited as proven in-repo. This is exactly the fix, and it is now the same in SPEC and plan.
- **I1-plan (construction re-pin) → PLAN P1 step 2 / SPEC §4.3 (retitled "UNIFORMITY-VERIFIED all-keys replacement"): RESOLVED.** Both docs now sanction: after miniscript-verifying condition-7 uniformity (every key in the receive body carries the identical `/<recvStep>/*`), a **global** substring replace `/<recvStep>/*` → `/<recvStep;chgStep>/*` on the checksum-stripped receive body, justified by the `/*`-suffix disambiguation, with **first-only/partial `replacen(…,1)` FORBIDDEN**, backstopped by the §8.4/§8.10 original-anchored oracle. The old infeasible constraint ("anchored on parsed per-key use-site positions from the key/use-site lexer, NOT a naive `str::replace`") is **gone** — §4.3 now explicitly reasons "a miniscript AST parse loses byte offsets, so there are no lexer 'positions' to anchor on," which removes the contradiction the plan surfaced. Sound: I verified the global replace cannot touch an origin digit (origins live inside `[...]` and end in `']`, never `/*`) and cannot cross-contaminate under multi-digit steps because condition-7 uniformity forbids mixed steps within a mergeable entry.
- **m1-plan (§8.18 zeroize) → PLAN P1 Gate: RESOLVED.** The gate line now names "§8.18 `lint_zeroize_discipline` regression-guard … intentional coverage, not new test work."
- **m2-plan (oracle helper) → PLAN §8.4: RESOLVED.** The oracle now uses the test-crate miniscript-direct pattern (`Descriptor::from_str` + `derive_at_index().address()`, mirroring `derive_receive` at `prop_backup_restore_roundtrip.rs:383`), split via `into_single_descriptors()` → `[chain-0, chain-1]`, asserting chain-0 == external-from-original AND chain-1 == internal-from-original. Explicitly NOT the `pub(crate)` `derive_receive_addresses` (test-crate-inaccessible).
- **m3-plan (§8.13 reframe) → PLAN P0: RESOLVED.** §8.13 now carries "NOTE (m3-plan): this is a REGRESSION-LOCK, not a red-first driver — the current `bool` impl already passes it, and P0 produces no `None`. Its teeth land in P1 … the implementer MUST confirm this test FAILS against a deliberately shape-based-`None` stub before wiring the explicit-param mechanism." Exactly the recommended framing (real signal via a counterexample stub).
- **m4-plan (oracle no-fallback) → PLAN §8.4: RESOLVED.** "The oracle MUST FAIL (not silently fall back to a re-authored `<0;1>`) if the `--json` read yields no descriptor."

## SPEC ↔ PLAN reconciliation consistency (the round-2 focus)
- **No residual "position-anchored replacement / str::replace forbidden" contradiction.** Full-text sweep of both docs for `anchor|position|str::replace|replacen|naive|lex_placeholders|lexer`:
  - Every `anchor`/`position` hit is a legitimate distinct usage: §3 "anchor points" (section title), §4.1 grouping-key "positional/ordered" (I4), §4.2 key material "positionally", §4.4/M8 Vec "position", the oracle "original-anchored" (anchors on the pre-merge originals), and §4.3's own "there are no lexer 'positions' to anchor on" (the resolution itself). None reinstates the old lexer-position-anchoring rule.
  - `str::replace` / "naive": **absent** from both docs. The only forbid is correctly scoped — "first-only/partial `replacen(…,1)` FORBIDDEN" (PLAN `:97`, SPEC `:164`) — which does NOT forbid the sanctioned global replace.
  - `lex_placeholders` hits are all correct-context: the floor-reject description (§1, §3 row, §7) and the explicit "CANNOT be used for the final step" statements (§4.1, §4.3, PLAN P1, risk register #1).
- **Mechanism is identical across docs:** SPEC §4.1 extraction == PLAN P1 step 2 extraction (miniscript parse, same fields); SPEC §4.3 construction == PLAN P1 step 2 construction (uniformity-verify + global replace on the receive body, receive-first ordering, actual step values, checksum recompute). PLAN risk register #1 updated to the miniscript mechanism.
- **No new drift and no funds-weakening:** using miniscript ONLY to read/validate (step, uniformity, `tap_tree`) while constructing via a string op on the *original* receive body avoids any miniscript-Display normalization risk; condition 2 (identical keys), the RED-first §8.1/§8.4/§8.10/§8.15 oracles, and the verify-bundle backstop are all intact. The funds property (no wrong-merge; no partial-rewrite false-pass) is preserved and the mechanism is now concrete and feasible.

## Notes
- **All prior gates cleared:** SPEC R0-GREEN at round 3 (rev-5 is a mechanism reconciliation, not a re-open); PLAN R0-GREEN here at round 2. Every finding across SPEC R0 (5I + 9M + I-a) and PLAN R0 (I1-plan + 4M) is resolved with no residual contradictions.
- **Trivial optional-tidy (NOT a finding):** the whole-diff-review focus line (PLAN `:170`) still phrases it "all-keys structural replacement (no partial rewrite)" — accurate as a description of what the reviewer checks, but could read as "structural" ≡ the old span-anchored idea; if touched, "all-keys uniform (global) replacement — no partial rewrite" matches §4.3's title exactly. Purely cosmetic; do not re-review for it.
- **Execution is now unblocked.** The single implementer executes P0 (mechanical type migration; §8.13 as the regression-lock with a shape-based-`None` counterexample) → P1 (miniscript-extraction guard matrix + uniformity-verified global-replace construction; all §8 oracles RED-first, weighted to §8.1 distinct-key + §8.4/§8.10/§8.15 original-anchored) → P2 (docs/examples lockstep) with per-phase full `cargo test -p mnemonic-toolkit` + clippy gates, then the mandatory post-impl whole-diff review, then the user checkpoint before the out-of-scope release ritual. Recommend the implementer, at P1, add a one-line assertion that miniscript `Descriptor::from_str` succeeds on each in-scope fixture shape (wpkh / wsh(sortedmulti) / sh(wsh) / single-key tr) before relying on its parsed fields — cheap insurance that the extraction seam holds across all covered templates.
