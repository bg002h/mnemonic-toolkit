# v0.13.0 P2.1 GREEN R1 LOCK review — `0b27bfe`

**Phase:** v0.13.0 P2.1 (CLI Args + Subcommand stub + main wiring + gui-schema nested-subcommand flattening fix; post-GREEN LOCK)
**Round:** R1, round 1
**Reviewer:** Opus (`feature-dev:code-reviewer`)
**Commits under review:**
- `fb8e16b` (RED — `tests/cli_gui_schema.rs` 7→10 subcommand bump + `slip39-cli-extendable-flag` FOLLOWUP)
- `0b27bfe` (GREEN — minimal `cmd/slip39.rs` stub + `cmd/mod.rs` export + `main.rs` dispatch + `cmd/gui_schema.rs` flattening)

**Baseline:** `81488e3` (P1c-E.3 R1 LOCK round 1, 0C/0I/0N clean)
**Date:** 2026-05-14

**Persistence note:** the reviewer agent (Opus, `feature-dev:code-reviewer`) ran without `Write`/`Edit`/`Bash` tools in this dispatch; full report content captured by the dispatching session from the agent's final message and persisted here verbatim, with structure preserved.

## Verdict

**0 Critical / 0 Important / 0 Nice-to-have at confidence ≥ 80 — RECOMMEND LOCK. P2.2 is unblocked.**

P2.2 (full handler impl + 5 `cli_slip39_*.rs` test files + lint anchor bumps + 6 SPEC patches + `secret_advisory::warn_if_world_readable` extraction) is unblocked. The P2.1 surface — Args shape, gui-schema flattening, FOLLOWUPs, and main dispatch — is structurally complete and matches the plan §3.1 / §4.2 / §5 / §9 contract.

## Verified clean against source + plan §3.1 + §4.2 + §5 + §9 LOCK criteria

### 1. `cmd/gui_schema.rs::build_schema` flattening correctness

`crates/mnemonic-toolkit/src/cmd/gui_schema.rs:97-129`:

- Leaf-vs-parent dispatch uses `nested.is_empty()` after filtering `help` — correct. Leaf subs (`bundle`, `convert`, `derive-child`, `export-wallet`, `final-word`, `verify-bundle`) flow through `build_subcommand(s)` unchanged; only `seed-xor` and `slip39` (the two `#[command(subcommand)]` parents) flatten.
- `help` filter applied at BOTH the top-level iteration (line 101) AND the nested iteration (line 105). Clap auto-adds `help` at every command level; both filters are required.
- Schema `version: 1` preserved at line 125 — confirmed via `cargo run -- gui-schema | jq '.version'` probe.
- `build_subcommand` sorts flags by long name (line 165); the recursion calls `build_subcommand(ss)` and overwrites only the `name` field, preserving the sorted-`flags` invariant.
- The "parent with BOTH top-level flags AND nested subs" edge case (where top-level flags would silently drop) is not exercised by current source — `Slip39Args` (`slip39.rs:39-43`) and `SeedXorArgs` (`seed_xor.rs:27-30`) each contain ONLY the `#[command(subcommand)]` field. Not a P2.1 bug; flagged here only for completeness in case a future nested-parent grows top-level flags.

### 2. `cmd/slip39.rs` Args / Subcommand structures vs plan §3.1

- `Slip39SplitArgs.passphrase: Option<String>` with `conflicts_with = "passphrase_stdin"` (`slip39.rs:74-75`) — R0 C1 fold landed.
- `Slip39CombineArgs.passphrase: Option<String>` with `conflicts_with = "passphrase_stdin"` (`slip39.rs:130-131`) — R0 C1 fold landed.
- `--group` uses `ArgAction::Append` + `parse_group_spec` (`slip39.rs:89-96`); ordering is preserved (`Vec` position == `group_idx` per R0 I4 fold — the actual library-forwarding transform is deferred to P2.2).
- `Slip39ToShape` `ValueEnum` with `#[clap(rename_all = "kebab-case")]` + `default_value = "entropy"` (`slip39.rs:140`, `154-161`) — correct; renders as SPEC §7 `dropdown` in `gui-schema`.
- `parse_group_spec`: `split_once(',')` + `u8::parse` on each half; range validation deferred to library (`slip39.rs:172-183`) — matches plan §3.1.
- Stub stems both render `"slip39 {split,combine}: P2.1 stub — full impl ships at P2.2"` via `ToolkitError::BadInput` (`slip39.rs:203-216`) — matches plan §9 LOCK criterion exactly; exit code 1 verified via `cargo run -- slip39 split ...; echo $?`.

### 3. `main.rs` wiring

`crates/mnemonic-toolkit/src/main.rs:57, 88`:
- `Slip39(cmd::slip39::Slip39Args)` enum variant inserted between `SeedXor` and `GuiSchema` — matches the established alphabetical+grouped ordering.
- Dispatch arm `Command::Slip39(args) => cmd::slip39::run(args, stdin, stdout, stderr)` mirrors `Command::SeedXor`'s arm shape exactly (`seed_xor.rs:97-107`); `.map(|_| 0)` adapter is NOT applied because `slip39::run` already returns `Result<u8, _>` — same as `SeedXor`.

### 4. `cmd/mod.rs` export

`crates/mnemonic-toolkit/src/cmd/mod.rs:10`: `pub mod slip39;` inserted alphabetically between `seed_xor` and `verify_bundle` — correct.

### 5. `tests/cli_gui_schema.rs` test bump

`crates/mnemonic-toolkit/tests/cli_gui_schema.rs:43-69`: test renamed to `gui_schema_lists_all_ten_subcommands`; alphabetical list

```
["bundle", "convert", "derive-child", "export-wallet", "final-word",
 "seed-xor-combine", "seed-xor-split", "slip39-combine", "slip39-split",
 "verify-bundle"]
```

matches plan §9 P2.1 LOCK criterion exactly. The full `cli_gui_schema` test file (16 tests) runs green.

### 6. FOLLOWUPs filed

- `slip39-cli-extendable-flag` at `design/FOLLOWUPS.md:1050-1058` — tier `v0.14-feature`, status `open`, companion link to `slip39-shamir-secret-sharing` present.
- `slip39-gui-schema-flattening-companion` at `/scratch/code/shibboleth/mnemonic-gui/FOLLOWUPS.md` (after the `mnemonic-gui-schema-mirror` block; uncommitted in that working tree, gated on `mnemonic-toolkit-v0.13.0` shipping). Cross-cites toolkit `design/PLAN_v0_13_0_p2.md` §4.2.

### 7. `parse_group_spec` unit-test coverage

`slip39.rs:223-240`: 3 tests cover canonical shape, missing comma, and non-numeric — sufficient for the P2.1 stub. Error format strings (`expected '<member_count>,<member_threshold>'`, `member_count:`, `member_threshold:`) are byte-stable and unambiguous.

### 8. Imports + clippy posture

- `use std::io::{Read, Write};` (`slip39.rs:37`) used by trait bounds in `run` / `run_split` / `run_combine`.
- No unused imports.
- `ValueEnum` (`slip39.rs:36`) used by `Slip39ToShape`.
- The `_args` / `_stdin` / `_stdout` / `_stderr` underscoring in stub bodies suppresses unused-variable lints — clippy-clean by construction. `cargo clippy --all-targets -- -D warnings` confirms clean.

## Findings

None at confidence ≥ 80.

## Notes (sub-threshold; not blocking)

- The error string in `parse_group_spec` reads `"... is not a valid 0..=255"` for `u8` parse failures (`slip39.rs:178`, `:181`). The user-facing SLIP-39 range is `1..=16`, not `0..=255`. The plan explicitly defers range validation to the library boundary (`BadGroupSpec` at SPEC §2.5 rows 4-5), so a value like `"32,3"` will parse cleanly here and refuse with a clear stem at the library layer. The `0..=255` phrasing is a `u8`-parse internal — users won't see it for in-range values. Not actionable for P2.1; consider rewording at P2.2 if a user complains.
- The `--language` flag is included on `Slip39CombineArgs` (`slip39.rs:144-145`) with the doc "ignored for `--to entropy`". Plan §6 risk 5 already flags this for a P2.2 happy-path round-trip pin.

## RECOMMEND LOCK

P2.2 (full handler impl + 5 `cli_slip39_*.rs` test files + lint anchor bumps + 6 SPEC patches + `secret_advisory::warn_if_world_readable` extraction) is unblocked.
