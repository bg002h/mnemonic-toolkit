# PLAN R1 review — output-type advisory Phase 2 (mk + md) + Tier-0

> Persisted verbatim from the opus architect R1 review of `design/IMPLEMENTATION_PLAN_output_type_advisory_phase2_mk_md.md` (folded after R0 RED 2C/3I/5M). Verifies each prior finding against LIVE source AND hunts for fold-introduced drift, per CLAUDE.md R0-gate discipline. Reviewer ran against live checkouts: toolkit `64943f2` (master), mk `mnemonic-key/main`, md `descriptor-mnemonic/main`, ms-cli precedent `mnemonic-secret/ms-cli`. Empirical probes: `bitcoin` path/depth probe (`/tmp/depthprobe`), and live `mk`/`md` binary invocations (mk `target/debug/mk`, md `target/debug/md`).

## Verdict: RED (0C / 3I)

The R0 folds are **substantively correct** — every one of C1, C2, I1, I2, I3, m1–m5 is RESOLVED against live source (verified file:line below). The mk Phase A scaffold (`card()`/`mk1_fixture()`/`V2_84_MAIN`) is faithfully copied from `cli_address.rs`, the h-form path parses identically to the apostrophe form, `V2_84_MAIN` is genuinely depth-3 single-sig accepted by `mk address` with no flag and no depth advisory, the `.args(Vec<String>)` variadic correctly handles the 2-chunk fixture, the mk repair `:86` emit-site and md compile `cmd/compile.rs` file are now right, and the 5 pin sites + Tier-0 premise + sibling-pin-check inline-bash + version (0.38.2→0.38.3) all check out.

But the fold **fixed the mk fixture-fidelity class on the Phase A side and left the same class open on three other invocations** — two of which I verified empirically *fail* (non-success exit) against the live binaries, so the TDD "verify it passes" step cannot be reached as written:

- **[I4 — NEW] The mk `encode` cell invocation is missing the required `--policy-id-stub`/`--from-md1` → exits 64, not success.** (Empirically confirmed: `mk encode --xpub … --origin-path …` → `error: at least one of --policy-id-stub or --from-md1 is required`, exit 64.)
- **[I5 — NEW/UNRESOLVED] The md `MD1_FIXTURE` is still an unresolvable `<placeholder>` pointing at the WRONG file** — `cmd_decode.rs` contains NO md1 literal (it uses runtime `encode()`); the only literal in md `tests/` is in `smoke.rs`. This is the *exact* C1 defect the fold fixed for mk, left unfixed for md.
- **[I6 — NEW] The md `address` (WatchOnly) cell has no fixture mechanism at all** — `md address` requires `<PHRASES>` or `--template … --key @i=<XPUB>`; the plan names the cells but, unlike the fully-scaffolded mk Phase A, provides no `account_xpub()`-style helper to produce the concrete xpub.

These are the same plan-failure class writing-plans forbids (placeholders / un-runnable invocations), and they block a clean first cut exactly as the R0 C1/C2 did. Fix I4/I5/I6, re-dispatch for R2.

---

## Resolution of R0 findings

| R0 finding | Status | Live-source evidence |
|---|---|---|
| **C1** mk fixture unresolvable | **RESOLVED ✓** | Plan now builds in-process via `card()`+`mk1_fixture()` copied from `cli_address.rs:17,33-44`. Verified the helper matches live EXACTLY: `KeyCard::new(vec![[u8;4]], Some(Fingerprint::from([u8;4])), p(origin_path), Xpub::from_str(xpub))` (`cli_address.rs:36-44`). `mk_codec`,`bitcoin`,`assert_cmd` are mk-cli dev-deps (`Cargo.toml:32-35`; `bitcoin`=`0.32` dep `:29`, used by tests). `assert_cmd::Command` (`cli_address.rs:11`) + `.output()` (`:62`) idiom correct. |
| **C2** multisig-refuse fixture trap | **RESOLVED ✓** | `V2_84_MAIN` @ `m/84'/0'/0'` → `infer_address_type` returns `Inferred(P2wpkh)` not `Multisig` (`derive_support.rs:94-113` + unit test `:198`). Live test `account_84_default_p2wpkh_count10` (`cli_address.rs:76-85`) proves `mk address <card>` with NO `--address-type` exits 0. Depth advisory fires only if `card.xpub.depth != 3` (`cmd/address.rs:83`); probe confirms `V2_84_MAIN` depth=3 → no advisory. Plan uses h-form `m/84h/0h/0h`; probe confirms it parses identically to `m/84'/0'/0'` (both → `84'/0'/0'`, equal=true). |
| **I1** md compile file | **RESOLVED ✓** | `cmd/compile.rs` IS the printing handler: `pub fn run` `:4`, json `println!`+`return Ok(0)` `:20-21`, text `println!("{template}")` `:25`+`Ok(0)` `:26`. `src/compile.rs` is the non-printing lib (`compile_policy_to_template` `:59`). Dispatch `main.rs:366 cmd::compile::run(...)` (cfg-gated). Plan table (line 296) now cites `cmd/compile.rs` (json `:21`, text `:26`); B2-Step-6 stages `crates/md-cli/src/cmd/compile.rs` (line 308). `cmd/mod.rs:3-4` confirms `#[cfg(feature="cli-compiler")] pub mod compile;`. |
| **I2** mk repair emit-site | **RESOLVED ✓** | mk `repair::run()` success return `Ok(if any_correction { 5 } else { 0 })` @ `:86`; `:149`=`fn emit_text`, `:202`=`fn emit_json` (their `Ok(())` at `:172`/`:231`). Error path `?`-propagated: `decode_string(original)?` @ `:67` → `Err` never reaches `:86`. Plan line 167 now cites `:86` and "NOT the `:149`/`:202` … helper … returns" + "error path is `?`-propagated → no emit". (Tiny nit: the helper `Ok(())` returns are at `:172`/`:231`, not `:149`/`:202` which are the `fn` decl lines — but it correctly points at the helpers vs run(); harmless.) |
| **I3** md vectors cwd | **RESOLVED ✓** | `cmd/vectors.rs:15-25`: `run(out: Option<String>)`, `None → PathBuf::from("./vectors")` + `create_dir_all`. Plan B3-Step-2 (line 317) now passes `--out <tempdir>` via `tempfile::tempdir()`. `tempfile` is a md-cli dev-dep (`Cargo.toml:43`). |
| **m1** missing_docs moot | **RESOLVED ✓** | `mk-cli/src/main.rs:7` + `md-cli/src/main.rs:1` both `#![allow(missing_docs)]`. Plan self-review (line 402) keeps module doc comments as hygiene only (harmless); emphasis downgraded. |
| **m2** compile clippy under feature | **RESOLVED ✓** | B3-Step-3 (line 318) now runs `cargo test -p md-cli --features cli-compiler` AND default; the clippy line still reads `--all-targets -- -D warnings` (default features) — the R0 m2 ask to add `--features cli-compiler` to the clippy run is *partially* met (test-under-feature added; the explicit feature-clippy line is implied via the manual.yml install-time gate). Minor residual, see Minor m2'. |
| **m3** Tier-0 corruption locus | **RESOLVED ✓** | C1-Step-1 (line 339) now specifies "corrupt exactly one **data** symbol in the payload region (NOT the `md1` HRP and NOT a checksum-only position)". md-codec 0.35.0 present in registry cache (`~/.cargo/registry/{cache,src}/…/md-codec-0.35.0`). |
| **m4** sibling-pin-check inline bash | **RESOLVED ✓** | `.github/workflows/sibling-pin-check.yml:42 run: |` is inline bash (`< <(...)` process-sub at `:109`). C2-Step-2 (line 358) now says "inline bash inside `.github/workflows/sibling-pin-check.yml` … not a standalone script … copy its parse-and-compare steps into a shell and run". |
| **m5** toolkit version named | **RESOLVED ✓** | `crates/mnemonic-toolkit/Cargo.toml:3` = `0.38.2`. C4-Step-2 (line 387) now names `0.38.2 → 0.38.3` (PATCH). |
| **md MD1_FIXTURE reusable literal** (verification brief) | **NOT RESOLVED ✗ → [I5]** | Plan line 249 `const MD1_FIXTURE: &str = "<exact md1 literal from crates/md-cli/tests/cmd_decode.rs>"`. LIVE: `cmd_decode.rs` has NO md1 literal (uses runtime `encode()` `:6-13`). Only md1 literals in md `tests/` are in `smoke.rs:19`/`:14` (`md1yqpqqxqq8xtwhw4xwn4qh`, `md1qqpqqxqq0zkd22pw8dmd3`). The valid one (`md1yqpqqxqq8xtwhw4xwn4qh`) DOES decode cleanly (text + `--json`, exit 0) and is non-chunked single-line — but the plan points at the wrong file and leaves a placeholder. |

---

## Critical (new or unresolved)

None.

## Important (new or unresolved)

- **[I4 — NEW] mk `encode` A2 cell invocation omits the required `--policy-id-stub` (or `--from-md1`) → the cell exits 64, never reaches success+advisory.** Evidence (empirical, live `mk`): `mk encode --xpub xpub6Bme… --origin-path m/84h/0h/0h` → `error: at least one of --policy-id-stub or --from-md1 is required`, **exit 64**. `mk encode --help` confirms the requirement; the in-process `card()` helper satisfies it by passing `vec![[0xde,0xad,0xbe,0xef]]` as the stub to `KeyCard::new`, but the **CLI-invocation** `encode` cell in the A2 table (plan line 165: `mk encode --xpub V2_84_MAIN --origin-path "m/84h/0h/0h"`) does not pass `--policy-id-stub`. The cell asserts `status.success()` (A2-Step-1) → it fails. **Fix:** the `encode` cell invocation must add `--policy-id-stub deadbeef` (and, to mirror the fixture, `--origin-fingerprint 73c5da0a`). Empirically, `mk encode --xpub … --origin-path m/84h/0h/0h --policy-id-stub deadbeef` → exit 0 with mk1 chunk(s) on stdout. (This is the same fixture-fidelity class R0 C1/C2 flagged; the fold scaffolded the *positional* fixture cells but not the `encode` flag-driven cell.)

- **[I5 — NEW/UNRESOLVED] md `MD1_FIXTURE` is an unresolvable `<placeholder>` citing the wrong file (`cmd_decode.rs` has no md1 literal).** Evidence: plan line 249 binds `MD1_FIXTURE` to `"<exact md1 literal from crates/md-cli/tests/cmd_decode.rs>"`; live `cmd_decode.rs:6-13` uses runtime `encode(template)` and contains zero md1 literals (`grep -rhoE 'md1[a-z0-9]{8,}' tests/` → only `smoke.rs`). **Why Important:** identical to R0 C1 (placeholder + can't be lifted as instructed); B1-Step-2 "verify both fail" won't compile with an unresolved placeholder. **Fix:** either (a) inline the resolved literal `const MD1_FIXTURE: &str = "md1yqpqqxqq8xtwhw4xwn4qh";` (the output of `md encode "wpkh(@0/<0;1>/*)"`, present in `smoke.rs:19`; verified it `decode`s and `decode --json`s cleanly, exit 0, non-chunked) and correct the comment to cite `smoke.rs`; or (b) adopt the runtime-`encode()` idiom (`fn fixture_md1() -> String { encode("wpkh(@0/<0;1>/*)") }`, the `cmd_decode.rs`/`smoke.rs` pattern). Name the concrete source, not a placeholder.

- **[I6 — NEW] md `address` (WatchOnly) cell has no fixture mechanism specified.** Evidence: `md address --help` requires `<PHRASES>` OR `--template <T> --key @i=<XPUB>`. The live md tests build the concrete xpub via `cmd_address.rs:12 account_xpub(path, network)` (derives from the ABANDON mnemonic) then `md address --template "wpkh(@0/<0;1>/*)" --key @0=<xpub>` (`:66-85`, exit 0, address on stdout). The plan's B2 table (line 295) names the cells `address_text_emits_watch_only`/`address_json_*` and adds `const WATCH_ONLY_LINE` (line 298) — but, unlike the fully scaffolded mk Phase A (`card()`/`mk1_fixture()`), provides **no** `account_xpub()` helper or concrete invocation for the md WatchOnly cell. **Why Important:** the engineer has no runnable fixture for the only md WatchOnly subcommand; the cell can't be written from the plan as-is. **Fix:** B2-Step-1 must specify the md address fixture: copy `account_xpub("m/84'/0'/0'", Network::Bitcoin)` from `cmd_address.rs:12-20` (deps `bip39`, `bitcoin::{Network,bip32::*,secp256k1::Secp256k1}` — all present: `bip39`/`bitcoin` are md-cli deps `Cargo.toml:33-34`), then invoke `md address --template "wpkh(@0/<0;1>/*)" --key @0=<xpub>` and assert `WATCH_ONLY_LINE` (and a json cell with `--json`).

## Minor (fold opportunistically)

- **[m2' residual] B3-Step-3 clippy line still default-feature only.** The R0 m2 ask was to add `cargo clippy -p md-cli --all-targets --features cli-compiler -- -D warnings`. B3-Step-3 added the `--features cli-compiler` *test* run but the clippy invocation (line 318) is default-feature. The compile emit is a verbatim 3-line copy, so risk is low (and manual.yml installs md-cli `--features cli-compiler`, catching it at toolkit-install time), but adding the feature-clippy line closes the gap cleanly.

- **[m6 — NEW] mk `repair` cell corruption idiom is loose.** Plan line 167 says "corrupt a bech32 char in the payload region". The robust idiom is the live `cli_repair.rs::flip_at` (`:48`) bech32-alphabet cyclic shift (guarantees in-alphabet → parseable-but-BCH-invalid; an out-of-alphabet char routes to a different error). Empirically a single in-alphabet flip of `mk1_fixture()[0]` → `mk repair` exits 5 with the advisory, so the cell works once the corruption stays in-alphabet. Recommend citing the `flip_at`/cyclic-shift pattern. (mk repair on a single chunk of a 2-chunk card per-chunk-corrects fine — verified exit 5; no chunk-count gate blocks repair.)

- **[m7 — NEW, informational] The mk fixture `card(V2_84_MAIN, "m/84h/0h/0h")` emits TWO chunks** (verified: account xpub + origin + stub overflows single-string). This is NOT a defect: `.args(mk1_fixture())` spreads the `Vec<String>` as separate positional args and `mk decode/inspect/address/derive/verify` all accept the 2-chunk input (verified exit 0 for each). The plan's `.args(mk1_fixture())` is correct. Worth a one-line plan note that the fixture is multi-chunk so a future reader doesn't "simplify" to a single `.arg()`.

---

## New-drift scan (what I checked → result)

- **mk scaffold imports compile against dev-deps?** ✓ `use assert_cmd::Command;` (plan :43) matches `cli_address.rs:11`; `assert_cmd::Command::cargo_bin("mk")` returns `assert_cmd::Command` which has `.arg()/.args(Vec<String>)/.output()` (live `cli_address.rs:56-63`). `use bitcoin::bip32::{DerivationPath, Fingerprint, Xpub}` + `use mk_codec::KeyCard` resolve (dev-deps present). md uses `use std::process::Command; use assert_cmd::cargo::CommandCargoExt;` (plan :243-244) — different idiom but valid: `cmd_decode.rs:3-4` uses exactly it, and `std::process::Command::cargo_bin("md").args([...]).output()` is valid via the extension trait.
- **`card()` returns `Vec<String>` (multi-chunk) — do cells pass `.args(...)` variadic?** ✓ All A1/A2 mk cells use `.args(mk1_fixture())` (plan :72, :207, table rows). `mk decode/inspect/address/derive/verify` accept multiple positional mk1 strings (verified live: 2-chunk fixture → exit 0 on all). The repair cell deliberately passes one corrupted chunk (`mk1_fixture()[0]`) — verified `mk repair <one-corrupted-chunk>` exits 5 (per-chunk BCH; no total-chunks gate on repair). `V2_84_MAIN` is defined (plan :53) before all uses; `mk1_fixture()` (plan :67) used consistently.
- **Remaining `<placeholder>`?** Found exactly one genuine unresolvable: md `MD1_FIXTURE` (plan :249) → [I5]. The `<mk1_fixture()>` notations in the A2 table (:166/:168/:169) are pseudo-notation for "spread the fixture", not unresolved (the fn is defined at :67) — acceptable. `<toolkit>`/`<tempdir>`/`<T>`/`<corrupted>`/`<COMMIT>` are descriptive meta-syntax, not code placeholders.
- **A1 scaffold ↔ A2/A3 cell consistency?** ✓ `mk1_fixture()`/`V2_84_MAIN`/`card()` used consistently; byte-parity constants `WATCH_ONLY_LINE`/`PRIVATE_KEY_LINE`/`TEMPLATE_LINE` match the ms-cli precedent (`advisory.rs:40-42`) byte-for-byte (`\u{2014}` em-dash). Module enum + `emit_output_class_advisory` are a faithful copy of `ms-cli/src/advisory.rs:26-45`.
- **Handler counts / fn-names.** ✓ mk 6 emitting (encode/decode/inspect/repair/derive/address), md 7 (encode/decode/inspect/bytecode/repair/compile + address WatchOnly) — match SPEC §3/§8. A2 table = 5 rows (decode wired in A1). md B2 table = 6 rows (decode wired in B1).
- **md emit sites re-grepped.** ✓ decode json`:24`/text`:30`; encode json`:69`/text`:95`; inspect json`:42`/text`:59`; bytecode json`:30`/text`:41`; repair single`:153` (slot `:152`), error `return Ok(2)` `:121`; address json`:57`/text`:65`(`Ok(0)`); verify `:45`; vectors `:73`. All match the plan tables.
- **5 pin sites + version + Tier-0.** ✓ `install.sh` md `descriptor-mnemonic-md-cli-v0.6.1`→v0.6.2; `install.sh` mk `mk-cli-v0.6.0`→v0.6.1; `install.sh` ms `ms-cli-v0.5.0` untouched; `manual.yml` mk v0.6.0→v0.6.1; `manual.yml` md v0.6.1→v0.6.2 (`--features cli-compiler`); `quickstart.yml` mk v0.6.0→v0.6.1. Toolkit `Cargo.toml:22 md-codec = "0.34.0"` (caret), `Cargo.lock` resolves 0.34.0; 0.35.0 published. FOLLOWUPS.md false-`resolved` entry at `:402`(Resolution)/`:403`(Status) — the heading `:396` the plan cites is the entry start; re-grep-instructed. Sibling-sweep FOLLOWUP at `:3394` (matches SPEC).
- **md verify/repair/gui-schema inert invocations.** ✓ `md verify` requires `--template` (plan B3 inert cell correctly uses it → exit 0 "OK" inert). `md gui-schema` exit 0 inert. `md repair` on corrupted literal succeeds (exit 0/5, advisory would fire). `md vectors` writes `./vectors` (I3 fix applies).

## Notes / risks

- **The fold's blind spot is uniform:** it lifted the mk *positional-fixture* scaffold (decode/inspect/address/derive/verify via `mk1_fixture()`) flawlessly, but three *non-positional* invocations were not given the same fixture-fidelity treatment — mk `encode` (flag-driven, needs `--policy-id-stub`), md Template cells (`MD1_FIXTURE` placeholder), md `address` (needs `--template/--key` concrete xpub). All three are the R0 C1/C2 class. Once I4/I5/I6 are folded with the concrete invocations cited here, the plan is runnable end-to-end.
- **mk `vectors` is a stdout corpus (SPEC §3), NOT a file-writer** — so the mk inert `vectors` cell (A3-Step-2) does NOT need `--out`; no cwd pollution there. Only **md** `vectors` writes files (I3). The plan correctly scopes the `--out <tempdir>` fix to md only.
- **Tier-0 premise re-confirmed at the doc level** (md-codec 0.34 pin, 0.35 published, false-`resolved` FOLLOWUP at `:402`). I did not rebuild the full toolkit to empirically observe the 0.34 FAIL → 0.35 PASS (SPEC R0 m3 marked it VERIFIED; the additive `chunk.rs` pre-pass is low-risk). The C1-Step-2/Step-5 empirical FAIL/PASS gate remains the right way to prove it at impl time.
- **Gate placement, phasing, lockstep, byte-parity, dead-code mechanism, transcript scope (sole `24-recover-md1`)** are all unchanged from R0 and remain correct. The design skeleton is complete; only the three fixture-fidelity invocations (I4/I5/I6) and two Minors (m2'/m6) need folding before R2.
