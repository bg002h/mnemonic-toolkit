# R0 review — `SPEC_test_hardening_T5_gui.md` (round 1) — Fable, adversarial

**Persisted verbatim per CLAUDE.md.** Reviewed against `mnemonic-gui` @ `5d88286` (HEAD = authoring SHA), toolkit tag `mnemonic-toolkit-v0.75.0`, the **exact pinned `mnemonic 0.75.0` binary** (wire-shape claims verified empirically), authoritative BIP-84 text, and an independent from-first-principles derivation.

## CRITICAL
**C1 — S3's core assertion ("recovered entropy hex == `00…00`") is unimplementable against the pinned toolkit's `restore --json` wire shape.**
- v0.75.0 `cmd/restore.rs` emits NO entropy field in any JSON arm: grep `"entropy` = 0 hits; envelopes carry `master_fingerprint`/`passphrase_applied`/`network`/`verification`/`wallets[]{wallet_type,descriptor,first_addresses}` (restore.rs:682-686,:1052-1059,:2402-2408,:3541-3550). Entropy consumed internally (:453-493), never re-emitted.
- Empirically (pinned binary): `bundle --template bip84 --network mainnet --slot "@0.phrase=abandon…about" --json` → `restore --from ms1=<emitted> --md1 <c1> --md1 <c2> --md1 <c3> --json` exits 0 with keys `{cosigners,mode,network,threshold,verification,wallets}` — no entropy substring; md1-arm envelope also lacks `master_fingerprint`.
- SPEC lines 31 + Acceptance §3 unsatisfiable. Verified replacement oracles: **(a)** bundle's emitted `ms1[0] == "ms10entrsqqqqqqqqqqqqqqqqqqqqqqqqqqqqcj9sxraq34v7f"` (empirically exact; = the published `ms vectors` constant already pinned `ui_harness_i4_realcli.rs:63` `MS1_ALL_ZERO`); **(b)** feed emitted ms1 → `ms decode --json` (MS_BIN, existing I4 idiom) asserting `entropy_hex`; **(c)** bundle's JSON `master_fingerprint == "73c5da0a"` (independently verified) as a free literal. Re-word S3 step 3 + Acceptance §3.

## IMPORTANT
**I1 — S3's input mechanism wrong on three verified counts.** (1) `bundle` has NO `--phrase`/`--phrase-stdin` at v0.75.0 (`cmd/bundle.rs` clap :23-180: seed = repeating `--slot @N.<subkey>=<value>`, subkeys phrase/entropy/ms1/seedqr per `slot_input.rs`); GUI `BUNDLE_FLAGS` (schema/mnemonic.rs:319-560) mirrors this, `allows_slots:true`. The `--phrase-stdin` the SPEC matched is under **xpub-search** (schema/mnemonic.rs:2888), not bundle. (2) Even where boolean `*-stdin` secrets exist, `assemble_argv` never emits them (`invocation.rs:325-354` `continue; // Boolean *-stdin secrets: preserve today's no-emit`). (3) `runner::run` pipes `Stdio::null()` (:147-156). Correct mechanism (verified end-to-end): `FormState::with_slots(SlotState{rows:[SlotRow{index:0,subkey:Phrase,value:"abandon…about"}]})` → `assemble_argv` emits `--slot "@0.phrase=…"` (shape pinned `tests/argv_assembler_slot.rs` cell_1) → plain `runner::run`, no stdin.

**I2 — S2's primary mechanism uncallable + misses the existing suite.** `is_descriptor_non_canonical` is a PRIVATE `fn` (conditional.rs:139, no `pub`) — a `tests/` integration test cannot make the "direct assertion" S2 names first. `tests/non_canonical_descriptor_account_pin.rs` (164 lines, 7 tests) ALREADY is the verdict-vs-consequence gate: asserts `bundle(&state)` → `--account` `PinValue(0)` fires (canonical :27-95) / lifts (non-canonical :98-128), the exact consumer chain conditional.rs:241-248 — apostrophe-only. S2's real gap = no h-notation case there; fix = extend that file's idiom. Direction: S2 asks only h-NonCanonical (pin-lifted); the S1 mutation's consequence symptom is the OPPOSITE — an h-**Canonical** descriptor misclassified NonCanonical → pin LIFTED → user `--account N` reaches toolkit → hard error on valid input (L12 class, :44-63). Require BOTH directions.

## MINOR
**M1 —** `runner.rs:1-13` citation wrong: `run` :147-156 / `run_with_stdin` :172-248; file 362 lines. Substance (stdlib Command, no GPU) correct.
**M2 —** S1 mutation-coverage: (a) the `wsh(multi(…))`-h-origins row is NOT mutation-covered (GUI regexes 4-5 are wrapper-prefix, no `'?h?`, conditional.rs:116-118) — keep it (pins toolkit-side h acceptance) but state the pkh/wpkh/tr(+use-site) rows carry the mutation teeth. (b) Capture each new row's `Expect` empirically (canonicity_drift.rs:5-7). Pre-captured (pinned binary): pkh/wpkh/tr h-origins, mixed `44'/0h/0'`, suffix-origin h, wsh(multi h-origins), use-site `/*h` → all `canonical`; `sh(multi` h-origin → `non-canonical`. (c) Extra row: double-marker `44'h` → exit 2 ParseFails ("invalid child number format") — pins where the GUI's `'?h?` structurally over-claims; safe (ParseFails rows never assert the GUI verdict).
**M3 —** count drift: canonicity_drift.rs=223 (SPEC 224); wire_shape_snapshot.rs=669; schema_mirror_defaults_drift.rs=190.
**M4 —** S3 plumbing: emitted `md1` is an ARRAY (3 chunks); restore takes repeating `--md1` (GUI RESTORE_FLAGS :770-773 `repeating:true`). Prefer the `--from ms1=<emitted>` + `--md1 …` leg (genuine seed round-trip + `verification` cross-check); md1-only restore derives the address from the embedded xpub (weaker round-trip).

## Verified GREEN
#13 shipped+gated (schema_mirror_defaults_drift.rs, PR #32=HEAD, in schema-mirror.yml full-suite, branch protection contexts confirmed); S1 harness (Expect :66-77, 19-row FIXTURES, 4 accumulators; zero h-fixtures; classifier `'?h?` conditional.rs:99-129); S1 mutation symmetry (all 19 apostrophe-pure → green under `'?h?`→`'?`; h-row REDs `disagreements`); S2 consumer chain real (conditional.rs:241-248); S3 gap real (wire_shape_snapshot only xpub-search+import-wallet; i4 4 decode cells; tutorial byte-gates re-captured transcripts incl. bundle→restore). **BIP-84 oracle VERIFIED three-way:** (a) authoritative bip-0084.mediawiki (m/84'/0'/0'/0/0 addr `bc1qcr8te4kr609gcawutmrza0j4xv80jy8z306fyu`); (b) independent stdlib-only derivation (BIP-39 indices [0×11,3], PBKDF2 seed `5eb00bbd…`, hand-rolled secp256k1 BIP-32, hash160+bech32) → identical pubkey+address, master fp `73c5da0a`; (c) the pinned binary's bundle→restore emits exactly this address. Scope guard sound (`lex_placeholders`/`parse_descriptor` reached only via `bundle_run_unified_descriptor`; template mode never lexes; `--descriptor` correctly deferred to ≥v0.76.0); independence (Cargo.toml:76 tag pin; classify-descriptor v0.20.0-era; NO-BUMP → parallel-safe); gate mechanics (PR+CI, 5 required contexts, MSRV 1.88, no fmt, clippy -D warnings, no lavapipe); determinism (no RNG/timestamp; byte-stable).

## VERDICT: OPEN (1C / 2I)
Converge by: re-speccing S3's entropy oracle (C1 — ms1-literal assert + optional `ms decode` leg + `first_addresses` == BIP-84 addr), swapping S3's input to the slot-Phrase idiom (I1), re-pointing S2 at extending `tests/non_canonical_descriptor_account_pin.rs` with BOTH h-directions (I2). The BIP-84 address literal is confirmed correct and may be pinned as-is.

---
**FOLD STATUS (opus, 2026-07-10):** C1 folded (S3 oracle re-spec: bundle's ms1[0] == published `ms10entrsqqqq…cj9sxraq34v7f` + master_fingerprint `73c5da0a`; restore's `first_addresses[0]` == BIP-84 `bc1qcr8…`; DROP the restore-entropy assertion — not emitted). I1 folded (S3 input → `--slot @0.phrase=…` via `with_slots`, plain `runner::run`, no stdin). I2 folded (S2 → extend `tests/non_canonical_descriptor_account_pin.rs`, BOTH h-directions; no private `is_descriptor_non_canonical` call). M1-M4 folded (runner citation; S1 mutation-teeth rows + empirical Expect capture + `44'h` ParseFails row; counts; md1-array/repeating `--md1`, prefer `--from ms1=` leg). BIP-84 literal confirmed pinnable. Re-dispatch T5 R0.