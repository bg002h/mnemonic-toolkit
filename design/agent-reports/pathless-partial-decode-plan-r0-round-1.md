# R0 PLAN REVIEW — IMPLEMENTATION_PLAN_pathless_partial_decode.md (round 1) — Fable, adversarial

**Persisted verbatim per CLAUDE.md.** Verified vs descriptor-mnemonic `a39c9d9f` (md-codec 0.41.0/md-cli 0.12.0) + mnemonic-toolkit `8e240d31` (code == `0a939443`, vendor/md-codec = 0.40.0). Empirical probes on built binaries. VERDICT: **OPEN (1C / 2I + 4M)**.

## CRITICAL
**C-1 — P2.1's pin bump silently executes the DEFERRED funds-labeled `toolkit-repin-sh-wpkh-canonical-flip` FOLLOWUP ("same-command-different-wallet") with zero coverage/docs/acknowledgment.** Toolkit `Cargo.toml:34` pins `md-codec = "0.40.0"` (no `sh(wpkh)` arm in vendored 0.40.0 `canonical_origin.rs`). Live md-codec is 0.41.0 WITH the F-A1 arm (`canonical_origin.rs:64-71`, `sh(wpkh(@N))`→`Some(m/49'/0'/0')`). Partial-decode's md-codec release = **0.42.0 atop 0.41.0** → the pin bump unavoidably crosses the sh(wpkh) boundary. `FOLLOWUPS.md:4845` (`toolkit-repin-sh-wpkh-canonical-flip`, DEFERRED) enumerates 4 flips at verified sites: `bundle.rs:1416-1418` + `verify_bundle.rs:1408-1414` consume `canonical_origin(...).is_none()` → (1) `bundle --descriptor "sh(wpkh(...))"` default origin flips `m/48'/0'/0'/1'`→`m/49'/0'/0'` (SAME COMMAND, DIFFERENT WALLET); (2) `--account≠0` succeed→refuse; (3) `--slot @N.path=` succeed→refuse; (4) pre-bump elided-sh(wpkh) bundles fail verify post-bump. Companion comment-only slug `canonical-origin-sh-wpkh-toolkit-mirror-divergence` (`FOLLOWUPS.md:4836`, 6 stale-comment sites incl. `error.rs:343-349` becomes wrong; `gui_schema.rs:1317-1320` `--classify-descriptor` verdict flips `non-canonical`→`canonical`, needs a test cell). Plan P2.1 says only "bump + re-vendor" — none of the rider scope appears. SPEC missed it too (both rounds), but the plan is where the bump becomes concrete. **Fold:** (a) fold full `toolkit-repin-sh-wpkh-canonical-flip` scope into P2 (4 flip tests + comment updates + gui-schema classify cell + CHANGELOG/manual migration note + resolve both slugs), OR (b) sequence the re-pin flip as its own R0-gated leg BEFORE P2. NOTE: `cli_template_from_tree` (`synthesize.rs:359-367`, no `Sh` arm) correctly keeps `:388`/`restore.rs:316` single-sig-template routing un-flipped — do NOT "fix" that mirror.

## IMPORTANT
**I-1 — P0.3 empty-override reject placement/mechanism underspecified (2 ways the plan's tests can't catch); SPEC acceptance #4 violable by the natural impl.** (a) Placement: "at validate.rs" naturally lands inside `validate_explicit_origin_required`'s per-idx loop, AFTER the `canonical_origin(&tree).is_some()` early-return (`validate.rs:222-224`). A canonical-shape wire (`wpkh(@0)`) with a present-but-zero-component `OriginPathOverrides[0]` (wire-expressible; `tlv.rs:461-477` rejects only empty vectors) passes decode but is rejected expand-side → recreates the decode/expand divergence SPEC I1 kills. Plan's only P0.3 test is non-canonical `sh(sortedmulti)` → gap survives. Fold: validate-side reject runs UNCONDITIONALLY (before the canonical early-return or as its own validator) + canonical-shape empty-override RED test. (b) Fatal-in-partial: if the reject lives in `validate_explicit_origin_required`, the natural `if !allow { validate()? }` skips it too → violates FATAL-in-partial. Fold: name the mechanism (separate validator, OR match-and-swallow ONLY `MissingExplicitOrigin`) + empty-override-under-partial RED test. Non-breaking re-confirmed (all vectors `origin_path_overrides: null`; all Rust-constructed overrides non-empty).

**I-2 — toolkit `mnemonic inspect` cannot decode NON-chunked md1 today; P2.3/cross-binary-parity tests unconstructible with natural fixtures + invites scope creep.** Proven: `mnemonic inspect --md1 md1yqpqqxqq8xtwhw4xwn4qh` (plain single-string) → `reserved-not-emitted: unsupported version 2` exit 3 (`inspect.rs:207` reassemble-only; reassemble misparses a single-payload header — no in-band dispatch, unlike `decode_md1_string`). Chunked-of-1 works. Small keyless dead cards (`sh(sortedmulti(2,@0,@1))`) are exactly the non-chunked form → an implementer writing P2.3 with one gets exit 3 for the wrong reason; the tempting fix (switch `:207` to a dispatching entry) is unreviewed scope. Fold: pin chunk-form fixtures (`--force-chunked`/chunked-of-1, mirror `tests/cli_inspect.rs:246` rechunk) for ALL toolkit-inspect + verify-bundle partial tests; state P2.3 does NOT change md1 intake dispatch; file the non-chunked-inspect intake gap as a FOLLOWUP.

## MINOR
- **M-1** P2.2 "BEFORE the compares" read as early-return makes mismatch>partial unreachable. Reword to verdict-selection: run compares, then `mismatch > partial > ok`; the partial gate guards the OK verdict only. (Exit plumbing clean: verify-bundle returns `Ok(if any_fail {4} else {0})` `:547`/`:805`; `inspect::run -> Result<u8>` `:88`; md-cli `Ok(u8)`, `Ok(4)` unused, `main.rs:259-271`.)
- **M-2** P1.1 incomplete: `md inspect` computes `compute_wallet_policy_id` unconditionally with `?` at `inspect.rs:20` → under partial errors before render; gate the COMPUTATION not just the line. Enumerate all 3 pid-derived outputs omitted on partial: `wallet-policy-id` line, `wallet-policy-id-fingerprint` line (`inspect.rs:62-65`, unmentioned), `wallet_policy_id` JSON key.
- **M-3** two SPEC-required tests dropped from the plan matrix: (a) cross-binary parity (md decode template == mnemonic inspect, both exit 4); (b) verify-bundle auto-repair non-firing on intact dead card (triggers `:2451-2461`/`:3119-3127`). Name them in the RED list.
- **M-4** cite drift (≤4 lines, non-load-bearing): header block `chunk.rs:346` (cited 348-361); comment `identity.rs:206-214` code at `:215`; `decode_md1_string` routing `decode.rs:101-106`.

## 9-check answers (condensed)
1. SPEC fidelity: no scope additions (repair strict everywhere verified; `:388` strict; no partial-repair sneak); drops = 2 M-3 tests + I-1(b) test. 2. P0 API buildable + three-layer threading sound + content-id after origin gate ✓. 3. P0.3 non-breaking + reachable, but placement/fatal-in-partial = I-1. 4. exit-4 addable both binaries; JSON hand-built Map ✓; M-2 nit. 5. `:2450`/`:3045` correct sole supplied-card sites; compares exclude origin ✓; `result:"partial"` addable; `:388`/`restore.rs:316` correctly strict; test-author note: `:2450` policy-flow partial reachable only with a `canonical_origin==None` EXPECTED tree → use `tr-multi-a`/`tr-sortedmulti-a`. 6. RED-proofs genuine (#1 doctored content-id constructible per `bch_adversarial.rs:365-377`; #2 false-pass genuine; #3/#5 genuine); #4 incomplete (I-1); unconstructible-fixture hazard = I-2. 7. sequencing/SemVer/version-sites correct (md-codec 0.41→0.42, md-cli 0.12→0.13; BIP `bip-mnemonic-descriptor.mediawiki:219-228` in md-cli leg; install.sh sibling md-cli pin frozen v0.11.2 `install.sh:35`; `.examples-build` FATAL pins `:43-44`); big miss = C-1; `42-md.md:212/:236` `--from-policy` examples → P1.2 advisory drifts those transcripts (P2.5 regen covers). 8. loci accurate ≤4 lines (M-4). 9. new holes: C-1, I-2, I-1(b).

## Required to reach GREEN
Fold C-1 (execute-or-sequence the sh(wpkh) re-pin scope + companion comment slug), I-1 (unconditional validate reject + mechanism + 2 RED tests), I-2 (chunk-form fixtures + no-intake-change + intake-gap FOLLOWUP), M1-M4 → re-dispatch. The P0 codec leg (P0.1/P0.2/P0.4) is sound as written; no impl starts until the plan is GREEN as a whole.

---
**FOLD STATUS (opus, 2026-07-11):** I-1, I-2, M1-M4 = clear plan-internal refinements, folding. C-1 = re-activates a DEFERRED funds-item (`toolkit-repin-sh-wpkh-canonical-flip`) beyond the approved scope → SURFACING to the user as an (a) absorb-into-P2 vs (b) separate-prerequisite-cycle decision before folding.
