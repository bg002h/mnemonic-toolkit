# R0 per-phase review — md1 BIP Phase-1 prose edit (BIP↔code alignment cycle) — Fable, adversarial, read-only

**Persisted verbatim per CLAUDE.md.** Target: `/scratch/code/shibboleth/descriptor-mnemonic/bip/bip-mnemonic-descriptor.mediawiki` (1237 lines) vs SPEC Part 2 + code at `crates/md-codec/src/{chunk,canonical_origin,identity,tlv,validate,origin_path}.rs`.

## Verified sound (load-bearing work landed)
- **(a) bit-vs-byte-vs-slack contradiction GONE.** Grep-clean for "bit boundaries"/"any valid division"/"3 bits of trailing slack". §Chunk fragment (829) whole-byte fragments, contiguous bit-37 boundary, recovery `⌊(bits−37)/8⌋` — bit-exact match to `chunk.rs:329/335`. §Reassembly steps match `reassemble()` incl. CSI re-derived from re-encoding (`chunk.rs:380`). CSI formula (271) matches `derive_chunk_set_id`. 320-bit budget (807) matches `SINGLE_STRING_PAYLOAD_BIT_LIMIT`.
- **(b) auto-dispatch contradiction GONE.** In-band bit-0 normative; "by reader role" excised; `got 9` trace row added.
- **(c) 93-cap contradiction GONE.** ≤80 data / 400 bits / 93 codeword excl HRP / 96 chars. Long code fully excised.
- **(d) scope-vs-corpus contradiction GONE.** Scope + sh-matrix MUSTs deleted; allow-list `{sh,wsh,wpkh,pkh,tr}`; pkh + pkh_basic consistent; FAQ rewritten.
- **B-I2** §Policy identifier preimage step-for-step match to `compute_wallet_policy_id` (`identity.rs:186-269`); 78-byte concat dropped; 12-word=132 bits; Md1EncodingId≠WalletPolicyId reconciled; fingerprint cite fixed (real CLI surfaces).
- **B-I3** validator table matches `validate.rs`/`tlv.rs` variant-for-variant incl. forbidden tap-leaf set; ChunkCount/IndexOutOfRange are real variants — no new fictional cites.
- **B-I5** erasure/guided/confidence → SHOULD/informative + 3 FOLLOWUP slugs; substitution t=4 BM+Forney kept normative.
- **B-I7/I8** preserve-verbatim + ascending re-emit (`tlv.rs:197-200`); varint encoder-MUST-minimal + decoder-lenient + chunked-fails-closed-via-CSI.
- **Expected BIP-states-target divergences (4) confirmed intended, NOT counted:** sh(wpkh)→49' table row (F-A1); `Vector.path` field (P2e); `<PAD_ERROR_VARIANT_TBD_P2>` placeholder (F-A8); 3 DG FOLLOWUP slugs not yet filed (Phase 3).
- **Cross-BIP consistent** (md1 831 ↔ mk1 364 agree; checksum layer consistent, shared init 0x23181b3).

## IMPORTANT (3)
**IMP-1. §Chunking determinism claims exceed normative content — two conforming encoders legitimately produce different chunk strings.** (807 "near-equal whole-byte slices" no formula; 829 "fixed deterministically"; 274 "identical strings"; 807 "smallest count that fits" vs the 320-bit formula.) "near-equal" admits 4-4-2 vs 4-3-3; "smallest count that fits" (45-byte) contradicts the 320-budget (which yields more chunks). Funds recovery unaffected (reassemble accepts any whole-byte division; CSI hashes assembled payload) but SHA-pinned corpora + the reproducibility assertion fail for chunked cards. "capped at 64" misreads as clamp; the reference REJECTS >64 (`chunk.rs:251`). **Fix:** make emit policy normative — `count = ⌈padded_payload_bits/320⌉`, MUST reject >64, `bytes_per_chunk = ⌈plen/count⌉`, full-size slices first + short remainder last; delete "smallest count that fits".
**IMP-2. §Payload non-zero-pad MUST-reject (B-I4) contradicts §End-of-section's unconditional tolerance.** (313 MUST-reject vs 651 "MUST be tolerated"; §End-of-section is pre-existing, the B-I4 fold didn't ripple into it.) An implementer transcribing §End-of-section builds a decoder violating §Payload's MUST — MUST-vs-MUST. **Fix:** amend step 4 — if all zero, clean end; if any non-zero, reject per §Payload.
**IMP-3. §Format-overview v0-scope paragraph contradicts the divergent-path/TLV machinery + the corpus.** (66-67 "all @i single shared path… per-placeholder paths deferred" vs divergent flag 391, divergent mode 400-412, TLV 0x00/0x02/0x03, `wsh_divergent_paths` vector 1181.) A decoder trusting it rejects legal reference cards — funds-relevant. Pre-existing (untouched by diff; source review missed it). **Fix:** rewrite 66-67 to actual v0.30 scope (per-@N divergence + on-card xpubs supported; only the standalone Xpub Card deferred); fix line 51 dangling "see Versioning below".

## MINOR (8)
1. Line 284 version-bits garbled (bits 3..1 are version HIGH bits). 2. Line 327 dangling §Backwards-Compat cross-ref for long-code removal. 3. Line 51 "see Versioning below" (no §Versioning). 4. "Tier 3" orphaned (Tier 0/1 defs deleted). 5. Preamble self-suggests BIP numbers. 6. Pad-MUST scope covers per-chunk symbol pad (discarded unchecked). 7. Line 305 present-tense `md decode dispatches` (false until F-A2 — same intended posture as sh(wpkh); add to phase ledger). 8. mk1 line 112 T_LONG cross-ref stale post-excision; md1 858 origin-path-bits cross-ref would help.

**VERDICT: NOT GREEN — 0 Critical / 3 Important (8 Minor).** Recovery-independence holds both directions; the 3 Importants are internal contradictions/determinism gaps of the class this cycle exists to eliminate — cheap localized prose folds.

---
**FOLD STATUS (opus, 2026-07-10):** IMP-1 folded (807 rewritten with the normative `count`+`bytes_per_chunk` split formula, MUST-reject>64, "identical strings"). CORRECTION (round-2 convergence caught this): "smallest count that fits" was removed from 807 but a SECOND copy survived at line 265 (chunk-header field table) — folded in round-2 (replaced with a cross-ref to §Chunking + "encoders MUST NOT vary it"). IMP-2 folded (651 zero-check conditional). IMP-3 folded (66-67 rewritten to v0.30 scope; 51 "Versioning" cite fixed). Minors 1 (284), 3 (51), 4 (Tier replace_all), 5 (BIP#), 6 (pad scope), 8 (mk1 112 T_LONG) folded; minor 2 already gone (excision removed the dangling cite); minor 7 tracked (BIP-states-target ledger); minor 8b (origin-path cross-ref) skipped (nice-to-have). Re-dispatched for convergence.
