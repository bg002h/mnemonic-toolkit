# R0 review — `SPEC_test_hardening_T3_wire_goldens.md` (round 1) — Fable, adversarial

**Persisted verbatim per CLAUDE.md.** Verified against live source: toolkit `da76e300` (=HEAD), descriptor-mnemonic `db0e1275` (=HEAD). T2's uncommitted edits present as described, disjoint from all T3 files. (Note: reviewed BEFORE T2 committed; T2 has since shipped — toolkit `c00ed813`, md `b9662e5`.)

## CRITICAL

**C1 — T3-b gap (a) "long-code string" is a DEAD wire mode: the long BCH code no longer exists in md-codec, and the SPEC's generating recipe hard-errors.**
`md-cli/src/cmd/encode.rs:36-45` (HEAD, shipped today in md-cli v0.12.0, `5aae2bd1`): *"F-A3: the long BCH code was removed in v0.12.0; md1 is regular-code-only... referencing it is now a hard error"* — `if args.force_long_code { return Err(CliError::BadArg(...)) }`. grep `long_code|LongCode|force_long|bch_*_long` across `md-codec/src/` = **zero hits** — no long-code encode OR decode path. SPEC line 25's recipe ("minimal 1-key template `--force-long-code`") is impossible. The recon's claim (§3 "already works") is false against its own SHA. Fold: (i) residual gaps = **3** not 4 (drop long-code); (ii) drop the "long-code cap off-by-one (`codex32.rs:20-28`)" RED mutation — with no long code, a cap mutation only moves the encode-refusal/chunking boundary, NOT a symmetric silent wire drift; (iii) SPEC line 28's manual-lockstep note becomes moot.

**C2 — T3-b part 1 ("Extend MANIFEST with ≥4 entries") is unimplementable under the "additive TEST/fixture only" invariant.**
`md-codec/src/test_vectors.rs:13-43`: `Vector` has NO `use_site_path_overrides` field, NO divergent-origin form (`path: Option<&'static str>` feeds only `PathDeclPaths::Shared` — `md-cli/src/cmd/vectors.rs:44-47`), NO long-code field. The generator **hardcodes keys empty** — `parse_template(v.template, &[], &fps)` (`vectors.rs:38`) — so a keys-non-empty MANIFEST entry silently emits a template-only fixture (wrong golden, no error). Closing (b)/(c)/(d) requires pub-struct field additions in `test_vectors.rs` (published API — `pub mod test_vectors`, `lib.rs:37`; `#[non_exhaustive]` makes it semver-additive but still an API change at unbumped 0.41.0) PLUS generator plumbing in `md-cli/src/cmd/vectors.rs` (shipped CLI src). Contradicts SPEC line 5 + acceptance #5. Fold options: (i) amend the SPEC to enumerate the schema+generator change surface with an explicit behavior-preservation criterion (all 15 existing vectors byte-identical) + squared NO-BUMP justification; or (ii) re-scope part 1 to construct the (b)/(c)/(d) descriptors DIRECTLY inside `tests/wire_golden.rs` (genuinely tests-only, but loses json_snapshots/template_roundtrip/corpus breadth).

## IMPORTANT

**I1 — T3-a RS-parity-order mutation "Gao decode still corrects" is factually WRONG single-line.** `rs.rs:205-264` is pure Gao interpolation; budget `2t+s ≤ m`. A single-line reversal/rotation in `rs_parity` (`rs.rs:134-161`) changes ~m parity symbols ≈ m errors > ⌊m/2⌋ → `Uncorrectable` → `tests/pipeline.rs::full_round_trip_mk1_and_md1` REDs, violating acceptance #1. Symmetric form needs a paired decode-side complement (two-line). Reword or drop.

**I2 — T3-a H0 mutation locus miscited + "each single-line" false for 3/4 classes.** H0 packing = `build_h0` (`pipeline.rs:213-220`, bits :215-218), NOT `build_geom`/:365 (that packs GEOM). The H0 swap needs write (:216-217) + read (:394-395) — two lines. Same for tag placement (emit split implicit in `build_data_symbols`, :631-638; decode `extract_tag_bits` :1093; K-geometry caveat when `(payload_bits+t)%11==0`). Genuinely single-line symmetric candidates: `SRC_MK1`/`SRC_MD1` code constant (shared by `source_kind_code`/`source_kind_from_code`, :196-209) and `CRC5_POLY`. Fix: correct locus; relax acceptance #1 to "single-locus symmetric mutation (≤2 mirrored lines)".

**I3 — T3-b TLV mutation cites the DECODE side.** `tlv.rs:210-260` is the decode loop (`pub fn read`, :212-314); encode is `pub fn write` (:86-208; tag emissions :121/:147/:171). The genuinely single-line symmetric mutation is swapping the tag const VALUES at `tlv.rs:11-19` (`TLV_USE_SITE_PATH_OVERRIDES=0x00`/`TLV_PUBKEYS=0x02`, shared by both sides). No test pins these consts → only a frozen-hex golden REDs. Class sound, citation wrong.

**I4 — T3-b chunk-split RED mutation cannot RED against the chosen frozen artifact.** The planned frozen consts pin `.bytes.hex` = the PRE-chunking payload (`vectors.rs:48-56`). A `chunk.rs` split-boundary mutation (`:266-273`; symmetric — `reassemble` concatenates in index order) changes only the emitted chunk STRINGS (`.phrase.txt`), never `.bytes.hex`. Fix: additionally freeze the `wsh_sortedmulti_2chunk` chunk-string literals (md1 string-deterministic; does NOT violate the "don't re-add ≥2-chunk" scope guard — pins the existing vector's wire) — or drop the chunk mutation from T3-b's RED list.

## MINOR
**M1 —** `wc-codec` has no mk/md deps (`Cargo.toml`: bip39+sha2 only) → `tests/wire_golden.rs` cannot consume the mk1/md1 string fixtures; the input `(payload bytes, payload_bits)` must ALSO be frozen consts (derived once from the toolkit fixtures, byte-identical across `word_card_adapter.rs:190-198`/`cli_word_card.rs:20/:25`). SPEC line 14 "only the resulting word list is newly frozen" is imprecise — both sides frozen, each with provenance.
**M2 —** CRC5 mutation: the replacement poly must be another PRIMITIVE degree-5 poly, else `tests/sync.rs::crc5_detects_every_single_bit_flip` (:71) can RED too. The "checkpoint byte-range" variant is multi-line.
**M3 —** RAID golden: pin a FIXED `array_id_seed` in the generating call (CLI path draws entropy — generate via the direct API).
**M4 —** Citation drift (fold-time sweep): `encode_payload` transit `vectors.rs:48` (SPEC :47); `bytecode.rs` encode_payload :15 (SPEC :14); `extract_tag_bits` `pipeline.rs:1093`; `Divergent` write loop `origin_path.rs:125-127` (SPEC :117-120); MD1 fixture `word_card_adapter.rs:190-198`.

## Confirmed accurate (no finding)
Visibility (wc_codec pub exports, no T2-a blocker); scope guard (`wsh_sortedmulti_2chunk` present, ≥2-chunk resolved, must not re-add; all 15 MANIFEST `keys:&[]`); self-reference (both `md vectors` + `vectors_output_matches_committed_corpus` transit `encode_payload`; frozen-hex layer genuinely needed); **#5 framing (T3-c clean)** — `md1_to_canonical` carries `total_bits`, mutation-gap not bug; range asserts survive `bytes.len()*8`; `md bytecode --json` reports `payload_bits` (honestly scoped as same-encoder). Oracle honesty accurate; NO-BUMP holds for T3-a/T3-c (and T3-b only after C2).

**VERDICT: OPEN (2C / 4I).** Two Criticals = T3-b scoping errors (C1 also falsifies a recon claim); T3-a needs mutation-mechanics correction (I1-I2); T3-c clean. Re-dispatch after fold.

---
**FOLD STATUS (opus, 2026-07-10):** C1 folded (long-code DROPPED — dead mode; residual = 3 shapes; drop the cap mutation). C2 → re-scope T3-b to option (ii) [construct (b)/(c)/(d) descriptors directly in `tests/wire_golden.rs` via public API, tests-only NO-BUMP] CONTINGENT on public-API feasibility — feasibility recon dispatched (`RECON_T3b_api_feasibility.md`); any shape needing a production struct/generator change is DEFERRED to a FOLLOWUP, not forced into NO-BUMP T3. I4 → also freeze the existing `wsh_sortedmulti_2chunk` chunk-strings. I1/I2 → replace T3-a mutations with `SRC_MK1`/`SRC_MD1` + primitive-`CRC5_POLY`; correct H0 locus; relax acceptance to ≤2 mirrored lines. I3 → cite `tlv.rs:11-19` tag consts. M1 (freeze input consts) / M2 (primitive CRC5) / M3 (fixed RAID seed) / M4 (citations) folded. Re-dispatch T3 SPEC R0 after fold.