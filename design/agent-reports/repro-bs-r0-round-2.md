## R0 round-2 — `reproducible-builds-musl` brainstorm spec

**Verdict: NOT-GREEN — 0 Critical / 3 Important.** Strong spec; the round-1 folds (C1, C2, I1, I2, I3) are all correct and I re-verified each against reality. The three Important findings are determinism-surface gaps that would let a build pass the §5 A/B gate yet still ship a non-provenance hash to an external rebuilder — exactly the failure class this cycle exists to close. None blocks the architecture; all are foldable into the spec body without re-deciding any fork.

### Round-1 fold verification (all CONFIRMED against reality @ toolkit `43298598`)
- **C1 (flag form)** — re-confirmed empirically on the pinned 1.85.0: `rustc --remap-path-prefix=/tmp=/build` compiles; `rustc -Cremap-path-prefix=...` → `error: unknown codegen option: remap-path-prefix`. Spec uses the top-level double-dash form throughout. ✓
- **C2 (config can't carry $PWD)** — sound; `.cargo/config.toml [build] rustflags` is verbatim (no shell expansion). Moving the variable from-side to the container ENV channel at the FIXED `/build/src` is the correct resolution, and the honesty caveat (local `cargo install` NOT reproducible-by-default) is accurate — I confirmed `scripts/install.sh` builds via `cargo install` at an arbitrary path. ✓
- **I1 (md slug)** — re-grepped descriptor-mnemonic `a759c79`: slug reads "⚠ phase 1 PARTIAL — status corrected 2026-06-24", states remap OMITTED, cross-cites this cycle-prep; `.cargo/config.toml` still has ONLY `codegen-units=1` + `strip="symbols"` (no remap). The "do not re-edit the corrected status; add the actual remap + flip RESOLVED on ship" framing is exactly right and avoids the inverted-discipline anti-pattern. ✓
- **I2 (cross passthrough)** — correct: toolkit has NO `Cross.toml` (verified absent), `cross` does not auto-forward host env; the explicit `[build.env] passthrough` list + direct residue assertions (not A/B-equality alone) are the right gate. ✓
- **I3 (epoch load-bearing)** — the unset-epoch-must-DIFFER probe is the correct way to prove `SOURCE_DATE_EPOCH` is honored for the cc leg; the same-calendar-day `__DATE__` coincidence hazard is real. ✓

### Per-question assessment
- **(a) recipe complete + correct?** Largely yes for the BINARY. `--remap-path-prefix` is the correct form for the 1.85.0 pin (trim-paths nightly-gated, re-confirmed); `--locked`, `LC_ALL=C`, `TZ=UTC`, `umask 022`, fixed `$PWD`/`$CARGO_HOME`, registry-under-CARGO_HOME, diff-ignores all present and consistent with the recon proof. **Gap: the published asset is a .tar.gz and the gzip wrapper is not pinned (Important #1).** Minor: `--remap-path-prefix=/cargo=/cargo` is an identity no-op (it "fixes" not "remaps" — only safe because `/cargo` is already canonical across builders); harmless but the spec should call it what it is.
- **(b) cc-under-musl residual de-risked?** Yes — this is the spec's strongest part. The per-arch musl double-build IS the first gate before any repro claim (P1 first action), the cc mitigations (`SOURCE_DATE_EPOCH`, `-ffile-prefix-map`, pinned cc via container digest, aarch64 cross-image DIGEST pin, direct `.o` cmp, `.comment`/`__DATE__`/host-path residue probes) are sound and correctly per-arch-separated. The epoch-load-bearing probe closes the same-day blind spot.
- **(c) hermetic container?** Right direction (digest-pin not tag, carries musl C toolchain). **But the apt layer and the git-dep miniscript fork are un-pinned trust roots (Important #3)** — "snapshot-pinned where feasible" leaves the container itself non-reproducible.
- **(d) POC-first phasing + NO-BUMP?** Sound and each phase independently shippable. NO-BUMP justified — verified no CLI/clap/wire-shape change, so manual-mirror and GUI `schema_mirror` gates don't fire. CHANGELOG/changelog-check correctly noted as tag-gated (this cycle is no-tag).
- **(e) verification model / provenance?** CI self-rebuild gate is well-defined (rebuild in pinned container → byte-compare → fail loud). **The published hash becomes genuine provenance ONLY once the gzip layer (Imp #1) and SOURCE_DATE_EPOCH derivation (Imp #2) are pinned** — otherwise the hash is reproducible same-machine but not rebuilder-portable.
- **(f) 5-site/reusable-workflow + stale-md-fix?** F6 correctly left as an R0 decision with a sound fallback; the remap-in-ENV shift genuinely strengthens centralization. Stale-md handled correctly (Imp #2 caveat about tag-derivation is the only addition).
- **(g) missing / over-scoped / supply-chain?** The three Importants. Not over-scoped — the GUI/gnu/multi-builder deferrals are well-justified.

### Required for GREEN
Fold the 3 Important findings into the spec body (gzip pin in §3.13 + P1; canonical SOURCE_DATE_EPOCH-by-commit-SHA derivation promoted out of §10 into §3.12/§8; container apt-layer + git-fork supply-chain pinning in §4) and re-dispatch — the reviewer loop continues after every fold per CLAUDE.md.