# R0 review — `SPEC_test_hardening_T2_never_wrong_payload.md` (round 1) — Fable, adversarial

**Persisted verbatim per CLAUDE.md.** Reviewed at toolkit `da76e300`, md `db0e1275`, mk `008182b`. Ground truth = direct source reads (3 repos) + **two empirical benchmarks compiled against md-codec @db0e1275** (scratchpad-only).

## Verified-correct foundations (no findings)
proptest dev-dep (toolkit `Cargo.toml:76`, mk `:38-40`). `parity_smoke.rs:84-95` silent-skip confirmed (HOME unset / binary absent → green; never runs in CI). Toolkit repair tri-state maps as needed: `RepairOutcome.set_verify` = `Blessed`/`Unverified{reason}`, Reject = `Err(RepairError::SetReassemblyMismatch)` (`repair.rs:439-476,1049-1078`); ms1 touched⇒Unverified (`:1163-1172`); indel = `IndelOutcome::{Unique,Ambiguous,Unrecoverable}` (`indel.rs:51-56,117-121`). Both decoders cap L≤4 + exact-root Chien + Forney nonzero (md `bch_decode.rs:432-440`, mk `:566-572`) → ≤t "emit exactly the original" is math-guaranteed (d=9; 20000/20000 2-error recoveries in bench). mk fmt 1.95.0. fuzz-smoke = explicit matrix + build gate; third target addable. **NO-BUMP confirmed sound all three repos** (tests/+fuzz/ only; parity_smoke removal tests-only; reference decoder in tests; no published-API/pin ripple).

## CRITICAL
**C1 — T2-a beyond-t leg (the F4 leg) is vacuous + mutation-unprovable + asserts a property master lacks.**
1. **Vacuous:** ">4-error output ≠ original OR refused" — a >4-distinct-substitution word is at Hamming distance >4; a bounded t=4 decoder (L≤4 cap) can NEVER return the original. `returned_original=0` across 4M beyond-t trials. Tests nothing.
2. **RED-proof unreachable:** removing `verify_mk1_set` (`repair.rs:1115`) only bites when a per-string correction yields a valid-but-wrong codeword whose set-decode fails — measured **≈3.5e-5/trial** (random 5-8-error). At CI-bounded proptest cases the mutation survives ≥97% (deterministically under a fixed seed). Acceptance criterion 1 unsatisfiable as written.
3. **Property false vs master:** single-string mk1 has NO cross-chunk hash (`mk pipeline.rs:9-11`) → forms its own group in `verify_mk1_set` → a wrong-fit parsing as xpub → `mk_codec::decode` Ok → **`Blessed`, no advisory** (`repair.rs:1049-1062`); md1 delegate always-Blessed (`:451-453`). A constructed version goes RED vs master day one → in a STOP+report cycle = guaranteed stall or silent weakening.

**Fix (concrete):** recast onto the real tri-state with DETERMINISTIC constructed vectors:
(a) **ms1:** any Ok with `repairs` non-empty ⇒ `Unverified` — RED deterministically under removing the Cycle-F demotion at `repair.rs:1163` (fires on every touched ≤t case; also rescues the "advisory" clause — ms1-only property).
(b) **mk1 multi-chunk:** construct a doctored chunk (copy a valid 2-chunk card, alter one payload symbol, recompute its string-layer BCH checksum via public `bch_create_checksum_*`, inject 1-4 subs so repair *touches* it) → the 4-byte cross-chunk hash deterministically fails set-decode ⇒ assert `Err(SetReassemblyMismatch)`; mutation "delete `verify_mk1_set`" ⇒ `Ok(Blessed)` ⇒ the test's independent `mk_codec::decode(corrected)` oracle fails ⇒ RED, single-mutation, deterministic.
(c) **single-string mk1/md1 fully-valid alias:** scope OUT as the known residual (cite eval F4's "payload-derived second oracle or demote"), FOLLOWUP entry, OR pin CURRENT Blessed behavior as a documented-residual KAT — do NOT assert it never blesses. Keep random >4 injection only as a never-panics/never-returns-original smoke.

## IMPORTANT
**I1 — T2-c's named RED mutation unreachable at proptest scale.** The defensive re-verify (mk `bch.rs:451`/`:504`) IS reachable (refutes dead-code reading) but barely: 4M trials → decode accepted 169 corrections, **29 failed re-verify (~7.25e-6/trial)**. Mined vector (md constants, synthetic 93-symbol codeword data `[(i*7)^13 & 0x1F]`): inject `{(59,21),(92,7),(47,8),(44,26),(50,8),(11,8)}` → decoder returns `pos=[14,86,89] mag=[7,18,24]` (L=3) which does NOT re-verify. At ≤4096 proptest cases P(RED)≈2.9%. **Fix:** mine per-repo KAT counterexamples against mk's own constants (`GEN_REGULAR`/`MK_REGULAR_CONST`; same 2-min mining program), pin as deterministic cells; run the mutation proof against the pinned corpus. Keep proptest implication (i) as always-on net; recast implication (iii) UNCONDITIONAL (≤4 injected ⇒ `Ok` AND ==original — as worded an `Err` vacuously satisfies the Ok-guarded form, syndrome-window mutations survive); pin (ii) with an exact-5-error cell (RED under relaxing `deg>4` at `bch_decode.rs:566`).

**I2 — #7 reference decoder: "brute-force ≤t" is infeasible; name the feasible independent form.** Exhaustive ≤4-error nearest-codeword = Σ_{i≤4} C(93,i)·31^i ≈ **2.7e12 checks/word** — strike. Feasible non-tautological form: **PGZ** (Gaussian-elimination locator, NOT BM), syndromes computed DIRECTLY from the received word (Σ cᵢ·(βʲ)ⁱ, not from md's residue/polymod), direct root trial over the 93-orbit, linear-system magnitudes (not Forney), GF from independently-generated exp/log tables — sharing only code-definition constants (GF(32) poly, β, j_start=77, n≤93, `MD_REGULAR_CONST`). **Firewall: no imports from `bch_decode.rs`.** Also: with only ≤4-error agreement samples the reference decoder is REDUNDANT (Gap-1 sweep already pins ≤t); its marginal value is on **beyond-t** words (accept/reject parity + miscorrected-output parity + the 29/4M re-verify class) — add beyond-t samples to the agreement set.

**I3 — #7 "completes in seconds" is FALSE.** Measured md-codec @db0e1275: **258.6 µs/decode debug, 13.4 µs release**. Full all-pairs 2-error (4,111,158 decodes) ≈ **17.7 min debug** / ~55s release. **Fix:** 1-error fully exhaustive (2,883 ≈ <1s) + all 4,278 position-pairs × a bounded seeded magnitude subset (~8 pairs ≈ 34k decodes ≈ 9s debug); full sweep `#[ignore]`-gated for release. Acceptance-rate cell: ≥1e5 trials (~26s debug), count = **any `Some` at e≥5** (RED under cap-relax; a wrong-output-only bound survives because the decoder returns *correct* 5-error decodes under cap-relax). Name the cap-relax mutation for this cell (SPEC gives none).

**I4 — T2-a generator + API precision (spurious-RED class).** (a) Substitutions: data-part only, lowercase charset, distinct positions, new≠old (HRP → `HrpMismatch`, uppercase → mixed-case reject md `chunk.rs:435-439`); multi-chunk ≤4 budget PER-CHUNK. (b) ms1 ≤t leg asserts equality only, NOT `Blessed`. (c) indel callable surface = `indel::recover_indel` (`recover_indel_card` is `pub(crate)`); property (Unique ⇒ ==original) sound (true re-edit always a candidate) but names no mutation — use "return first hit without the dedup/ambiguity fold" (`indel.rs:116-121`), RED on any ambiguous seeded case (constructions at `repair.rs:2432+`).

## MINOR
**M1** — "~1.15e-4" provenance: eval measured 6.5e-5 at e=5 (trailing chunk), 1.7e-4 at L=92; bench 5-8-mix at L=93 ≈8.5e-5. Re-measure at the test's own (L, seed, error-mix) before pinning.
**M2** — md fmt job floats `stable` (`ci.yml:56-59`), 1.85.0 pins build/test/clippy; phrase the criterion as "1.85 compilation compatibility + stable fmt".

**FOLD STATUS (opus, 2026-07-10):** C1 folded (T2-a beyond-t recast onto the real tri-state: (a) ms1-demotion pinned [RED under removing repair.rs:1163], (b) mk1 multi-chunk doctored-chunk constructed vector [Err(SetReassemblyMismatch); RED-deterministic under deleting verify_mk1_set], (c) single-string alias SCOPED OUT as documented-residual KAT + FOLLOWUP, (d) random >4 = smoke). I1 folded (T2-c: mined deterministic KAT cells vs MK constants + unconditional implication (iii) + exact-5-error cell; proptest (i) as always-on net). I2 folded (#7 = PGZ — direct syndromes, Gaussian-elim locator NOT BM, independent GF tables, firewall no bch_decode imports; beyond-t agreement samples). I3 folded (1-error exhaustive + bounded 2-error subset, full sweep #[ignore]; acceptance-rate cell ≥1e5 trials, any-Some-at-e≥5, cap-relax mutation named). I4 folded (data-part/lowercase/distinct generator, per-chunk budget, ms1 equality-only, indel::recover_indel + named fold-mutation). M1 (re-measure bound), M2 (md floats stable) folded; FOLLOWUP `repair-single-string-fully-valid-alias-second-oracle` added (acceptance #6). Re-dispatched for convergence.

**VERDICT: OPEN — 1 Critical / 4 Important.** Skeleton right (3 legs, independent oracles, always-on parity replacement, NO-BUMP); ≤t legs sound as-is. The two F4-pinning legs (T2-a beyond-t, T2-c re-verify) cannot pass the SPEC's own RED-under-mutation acceptance (rates 3.5e-5 / 7.25e-6) and T2-a asserts a never-bless property master lacks for single-string cards. All fixes concrete + bounded (deterministic constructed/mined vectors, PGZ, sweep resize). Fold + re-dispatch; do NOT begin implementation on this revision.
