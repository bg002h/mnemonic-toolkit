# SPEC R0 review (formal gate) — output-type-stderr-advisory Phase 1

**Date:** 2026-05-31 · **Reviewer:** opus architect · **SHA:** mnemonic-toolkit `18cfdce` · **Target:** `design/SPEC_output_type_advisory.md`
**Verdict: RED (1C/3I/4m).** Fold + re-dispatch.

## Folds confirmed landed: C2 (passphrase-of-xpub inert — input-only), C3 (TTY sites confirmed), I1-premise (convert gate :1099), I2 (decode/verify/compare-cost/gui-schema no-stderr-param), I3 (nostr per-branch :186/:251), I4 (ms-cli no toolkit lib dep — Cargo.toml verified), I5 (seedqr exclusive :280-297, electrum branch), M5 (em-dash + '> file.txt' :62). Multi-artifact predicates ARE computable at emit sites (no second pass).

## Critical
**C1 — the auto-repair short-circuit path is an un-enumerated D9 emit site reached from 4 "inert"-labelled commands.** `try_repair_and_short_circuit` → `emit_repair_report` (`repair.rs:1333`) calls `secret_on_stdout_warning(outcome.kind, stderr)` + writes the repaired (secret-for-ms1) card to STDOUT. Invoked from `cmd/verify_bundle.rs:1258,1301,1501,1521,1652,2022` (SPEC §3: inert), `cmd/xpub_search/seed_intake.rs:182` (all 4 xpub-search modes — SPEC §3: inert), `cmd/inspect.rs:135`, `cmd/convert.rs:994`. Plus the cmd-path emit `cmd/repair.rs:216`. SPEC §4.1 omits both → (a) if the legacy helper is removed, `:1333`/`cmd/repair.rs:216` won't compile; (b) §5 re-word misses `:1333` → drift; (c) §3 inert label wrong for the exit-5 auto-repair branch (ms1 legitimately on stdout → must emit P-line). *Fix:* §4.1 enumerate `repair.rs:1333` + `cmd/repair.rs:216` as replacement sites (route through worst_class_on_stdout); §3 add a per-branch note that the auto-repair short-circuit branch of verify-bundle/xpub-search/inspect/convert emits the repaired card's class (ms1→P, mk1→W, md1→T); P3 inert-audit tests the NORMAL branch only (or covers the auto-repair branch as emitting a line); §5 add `:1333`/`cmd/repair.rs:216`.

## Important
**I1 — `electrum_decrypt.rs:149` is a 3rd `_unconditional` caller** (§4.1 lists only silent_payment/nostr). Add it. (grep: 3 callers — silent_payment:286, nostr:251, electrum_decrypt:149.)
**I2 — re-word sweep under-scoped.** 12 toolkit test files assert `secret material on stdout` (cli_bundle_full, cli_bundle_slip0132_info, cli_indel, cli_silent_payment, cli_nostr, cli_electrum_decrypt, cli_inspect, cli_slip39_advisories, cli_bundle_multisig, cli_bundle_watch_only, cli_derive_child, cli_secret_in_argv_warning) — SPEC says ~9. FOUR doc trees, each with its own verify-examples/lint gate: `docs/manual`, `docs/technical-manual`, `docs/manual-gui` (`4b-slip39-combine.md`), `docs/quickstart` (`docs/quickstart/Makefile`, `20-singlesig/{23-bundle,26-recover}.md`, `30-multisig/32-bundle.md`) — §5 named only 2. *Fix:* §5 enumerate all 4 doc trees + 12 test files (+3 ms-cli: cli_repair.rs, main.rs, README.md); budget P5 against all 3 sibling verify-examples gates + docs/quickstart/Makefile.
**I3 — `convert --to path`/`--to fingerprint` is neither secret nor watch-only.** Non-secret convert target set `{Xpub, Fingerprint, Path, Mk1, Address}` (`convert.rs:31-52`). A derivation path / 4-byte fingerprint is not usable public key material → emitting `watch-only — public keys only, cannot spend` is factually wrong. *Fix (controller picks (a)):* convert = max over targets {secret→P, xpub/address/descriptor/mk1→W, path/fingerprint→inert}; so convert is P/W/inert-conditional (can be inert if only path/fingerprint). Refine principle (a): public key MATERIAL (xpub/address/descriptor/mk1) = watch-only; bare path/fingerprint = inert.

## Minor
M1 — citation drift (repair.rs:215→:216; inspect helper :156; ms repair literal :108) — re-grep at plan time. M2 — slip39.rs:684 is a FUSED line: replace the "reconstructed secret material" prefix with the unified P-line, KEEP "verify the recovered wallet's address" as the addendum. M3 — §7 must enumerate the exact addendum bytes (final_word:104, seed_xor split:244+combine, slip39 split:548+combine:684) + note the 1-line→2-line doubling for transcript re-capture sizing. M4 — ms-cli/toolkit emit_* helpers take only stdout; advisory emitted at run-level (post-emit_*), caller passes `std::io::stderr().lock()`.

## Controller folds: C1 → enumerate emit_repair_report+cmd/repair.rs:216 (§4.1/§3/§5/P3). I1 → +electrum_decrypt:149. I2 → 4 doc trees + 12 tests. I3 → path/fingerprint inert (option a). M2/M3/M4 noted.
