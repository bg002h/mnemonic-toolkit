# SPEC R0 review — ms1-repair-demote-to-candidate — round 1

**Verdict: NOT GREEN (1 Critical / 3 Important / 3 Minor)**
**Reviewer:** Fable architect (funds-weighted, read-only), per user directive "fable for review, opus for fold" (2026-07-09). Verified @ toolkit `4c554295`, ms `c2fd4eb`, mk `1c9fbf7`.
**Dispatched:** 2026-07-09 (Cycle F, SPEC R0 loop round 1). Persisted verbatim per CLAUDE.md.

The demotion half (Option B) is verified sound end-to-end — the "zero per-site edits" claim is TRUE as traced. The auto-oracle half anchors to the wrong ground truth; a strictly stronger, simpler oracle is already in scope at both sites.

## Citation verification — ALL ACCURATE (no drift/wrong)
Ms1 arm `repair.rs:1112-1150`, hardcoded `Blessed` @:1148; `repair_via_ms_codec` @:1183 branches `corrections.is_empty()` @:1190; `SetVerify::Unverified{reason}` @:459-465; `indel_exit_code` @:1462-1474; `cmd/repair.rs::run` generic `candidate_seen` @:164-167 → `indel_exit_code` @:244-248 (kind-agnostic); `try_repair_and_short_circuit` @:1675-1707 gates @:1690/:1701. **KEY CLAIM VERIFIED TRUE by census — 9 sites exactly** (`inspect.rs:133`, `convert.rs:1051`, `xpub_search/seed_intake.rs:184`, `verify_bundle.rs:{2079,2122,2339,2360,2503,2974}`); no other `set_verify` consumer outside repair.rs+cmd/repair.rs; no other production `repair_card` caller. ms-codec `decode_with_correction`→`Vec<CorrectionDetail>` (`decode.rs:242`, empty⇔already-valid, defensive re-verify present) → NO-BUMP holds. ms-cli `cmd/repair.rs:123-124`. verify_bundle 2 ms1 sites; `SuppliedCards` @:1990; single-sig mk1 decoded AFTER ms1 @:2107; multisig `card_for_cosigner` @:2377-2444 before loop; prior-art `emit_full_path_parent_fingerprint_check` @:3239; `KeyCard.origin_path/.xpub/.origin_fingerprint` public. Manual sentences @:750/:818-820/:3056-3059 + the 4-chapter "consistent" claim ACCURATE (and already false post-Cycle-E). Pins: ms-cli 0.13.2; 4-site advance list complete (`manual-gui.yml:165` correctly excluded).

## C1 (CRITICAL) — auto-oracle compares supplied-vs-supplied, ignoring the ground truth in scope; can Bless-exit-5 a bundle that contradicts the typed seed
Both ms1 sites live in check-emitters receiving `expected: &Bundle`; the ms1 site is only reachable when `expected.ms1[i]` is NON-EMPTY (watch-only slots skip @:2027-2044/:2450-2469) — i.e. a user-typed secret slot synthesized a byte-exact ground-truth ms1, which is literally the target of the adjacent `ms1_entropy_match` (:2056/:2480). The SPEC's oracle instead derives an xpub from the corrected seed and compares to the SUPPLIED mk1 (itself under test); precondition is only "mk1 decodes cleanly" (§3), NOT "mk1 matches expected" (`mk1_xpub_match` runs AFTER the ms1 block, and a Bless short-circuit via `?` @:2085 means it NEVER runs).
**HOLE input:** `verify-bundle --template bip84 --slot @0.phrase="<seed E>" --ms1 <corroded→corrects to wallet A's ms1> --mk1 <clean mk1 A> --md1 <md1 A>` → derived xpub(corrected-A-seed, origin_path)==mk1_A.xpub → MATCH → Bless → exit-5 "recovered" while BOTH cards mismatch the typed seed E; neither entropy/xpub match vs E ever executes. Multisig identical (card_for_cosigner mapped supplied-md1-vs-supplied-mk1). Ships ~100-200 LOC to deliver a WEAKER oracle than one string compare.
**Fix:** redefine the verify-bundle auto-oracle as **`corrected_ms1 == expected.ms1[i]`** (byte compare vs the ground truth already in scope). Bless iff byte-equal (no aliasing window); passphrase confound VANISHES (entropy-only, no derivation); no mk1 reorder (§5.9 gone), no clean-mk1 precondition (§5.6), no multisig degrade (§5.7), no privacy-mode/mk-codec dependency. The derivation-xpub oracle moves wholesale to the DEFERRED `repair --verify-against` follow-on (where standalone repair has no `expected` → it's the only oracle). Re-derive §3/§4-verify-row/§5.4-5.9/§8.2-8.4.

## I1 (Important) — `--max-indel` path bypasses the demotion → a pure-indel ms1 recovery still exits 5
`cmd/repair.rs:169-224`: indel recovery never touches `repair_card`/`SetVerify`. A `Unique` candidate with `subst_count==0` → `indel_exit_code(false,false,1)`=exit 5. `is_indel_trigger` (`repair.rs:1442-1454`) includes `TooManyErrors`, so a >4-subst ms1 can route into indel search at `--max-indel≥1` and exit 5. Residual funds risk small (candidates full-checksum revalidated ~2⁻⁶⁰; multi-hit→Ambiguous→4) but the §0.1/§2 "DEMOTES every ms1 correction" headline + §4 table are FALSE as written. **Fix:** scope the indel path — (a) demote ms1 `Unique` pure-indel to exit 4 (uniform "touched⇒candidate"), OR (b) keep 5 with a documented justification (full-checksum revalidation + exhaustive ambiguity enumeration = real self-verification, unlike bounded-distance correction which SPENDS checksum distance) — add a §4 indel row + a pinning test either way.

## I2 (Important) — §4 row 3 misdescribes verified behavior; silent fall-through is an unratified UX regression
`repair.rs:1690-1703`: an `Unverified` outcome falls through `Ok(())` with ZERO output (no report/advisory/candidate). Post-demotion, convert/inspect/xpub on a corrected ms1 → "original typed decode error, candidate invisible." Cycle E accepted this for mk1 (partial set can't proceed); for ms1 the corrected candidate is a COMPLETE usable string withheld purely for verification — hiding its EXISTENCE is a distinct product decision. **Fix:** choose explicitly — (a) ratify silent fall-through + correct §4 row + rewrite the per-kind auto-fire tables `41-mnemonic.md:818-820` (their "Auto-fire (exit 5 + repair report)" rows become false for ms1); OR (b) emit a one-line stderr advisory at the ms1 fall-through ("a candidate correction exists but cannot be self-verified — run `mnemonic repair` to inspect"), specifying the hook (gate shared with mk1).

## I3 (Important) — `Reject → exit 2 via new typed error` aborts the forensic check table mid-emission, inconsistent with verify-bundle's mismatch contract
Check failures (incl. today's `ms1_entropy_match` mismatch — the semantically identical event) emit the FULL table + `result: mismatch` + exit 4 (`Ok(if any_fail {4} else {0})` @:548). A typed `Ms1OracleXpubMismatch` via `?` from inside `emit_verify_checks` discards remaining rows + the `--json` envelope. **Fix:** on mismatch, push failed `ms1_decode`/`ms1_entropy_match` rows with a loud detail, let the run finish → exit 4. Under C1's expected-ms1 oracle this is the natural shape and the new `ToolkitError` variant is UNNECESSARY.

## Minors
- **M1** — `--json` `verdict` (§6): `"reject"` unreachable in the standalone envelope; specify WHICH envelope (`cmd/repair.rs:279` standalone vs auto-fire `emit_repair_report_json`) + reachable value set per kind.
- **M2** — the manual "principled distinction" must NOT literally state "exit-5 ⇒ an oracle verified it": retained `mk repair` exits 5 for an `Unverified` single-plate (advisory only, reassembly NOT run). Phrase exit-5 as "verified now, or verifiable-by-reassembly later."
- **M3** — test gaps: (a) mixed-kind precedence `mnemonic repair --ms1 <corrupted> --mk1 <clean>` → exit 4 dominates; (b) `--json` verdict test; (c) indel pin (I1); (d) collapses under C1.

## Funds attacks
Demotion false-Bless / false-Candidate / clean=0 / uncorrectable=2 → all SAFE. Auto-oracle wrong-bundle → **HOLE (C1)**. Chained/privacy/multisig-partial/passphrase → SAFE-as-specified but all MOOT under the C1 fix. Indel route → **HOLE-in-the-claim (I1)**.

## SemVer/cross-repo/scope — all verified correct
toolkit MINOR v0.81.0 + ms-cli MINOR 0.13.2→0.14.0; ms-codec/mk-codec/md* NO-BUMP genuine; no GUI/schema_mirror; 4-site pin advance complete; ms-cli manual `cargo publish` + release order correct. Deferring `--verify-against` sound (strengthened by C1 — the derivation oracle belongs there).

**Gate stands. Fold C1/I1/I2/I3 (C1 substantially rewrites §3-§5 + SHRINKS the cycle), persist, re-dispatch round 2.**
