# R0 SPEC Review — SPEC_pathless_partial_decode.md (round 1) — Fable, adversarial

**Persisted verbatim per CLAUDE.md.** Verified vs descriptor-mnemonic `a39c9d9f` + mnemonic-toolkit `0a939443` (vendor/md-codec = 0.40.0). VERDICT: **OPEN (1C / 3I + 7 Minor)**.

## CRITICAL
**C-1 — chunk-layer partial plumbing unspecified AND mis-specified; as written unimplementable, and the obvious shortcut bypasses the cross-chunk content-id oracle (funds-adjacent aliasing).** No named partial consumer calls `decode_payload` directly — all reach it via `decode_md1_string`/`reassemble` (which the SPEC leaves strict): md decode/inspect (`decode.rs:11-14`/`inspect.rs:13-16`; `decode_md1_string` routes chunk-forms into `reassemble(&[s])` `decode.rs:101-103`); toolkit inspect (`inspect.rs:207` reassemble only); verify-bundle supplied-card (`verify_bundle.rs:2450`/`:3045` reassemble only → the SPEC's explicit gate is UNREACHABLE, a dead card never yields a Descriptor to query); repair (`decode_with_correction`→`decode_md1_string`/`reassemble` `chunk.rs:633`/`:641`, toolkit `repair.rs:1641`, indel `:1491`). `reassemble`'s reject fires inside `decode_payload` step 6 (`chunk.rs:381`→`decode.rs:75`) BEFORE the content-id check (`chunk.rs:383-391`). The tempting fix (catch `MissingExplicitOrigin`, re-decode concatenated bytes via `decode_payload_partial`) SILENTLY DROPS the derived-chunk-set-id oracle — the aliasing class v0.86.0 + `prop_repair_never_wrong.rs:439-483` guard. Feasibility of the correct fix verified: `compute_md1_encoding_id`→`encode_payload` runs no origin gate (`encode.rs:100-110`). **Fold:** (a) partial-allowing variants at `decode_payload` + `decode_md1_string` + `reassemble` (+ `decode_with_correction` for repair); (b) invariant VERBATIM: partial relaxes ONLY `validate_explicit_origin_required`; per-chunk BCH, chunk-header consistency (`chunk.rs:348-361`), index-gap (`:363-372`), derived-chunk-set-id (`:383-391`) stay enforced; empty-override `EmptyTlvEntry` reject stays FATAL in partial mode; (c) RED tests: chunked dead card partial-renders+exit4; chunked dead card with doctored chunk-set-id REJECTS under partial; (d) delete the "reassemble keeps the strict gate" sentence.

## IMPORTANT
**I-1 — verify-bundle supplied-card decode-site map incomplete; acceptance #2's universal partial/exit-4 unmet for the dead keyless-template class.** Named `:2450/:2846/:3045`; `:2846` is the consumption match of `:2450` (restate). Four more decode sites: routing dispatch `:388` (`if let Ok(d)=reassemble(...)` → singlesig/multisig template), restore twin `restore.rs:316`, advisory decoders `:3193`/`:3403` (bail-silently, strict OK). A dead keyless template card (`wsh(multi(2,@0,@1))` no `--path`, a SPEC golden shape) falls through `:388` strict → lands on `--template is required` (`:435-439` ModeViolation) / policy mismatch: never `result:partial` (fail-closed, no false-pass, but contradicts acceptance #2; and opting `:388` into partial would route unresolved-origin cards into template-completion paths not designed for them). **Fold:** pin `:388`/`restore.rs:316` STRICT + document fall-through; scope acceptance #2 to policy/descriptor flows (or explicitly design template-route partial); list `:3193`/`:3403` strict-and-bail.

**I-2 — repair demote internally inconsistent + unstated mechanics.** Table allows "exit-4 (or excluded)"; exclusion = exit 2 md-cli (`repair.rs:124` Ok(2); success `:166` Ok(5|0)) → pinned exit-4 test fails. Needs partial `decode_with_correction` (C-1); must COMPOSE with v0.86.0 non-chunked demote (`toolkit/repair.rs:1660-1676`); cover indel oracle (`repair.rs:1491`, sole partial candidate else "recovered(5)" per precedence `:1537-1539`); pin UNTOUCHED dead card disposition (`md repair` today exit 2 strict; post 0/4 unpinned). **Fold:** pick demote everywhere, name the variant, state v0.86.0 composition + indel + untouched-card, add md-cli exit-4 to manual table (md-cli returns 4 nowhere today `main.rs:259-271`).

**I-3 — install.sh sibling-pin clause re-arms the v0.75.0 post-tag-revert incident.** "sibling pins FROZEN unless this bumps the md-cli pin" + this cycle publishes md-cli ⇒ reads "bump it"; the v0.75.0 record: md-cli sibling pin is a FROZEN baseline, bumping broke `sibling-pin-check`, caught post-tag, revert+re-cut. **Fold:** unconditional — toolkit install.sh SELF-pin only; md-cli sibling pin FROZEN regardless of the md-cli release.

## MINOR
- **M-1** verify-bundle result values are `ok`/`mismatch` (`verify_bundle.rs:525/:772/:977/:1737`, `schema_version:"4"`), not "ok/fail". Precedence: any failed check ⇒ `mismatch` beats `partial` (structurally-WRONG dead card must not read as merely "partial").
- **M-2** `unresolved_origin_indices()` → `Vec<u8>` (idx is `u8` throughout: `Descriptor.n:u8`, `MissingExplicitOrigin{idx:u8}`).
- **M-3** md-cli decode/inspect JSON is a hand-built `serde_json::Map` (conditional-insert, `decode.rs:19-31`/`inspect.rs:22-49`), not a `skip_serializing_if` struct — fix wording.
- **M-4** record dispositions for remaining strict consumers: `md bytecode` (`bytecode.rs:10-13`), `md verify` (decodes at `verify.rs:18-23` BEFORE byte-compare — "SAFE via strict decode"), `mnemonic word-card` (`word_card_adapter.rs:103`), xpub-search intake (`descriptor_intake.rs:224`). All fail-closed/unchanged.
- **M-5** BIP phasing contradiction: item 4 "lockstep with md-codec" but phasing defers BIP to P3 after toolkit; fold the BIP edit (`bip/bip-mnemonic-descriptor.mediawiki:~224`) into the descriptor-mnemonic leg so md-codec/md-cli tags don't ship stale "under separate design".
- **M-6** `result:"partial"` is a `--json` wire-shape change (new enum value); confirm mnemonic-gui only displays verify-bundle output (no result-string parsing) before informational-FOLLOWUP-only, per the paired-PR wire-shape rule.
- **M-7** `identity.rs:215-217` "unreachable-but-safe" comment goes stale once partial Descriptors exist in-process; update in P0 (policy-id stays fail-closed via `expand_per_at_n`).

## 10-check answers (condensed)
1. Call-site map enumerated: NO origin-consuming path can silently go partial (strict default protects everything unlisted); the holes are the inverse — partial can't reach its consumers (C-1) + incomplete site list (I-1). 2. reassemble/chunked = the Critical (content-id post-decode_payload). 3. verify-bundle sites right for policy/descriptor flows but unreachable w/o C-1 + template route outside (I-1). 4. query buildable on `validate` semantics; empty-override reject NOT breaking (no encoder emits origin_path_overrides; all vectors null; 12 committed MissingExplicitOrigin pins stay green). 5. repair demote needs C-1 + I-2 gaps. 6. citations accurate except M-1/M-2/M-3/I-1 `:2846`. 7. JSON coherent except M-1 precedence/M-3 wording. 8. sequencing/SemVer right; sibling-pin trap (I-3); BIP phasing (M-5); name fuzz/Cargo.lock explicitly; encode advisory can drift md-encode manual transcripts (regen). 9. test matrix missing: chunked-dead+content-id-mismatch (C-1), :2450 multisig negative, toolkit inspect exit-4/JSON, dead-template verify-bundle (I-1), untouched-dead repair (I-2), auto-repair non-firing on intact dead card. 10. residual misrepresentation: chunk-fallback content-id bypass (C-1); secondary result:partial masking mismatch (M-1), repair exit-0 on untouched dead (I-2 tail).

## Required to reach GREEN
Fold C-1 (three-layer + decode_with_correction partial plumbing, oracle-intact invariant, EmptyTlvEntry-fatal-in-partial, 2 RED tests, delete contradicting sentence), I-1 (complete site map, pin :388 strict + dead-template behavior, rescope acceptance #2), I-2 (pin demote, compose v0.86.0, cover indel + untouched, md-cli exit-4 table), I-3 (unconditional frozen sibling pin) + Minors — re-dispatch per reviewer-loop.

---
**FOLD STATUS (opus, 2026-07-11):** Folding. KEY REFINEMENT on I-2: under our per-call-site opt-in, the cleanest+safest repair handling is to keep `repair` STRICT (does NOT opt into partial decode) → no candidate-set enlargement, no Blessed-partial, dead cards stay un-repairable/exit-2 (UNCHANGED). Repairing a corrupted pathless card → exit-4 partial candidate becomes an explicit P1 FOLLOW-UP, not built now. This dissolves I-2's demote-mechanics/v0.86.0-composition/indel complexity and removes funds-surface. → C-1 partial plumbing then only needs decode_payload + decode_md1_string + reassemble (NOT decode_with_correction). All other folds (I-1, I-3, M1-M7) applied. Flag the repair de-scope to the user. Re-dispatch convergence R0.
