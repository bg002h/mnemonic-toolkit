# R0 review — `SPEC_docs_repair_exit_code_gui_manual.md` (round 1) — Fable, adversarial

**Persisted verbatim per CLAUDE.md.** Baseline: all 4 trees == origin (toolkit `1329131f`, ms `430008b`, mk `fa51615`, md `fb1257f4`).

## Probe 1 — ground-truth table vs live source (load-bearing): mostly ✓, but toolkit rows over-claim (see I2)
`ms repair`→4 (`ms-cli repair.rs:153`); `mk repair`→5 always (`mk-cli repair.rs:165`, Unverified=stderr advisory only); `md repair`→5 (`md-cli repair.rs:166`); toolkit `--ms1`→4 (`repair.rs:1163-1166`→`indel_exit_code`, tests `cli_ms1_repair_demote.rs:195…`); `--mk1`→5 bless/4 candidate (`verify_mk1_set`, tests `cli_mk1_repair_reverify.rs:502`/`:455`); `--md1`→5 (`repair.rs:451-453` always Blessed). `main.rs:241-245` maps u8→ExitCode. Standalone CLIs have NO `--max-indel`/`--max-subst` (rows exact); toolkit rows over-claim (I2).

## Critical
**C1 — "NOT 4i-repair.md, already correct" is FALSE; 4i carries the exact live wrong-exit-code assertion this fix targets.** `4i-repair.md:96` `| 5 | at least one chunk corrected (incl. a unique --max-indel recovery) |` — false for `--ms1` (touched subst→4 since v0.81.0) + `--mk1` partial-set (4 since v0.80.0). `:97` exit-4 row omits the Cycle-E/F candidate tier. `:100-124` worked example = `mnemonic repair --ms1` one subst at `--max-indel 0`, asserts "exit code `5`" (`:123-124`, line-broken → the recon's uniformity-grep missed it). Live: exit 4 + a `repair: correction UNVERIFIED` stderr advisory the example omits. **Fix:** 4 files not 3 — rewrite `4i:96-97` + fix `:123-124` to 4 + the advisory; amend acceptance to grep worked-example exit assertions (`` code `5` ``), not only uniformity phrases.

## Important
**I1 — pinned-tier contract unresolved; the fix as-specced is wrong for the book's OWN audience.** `docs/manual-gui/pinned-upstream.toml` pins `mnemonic-gui-v0.57.0`→toolkit-v0.75.0 (pre-Cycle-E/F); even `mnemonic-gui-v0.58.0` pins the same → NO GUI release exists whose tier has the exit-4 semantics. Against the pinned tier the book's exit-5 claims are TRUE; the corrected table is FALSE (a user replaying 6a's example in the pinned GUI sees exit 5). `manual-gui.yml` Job-1b contract = "the worked-example stdout/stderr a GUI user sees" = the pinned tier. No CI gate catches it (probe 4) → the book silently flips pinned-tier-true→false. **Fix (pick one):** (a) version-scoped prose — "since toolkit v0.80/0.81 (ms-cli v0.14, mk-cli v0.12); the tier pinned by this manual (v0.75.0) predates the demotion and still exits 5" — cheap, NO-BUMP; or (b) gate on the next GUI pin bump to a Cycle-F tier (blocked — no such tag).
**I2 — toolkit rows over-claim "ANY correction"; the `--max-indel` path inverts two rows.** ms1 *unique-indel* recovery = exit 5 (deliberate: indel re-validates the FULL checksum; test `cli_ms1_repair_demote.rs:306`; `candidate_seen` set only in the subst branch). mk1/md1 indel that consumed a subst = 4 via `substitution_seen` (`repair.rs:1491`). Harmless for 6a/5A/79 (no indel flags on standalone CLIs) but LOAD-BEARING once 4i joins (C1). **Fix:** scope the table to the default subst/ECC path (`--max-indel 0`) + add the toolkit indel tier (unique→5; subst-bearing/ambiguous→4). Shipped prose has this at `docs/manual/src/40-cli-reference/43-ms.md:371-373`.
**I3 — 5A inventory incomplete:** a second uniformity claim at `5A-repair.md:11-12` (intro), uncited (SPEC lists only :63/:67-68). Add it. Book-wide sweep: no other uniformity claims outside the 4 repair chapters (6a:47,51-53,75; 79:55,59-61 confirmed).

## Minor
M1 — name the exit-4 verdict per shipped surfaces: ms-cli `--json` = `"candidate"`/`"blessed"`; model table = "VERIFY-ME Candidate" (`43-ms.md:356`). Cite `43-ms.md:354-383` as the verbatim adaptation source (resolves I2 nuance too). M2 — md1 parenthetical: prefer "the sibling delegate only returns Ok on full decode success (`repair.rs:451-453`)" over "chunk-set-hash" (which caused a prior docs conflation, FOLLOWUPS `anti-collision-16bit-invariant-false`). M3 — the mk-standalone-vs-toolkit 4/5 asymmetry is REAL + DELIBERATE (Cycle E, mk1 watch-only) — document as designed, cite Cycle E, no behavioral-alignment FOLLOWUP needed.

## Probe 4 (gates): no code/schema change; non-gui manual already correct (`43-ms.md:354-383`). Edit must pass `make -C docs/manual-gui html` + `make lint` (mermaid figures-cache, markdownlint, lychee, gui-schema-coverage — **preserve `{#…-repair-…}` anchor IDs**). verify-examples/tutorial unaffected (zero repair transcripts gated). Nothing fails CI either way → I1 needs a decision, not a gate.

## VERDICT: OPEN (1C / 3I)
C1/I2/I3 mechanical; I1 needs a one-line direction (recommend (a) version-scoped prose).

---
**FOLD STATUS (opus, 2026-07-11):** C1 (4i added → 4 files; worked-example grep in acceptance), I1 (option (a) version-scoped prose — pinned v0.75.0 tier exits 5, note v0.80/81 demotion to 4; strictly-improving + NO-BUMP + fulfills "drive docs now"; pinned-tier subtlety surfaced to user), I2 (table scoped to `--max-indel 0` subst/ECC path + toolkit indel tier; cite 43-ms.md:371-373), I3 (5A:11-12 added), M1 (candidate/blessed naming + 43-ms.md:354-383 source), M2 (md1 phrasing), M3 (mk asymmetry = designed, cite Cycle E) — all folded. Convergence R0 to re-dispatch after the SPEC edits.