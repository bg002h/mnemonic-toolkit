# Implementation review — toolkit v0.53.2 audit minors M1+M2+M3+M13 (2026-06-10)

Reviewer: Fable 5 implementation-review agent (post-impl, pre-commit). Plan @ design/PLAN_minors_m1_m2_m3_m13.md (R0 GREEN r2). Verdict: YELLOW (0 Critical / 1 Important / 1 minor) — I-1 resolved via option (b): the M14 deviation is now RECORDED in the combined FOLLOWUPS entry (M14 was already tracked in descriptor-mnemonic as the pre-existing `taptree-leaf-validator-shallow` index line; a second entry would fragment the record). m-1 resolved by persisting this file. Review verbatim below.

---

## Critical

None.

## Important

**I-1 — Plan Phase 3 deliverable missing: the M14 companion entry in descriptor-mnemonic's FOLLOWUPS was never filed.**
- Evidence: `design/PLAN_minors_m1_m2_m3_m13.md:33` ("+ the M14 companion entry in descriptor-mnemonic's FOLLOWUPS (docs errand)") and `:41` ("only its descriptor-mnemonic FOLLOWUP entry is filed in Phase 3. The secrets.rs doc reword + M14 entry are IN Phase 3") — both phrase it as an act performed this cycle, paired with the secrets.rs reword (which WAS done). But `/scratch/code/shibboleth/descriptor-mnemonic` is clean (`git status` empty, HEAD = 271f34b, no commits since the backlog filing) and contains no promoted M14 entry — no `taptree-leaf-validator-recurse-or-doc` (the audit's stated disposition slug, `design/agent-reports/constellation-architecture-audit-2026-06-10.md:136`); only the PRE-EXISTING audit index line `taptree-leaf-validator-shallow` at `descriptor-mnemonic/design/FOLLOWUPS.md:14` (committed before this cycle).
- Concrete fix (pick one, before commit/tag): (a) file the promoted `###` entry in descriptor-mnemonic's `design/FOLLOWUPS.md` per the plan; or (b) if the orchestrator judges the pre-existing index line `:14` already satisfies the errand, record that deviation explicitly (toolkit combined FOLLOWUPS entry or commit message) so the plan-vs-ship delta is auditable — silent omission against a GREEN plan is what the hard gate forbids.
- Note: the toolkit diff itself makes NO false claim about M14 (CHANGELOG/FOLLOWUPS never mention it), so this is a missing cross-repo errand, not a doc inaccuracy in the reviewed tree.

## Minor

**m-1 — FOLLOWUPS/CHANGELOG cite an impl-review artifact not yet on disk.** `design/FOLLOWUPS.md` (new `audit-minors-m1-m2-m3-m13` entry) says "(+ impl review)" and `CHANGELOG.md` cites `design/agent-reports/minors-m1-m2-m3-m13-*.md`; only the two R0 round reviews exist. Persist THIS review verbatim to `design/agent-reports/` before the fold-and-commit step (CLAUDE.md convention requires it regardless).

## Verdict

**YELLOW — 0 Critical / 1 Important.** Everything inside the toolkit diff itself verified clean; the sole Important is the unfiled cross-repo M14 docs errand (one-paragraph fix). Re-dispatch is mechanical once filed/recorded.

### Verification record (all checks performed, tree left byte-identical as found — `git status` tracked-set unchanged, 18 M + 25 D)

1. **M1 — PASS.** Slot-exact check in BOTH branches (`bundle.rs` Single: `check_mk1_xpub_binding(pubkeys, 0, …)` before the stub check; Multi: per-card loop). `pubkeys: None` = distinct defensive failure; verified all 4 emission sites set `Some` (`synthesize.rs:149,434,607,833`). Helper comparison matches `verify_bundle.rs` (`xpub_to_65` + `XpubNotInPolicy` at :1636/:1652). **Deviation premise verified:** `synthesize_multisig_full` doc (`synthesize.rs:358-359`) literally says "self-multisig … all N xpubs are byte-identical" → swaps undetectable; the watch-only fixture with TREZOR_24 + BIP39_TEST_2 distinct seeds is correct. **RED confirmed by scratch-revert:** neutering both call sites → all 3 cells fail with `got Ok(())` (mutations pass every OTHER check — right reason proven, incl. the cross-slot-swap cell that only slot-exactness catches); restored byte-identical (sha256 `4523ec9a…dd48` before/after) and re-green. Right-reason guard: `assert_binding_failure` pins both the `mk1_xpub_binding` card label and the slot (`[1]`, `[0]`).
2. **M3 — PASS.** Advisory in `src/repair.rs:297-313`, per-occurrence on RAW pre-expansion values (before `expand_dashes`); unconditional for non-`-` `--ms1`; HRP-probe (`classify_hrp_prefix == Ok(Ms1)`) gates positionals only; `alternative` = `--ms1 -` both. **All `resolve_groups` callers enumerated: exactly 2** (`cmd/inspect.rs:105`, `cmd/repair.rs:111`), both threaded with `stderr`. Lint Route rows gained the `secret_in_argv_warning` anchor (`lint_argv_secret_flags.rs:92-93`) and the lint passes. Guard cells (`--ms1 -` stdin ×2, positional mk1/md1-only, stdin-expanded chunks) + RED cells (inline ×2, mixed per-occurrence, indel-relaxed corrupted prefix) all present and green; existing inspect/repair tests unbroken (full suite).
3. **M2 — PASS.** `gate.rs:453` narrowed to `Err(miniscript::Error::NonTopLevel(_)) => None`; `:454-461` debug_assert names the unexpected variant then returns `None` (release fail-closed, sole caller already root-falls-back → behavior unchanged); INVARIANT comment updated in place; cell `localize_non_top_level_subtree_defers_to_none` (`gate.rs:748`) lives in the file's existing `#[cfg(test)]` module and self-checks its fixture renders NonTopLevel.
4. **M13 — PASS.** Exactly 25 `D` entries; dir now holds only `bip84-mainnet-0-false-true.txt`; `cli_self_check.rs:1-8` module doc corrected; grep confirms no test enumerates the dir (`read_dir`/`WalkDir`/`insta::glob` absent; only `cli_self_check.rs:16` reads v0_2).
5. **Docs/ritual — PASS** (modulo I-1/m-1). CHANGELOG `[0.53.2]` claims all verified true of the diff (incl. the deviation-recording sentence about distinct per-slot seeds). Manual rows (repair `:2863`, inspect `:3111`) match `secret_advisory.rs:40-45`'s format string verbatim (`warning: secret material on argv (--ms1) — pipe via --ms1 - to avoid /proc/$PID/cmdline exposure`) + the `(positional ms1)` label + the indel note only where `--max-indel` exists. FOLLOWUPS: all 5 index lines flipped (4 minors-hunk + the `[obs]` line), combined promoted entry present and accurate, `orphaned-v0_2-md1-vectors-no-harness` status flipped with the delete-vs-harness rationale. secrets.rs reword verified against the live GUI source: `mnemonic-gui/src/secrets.rs:141-145` IS a 3-token hand list, and `tests/schema_mirror_secret_drift.rs:4-6` IS `{(subcommand, flag_name) | secret==true}` set-equality — the old "token-for-token parallel enumeration" claim was indeed stale. Version 0.53.2 at Cargo.toml, Cargo.lock, both README markers (gated by `readme_version_current` test, green), `scripts/install.sh:32`.
6. **Gates — ALL GREEN.** `cargo test --workspace`: 156 result lines, zero non-ok. `cargo test --bin mnemonic self_check`: 6/6. `cargo clippy --workspace --all-targets -- -D warnings`: clean. Full manual lint (all 6 stages: markdownlint 0 errors, cspell 0 issues, lychee 0 errors, flag-coverage, glossary, index): `[lint] OK` against the rebuilt v0.53.2 release binary + pinned sibling bins.
