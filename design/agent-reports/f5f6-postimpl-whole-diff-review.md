# F5+F6 post-implementation whole-diff review — GUI recovery-form wiring

**Reviewer:** fresh Fable (post-impl whole-diff, read-only, adversarial). `feature/f5-f6-recovery-wiring` @ `3c5c7dd9` vs GUI master `f5cb11f`. 11 files, vs SPEC/plan (both R0-GREEN r2).
**Dispatched:** 2026-07-10 (F5+F6, post-impl round 1). Persisted verbatim per CLAUDE.md.

## VERDICT: GREEN — 0 Critical / 0 Important / 3 Minor (observations, none gating).

**Release-readiness (mnemonic-gui-v0.58.0):** ship-ready as-is; proceed to the release ritual — Cargo.toml+lock 0.57.0→0.58.0, `README.md:47` `--tag` bump, CHANGELOG v0.58.0 entry, file `gui-dropdown-none-opts0-materialization-audit` in BOTH repos (top target `verify-bundle --template`), flip eval F5+F6, PR + the 5 required contexts. Watch `snapshots` (CI lavapipe arbitrating the llvmpipe-regenerated PNG at dify 0.6). #13 shipped in-cycle → NO `gui-schema-defaults-drift-gate` FOLLOWUP needed.

## Per-part
**1. F5 split — CORRECT, no collision possible.** `subcommand_argv_tokens` (`invocation.rs:35-43`), closed 5-parent set, `strip_prefix(*p).and_then(strip_prefix('-'))`. Censused ALL 4 tab schemas: mnemonic's 20 flat subs + every md/mk/ms sub — ZERO begin with any `<parent>-` prefix → no flat sub corruptible on any tab. Bare-parent edge covered by a unit assert. Per-token `mask.push(false)` keeps argv/mask parallel; single assembler → Run + Copy both fixed. **Live-verified (pinned v0.75.0):** all 12 nested pairs `--help` exit 0; both flattened exit 64. The 8 updated assertions are strictly stronger (positive `windows(2)` + negated flattened) in pre-existing widget-driving cells → non-tautological.
**2. F6 consts — exact.** Both `_INFER` have `""` at opts[0] (`schema/mnemonic.rs:38,2872`); exactly 2 consumers (`address-of-xpub --address-type`/`--network`); shared `NETWORKS`/`XPUB_SEARCH_ADDRESS_TYPES` byte-unchanged (latter only gained `#[allow(dead_code)]`+comment). Suppression rides on the `emit_one` `!v.is_empty()` guard (`:453-454`). `tests/f6_address_of_xpub_infer.rs` proves virgin-emits-nothing (argv + both masked copy flavors + no `""` artifact), the inverse, + the PREPEND census. `#[allow(dead_code)]` deviation acceptable (clippy `-D` gate).
**3. I-3 migration — closes the funds hole FULLY.** Drop (`persistence.rs:401-418`) (a) exact-key-gated `"mnemonic:xpub-search-address-of-xpub"` (`f6_migration_never_touches_other_forms` proves key-gating); (b) value-gated on the old literals (`p2tr`/`signet` preserved, tested); (c) idempotent (`Dropdown("")` survives reload, tested); (d) BEFORE the default_value gate (both None-default reachable); drop→absent→re-materialize `opts[0]=""`→emits nothing. Traced every escape path (schema-version mismatch → safe-virgin; malformed keys fail-open but can't rehydrate this form; alternate state path same `load()`; v0.57.0 downgrade re-migrated on next start). **No path leaves stale `p2pkh`/`mainnet` emitting.** Only a deliberate `p2pkh`/`mainnet` is reset (spec-G7 accepted fail-safe).
**4. #13 drift gate — non-tautological, MUTATION-PROVEN.** Built a shim `mnemonic` emitting mutated `gui-schema`: injected default change → FAILED with the exact drift line; injected choices deletion → FAILED the choices leg. Real binary GREEN (live-proves the `""`-strip). Typed-default normalization unit-tested; 1-entry `--feerate` allowlist explicit; choices scoped to non-null JSON; mnemonic-only. Binary resolution `MNEMONIC_BIN`→PATH→loud-SKIP; CI-live (`schema-mirror.yml:127-133`).
**5. Snapshot — exactly one PNG.** `mnemonic-xpub-search-address-of-xpub.png` (44018→44546B); rendered → both dropdowns read `(none)`, form intact (not corruption).
**6. Suites (reviewer-run):** `cargo test` = **684 passed / 0 failed** (incl. f5_nested_argv 3, f6_infer 4, persistence 16 [+4 migration], drift 2 — RUN not skipped, PATH mnemonic v0.75.0 = pin). clippy `--all-targets -D` exit 0; `--no-default-features -D` + `build --no-default-features` exit 0. Confirmed `.github/workflows/build.yml:43-60` headless gate = build+clippy (no test) — matches the impl's `--no-default-features` test-compile caveat.
**7. No scope creep:** exactly 11 files; no Cargo.toml/lock (deferred to ritual), no pin change, no fmt, GUI-only.

## Minor (observations — no fold)
- **M-1** `resolvable()` (`schema_mirror_defaults_drift.rs:83-91`) probes hardcoded `"mnemonic"` while `run_gui_schema_json` resolves via `pinned-upstream.toml entry.bin` — consistent today (`bin="mnemonic"`); cosmetic.
- **M-2** The migration is unconditional (not version-stamped): a post-v0.58.0 user deliberately selecting `p2pkh`/`mainnet` loses it on restart (in-session unaffected) = spec-G7's accepted fail-safe; note in the audit FOLLOWUP as future de-migration material.
- **M-3** `f5_nested_argv.rs` flat pass-through: 4 e2e + 6 unit; the census shows all 20 flat mnemonic subs safe → representative, fine given the closed-set proof.
