# R0 review — PLAN_readme_inventory_refresh — round 2

**Verdict: GREEN**

All nine round-1 findings folded faithfully; the two mirror-agreement questions resolve cleanly; no new contradiction. Minors are polish, none blocking.

## Round-1 fold verification (ALL RESOLVED)
- **C1** visible CHANGELOG/Subcommands pointers both openers; no invisible-marker reference survives.
- **I1** §2/§4 AGREE: Descriptor-construction group after Decrypt/repair/inspect == inline comma-group after the xpub-search group (the same relative slot; manual orders build-descriptor last, :3878).
- **I2** extended restore clause mandated in BOTH openers; facts check (md1-ALONE = [0.44.0]:174; taproot NUMS = [0.49.1]:45-47).
- **I3** restore → Wallet import/export with the argument stated ([0.45.0]:161 corroborates the shared emitter dispatch).
- **I4** manual intro fixed same-commit ("Twenty" listing 21; 3 omissions confirmed; all 3 anchors exist) + FULL manual lint escalated.
- **M1-M4** per prescriptions; M4's grep correctly scoped to the READMEs only (the manual has 2 LEGITIMATE "single-sig restore" mode-contrast uses at :770/:946 that must survive).

## Critical
None.
## Important
None.
## Minor
- **M-r2-1** pin the 3 manual-intro insertion positions (restore between export/import-wallet; addresses before decode-address; build-descriptor before the terminal gui-schema).
- **M-r2-2** optionally mirror "via ms-shares" into the root opener's splitting clause (3 words).
- **M-r2-3** root gains a 2nd CHANGELOG pointer near :16 — acceptable; don't let anyone "deduplicate" the load-bearing one.

## Empirical probes run
Repo bc373ed tracked-clean; both README regions + the manual intro re-read (stale-site greps hit exactly the expected 5/2/2); manual section-order census + anchors; subcommand census from the CURRENT source Commands enum = 24 (both on-disk binaries stale — 0.24.0 installed / 0.48.0 release build — the enum is ground truth); CHANGELOG anchors for every claim ([0.40.0]/[0.44.0]/[0.45.0]/[0.49.1]/[0.50.0]-[0.52.0]).
