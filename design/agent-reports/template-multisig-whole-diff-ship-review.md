# #28 phase 2 — WHOLE-DIFF adversarial execution review (toolkit v0.60.0 ship gate, opus architect, verbatim)

> Reviewer: opus architect (Opus 4.8, 1M) — the mandatory, non-deferrable post-implementation WHOLE-DIFF adversarial execution review immediately before tag + ship as toolkit **v0.60.0** (CLAUDE.md final ship gate). Branch `feature/bundle-md1-template-multisig`, HEAD `dfd238ea`, clean fast-forward ahead of `origin/master` (`e4cbcd7e`) by 21 commits. Reviewed delta `git diff origin/master..HEAD`. Verified against md-codec **0.37.0** (registry, `Cargo.lock`), mk-codec 0.4.0, rust-miniscript 13.x. All tests + clippy + the manual lint + a live end-to-end CLI reproduction run BY ME on HEAD. Read all six per-phase R0 reviews + the C1 advisory + the SPEC/brainstorm reviews; my unique lens is the CROSS-PHASE / integration / ship-readiness view the isolated per-phase reviews structurally could not see.

**Verdict: GREEN — 0 Critical, 0 Important.**

The funds-safety surface is sound end-to-end as a WHOLE — not just per-phase. The make-or-break gates all hold: completed addresses == an INDEPENDENT oracle (rust-miniscript / Bitcoin Core); the C1 carried-origin-never-loaded invariant is structurally airtight for BOTH the canonical empty-elide AND the general Divergent-carry; per-slot origins are BUILT FRESH from the supplied keys at the cosigners' actual family (`compute_default_origin_path` is provably NEVER on the completion path); and the search refuses loudly (exit≠0) on wrong-assignment / ambiguity / no-match / duplicate-keys / over-supply / weak-prefix — never silently completing. The P4 shared-core refactor preserved restore's every floor in the same order (byte-equal but for one local var rename). Emit and completion cover a consistent shape set with the asymmetry in the safe direction. All 7 version sites are at 0.60.0; the manual flag-coverage gate is GREEN; the CHANGELOG is accurate; SemVer-MINOR is justified. The two P6.1 deviations are correctly dispositioned (the verify-bundle `--from` argv advisory is REAL — I reproduced it live; the fuzz-build break is genuinely pre-existing and unchanged). New-clap-surface lockstep debt is recorded. **The toolkit may be tagged + shipped v0.60.0.**

---

## Regression / build evidence (counts I ran on HEAD `dfd238ea`)

| gate | result |
|---|---|
| `cargo test -p mnemonic-toolkit` (WHOLE suite) | **PASSED=3234, FAILED=0, IGNORED=14**; exit 0; 0 `FAILED`/`error[` lines |
| `cargo clippy -p mnemonic-toolkit --tests --bins --lib -- -D warnings` | **exit 0, clean** (re-run twice) |
| `make -C docs/manual lint` (4 sibling bins built + supplied) | **OK** — all 6 steps incl. **4/6 flag-coverage** + 5/6 glossary green |
| `--test cli_restore_md1_template_multisig` | **27 passed** |
| `--test cli_verify_bundle_md1_template_multisig` | **7 passed** |
| `--test cli_bundle_md1_template_multisig` | **13 passed** |
| `--test prop_template_completion_roundtrip` | **5 passed** |
| `--test bitcoind_differential` | **2 passed** (unconditional anti-vacuity legs), **2 ignored** (`#[ignore]` node rows) |
| `--test lint_argv_secret_flags` | **4 passed** (incl. `from_axis_set_equals_gui_schema`) |
| live CLI reproduction (verify-bundle `--from` argv advisory) | **emitted** (see §5a) |
| fuzz-build `RUSTFLAGS=--cfg fuzzing cargo build --lib` | **E0433 ×2** — pre-existing, see §5b |

`git diff --name-only origin/master..HEAD` does NOT include `crates/mnemonic-toolkit/src/mlock.rs` — g6 fmt-exemption intact. `error.rs` is NOT in the diff → no new `ToolkitError` variant → the alphabetical-ordering rule is N/A. Production src delta is exactly the claimed 6 files, +3330/-115.

---

## 1. End-to-end funds-safety, as a whole — SOUND

**`compute_default_origin_path` is NEVER on the template-completion path (the load-bearing grep).** Four hits in `src/` total: `bundle.rs:1445` + `bundle.rs:2210` (definition) and `verify_bundle.rs:1373`. I read each context: `bundle.rs:1445` is the KEYED descriptor-mode emit's `is_non_canonical` BIP-48 default-inference (guarded by a KEYED canonicity probe); `verify_bundle.rs:1373` is `run_full`'s KEYED wallet-policy path (the same mirror). **`restore.rs` has ZERO hits** — and `complete_multisig_template` (the shared completion core, restore.rs:1416-1814) lives there. The own origin on the completion path is built ONLY via the precedence `--origin (explicit) → cosigner_family with the own --account substituted (own_origin_from_family, :1212) → lazy canonical_fallback (:1540, BIP-48 ONLY for a canonical multisig, errors for a general policy naming --origin)`. Per-slot origins are built fresh in `build_candidate` (:1681) from `pool[pi].origin`. No BIP-48 forcing.

**C1 — carried-origin-never-loaded — structurally airtight for BOTH cases.** I read the whole of `build_keyed_template_descriptor` (synthesize.rs:283-327): it constructs the output `Descriptor` literal building `path_decl.paths` FRESH from `slots[i].origin` (:294-300, :313-316) and copies from `keyless_template` ONLY `use_site_path`, `tree`, `use_site_path_overrides`, `origin_path_overrides`, `unknown` — `keyless_template.path_decl` is **never read** (no field access anywhere in the body). This holds for the canonical empty-elide and the general Divergent-carry alike. Pinned non-vacuously by `carried_origin_never_loaded_into_completion` (canonical) + `general_policy_carried_origin_never_loaded` (Divergent tamper to `m/99'/0'/N'`) — both green in the 27-test restore suite.

**Canonical (`wsh(sortedmulti)`/`wsh(multi)`/`sh(wsh)`) trace.** Emit elides origins to `Shared(empty)` (canonical_origin is Some); decode re-derives; completion builds fresh at BIP-48-via-cosigner-family. `wsh_sortedmulti_id_search_completes_to_golden` + `..._wsh_multi_*` + the sh-wsh rows complete to the rust-miniscript golden (`Descriptor::from_str → into_single_descriptors → derive_at_index → .address()`) byte-equal. Independent oracle confirmed.

**General/thresh (degrade2-class `wsh(or_i(...))` BIP-84) trace.** Emit carries the source per-`@N` Divergent origins (canonical_origin is None) so `md decode` accepts (`validate_explicit_origin_required` satisfied); completion ignores the carried, builds fresh at BIP-84-via-cosigner-family. `general_policy_id_search_completes_to_golden` + `degrade2_structured_completes_to_golden` (a full degrade2 analog with after()/older()/sha256()/inner multi(2,…) over 4 divergent BIP-84 origins, multi-own `--account 0,3`) complete to `general_golden_addresses` (independent rust-miniscript) byte-equal. A BIP-48 rebuild would FAIL the search here → positive proof the per-slot build honors purpose 84'.

**The refuse directions — ALL exit≠0, never silent (verified by test + by reading the arms restore.rs:1786-1805):** wrong key→slot (`floor_wrong_cosigner_no_match_refuses`, `general_policy_wrong_family_no_match`, `swapped_assignment_no_match_refuses`) → `SearchOutcome::None` → `✗ NO MATCH` + `RestoreMismatch` exit 4; ambiguous → `SearchOutcome::Ambiguous` → `✗ AMBIGUOUS` + `BadInput`; no-match-mode (no id/addr/explicit) → `ModeViolation` exit 2 (I reproduced this live); duplicate supplied keys → `reject_duplicate_keys` BEFORE search → exit 4 (`floor_duplicate_cosigner_key_refuses`); over-supply / `--own-account-max` → `ModeViolation`/`BadInput` exit 1/2 (`pool_larger_than_slots_refuses…`, `own_account_max_flag_refuses…`); weak `--expect-wallet-id` prefix → `validate_prefix_strength` over the realized S → refuse.

**Realized S = n!.** The every-slot gate pins `pool.len() == n` (under-supply :1626, over-supply :1635, both plain runtime `return Err`, NOT debug_assert), so the engine enumerates exactly `n!` and `realized_s = perm_count_u128(n, n)` (:1662) — the prefix-strength + adaptive-cap are sized to the truly-enumerated space (the round-1 I-1 sizing-fidelity break is structurally closed, confirmed in the P3a-r2 review and by re-reading :1626-1662).

## 2. The P4 shared-core refactor did NOT weaken restore — CROSS-PHASE check (highest risk)

I performed a precise structural diff of the floor sequence between the P3b inline body (`6fbc96c4`) and HEAD's extracted `complete_multisig_template`, normalizing `args.`→`ctx.` and extracting every gate/floor marker in order (`own_account_max.is_some` / `pool.len() < n` / `pool.len() > n` / `reject_duplicate_keys` / `perm_count_u128(n, n)` / `debug_assert_eq!` / `validate_prefix_strength` / `sorted_shape` / the three `SearchOutcome` arms / `build_keyed_template_descriptor(d,…)`). **The two sequences are IDENTICAL** — the only diff line is a local-variable rename (`let cand` → `let completed`) at the final build, which is semantically inert (both call `build_keyed_template_descriptor(d, &triples)`). Every floor is present in the same order; the `--own-account-max` I-1 gate moved INTO the core (restore.rs:1434) so it now fires for BOTH surfaces (verify passes `own_account_max: None`, verify_bundle.rs:865, so verify can never over-supply); C1 is intact (the build is unchanged); the seed-resolution hygiene was extracted verbatim into `resolve_template_completion_seed`. Restore's pre-P4 behavior is preserved by construction.

**verify == restore parity, byte-equal.** `verify_multisig_template` (verify_bundle.rs:808) calls the SAME `complete_multisig_template` then recomposes via the SAME `candidate_descriptor_string` (the #25 multipath engine restore emits with). `verify_bundle_canonical_wsh_multi_template_id_search_ok` asserts `verify.first_receive == golden[0] == restore.first_addresses[0]` on an ORDER-DEPENDENT `wsh(multi)` — a genuine three-way byte-equal anchor (the P5 fold of P4's M-1). The verify search boundary is not weaker than restore's; it is the same code.

## 3. Emit (P2) ↔ completion (P3/P4) mutual consistency — no silent gap

**Emit gate** `template_admissible` (synthesize.rs:1113): n==1 keeps the phase-1 strict `cli_template_from_tree` gate; n≥2 admits iff `to_miniscript_descriptor().is_ok() && !has_hardened_use_site` → admits `wsh(multi/sortedmulti)`, `sh(wsh)`, general/thresh, `tr(NUMS,multi_a)`; refuses `tr(sortedmulti_a)`, `sortedmulti`-in-combinator, hardened use-sites (`TemplateFormUnsupportedShape`). Runs on the KEYED input before the keyless mutations (correct ordering — it needs pubkeys to render).

**Round-trip.** A general template emitted by P2 carries real origins → DECODES (`validate_explicit_origin_required` satisfied, pinned by `general_policy_template_md1_decodes_with_carried_origins` + the C1 negative pin) AND completes (P3 builds fresh, ignoring the carried). Canonical elides → decodes (re-derived) → completes.

**Restore routing** `is_multisig_template = !d.is_wallet_policy() && d.n >= 2` (restore.rs:312) routes ANY keyless n≥2 md1 to completion — a SUPERSET of what emit produces. The asymmetry is in the SAFE direction: a hand-crafted unsupported keyless shape (tr(sortedmulti_a)/hardened) reaching restore fails safe in every evaluator branch — the id evaluator's `compute_wallet_policy_id` → `Err(_) => false`; the address evaluator's `candidate_descriptor_string`(→`faithful_multisig_descriptor`→`to_miniscript`, which errors on root `SortedMultiA`) is `let Ok(..) else { return false }` (restore.rs:1755) — so no permutation matches → `SearchOutcome::None` → refuse (exit 4), never silent completion. There is no shape where emit produces something restore silently mis-completes, nor vice-versa.

## 4. SemVer + version-site + manual — CORRECT

**SemVer-MINOR justified (purely additive).** New flags only; no existing flag/wire/exit-code removed or repurposed. The one subtlety: `restore --account` changed type `u32 → Vec<u32>` (`#[arg(long, value_delimiter = ',', default_value = "0")]`). This is **backward-compatible**: `--account 0` parses to `[0]`, and every single-sig consuming site reads `account_primary()` (restore.rs:243 = `self.account.first().copied().unwrap_or(0)`), identical to the prior scalar; only the multisig completion path (restore.rs:1367) reads the full list. No behavior change for any pre-existing invocation.

**All 7 version sites at 0.60.0:** `crates/mnemonic-toolkit/Cargo.toml:3`, root `README.md:13`, crate `README.md:9`, `scripts/install.sh:32` (`mnemonic-toolkit-v0.60.0`), `Cargo.lock:727`, `fuzz/Cargo.lock:575`, `CHANGELOG.md:9` ([0.60.0] entry). `git grep -n '0\.59\.1' -- ':!design/' ':!*recon*' ':!CHANGELOG.md'` returns exactly ONE hit — a prose "since v0.59.1" provenance marker in the manual (`docs/manual/src/40-cli-reference/41-mnemonic.md:78`), NOT a version-site. The binary self-reports `mnemonic 0.60.0`.

**Manual documents EVERY new flag.** `make -C docs/manual lint` (bidirectional flag-coverage, clap `--help` ↔ manual) is GREEN. Confirmed by content: restore's `--cosigner`/`--search-address`/`--search-addr-min`/`--search-addr-max`/`--search-chain`/`--accept-search-time`/`--own-account-max` and verify-bundle's `--from`/`--cosigner`/`--search-*`/`--accept-search-time` all appear in `41-mnemonic.md` (the verify-bundle flag table rows 678-684 + the restore prose 209-247). The CHANGELOG [0.60.0] entry accurately describes the feature, the conditional-origin emit, the three completion modes, the funds-safety floors, the `--own-account-max` deferral, and the no-codec-bump rationale.

## 5. The two P6.1 deviations — CORRECTLY dispositioned

**(5a) `lint_argv_secret_flags.rs` FROM_ROUTES `verify-bundle --from` — a REAL stale-registry fix, NOT papering-over.** I reproduced it LIVE: a throwaway test driving the real binary with `verify-bundle --md1 … --mk1 <stubs> --from phrase=<SEED> --account 0 --cosigner <mk1>` emitted to stderr: `warning: secret material on argv (--from phrase=) — pipe via --from phrase=- to avoid /proc/$PID/cmdline exposure`, then refused with the no-mode `ModeViolation`. So verify-bundle's `--from` genuinely shares restore's `resolve_template_completion_seed` (restore.rs:1241), which emits the argv-leak advisory (:1269-1275, sentinel-excluded for `=-`/`@env:`), handles `=-` stdin (:1252, :1299) and `@env:` (:1302), and gates `--passphrase-stdin` coexistence (:1262). The registry route's `evidence: &["resolve_template_completion_seed", "=-"]` OR-list is satisfied by the `resolve_template_completion_seed` token present in `verify_bundle.rs` (the test's `every_route_has_nonargv_channel_evidence` passes). The fix reflects real production behavior.

**(5b) `fuzz-build-broken-unrestorable-advisory-references-bin-only-cmd` — GENUINELY pre-existing, shipping-with-latent acceptable.** I confirmed: (i) `unrestorable_advisory.rs:116-117` references `crate::cmd::restore::{taproot_override_card, restorable_taproot_override_card}` IDENTICALLY on `origin/master` (the `crate::cmd::` ref count is **2 on both HEAD and master**); (ii) this cycle touches NEITHER `unrestorable_advisory.rs` NOR the `cfg(fuzzing)` mount in `lib.rs` (the only lib.rs change is the unconditional `pub mod permutation_search` mount + a doc comment — no `cmd::` ref, not fuzzing-gated); (iii) `RUSTFLAGS=--cfg fuzzing cargo build -p mnemonic-toolkit --lib` fails with exactly E0433 ×2 (`could not find cmd in the crate root`) — the same break as v0.59.1/v0.59.0. The fuzz `Cargo.lock` version-site IS synced to 0.60.0. The real ship gates (full test suite, clippy -D warnings, manual lint, version-site consistency) are GREEN; only the instrumented `--cfg fuzzing` build is broken, identically to the prior two releases. Shipping with it latent is acceptable; the FOLLOWUP (preferred fix (a): move the predicates to a lib-crate module) + the "add a CI fuzz-build step" note are filed accurately.

## 6. New-clap-surface lockstep debt — RECORDED, not silently owed

`mnemonic gui-schema` emits all new flags: restore gets `--cosigner`/`--search-address`/`--search-addr-min`/`--search-addr-max`/`--search-chain`/`--accept-search-time`/`--own-account-max`/`--from`/`--expect-wallet-id`; verify-bundle gets the same minus `--own-account-max` (correctly restore-only). The CHANGELOG [0.60.0] Notes record "New clap flags on `restore` + `verify-bundle` → GUI schema-mirror + manual updated in lockstep this cycle," and the P4 review enumerated the exact P6 schema-mirror list. No flag is unaccounted-for. (NOTE for the orchestrator's post-tag GUI paired PR — see Minor m-1: the gui-schema reports `restore --account` as scalar `kind:"number"` while the clap type is now multi-value `Vec<u32>`; per CLAUDE.md the `schema_mirror` gate is flag-NAME parity, not value/wire-shape, so this is paired-PR value-shape discipline, not a gate break — but the GUI's `--account` handling should learn the comma-list form.)

## 7. Regression + clean build + panic-scan — clean

Full suite 3234 passed / 0 failed; clippy `-D warnings` exit 0; manual lint OK; `mlock.rs` untouched (g6). **Panic-scan of the new funds-safety production code:** every `.unwrap()`/`.expect()`/index on the completion path is guarded —
- `permutation_search.rs`: `search()` `?`-propagates `factorial(n)`/`checked_mul` overflow → `SearchSpaceTooLarge` BEFORE any `unrank_permutation` call, so `unrank_permutation`'s internal `.expect()` (:499) is unreachable for the validated small n; `matches.lock().unwrap()`/`into_inner().unwrap()` (:631/:637) only panic on lock poisoning (a thread panic already in flight, which propagates out of `thread::scope`).
- `restore.rs` completion core: `expect_wallet_id.as_deref().unwrap()` (:1698) / `search_address.as_deref().unwrap()` (:1723) guarded by `id_search`/`addr_search`; `pool[pi]` (:1685) over an engine assignment that is a permutation of `0..n` with `pool.len()==n`; `own_keys[0]` (:1985) guarded by `own_keys.len()!=1` return; `slots[empty[0]]` (:2004) / `s.unwrap()` (:2006) guarded by `empty.len()!=1` + full slot-fill.
- The address-path helpers (`candidate_descriptor_string`, `faithful_multisig_descriptor`, `script_pubkey_at`, `address_to_script_pubkey`) all return `Result` and are consumed with `let Ok(..) else { return false }` / `?`. No `.unwrap()`/`panic!` on card/seed/search-controlled input.

## 8. Process-gap notes

The per-phase reviews ran targeted `--test` targets; the full `-p` suite caught the stale `lint_argv_secret_flags` registry only at P6.1. I ran the WHOLE suite (3234 tests) + the manual lint + a live CLI reproduction + the `--cfg fuzzing` build — surfaces no per-phase review individually exercised. No additional untested-by-review surface found: the gate-relevant clippy is `--tests --bins --lib` (the `--all-targets` RED was only untracked scratch benches, now correctly `.gitignore`d so CI's tracked-surface clippy is clean). The `synthesize::tests` compile into the `--bins` target (a pre-existing crate quirk, documented in the P2 review) — they DO run under the default `cargo test -p`, which I ran. The one residual surface NOT gated in CI is the `--cfg fuzzing` build (deviation 5b) — pre-existing, FOLLOWUP filed with a "add a CI fuzz-build step" note.

---

## Findings

- **Critical:** none.
- **Important:** none.
- **Minor (non-blocking, do not gate the tag):**
  - **m-1 (lockstep value-shape, for the post-tag GUI paired PR):** `gui-schema` reports `restore --account` as scalar `kind:"number"` though the clap type is now multi-value `Vec<u32>` (comma-delimited). The `schema_mirror` gate is flag-NAME parity (passes), so this is not a gate break — but the GUI's `--account` consumer should learn the `0,1,2,3` comma-list form when the paired schema-mirror PR lands. Already within the recorded lockstep debt (§6); calling it out so the value-shape isn't missed.
  - **m-2 (carried from P3a/P3b, untracked scratch):** `cargo clippy --all-targets` is RED from the two untracked `examples/*_bench.rs` calibration scratch files (`format_collect`). They are `.gitignore`d (so CI's tracked-surface clippy is clean) but cargo still compiles them under `--all-targets`. Delete them or formalize as `#[ignore]` benches (plan §9) in a future cycle. No code-under-review change.
  - **m-3 (informational, from P5):** the gated `bitcoind_template_completion_differential` eprintln hardcodes a `"v27.0"` label while the opportunistic run used Satellite v0.2.4 (Core v25 fork). Cosmetic label, not a version check; `deriveaddresses` output is version-stable for these shapes; the CI gate is the unconditional anti-vacuity leg. Acceptable.

## Gate decision

**GREEN — 0 Critical, 0 Important.** The whole-diff funds-safety posture is sound: the independent-oracle equality holds for canonical AND general shapes; C1 is structurally airtight both ways; per-slot origins are built fresh from supplied keys with `compute_default_origin_path` provably off the completion path; every refuse direction is loud (exit≠0). The P4 refactor preserved restore's floors byte-for-byte and gave verify a byte-equal engine. Emit and completion cover a consistent shape set with the gap in the safe (refuse) direction. SemVer-MINOR is justified and backward-compatible; all 7 version sites are at 0.60.0; the manual flag-coverage gate is GREEN; the CHANGELOG is accurate; both P6.1 deviations are correctly dispositioned (one reproduced live, one confirmed pre-existing); the lockstep debt is recorded. Full suite 3234/0/14, clippy `-D warnings` clean, manual lint OK, mlock untouched, no new error variant. **The hard gate is satisfied — the toolkit may be tagged + shipped v0.60.0.** The post-tag step is the GUI schema-mirror paired PR (the flags are enumerated in §6).
