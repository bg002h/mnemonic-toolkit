# R0 review — `SPEC_md1_bip_alignment_and_code_honesty.md` (round 1)

**Reviewer:** Fable, adversarial, read-only. Persisted verbatim per CLAUDE.md. Ground truth verified against `descriptor-mnemonic` @ `origin/main ef1f3e71` (tree clean, `md 0.11.3` release binary re-run live), the Fable BIP review, and the BUGLIST.
**Dispatched:** 2026-07-10 (MD SPEC, R0 round 1).

**What checks out (verified, not taken on faith):** A1/A2/A3 repros reproduce live exactly as claimed (`md1yqpqqxpsq258xsks3kh0ye` self-rejects; chunked string → `got 9, expected 4` while `md repair` round-trips; `--force-long-code` output byte-identical, `let _ = args.force_long_code` at `crates/md-cli/src/cmd/encode.rs:109-113`). `canonical_origin()` table as cited (`canonical_origin.rs:45-78`); decode gate no-ops when `Some` (`validate.rs:219-224`) so **existing explicit sh(wpkh) cards keep decoding** — F-A1 is wire-safe; there is no encoder collapse of explicit==canonical (`encode.rs:119` writes `path_decl` verbatim), so no existing card's bytes change. F-A2's dispatch is **strictly additive**: the usable version set {4,8,12} is all-even, so every currently-valid single-payload string has first-symbol LSB=0 (`header.rs:1-10,27`; `chunk.rs:4-6`), and `reassemble` accepts a 1-element slice (`chunk.rs:311-357`, chunked-of-1 shipped v0.34.0, live-verified via `repair`). A stray chunk-i-of-N through the dispatch yields the correct `ChunkSetIncomplete`, and no recursion cycle exists (`reassemble` calls `decode_payload`, not `decode_md1_string`). F-A8's rollback confirmed value-blind (`tlv.rs:286-302`); the reference encoder always zero-pads (BitWriter + `wrap_payload`), so the zero-check rejects no well-formed card. md-codec `Error` confirmed insertion-ordered (`error.rs:20`ff). `bch.rs:19-31` comment confirmed wrong about BIP-93 (init IS `0x23181b3`). Corpus = four files/vector (`cmd/vectors.rs:39-73`) matching B-I6. The DG ledger is accurate: `md repair` genuinely does BM+Forney substitution correction, t=4 (`chunk.rs:536-601`), so "keep substitution-correction normative" is honest. Completeness vs the BIP review: every md1 C1-C4/I1-I9/M1-M8 item is mapped; DG-1…DG-4 ledgered, DG-5 correctly dissolved by implementing A8.

## CRITICAL

**C-1. The self-rejecting-card defect is a CLASS; F-A1 fixes one instance of it and P4 then writes the dishonesty into the BIP.** Live-verified at `ef1f3e71`: `md encode` with no `--path` exits 0 and prints an engravable card for `tr(@0/<0;1>/*,pk(@1/<0;1>/*))` → `md1yppqqxqj2s4dk6hk0wrt5n3`, `wsh(or_d(pk(@0…),pk(@1…)))` → `md1yppqqxpxpg2srvh08vnhjktj2`, and `sh(sortedmulti(2,…))` → `md1yppqqxp3cg2x3r70ckk4kjaf` — **all three decode-reject** with `non-canonical wrapper requires explicit origin`. This is every `canonical_origin=None` shape (`canonical_origin.rs:56,74,77`), exactly what the stale MANIFEST comment (`test_vectors.rs:36-40`) already admits for `tr_with_leaf`. Concrete failure: a user engraves a taproot-with-tree or miniscript backup today, discovers at recovery time the reference's own decoder refuses it — the same funds/usability class the SPEC itself assigns to A1. Worse: B-C2 (SPEC line 62) will make "depth-0 with no table entry ⇒ decoder MUST reject" *normative* while the shipped encoder keeps minting that rejected form — the precise BIP-vs-code dishonesty this cycle exists to end. These shapes have no standard canonical path, so the sh(wpkh)-style table fix can't cover them; BUGLIST A1 option (b) already names the right fix. **Fix:** add **F-A1b — encode-side fail-closed gate** mirroring `validate_explicit_origin_required` (refuse emit when the wrapper is non-canonical and any `@N` lacks an explicit origin; error text points at `--path`), at the same layer as the encoder's other validators (`encode_md1_string`/`split`, leaving `encode_payload` as the ungated primitive for negative-test construction). Recovery-safety: encode-only, no wire change, no previously-*decodable* card affected. RED-first per-shape refusal tests + with-`--path` success tests; B-C2 gains the encoder-side MUST. Note the interaction with F-A4: the `sh(sortedmulti)` advisory test must pass `--path` or it hits the new refusal instead.

## IMPORTANT

**I-1. F-A1's stated symmetry mechanism is factually wrong and its consumer set is incomplete — the fix works, but for a different reason than the SPEC says, and one invariant is left untested.** `identity.rs:216` is not "encode": it is `compute_wallet_policy_id`'s L14 canonical-fill (`identity.rs:176-220`). The wire encoder never consults `canonical_origin` (`encode.rs:119` writes `path_decl` verbatim; md-cli leaves it empty absent `--path`). Round-trip symmetry arises solely because the decode gate (`validate.rs:222`) becomes a no-op. There is a **third** consumer the SPEC omits: `canonicalize.rs:464-469` (`expand_per_at_n`'s `MissingExplicitOrigin` gate) also flips. Consequence to pin: the L14 "stable across origin-elision" invariant now activates for sh(wpkh) — the **WalletPolicyId (12-word anchor) of an elided card must equal the explicit-49′ form's**, and `compute_wallet_policy_id` on elided sh(wpkh) flips from error to success. Neither is in F-A1's test list. **Fix:** correct the cite, enumerate all three consumers, add the policy-id-equality test (elided == explicit-49′) and the previously-erroring-now-succeeds pin.

**I-2. Toolkit ripple for F-A1 is understated — a documented "mirror" invariant breaks by design, and naively restoring it would reverse a pinned funds-path refusal.** `mnemonic-toolkit/crates/mnemonic-toolkit/src/synthesize.rs:349-368` (`cli_template_from_tree`) documents itself as "Mirrors the shape dispatch of `md_codec::canonical_origin::canonical_origin`" and lists "bip49 `sh(wpkh)`" in the None class; `template_admissible` (:1092-1112) deliberately pins bip49 REFUSED (phase-1 R0 pins); mutation-3 (:1141+) elides origins on the `canonical_origin` verdict. After the re-pin, `canonical_origin(sh(wpkh))=Some` while the toolkit table intentionally excludes it — no runtime flip (the admission gate fires before mutation-3), but the "mirrors" claim becomes false on a funds-path dispatch, and an implementer "fixing the mirror" by adding sh(wpkh)→Bip49 would reverse the pinned refusal. **Fix:** the SPEC's toolkit-re-pin bullet must name this site explicitly: update the synthesize.rs invariant comments to state the now-intentional divergence, KEEP the bip49 template refusal pinned, and mirror a FOLLOWUP entry in both repos per the cross-repo rule.

**I-3. F-V3's NUMS-taproot vector is not expressible in the current corpus machinery and is never scheduled.** A meaningful `is_nums=1` vector is `tr(NUMS_H,{…})` → `tree: Some` → `canonical_origin=None` → needs an explicit origin — but `Vector` (`test_vectors.rs:13-32`) has no path field and the `md vectors --out` runner (`cmd/vectors.rs:30-38`) supplies none; the only path-free `is_nums=1` form (keypath-only NUMS, no tree) is an unspendable degenerate with zero placeholders. The phasing (line 104) carries only the sh_wpkh half of F-V3 (P0); the NUMS vector appears in no phase. **Fix:** specify the mechanism — add `path: Option<&'static str>` to the `#[non_exhaustive]` `Vector` + runner support (which also enables un-omitting `tr_with_leaf`) — assign the NUMS vector to a phase, and ripple the new field into B-I6's BIP file-format description.

**I-4. Phasing order defect: P4 (BIP text incl. the B-I6 §Test Vectors rewrite) precedes P5 (F-V2 + corpus regen), so the BIP's vector table is finalized against a corpus that then changes** — intra-cycle drift in the exact artifact the cycle exists to lockstep. **Fix:** move F-V2 (and the NUMS vector) ahead of P4, or add an explicit P5 step "re-sync BIP §Test Vectors to the final regenerated corpus" and require acceptance #4's independent-reader check to run against the final corpus.

## MINOR

- **M-1.** F-A3's removal-option ripple overstates GUI impact: `schema_mirror` mirrors only the toolkit's `mnemonic gui-schema` surface (per toolkit CLAUDE.md); removing an **md-cli** flag has manual-only ripple. Doesn't change the ruling below; correct the claim.
- **M-2.** F-A5 must rewrite the whole `bch.rs:19-31` narrative block — the "deliberately NOT codex32/BIP-93's initial residue `1`" sentence AND the "Only `ms1` must use `1`" claim — or the comment stays self-contradicting; keep byte-consistent with the mk-codec A6 companion fix.
- **M-3.** F-A4: `cmd/encode.rs` has an early-return `--json` branch (:51-82); the advisory must fire on both branches (precedent: `emit_output_class_advisory` is called in both) — add a `--json` stderr test.
- **M-4.** md's `TooManyErrors` user-facing text "more than 8 errors" (`error.rs:421-427`) conflates detection radius 2t=8 with correction capacity t=4 — the same myth class the cycle fixes on the mk side (A6/mk1-I2) but leaves on md. Fix the message (or at minimum, forbid the BIP's kept-normative correction claims from citing it).
- **M-5.** Acceptance #3's "except sh_wpkh gaining a canonical elided form" — sh_wpkh is currently *absent* from MANIFEST, so it is an addition, not a change; phrase precisely so the corpus-diff gate stays mechanical.

## Rulings on the deferred decisions (axis 6)

- **A3 — RULING: hard error, keep the flag in clap** (SPEC's preference confirmed). Honest, minimal ripple (no flag-name change → manual flag-coverage lint stays green; help-text update ripples to the manual chapter, already covered). The long code is excised from the BIP (DG-4), so a flag referencing a nonexistent mode must not exit 0; matches the constellation's "refusals exit≠0" precedent. Removal buys nothing now and belongs to a future surface-cleanup cycle.
- **A4 pkh — RULING: silent, no note.** pkh is funds-safe, has a canonical BIP44 default in the table, ships in the corpus, and the toolkit itself emits bip44. A note would fire on every legitimate encode, churn manual transcripts, and dilute the sh(multi) footgun warning's signal. B-C4 documents pkh as fully supported.
- **B-I8 — RULING: encoder-MUST minimal + decoder-lenient, documented** (SPEC's preference confirmed). A decode-side minimality check would violate this cycle's own recovery-safety invariant (a previously-decodable non-minimal single-string wire flips to reject) for zero real-card benefit (the reference always emits minimal). The BIP should additionally state the consequence that non-minimal **chunked** wires already fail closed via the CSI mismatch.

## Verdict

The core recovery-safety claims all verified (F-A1 wire-safe, F-A2 strictly additive, F-A8 rejects no well-formed card), the BIP-review coverage is complete, and the DG ledger is honest — but the cycle cannot claim "code honesty" while `md encode` keeps minting dead cards for the whole non-canonical class (C-1), and four Important gaps (consumer-set/tests, toolkit mirror trap, NUMS-vector mechanism, P4/P5 ordering) need folding.

**VERDICT: NOT GREEN — 1 Critical / 4 Important open** (5 Minor). Fold and re-dispatch.
