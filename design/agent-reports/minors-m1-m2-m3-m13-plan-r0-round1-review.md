# R0 round-1 architect review — PLAN_minors_m1_m2_m3_m13 (2026-06-10)

Reviewer: Fable 5 architect agent (R0, pre-implementation). Plan @ design/PLAN_minors_m1_m2_m3_m13.md, source b822fb9. Verdict: RED (0 Critical / 4 Important / 7 minor). Review verbatim below.

---

## Critical

None.

## Important

**I-1 — M1 Multi-branch check strength: the recon's slot-exact recommendation was silently dropped; plan ships the weaker order-agnostic membership with no recorded decision.**
Plan Part 1 says "assert `xpub_to_65(&card_xpub)` is a member of `desc.tlv.pubkeys`" for BOTH branches. The recon (cycle-prep-recon-minors-m1-m2-m3.md:24) explicitly handed the spec a stronger option: "Self-check sees emission ORDER, so the Multi assert can be slot-exact (`pubkeys` contains `(i as u8, xpub_to_65(&decoded_cards[i].xpub))`)". I verified the slot-exact premise holds: every `self_check_bundle` call site (`cmd/bundle.rs:435,1643,1690,1946`) reaches it via `synthesize_descriptor`/`synthesize_unified`, which build `tlv.pubkeys` and the per-cosigner mk1 cards from the SAME slots vector in the same order (`synthesize.rs:590-607,816-833`; import-json re-synthesizes too — `bundle.rs:1714-1716` step 6, md1 is NOT passed through verbatim). Order-agnostic membership cannot catch a cross-slot mis-emission (cards swapped between cosigner slots pass membership AND the existing stub-equality check at :2126-2141). Fix: amend Part 1 to slot-exact `(i as u8, …)` matching in Multi (Single = `(0, …)`), or record an explicit rationale for membership-only. As written, the implementer ships the weaker check unexamined.

**I-2 — M13 deletion makes `cli_self_check.rs:5` actively false; the recon-recommended module-doc fix is missing from Part 4.**
`crates/mnemonic-toolkit/tests/cli_self_check.rs:5` reads: ``Fixtures live at `tests/vectors/v0_2/{bip84,wsh-sortedmulti}-mainnet-0-false-true.txt` `` — and `wsh-sortedmulti-mainnet-0-false-true.txt` is IN the 25-file delete set. Today the doc merely over-claims (the `[obs] self-check-header-claims-unread-vector` finding); after Part 4 it references a file that does not exist. The recon's own scope recommendation (recon:84) included "fix the `cli_self_check.rs:5` module-doc over-claim (sweeps the `[obs]` line for free)"; the plan dropped it. Fix: add the one-line doc correction to Part 4 and mark the `[obs]` index line resolved alongside.

**I-3 — M3 has real manual-prose impact; the plan's "no manual impact" claim resolves the recon's caveat the wrong way.**
The recon (line 88) said: "check `docs/manual` only if it enumerates stderr advisories per command." It does. `docs/manual/src/40-cli-reference/41-mnemonic.md:3109` is an advisory table row inside the `mnemonic inspect` section (`## mnemonic inspect` at :3001), and the manual quotes the exact secret-argv advisory line verbatim for other commands (:426-428, :1378, :1501 — the last one even specifies "per-occurrence" semantics). Adding a new stderr advisory to inspect/repair without touching the manual leaves the documented advisory surface stale — the manual is the constellation's single source of truth for the end-user surface (CLAUDE.md). Fix: Phase 3 adds the secret-argv advisory row/prose to the inspect (:3001+) and repair (:2731+) sections, and runs the FULL manual lint (not just flag-coverage — the v0.50.0 cspell ship-detour lesson). The flag-coverage lint is indeed unaffected (no clap change) — that part of the plan is correct.

**I-4 — FOLLOWUPS bookkeeping: plan contradicts the backlog entry's own convention, and undercounts the index lines (4, not 3).**
(a) The `audit-2026-06-10-backlog` entry instruction (`design/FOLLOWUPS.md:13`) reads: "Promote any line to its own `### <id>` entry when worked; resolve here as fixed." Precedent followed it: I3 was promoted + resolved at v0.53.1 (`FOLLOWUPS.md:16`). The plan's "promote nothing — minors stay index-line-resolved with a pointer to this plan" (Phase 3) is a deviation from the recorded convention and from recon point 7, with no justification. Either promote the 3 minors per convention, or amend the plan with an explicit recorded rationale for the lighter-weight path. (b) Phase 3 says "mark the 3 index lines resolved" — there are FOUR affected index lines: `self-check-no-mk1-xpub-binding`, `localize-broad-error-collapse`, `inspect-repair-no-argv-advisory`, AND `orphaned-v0_2-multisig-goldens` (M13's own audit slug, verified present in the index), which must be marked resolved-by-reference to the `### orphaned-v0_2-md1-vectors-no-harness` resolution.

## Minor

**m-1 — `resolve_groups` signature change unstated; advisory must fire on RAW pre-expansion values.** Current signature is `resolve_groups<R: Read>(args, subcmd_name, stdin, relax_hrp_for_indel)` (`src/repair.rs:262`) — no stderr writer. Both callers have `stderr: &mut E` in scope (`cmd/inspect.rs:88-105`, `cmd/repair.rs:101-111`), so threading is mechanical, but the plan should say so. Also pin that the advisory inspects the raw flag values + positionals BEFORE the `-` expansion at :339-341 (post-expansion, stdin-supplied chunks are indistinguishable from inline → false-fire on `--ms1 -`). The provenance is available pre-expansion; just make it explicit.

**m-2 — Indel-relaxation edge under-covered by "classifies as ms1 (HRP probe)".** Under `repair --max-indel ≥ 1` the strict typed-flag HRP gate is skipped (`repair.rs:285`) precisely so a corrupted-prefix ms1 (`s10…`) can flow through — that value will NOT HRP-classify as ms1, so a probe-gated advisory silently skips secret material that IS on argv. Fire unconditionally on a non-`-` `--ms1` flag value (the flag declares the kind); HRP-probe only the positionals.

**m-3 — Pin the advisory `flag` string for positional ms1 and reconcile "ONCE per invocation" with the per-occurrence precedent.** `secret_in_argv_warning(stderr, flag, alternative)` (`secret_advisory.rs:40-45`) needs a flag label for the positional case (e.g. `positional ms1`); the manual's existing wording for multi-source commands is "per-occurrence" (:1501). Spec should fix both before impl.

**m-4 — Optional Route-row evidence hardening dropped.** Recon (line 50) suggested appending `"secret_in_argv_warning"` to the inspect/repair rows' evidence at `tests/lint_argv_secret_flags.rs:92-93` once the call lands in `src/repair.rs`. Cheap, makes the rows discriminating; plan dropped it silently. Adopt or note as out-of-scope.

**m-5 — M1 test mechanics: bin-crate location + guard cells.** The v0.47.4 precedent lives in `mod self_check_ms1_tests` inside `cmd/bundle.rs:2300` (BIN crate) — the Phase 1 red-run must use `cargo test --bin mnemonic` (the Cycle B lesson; `--lib` runs zero of these). `mk_codec::encode_with_chunk_set_id(&card, csi)` is exposed (7 toolkit call sites in `synthesize.rs`) so the synthesize-then-mutate RED is feasible as planned. Add an explicit green guard cell (or cite the existing `cli_self_check.rs` happy-path + wif-slot tests as the guards) per the v0.47.4 G-A/G-B discriminating-guard lesson. No false-fail risk found: `pubkeys: None` has zero emission sites (grep-verified), import-json re-synthesizes md1, and `privacy_preserving` strips only fingerprints.

**m-6 — "README markers ×2" is correct but ambiguous.** It's one marker in each of TWO files — root `README.md:13` + `crates/mnemonic-toolkit/README.md` (verified via the v0.53.1 release commit `ee77905` diffstat); root README has exactly one `toolkit-version` marker. Spell out the paths so the implementer doesn't hunt for two markers in one file.

**m-7 — M2 verified clean; one test-placement note.** Pin (`Cargo.lock`: miniscript `13.0.0` @ git rev `95fdd1c…`), `NonTopLevel` already matched at `gate.rs:415` (compiles today → variant exists), sole caller `gate.rs:353` with `unwrap_or_else(|| "root")` confirmed, existing sanity-localization tests route non-B subtrees only into the NonTopLevel case (debug_assert won't fire on the current suite; Phase 2's full-workspace run is the backstop). The new cell goes in gate.rs's existing `#[cfg(test)]` module (`:783+`) since `localize` is private — worth one line in the plan.

## Verdict

**NOT GREEN — 0 Critical / 4 Important.** The core directions are sound and recon-verified (M1 reuse surface exact; M3's `resolve_groups` chokepoint is the sole intake for both subcommands with provenance available pre-expansion and no exact/empty-stderr assertions anywhere in `tests/` — the highest-risk claim checks out; M2's debug_assert variant is the right reading of the no-op ambiguity; M13's 26/1 counts, `FOLLOWUPS.md:157` entry, and absence of any directory-enumerating test all verify). Fold I-1 (slot-exact decision), I-2 (cli_self_check.rs:5 doc fix into Part 4), I-3 (manual advisory prose + full manual lint in Phase 3), and I-4 (FOLLOWUPS promote-or-justify + 4th index line), then re-dispatch.
