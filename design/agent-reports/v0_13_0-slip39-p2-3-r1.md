# v0.13.0 P2.3 GREEN R1 LOCK review — `e68a042`

**Phase:** v0.13.0 P2.3 (manual lint mirror — minimum-viable `41-mnemonic.md` stub for `mnemonic slip39 {split,combine}`; post-GREEN LOCK)
**Round:** R1, round 1
**Reviewer:** Opus (general-purpose, self-persisting per memory `feedback_opus_primary_review_agent`)
**Commits under review:**
- `c9847dd` (RED — `docs/manual/tests/cli-subcommands.list` adds `mnemonic slip39 split` + `mnemonic slip39 combine`; demonstrates `lint.sh` 4/6 `flag-coverage` FAIL on the 3 strictly-novel-to-slip39 flags `--group`, `--group-threshold`, `--iteration-exponent`)
- `e68a042` (GREEN — `## mnemonic slip39` H2 added between `## mnemonic seed-xor` and `## mnemonic gui-schema`; chapter intro "Eight"→"Nine" + cross-link + mirror v0.12.0→v0.13.0)

**Baseline:** `d40eb0c` (P2.2 R1 LOCK round 1, 0C/0I/3N/0n clean LOCK + N-1/N-2 SPEC mini-folds inline)
**Date:** 2026-05-14

## Verdict

**0 Critical / 0 Important / 0 Nice-to-have / 0 notes at confidence ≥ 80 — RECOMMEND LOCK. P3 (canonical chapter prose + Trezor smoke recipe) is unblocked.**

P2.3 is the smallest sub-phase of v0.13.0 SLIP-39 by design — a minimum-viable manual stub that satisfies the `flag-coverage` lint after the new `mnemonic slip39 {split,combine}` CLI surface landed at P2.2 GREEN (`19f00a5`). The stub matches plan §4.5 Q4 resolution exactly, plan §5 P2.3 row commit titles match verbatim, and all three plan §9 P2.3 LOCK criteria are met. The full lint passes `[lint] OK`; the cargo test baseline (978 / 0 / 8) is unchanged; clippy is clean.

## Verified clean against source + plan §4.5 + §5 P2.3 row + §9 P2.3 LOCK criteria

### 1. Commit graph — only the GREEN commit between RED and HEAD

`git log --oneline c9847dd..HEAD` returns a single line: `e68a042 docs(manual): v0.13.0 P2.3 GREEN — minimum-viable 41-mnemonic.md stub mentioning all slip39 flag strings (P3 fleshes out canonical chapter)`. No drift commits.

### 2. RED commit shape — exactly 2 lines added in the right position

`git show c9847dd -- docs/manual/tests/cli-subcommands.list` shows 2 additions only, between `mnemonic seed-xor combine` (line 17 in the new file) and `mnemonic gui-schema` (line 20):

```
mnemonic slip39 split
mnemonic slip39 combine
```

Alphabetical-within-block ordering preserved: `seed-xor split`, `seed-xor combine`, `slip39 split`, `slip39 combine`, then `gui-schema`. Matches plan §4.5 verbatim. RED commit body cites the 3 strictly-novel `--group` / `--group-threshold` / `--iteration-exponent` flags as the failing-lint witnesses, which is correct per the kickoff's RED expectation.

### 3. Chapter intro — "Nine subcommands" + slip39 cross-link + v0.13.0 mirror

`docs/manual/src/40-cli-reference/41-mnemonic.md:1-12` reads:

```
# `mnemonic` reference

The integration-layer CLI for the m-format constellation. Nine subcommands:
[`bundle`](#mnemonic-bundle), [`verify-bundle`](#mnemonic-verify-bundle),
[`convert`](#mnemonic-convert), [`export-wallet`](#mnemonic-export-wallet),
[`derive-child`](#mnemonic-derive-child), [`final-word`](#mnemonic-final-word),
[`seed-xor`](#mnemonic-seed-xor), [`slip39`](#mnemonic-slip39), and
[`gui-schema`](#mnemonic-gui-schema) (introspection only, no user-facing
semantics). Run any with `--help` for the latest flag set; this chapter
mirrors v0.13.0.
```

All three plan §9 P2.3 LOCK criterion #3 sub-asserts pass:
- "Eight" → "Nine" (line 3) ✓
- cross-link `[`slip39`](#mnemonic-slip39)` inserted between `seed-xor` and `gui-schema` (line 7) ✓
- mirror version `v0.12.0` → `v0.13.0` (line 10) ✓

### 4. New H2 placement matches list ordering and intro list ordering

`grep -n '^## ' docs/manual/src/40-cli-reference/41-mnemonic.md` shows:

```
14:## `mnemonic bundle`
54:## `mnemonic verify-bundle`
94:## `mnemonic convert`
136:## `mnemonic export-wallet`
181:## `mnemonic derive-child`
214:## `mnemonic final-word`
295:## `mnemonic seed-xor`
420:## `mnemonic slip39`
462:## `mnemonic gui-schema`
```

`slip39` H2 lands at line 420, between `seed-xor` (295) and `gui-schema` (462). Three-way consistent ordering across (a) chapter H2 sequence, (b) intro cross-link list, (c) `cli-subcommands.list`. ✓

### 5. Flag-coverage cross-check — every emitted flag present in chapter AND in slip39 H2

Live `--help` capture (against the just-built `target/debug/mnemonic` from the GREEN HEAD):

```
slip39 split:   --from --group --group-threshold --help --iteration-exponent
                --json-out --language --passphrase --passphrase-stdin
slip39 combine: --help --json-out --language --passphrase --passphrase-stdin
                --share --to
```

Union (11 distinct flags): `--from --group --group-threshold --help --iteration-exponent --json-out --language --passphrase --passphrase-stdin --share --to`.

`grep -F -c -- "<flag>"` against `41-mnemonic.md`: every flag has ≥ 1 chapter occurrence (the lint requirement). Counts: `--from` 17, `--group` 2, `--group-threshold` 1, `--help` 14, `--iteration-exponent` 1, `--json-out` 9, `--language` 15, `--passphrase` 14, `--passphrase-stdin` 6, `--share` 17, `--to` 4.

Slipping inside the slip39 H2 specifically (kickoff's narrative-completeness check — extracted via `awk '/^## .mnemonic slip39/,/^## .mnemonic gui-schema/'`): every flag has ≥ 1 occurrence in the H2 itself. Counts: `--from` 1, `--group` 2, `--group-threshold` 1, `--help` 4, `--iteration-exponent` 1, `--json-out` 2, `--language` 3, `--passphrase` 4, `--passphrase-stdin` 2, `--share` 2, `--to` 2.

The slip39-novel set per the kickoff (`--group`, `--group-threshold`, `--iteration-exponent`, `--share`, `--to`) is fully covered inside the slip39 H2 — not relying on substring inheritance from other subcommands' sections.

### 6. Full lint — `[lint] OK`

`make -C docs/manual lint MNEMONIC_BIN=$(realpath target/debug/mnemonic) MD_BIN=md MS_BIN=ms MK_BIN=mk` exits 0 and the final line is `[lint] OK`. All 6 steps pass:

```
[lint] === 1/6 markdownlint ===   Summary: 0 error(s)
[lint] === 2/6 cspell ===         Issues found: 0 in 0 files
[lint] === 3/6 lychee ===         91 OK / 0 Errors
[lint] === 4/6 flag-coverage ===  no FAILs (sibling-bin WARNs benign)
[lint] === 5/6 glossary-coverage ===
[lint] === 6/6 index bidirectional ===
[lint] OK
```

The 18 sibling-CLI WARNs (`md encode --help`, `ms encode --help`, `mk encode --help`, …) are pre-existing env quirks (those bins aren't on PATH in this dev environment); they predate P2.3 and are explicitly listed in the kickoff "Things to NOT flag". Plan §9 P2.3 LOCK criterion #2 satisfied.

### 7. Cargo test baseline preserved — 978 / 0 / 8

`cargo test --tests --no-fail-fast 2>&1 | awk '/test result:/ {ok+=$4; fail+=$6; ig+=$8} END {printf "%d / %d / %d\n", ok, fail, ig}'` returns `978 / 0 / 8` — bit-exact with the P2.2 R1 LOCK baseline at `d40eb0c`. P2.3 touched zero Rust source so this is the expected outcome; verified anyway.

### 8. Clippy clean

`cargo clippy --all-targets -- -D warnings` finishes with no output beyond `Finished `dev` profile [unoptimized + debuginfo] target(s) in 0.08s` — no warnings, no errors. (Same expectation as #7: zero Rust changes, but verified.)

### 9. No `\index{}` markers added — bidirectional check not triggered

`git show e68a042 -- docs/manual/src/40-cli-reference/41-mnemonic.md | grep -c '\\index{'` returns `0`. The stub correctly avoids `\index{}` markers — adding any would have required a matching entry in `docs/manual/src/60-appendices/69-index-table.md` per lint step 6/6. P3's canonical chapter is the appropriate place to add index entries (e.g. `\index{slip39!K-of-N share-splitter}`).

### 10. Anchor sanity — `mnemonic-slip39` matches the cross-link

The chapter intro cross-link is `[`slip39`](#mnemonic-slip39)`. Pandoc/mdBook strips backticks and lowercases-with-dashes, so `## ​`mnemonic slip39`` generates anchor `mnemonic-slip39`. Consistent with neighbor anchors `mnemonic-seed-xor` and `mnemonic-gui-schema`. The lychee step (3/6, 91 OK / 0 Errors) cross-validates the anchor resolves.

### 11. Plan + SPEC drift — none

- `design/PLAN_v0_13_0_p2.md` §4.5 prescribes "ADD 2 lines under the `# mnemonic` block, after `mnemonic seed-xor combine` and before `mnemonic gui-schema`" — RED commit matches verbatim.
- §5 P2.3 row prescribes the exact RED + GREEN commit titles — both commits use the prescribed titles verbatim.
- §9 P2.3 LOCK criteria are three bullets: (a) list contains the 2 slip39 lines (verified #2), (b) `make lint ... ` passes (verified #6), (c) chapter intro 8→9 + cross-link (verified #3). All three met.
- §7 Q4 resolution: "Minimum-viable manual stub at P2.3; canonical chapter at P3. `lint.sh:84` flag-coverage step does `grep -oE '--[a-z][a-z0-9-]+'` on `--help` then asserts each appears in the chapter — a 30-line stub listing each flag string is sufficient." The GREEN H2 is ~42 lines (intro paragraph + two flag tables); slightly above the §7 Q4 estimate of 30 lines but well within the spirit of "minimum-viable."
- GREEN commit body explicitly defers `worked example, JSON envelope schema, refusal taxonomy, advisory matrix, Trezor interop recipe` to P3 per plan §2.3 out-of-scope-at-P2 list — alignment confirmed.
- GREEN commit body references no SPEC patches — correct, since SPEC patches were P2.2 GREEN's job and there are no new P2.3 SPEC obligations.

## Findings

None at confidence ≥ 80.

## Notes (sub-threshold; carry-over candidates only)

None worth carrying forward. The intentional out-of-scope deferrals (worked example, JSON envelope, refusals taxonomy, advisory matrix, Trezor recipe, `\index{}` entries) all map cleanly to P3 work and need no separate carry-over tracking — they're already first-class agenda items in the P3 phase split.

## RECOMMEND LOCK

P3 (canonical `41-mnemonic.md` chapter prose + Trezor smoke-test recipe) and PE (release rollup + `mnemonic-toolkit-v0.13.0` tag) are unblocked.
