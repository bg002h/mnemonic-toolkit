# P1 per-phase R0 — round 1 — `examples-pdf-un-ci-gated` (P1: pin + prose + determinism + B-static + regen-proof)

- **Reviewer:** opus architect (adversarial P1 R0 gate; **all proofs independently re-run** — binary built from source + golden regenerated by the reviewer, NOT trusted from the implementer's report).
- **Change under review:** branch `feat/examples-pdf-modernize` @ **`0b494660`** (off master `b8067ebe`).
  `git diff b8067ebe..0b494660` = **ONLY** `.examples-build/gen.sh` (+83/−59) + `.examples-build/.gitignore` — confirmed via `git diff --name-only`. The golden `Examples.md` is intentionally NOT committed (P2 tracks it) — verified `git ls-files .examples-build/Examples.md` = empty.
- **Authority composed (not re-litigated):** plan §P1 (`IMPLEMENTATION_PLAN_examples_pdf_modernize_and_gate.md`), spec `SPEC_…`, spec-R0 + plan-R0 (both GREEN 0C/0I), OQ1 ruling.
- **Pinned binary:** `cargo build --bin mnemonic` at `0b494660` → `target/debug/mnemonic` reports **`mnemonic 0.75.0`** (crate `Cargo.toml` = 0.75.0), satisfying the `gen.sh:44` FATAL pin. (Build was a 0.14 s no-op — the P1 diff touches only docs, so no `crates/**` source changed since the binary was last built; cargo's input-hash check confirmed nothing stale. Functionally re-proven correct: its output is byte-identical to the implementer's golden AND to the committed PDF's funds tokens.)
- **Machine advantage exploited:** this box has `bitcoind`, `bitcoin-cli`, AND `mnemonic-depth2` on PATH (like the recon author) — enabling a *positive* sentinel + present-vs-absent determinism proof, not just an absence argument.

---

## VERDICT: GREEN — 0 Critical / 0 Important. **Advance to P2.**

Every load-bearing P1 claim was independently reproduced. Determinism is REAL (not asserted): double-regen, cross-env under `env -i`, and depth2/bitcoind-present all byte-identical, with a clean leak-grep. The funds surface is byte-stable versus the committed `docs/Examples.pdf` (identical fingerprint + checksum multisets, all xpubs/addresses/descriptors present). Both B-static freezes are faithful to LIVE 0.75.0 (§6.6 address triply-confirmed incl. a real `bitcoin-cli deriveaddresses` run; depth-2 mainline STILL refuses, exit 2). The sentinel never tripped. The preflight fails loud. The whole delta is ⊆ the spec §3 presentational enumeration. Findings are **Minor / observational only** — none blocks P2.

---

## 1. Determinism (THE load-bearing risk) — REAL. No residual flap vector found.

Built `mnemonic 0.75.0` from source; ran `gen.sh` via `EXAMPLES_BIN_DIR=…/target/debug`.

| Proof | Method | Result |
|---|---|---|
| **(i) Double-regen byte-identical** | two runs, identical env | `cmp A B` → **IDENTICAL** (both 103853 B) |
| **(ii) Cross-env byte-identical** | `env -i HOME=/tmp/other_home USER=someoneelse LOGNAME=someoneelse PATH=/usr/bin:/bin EXAMPLES_BIN_DIR=…`, cwd `/tmp/other_cwd` | `cmp A C` → **IDENTICAL**. `env -i` wipes ALL ambient env (locale/TZ/USER/HOME/cwd) → proves output is a **pure function of (repo tree, binary)**; the `REPO`/`install.sh`/env-scrub fixes fully eliminate path/user/locale/TZ leaks. |
| **(iii) depth2/bitcoind present-vs-absent** | regen with `mnemonic-depth2`+`bitcoind` reachable (real HOME, `~/.cargo/bin` on PATH) vs (ii)'s absent case | `cmp A D` → **IDENTICAL**; author-machine dependence removed by B-static. |
| **Implementer golden reproduced** | `cmp .examples-build/Examples.md <my regen>` | **IDENTICAL** byte-for-byte — the golden that becomes the CI reference is reviewer-reproducible. |

**Leak-grep on my independent regen — clean:**
- `/scratch` → none. `/home/bcg` (real author home) → none. word `bcg` (real `$USER`) → none. hostname `13764k` → none. `HH:MM:SS` timestamps (`$(date)` tell) → none.
- Only `/home/user` (the intended scrub, 7×) under `/home/`. Three surviving date strings, all accounted for and deterministic: `date: "2026-07-05"` (pinned static YAML), `Pointer current as of 2026-05-25` (baked into the 0.75.0 binary's `--help` btcrecover pointer — a fixed binary property, verified NOT `$(date)` since it shows a past date), and `recorded 2026-06-15` (the frozen depth2 STATIC-CAPTURE label).

**Conclusion:** the gate will not flap. Strongest part of the phase.

## 2. S1 funds-stability vs committed `docs/Examples.pdf` — CONFIRMED byte-stable. No derivation drift.

Extracted the committed PDF (`pdftotext`, commit `a0a43d03`, 215270 B) and compared funds-critical tokens to my independent 0.75.0 regen:

- **Fingerprint multiset IDENTICAL:** `28645006`×79, `73c5da0a`×113, `b8688df1`×99 (regen == PDF).
- **Checksum multiset IDENTICAL:** all 15 distinct `#xxxxxxxx` with equal counts — incl. `#4wup4at0`×2, `#8nz0lwja`, `#5trrgdg0` (depth2 static), `#mk8vdqmt` (§6.6), `#yjp7hj7w`×2, `#4ld0crxa`×4, …
- **All 5 `bc1` addresses present** in the de-wrapped PDF: `bc1p550…00n8c8`, `bc1p6yc7…twvn66`, `bc1p9stc…t3d2c`, `bc1q4g75…rp6qss`, `bc1qksse…84qtpl`.
- **All 30 distinct xpubs present** (forward 30/30, 0 missing; the 3 reverse "extras" are de-wrap artifacts — each is `<a regen xpub>`+`note` glued after whitespace removal, prefix-confirmed).
- **Load-bearing descriptors present:** `wsh(sortedmulti(2,`, `tr(50929b74…,sortedmulti_a(2,`, `tr([73c5da0a/84…`, and the checksums above.

**The only deltas vs the old PDF are presentational — exactly the spec §3 classes, direction-verified both ways:**
- **Cards twice→once:** PDF `printed twice`=1 → regen `printed once`=1, `printed twice`=0.
- **md1 hyphen→space:** PDF `md1fg-dxlpq-pqpm6-…` (39 hyphen-md1 lines) → regen `md1fg dxlpq pqpm6 …` (0 hyphen-md1 lines) — **same characters, separator only**.
- **`--help` +gen-man +word-card:** the PDF's 4 `gen-man` are the `install.sh --dry-run` lines (`… mnemonic gen-man --out …` ×4 CLIs), NOT the Commands block; regen adds the 5th (the new `--help` Commands row) + `word-card` (PDF 0 → regen 1).
- **Install pin `v0.73.3→v0.75.0`** (`mnemonic-toolkit-v…` ×3), path normalization `/home/bcg → /home/user`, and the §6.6 + Appendix-B restructures.

No address / xpub / descriptor / checksum / fingerprint changed anywhere. **S1 not tripped.**

## 3. B-static freezes faithful to LIVE 0.75.0 + still-refuses — CONFIRMED.

- **§6.6 `deriveaddresses` freeze faithful (triple-confirmed):** the frozen static address `bc1p550zvnachy40z6hh8llka93mkm0c3635samp264ck6rfd0dcdc8s00n8c8` equals (a) the LIVE 0.75.0 `mnemonic restore` first-recv in my regen (line 1273) and (b) an **independent `bitcoin-cli deriveaddresses`** run I performed on the exact frozen receive descriptor `tr(NUMS,sortedmulti_a(2,…/0/*…))#mk8vdqmt` `[0,0]` → returned `["bc1p550…00n8c8"]` byte-for-byte. `deriveaddresses` is a version-independent descriptor→address function; the frozen "recorded from Bitcoin Core v27.0" label is honest provenance. As plan-R0 noted, the kept LIVE `restore` re-derives this every CI run → any future drift reds the gate (doubly-safe).
- **Mainline depth-≥2 STILL refuses at 0.75.0 (standing proof does NOT invert):** independently reproduced the depth-2 card (`bundle tr4.desc → jq .md1[] → depth2.md1`) and ran `mnemonic restore … depth2.md1` → **EXIT 2**, `error: taproot tree depth ≥2 (≥3 leaves) is not yet restorable …`. The B-static framing (mainline refuses; the twin reconstructs) remains true. The kept-LIVE mainline refusal at gen.sh's `run` (regen line 1392-1393) is a legitimate per-CI-run assertion the cap still holds.
- **Frozen depth2 transcript labelled as a special-build artifact:** loud, honest label present — *"(STATIC CAPTURE — recorded 2026-06-15 from the experimental `mnemonic-depth2` build at v0.55.3. This block is NOT regenerated by `gen.sh` and is not reproducible with a released binary or in CI; … Everything else in this document is live-captured and CI-gated.)"* Descriptor `…#5trrgdg0` + `first recv: bc1p6yc7…` + 12-cosigner block + `UNVERIFIED:` + `note: stdout is watch-only` all present and frozen verbatim.

## 4. Sentinel — CONFIRMED: gen.sh NEVER invokes bitcoind / bitcoin-cli / mnemonic-depth2 in the regen path.

- **Runtime PATH-shim sentinel:** placed fail-loud wrappers (touch a marker + `exit 42`) for all three tools FIRST on PATH (with the real `~/.cargo/bin` still present) and regenerated → marker file **never created**, output byte-identical to baseline. Nothing invoked them.
- **Source grep:** `command -v (bitcoind|bitcoin-cli|mnemonic-depth2)` → **none** (runtime branches fully deleted). The `run`-line "bitcoin" hits are `mnemonic … --format bitcoin-core` (the toolkit's format flag, not the daemon); the bare `bitcoin-cli`/`mnemonic-depth2` textual hits are all inside `show '…'` blocks, comments, or the static heredoc — never executed.

## 5. The 11 pin sites + coupled prose — all correct; 3 residual `0.55.3` are legitimate history.

- 6 `0.75.0` in `gen.sh` (header, subtitle, intro, Appendix A, §6.3 attribution, Appendix-B framing) + `.gitignore` reworded (pin 0.75.0 + "Examples.md becomes a tracked CI golden"); FATAL pin `gen.sh:44` = `"mnemonic 0.75.0"`; `date:` static `2026-07-05`.
- **Residual `0.55.3` = exactly 3, all unambiguous history/experimental attributions:** `gen.sh:555` "the non-NUMS internal-key feature, **shipped in v0.55.3**"; `:731` "the twin below was built from **the 0.55.3-era experimental branch**"; `:765` static-capture label "build **at v0.55.3**". `v0.49.1` (`:689`) correctly left as history.
- **Coupled prose fixed:** the "printed twice" time-bomb → "printed once, grouped …" (`gen.sh:232`). Stale-prose sweep `grep -niE 'printed twice|once unbroken|unbroken.*grouped|hyphen'` = **none**. No other output-format prose left stale (matches plan-R0's `twice|unbroken|grouped|hyphen` = only that one site).

## 6. Preflight fails loud — CONFIRMED.

Hid `jq` (sanitized PATH with every tool except jq) and ran gen.sh → **EXIT 1**, stderr `FATAL: required tool 'jq' not found on PATH`, **stdout 0 bytes** (no document body emitted), and no `command not found` string anywhere. So a missing tool can NEVER be captured into the doc via `run`'s `eval … 2>&1`. The preflight loop (`gen.sh:37-40`) + version gate (`:43-44`) both precede the first emission (`cat <<'MD'` at `:106`).

## 7. Diff ⊆ §3 — CONFIRMED.

The whole baseline→golden delta falls within the spec §3 enumerated classes (funds-token identity in §2 above + presentational-tell direction, verified hunk-class by hunk-class). No unenumerated hunk; in particular no funds-critical token moved. Change scope: only `.gitignore` + `gen.sh`.

## 8. Pandoc render (gate vi) — CLEAN.

`pandoc <my regen> --include-in-header=preamble.tex --listings --pdf-engine=xelatex -f markdown-smart` → **EXIT 0**, empty stderr, 214535 B / **25-page** PDF.

---

## Findings by severity

**Critical:** none.
**Important:** none.

**Minor / observational (none blocking P2):**
- **m-1 (provenance label vs test harness — no action).** I verified §6.6 faithfulness with `bitcoin-cli` from "Bitcoin Satellite v0.2.4" (a Core-compatible derivative), while the frozen label reads "Bitcoin Core v27.0." `deriveaddresses` is a version-independent descriptor→address function and the address matched exactly; the label is honest provenance of the *original* recording, immaterial to correctness.
- **m-2 (`HOME=/home/user` is a nonexistent dir — already flagged plan-R0 M-3, re-confirmed harmless).** No executed tool touched `$HOME` in a flap-inducing way: the `env -i` cross-env regen (which also relocates HOME) is byte-identical to baseline, and no `$HOME` warning appeared in any regen's stderr. Fully mitigated; noted only.
- **m-3 (gate (iv) baseline is the PDF, not a baseline `.md` — stronger, not weaker).** The plan's P1 gate (iv) "diff fresh golden vs the Jun-26 baseline `Examples.md`" cannot be a literal md-vs-md diff because that gitignored baseline was overwritten by the P1 regen. I performed the equivalent against the committed `docs/Examples.pdf` text — a strictly stronger funds check (identical fingerprint + checksum multisets + all xpubs/addresses/descriptors), backed by the presentational-tell direction. P2's born-green author≠runner PR + the P3 whole-diff review remain the backstops.

## Cleanliness

Repo left clean: golden **not** committed (`git ls-files .examples-build/Examples.md` empty); no tracked changes under `.examples-build/` or `target/` from this session (all my regens went to an isolated scratchpad; gen.sh's `work/`+`preamble.tex` writes are gitignored transients). No built-binary clutter added (build was a no-op). Scratchpad PDFs/shims/bin-dirs deleted.

## Bottom line

**GREEN, 0C/0I — advance to P2.** Determinism is empirically airtight (double-regen + `env -i` cross-env + depth2/bitcoind-present, clean leak-grep, implementer golden reproduced); the funds surface is byte-stable vs the committed PDF (identical fingerprint/checksum multisets, all keys/addresses/descriptors); both B-static freezes are faithful to live 0.75.0 (§6.6 triple-confirmed incl. a real Core `deriveaddresses`; depth-2 mainline still refuses exit 2 with the loud experimental freeze labelled); the sentinel proves zero bitcoind/depth2 invocation; the preflight fails loud with no doc-body leak; the delta is ⊆ §3. All 11 pin sites correct with the 3 residual `0.55.3` being legitimate history. Proceed to P2 (gate + golden + born-green PR + negative red-CI proof).
