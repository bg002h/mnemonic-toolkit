> Reviewer: opus architect — P5 per-phase R0 EXECUTION review · `mnemonic-toolkit` `feature/own-account-subset-search` @ `b50b522f` (`test(subset-search): P5 over-supply subset-search property + bitcoind row`) · 2 files (tests-only): `tests/prop_subset_search_roundtrip.rs` (new, +668) + `tests/bitcoind_differential.rs` (+286). Contract: `design/IMPLEMENTATION_PLAN_own_account_subset_search_2026-06-20.md` P5 (L46-48) + `design/SPEC_own_account_subset_search_2026-06-20.md` §3/§7-P5/§9 + the P2/P3/P4 exec reviews (the over-supply subset-search is the NET-NEW surface). Maximally adversarial, source-verified: I read the full diff + the production over-supply path in `restore.rs`, ran the new property twice (default + `PROP_CASES=80`/`PROP_CASES_OPTIN=24`), ran clippy `-D warnings`, ran the FULL `cargo test -p mnemonic-toolkit` suite and SUMMED every `test result:` line, and authored **three breaking mutations** in throwaway in-place edits (reverted to byte-identical) to prove each load-bearing oracle is non-vacuous, NOT inspected.

**Verdict: GREEN — 0 Critical, 0 Important.** (1 Minor, forward-looking.)

Every oracle is the INDEPENDENT rust-miniscript / Bitcoin Core derivation asserted equal to the toolkit's completion AND proven to DIFFER from a wrong assembly — verified by mutation, not by reading the in-file `assert_*`. The property genuinely drives the over-supply `S_own = C(K,1)·N!` enumeration (never the `n!` exact path) — confirmed against the production dispatch in `restore.rs`. The wrong-account discriminators RE-DERIVE the key (a genuinely different xpub), not the opaque origin string. The diff is exactly the 2 declared test files with ZERO `src/`/`mlock`/`Cargo`/`.lock` churn and a pristine working tree. Full suite **3482 passed / 0 failed / 16 ignored** (196 result lines incl. the doc-test), clippy clean, no flake at 80/24 cases. **The gate advances to P6 (ship).**

## Over-supply axis genuinely exercised (NOT the exact path)
The headline `subset_search_resolves_random_own_account_to_golden` (`prop_subset_search_roundtrip.rs:372`) generates `own_acct ∈ 0..5` (RANDOM, not always 0), `slack ∈ 1..3`, and sets `own_account_max = own_acct + 1 + slack` — so `K ≥ 2 > 1 = j` ALWAYS (asserted at `:391`). The own account is NEVER passed via `--account` (`complete_own_account_max` `:280-324` passes only `--own-account-max K` + `--from phrase=SEED_A` + the real `--cosigner` cards). I traced this into production: `restore.rs:1683` `over_supply = own_account_max.is_some()`; `:1686-1691` builds `own_accounts = (0..K)`; the over-supply branch at `:1850` computes `realized_s = ps::s_own(k_own, j=n−m_cosigners, m_cosigners, sorted)` = `C(K,1)·N!` (or `C(K,1)` sorted), and `:1962-1971` selects `Enumeration::OwnAnchored` (NOT `FullPermutation{n}` which is the `n!` exact path), with `early_exit = over_supply || opt_in` (`:2080`). So `K ≥ 2` provably takes the `OwnAnchored` over-supply path with cardinality `> N!`. The contract's `j = N − M = 1` (single own slot, M cosigners exact) ⇒ `S_own = C(K,1)·N!` is correct per SPEC §3 (`S_own = C(K_own,j)·N!`, collapses to `N!` only at `K=j=1`).

## Oracle independence — disjoint sources
The golden is `golden_addresses(descriptor, count)` (`:260-274`): rust-miniscript `Descriptor::<DescriptorPublicKey>::from_str` → `into_single_descriptors()` → `derive_at_index(i)` → `.address(Bitcoin)` from the ORIGINAL concrete descriptor (`GenWallet::descriptor()` `:179-194`, built from `key_str`/`xpub_at` BIP-32 derivation). The completed addresses come from the toolkit's `restore --json` `wallets[0].first_addresses` (`:314-323`). md-codec/toolkit-synth is on the toolkit side; rust-miniscript is the golden — **disjoint**. In `bitcoind_differential.rs` the golden is `derive_receive` (`:233`, the STRESS-A rust-miniscript path) and the third source is Bitcoin Core `deriveaddresses` (`core_addresses` `:319`) — three disjoint derivations.

## Anti-vacuity per cell — the heart (all load-bearing under mutation; no vacuous cell found)
- **Headline property is non-vacuous (MUTATION-PROVED).** I forced the golden to derive at `own_acct+1` (wrong account) while leaving the restore at the true range. Test went **RED** at the minimal input `family=0, script_idx=0, own_acct=0, slack=1` (toolkit resolved own@0: `bc1q2a4vm7…`; mutated own@1 golden: `bc1qutaes7…`). Proves the toolkit output and the golden are NOT trivially equal — the search genuinely resolves the true (random) account, and a wrong assembly diverges. Reverted byte-identical.
- **`oracle_own_account_change_changes_address_subset_axis` (`:418`) is load-bearing (MUTATION-PROVED).** It asserts `golden(own@0) != golden(own@3)` for the SAME shape across all 4 scripts. I mutated `at3`'s own account 3→0 (equal to `at0`) — test went **RED** (`assertion left != right failed`, identical `bc1q2a4vm7…` both sides). It RE-DERIVES the own key at the other account (`canonical_path` → a different `48'/0'/{acct}'/2'` xpub), NOT an origin-string rewrite — confirmed structurally (the whole `GenWallet`/`descriptor()` chain derives from the xpub) and by the mutation.
- **`subset_search_unreachable_own_account_no_match_refuses` (`:469`) NO-MATCHes through the REAL search.** own@4 with `--own-account-max 3` (range 0..2 excludes 4) → I ran it: exit **4** + stderr contains "NO MATCH". This is a genuine search refuse (`RestoreMismatch`), not a parse/input error — the search reached its full `OwnAnchored` space over 0..2 and found no own candidate reproducing the own@4 id.
- **Opt-in cells: golden built from ONLY the real cosigners; outsider a genuine non-member.** `cosigner_subset_search_drops_outsider_completes_to_golden` (`:614`): `golden_addresses(w.descriptor(), 2)` uses ONLY {A,B,C}; `complete_cosigner_subset` (`:543`) supplies the real cards PLUS a `SEED_OUTSIDER` card under `--search-cosigner-subset`. A pass proves the search DROPPED the outsider. `opt_in_outsider_is_genuinely_distinct` (`:651`) asserts `golden(real) != golden(with_outsider)` — so "drop the outsider" is a discriminating selection, not vacuous. Both GREEN.
- **`every_generated_shape_completes_via_subset_search` (`:513`) + `oracle_accepts_faithful_reconstruction` (`:450`)** pin coverage (own@3, all 8 shapes complete) and the dual (the oracle accepts a faithful reconstruction — not so strict it false-fails). Both GREEN.
- I searched for a cell that could pass while the search resolved the WRONG account/subset, or where the golden coincides with the toolkit output by construction. None found — every equality oracle has an independent source on each side, and the two `assert_ne!`/discriminator cells are mutation-confirmed to fail when the distinction is removed.

## The bitcoind row
`bitcoind_subset_search_completion_differential` (`bitcoind_differential.rs:~1150`) is `#[ignore = "requires a pre-running offline -chain=main bitcoind…"]` and CONNECT-ONLY: `read_wiring()` (`:275`) returns `None` (skip) when no env, `Some` when all three set, and `panic!`s on partial provision (`:286`) — unset→skip, set-but-broken→panic, exactly the contract. It asserts the INDEPENDENT golden (`reported == derive_receive(case.descriptor)`) BEFORE the Core compare (`complete_subset` then `assert_eq!(reported, independent)` precedes `core_addresses`), plus a cross-check vs Core on the ORIGINAL descriptor (catches a different-but-Core-valid reconstruction). It completes via `--own-account-max 5` with own at BIP-48 account 3 (`subset_corpus()` `:~960`, `own_account: 3, own_account_max: 5` ⇒ `S_own = C(5,1)·2! = 10`). The DEFAULT-CI `subset_search_completion_anti_vacuity_leg` (`:~1010`, NOT `#[ignore]`) runs the completion↔independent-oracle equivalence UNCONDITIONALLY (no node) and the own@3≠own@0 discriminator. **MUTATION-PROVED:** the discriminator's `wrong_own_key = key_str(SEED_A, "48'/0'/0'/2'")` RE-DERIVES at account 0 (a genuinely different xpub), not an origin rewrite — I changed it to `"48'/0'/3'/2'"` (the TRUE account) and the `assert_ne!` went **RED** (identical `bc1qqz0ggy…` both sides), confirming the implementer's origin-rewrite-was-vacuous bug-fix is correctly applied here. Reverted byte-identical. I ran the leg: GREEN; the `#[ignore]` row correctly skips without a node.

## Tests-only / no regression (the SUMMED full count I ran myself)
`git diff --name-only b50b522f^..b50b522f` = exactly `tests/bitcoind_differential.rs` + `tests/prop_subset_search_roundtrip.rs`. Grep for `src/|mlock|Cargo|.lock` in the commit diff → **NONE** (clean tests-only; the implementer's fmt-gotcha churn was reverted — confirmed zero src/mlock in the commit AND a pristine working tree after my mutations). I ran the FULL `cargo test -p mnemonic-toolkit` and SUMMED all 196 `test result:` lines (incl. the `Doc-tests` line): **3482 passed / 0 failed / 16 ignored** — exactly P4's baseline (3474/0/15) + 8 new pass + 1 new ignored (the bitcoind row). No build errors, no failures anywhere. `cargo clippy -p mnemonic-toolkit --tests --bins --lib -- -D warnings` clean. The new property file alone: 7 pass / 0 fail; bitcoind subset rows: 1 pass + 1 ignored.

## Determinism / no flake
`ProptestConfig { failure_persistence: None, max_global_rejects: 4, cases: PROP_CASES env-tunable (24 / opt-in 8) }`. A counterexample is reproducible: the headline mutation printed `minimal failing input: family = 0, script_idx = 0, own_acct = 0, b_acct = 0, c_acct = 0, slack = 1` plus the full descriptor — a deterministic, re-derivable case, not a heisenbug (the model is a pure function of the proptest scalars; the CLI spawns are deterministic). I ran the property twice (default, then `PROP_CASES=80 PROP_CASES_OPTIN=24`): GREEN both times (3.18s / 12.02s), no flake, no timeout, no global-reject blowup.

## Minor (forward-looking, NOT a gate finding)
- **m-1 — module-doc references a stale cell name.** `prop_subset_search_roundtrip.rs:38` (the `//!` NON-VACUITY doc) cites `subset_search_wrong_account_no_match_refuses`, but the shipped cell is named `subset_search_unreachable_own_account_no_match_refuses` (`:469`). Doc-comment only — the test exists, passes, and does what the doc describes (loud NO-MATCH on an unreachable own account); the name drifted between draft and ship. Cosmetic; fix opportunistically in P6 or a later touch. Does not affect correctness, coverage, or the gate.

GREEN (0 Critical, 0 Important). Over-supply axis genuinely exercised (`OwnAnchored`, `K ≥ 2 > j`, never `n!`); every oracle independent + mutation-proved non-vacuous; no vacuous cell; no exact-path masquerade; no origin-only rewrite; no regression behind a partial run (full 3482/0/16 summed). **The gate advances to P6 (ship).**
