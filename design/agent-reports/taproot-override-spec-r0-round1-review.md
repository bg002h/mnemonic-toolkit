# SPEC R0 round 1 — restore-md1-taproot-use-site-override-arm (#26) (opus architect, verbatim)

> Reviewer: opus architect (read+bash; toolkit `f7e6fca`/local `4e21d94`, md-codec `c85cd49` 0.36.0). **Verdict: GREEN — 0 Critical, 0 Important.** 3 Minors carried to the (HELD) plan-doc. UNCOMMITTED (freeze). SPEC R0 converged round 1.

---

**Verdict: GREEN — 0 Critical, 0 Important.**

## Verified correct

**Cross-cycle dependency premise (§2) — all 3 #25 as-designed artifacts confirmed.**
- `to_miniscript_descriptor_multipath` and `has_hardened_use_site` are genuinely ABSENT today (0 grep hits in both `descriptor-mnemonic/crates/` and toolkit `crates/`). `taproot_override_card` / `restorable_taproot_override_card` also absent. All are #25 deliverables.
- #25's plan P1.3 (`IMPLEMENTATION_PLAN_restore_per_key_use_site_override_2026-06-19.md:33-35`) builds the multipath builder by calling `node_to_descriptor(&d.tree, &keys)` — the SAME shared walker that already handles `Tag::Tr` (`to_miniscript.rs:153-173`) → `Terminal::MultiA` leaf (`:411-416`). The walk is key-agnostic; only `keys` construction differs. So the builder is taproot-agnostic by construction. §2's "free re-route" statement is accurate.
- #25 plan P2.2 (`:59-61`) commits to rewiring `faithful_multisig_descriptor` (`restore.rs:1105/1109`, today calling `to_miniscript_descriptor(d,0)` with the C2 chain-0 collapse + `multipath: d.use_site_path.multipath.clone()` re-promote at `:1125`) to the multipath builder. §2's claim is accurate.
- #25 SPEC:68-69 + plan P2.3-P2.4 define `taproot_override_card(d)` = `Tag::Tr` root ∧ `use_site_path_overrides.is_some()` as ONE named predicate reused by guard AND advisory, AND add `TaprootUseSiteOverride` advisory citing the #26 slug. The #26 SPEC's §2/§3 narrowing premise rests on these and they are exactly what #25 delivers.

**§3 single-predicate parity — structurally closed, no silent-wrong-address gap.** Traced the three wire-sites against the `restorable_taproot_override_card` definition: for any `taproot_override_card(d)`, let R = restorable. Guard admits iff R (the `has_hardened` disjunct is subsumed because R ⟹ `!has_hardened`); classify reroutes iff R; advisory fires iff `!R`. All three partition exactly on R ⇒ guard-admits ⟺ classify-reroutes ⟺ advisory-silent. No taproot-override shape escapes the partition.

**§3 helpers are realizable from `d.tree` at guard-time.** `tree::Node` exposes `Body::Tr { is_nums, key_index, tree: Option<Box<Node>> }` (`tree.rs:49-57`); the leaf is `tree`'s inner `Node` with `tag == Tag::MultiA` vs `Tag::SortedMultiA` (both via `Body::MultiKeys`, `tree.rs:411/423`). A toolkit predicate CAN distinguish `MultiA` from `SortedMultiA` and read `is_nums` directly from `d.tree.body` — exactly as `classify_taproot_restore` already does (`restore.rs:698-739`). The guard runs BEFORE classify (`:1247` then dispatch `:1282-1290`), confirmed; the predicate inspects `d.tree` directly, not classify's verdict. ✓

**§3.2/§4 classify-reroute reaches the faithful arm correctly.** `classify_taproot_restore:696`; `Template(TrMultiA…)` arm `:728-731`; `TrSortedMultiA` `:733-738`; `refuse_at_in_both:777` (the n≥3 funds-safety crux, unchanged). `GeneralFaithful` → `template_opt = None` (`:1286`) → `faithful_multisig_descriptor` (`:1344`/`:1105`) → Display-fidelity guard (`:1354-1368`). The faithful arm does NOT assume wsh: it routes through md-codec's `node_to_descriptor`, which dispatches on the top-level tag including `Tag::Tr`, and the NUMS internal key passes through `ReconstructTranslator::pk` as a `Single` H-point (`restore.rs:1040-1047`, strict-NUMS x-only equality). Non-override `tr(multi_a)` stays on the `Template` fast path (only `restorable_…` cards reroute). ✓

**§4 D7 NUMS scope — structurally correct.** `expand_per_at_n` (`canonicalize.rs:420-471`) iterates `0..d.n` (cosigner table), resolving each `use_site_path` per-`@N` override (`:458-460`). The NUMS internal key is synthesized separately in `node_to_descriptor`'s `is_nums:true` branch (`to_miniscript.rs:161-165`, `build_nums_internal_key`, no origin/path/override). So #25's per-`@N` loop need not cover the internal key. Non-NUMS internal stays refused (`tr_internal_is_nums` false → `restorable_…` false); `@`-in-both refused structurally at `refuse_at_in_both:777-795`. No silent-wrong path.

**§5 test floors — non-vacuous and constructible.** `prop_backup_restore_roundtrip.rs::derive_receive:383` parses an arbitrary descriptor STRING through rust-miniscript (`into_single_descriptors`, multipath-split), O3 assert at `:437`. The divergent override card is constructible: `bundle --descriptor` of a `tr(NUMS, multi_a(2,@0/<0;1>/*,@1/<2;3>/*))` encodes `use_site_path_overrides` via `parse_descriptor.rs:194-201` (pushes per-`@N` iff `usp_i != baseline`) + `make_use_site_path:223-236`; the ONLY override guard in `cmd/` is `restore.rs:1247` — no taproot+override emit-refusal. The existing pinned `per_key_use_site_override_refused` (`cli_restore_multisig_general.rs:414`, `bundle_general("wsh(multi(2,@0/<0;1>/*,@1/*))")`) proves divergent override cards round-trip through the harness today, and `cli_restore_taproot.rs` proves `bundle --descriptor "tr(NUMS,…)"` works (`:124/:146/:170`). Floor-2 (divergent `@1` suffix) is genuinely non-vacuous against the suffix-collapse bug class. Bitcoind corpus already has `tr-nums-multi_a-2of3` (`bitcoind_differential.rs:112-114`), `#[ignore]`/env-gated (`:314/:320`).

**§6 SemVer (PATCH) — sound and precedented.** Repeated repo precedent for "capability unlock of previously-refused cards on an existing flag, no clap/wire/dropdown/`ToolkitError` change = PATCH": v0.55.1 (general single/2-leaf `tr(NUMS,…)` faithful restore) was PATCH; v0.55.3 (non-NUMS taproot reconstruction) was PATCH; v0.54.x stated "a new refusal/behavior on an existing flag combination → no schema_mirror/GUI/manual/sibling lockstep." #25 SPEC:6 treats its analogous toolkit leg as PATCH. `ModeViolation` (toolkit `error.rs:250`) and `HardenedPublicDerivation` (md-codec `error.rs:357`) both exist — no new variant needed. No clap flag / dropdown / wire-shape change.

**§8 umbrella-gated sortedmulti_a — coherent, cross-cited, not citation-decay abuse.** The unconditional hard-`Err` at `to_miniscript.rs:423-428` ("rust-miniscript v13 has no Terminal::SortedMultiA") is exact. The umbrella `taproot-coverage-cycle-on-miniscript-gt-13-1-0` exists (`FOLLOWUPS.md:4189`), the component `md-codec-sortedmulti-a-to-miniscript-rendering-gap` exists (`:352`). Designing the deferred renderer-arming + explicitly riding the umbrella trigger (not implementing now) is the correct posture — interim loud-refuse keeps it funds-safe (floor-3 negative test backstops the narrowing).

**M1/M2 brainstorm-R0 carry-forward — both honored.** M1 (single shared predicate) is the SPEC's §3 centerpiece. M2 (re-grep at plan time) is committed in §7/§9 (plan-doc HELD until #25 ships; re-grep against POST-#25 source).

## CRITICAL
None.

## IMPORTANT
None.

## MINOR

**Min-1 — §8 FOLLOWUPS line citation imprecise (`:4192` vs the component at `:352`).** §8 says "the umbrella component `md-codec-sortedmulti-a-to-miniscript-rendering-gap` (FOLLOWUPS.md:4192, option (b))." `:4192` is the umbrella's `**What:**` line; the *component* `md-codec-sortedmulti-a-to-miniscript-rendering-gap` is a SEPARATE entry at `FOLLOWUPS.md:352`, and the "option (b)" discussion for it (with the note that option (b) is "mis-framed: encoding already works; what's missing is a renderer") is at `:94`. Both anchors are real, so this is not a load-bearing falsehood — but the citation conflates the umbrella line with the component entry. *Fix:* at plan time, cite the umbrella at `:4189` and the component at `:352` separately; drop the "(b)" tag or point it at the `:94` discussion. (Already covered by the SPEC's own §9 "re-grep at plan time" discipline, but worth naming.)

**Min-2 — §4/§5 hardened-taproot-override fires TWO advisories; the SPEC's "single source ⇒ exact parity" phrasing slightly overstates.** For a `tr(NUMS, multi_a)` card with a hardened use-site: `has_hardened_use_site` ⇒ #25's `HardenedWildcard` advisory fires; AND `taproot_override_card && !restorable` ⇒ #26's `TaprootUseSiteOverride` advisory fires. Both fire. This is NOT a parity violation (restore refuses; ≥1 advisory fires; the advisory `Vec` already permits 0..=3 entries), but the §5 floor-7 wording "advisory fires IFF restore refuses … driven by the SAME expression" elides that two distinct advisories can co-fire on the hardened-taproot intersection. *Fix:* at plan time, note in the floor-7 test that the hardened-taproot row asserts `restore refuses AND (≥1 of {HardenedWildcard, TaprootUseSiteOverride}) fires` rather than "exactly the TaprootUseSiteOverride advisory" — the §4 matrix already correctly routes hardened-taproot to the `has_hardened` row, so the design is right; only the parity-test assertion needs the "≥1" framing.

**Min-3 — §5 floor-1 cites `prop_backup_restore_roundtrip.rs::derive_receive` as the golden source, but should reuse #25's I1 independence discipline explicitly.** The independence argument (shared derivation ENGINE is acceptable; reconstruction LOGIC differs) is correct and the brainstorm R0 already validated `derive_receive:383` as a non-vacuous golden. But #25's SPEC I1 (`SPEC_restore_per_key…:80`) warns that a *self-referential* harness (both sides from the same render) passes VACUOUSLY. The #26 floor-1 golden is a HAND-WRITTEN divergent descriptor (not restore's own output), so it IS independent — but the SPEC should state this explicitly to forestall an implementer accidentally feeding restore's output back as the golden. *Fix:* at plan time, pin the floor-1 golden descriptor string as a hand-written literal (e.g. with the `@1` suffix `<2;3>` distinct from `@0`'s `<0;1>`) and assert it is NOT derived from the restore output — mirroring #25 plan §4-I1.

## To turn GREEN
Already GREEN. No Critical or Important findings. The SPEC accurately states #25's as-designed dependency surface, the §3 single-predicate parity is structurally closed (no silent-wrong-address gap), the test floors are constructible and non-vacuous, the PATCH SemVer is repo-precedented, and the umbrella-gated sortedmulti_a deferral is coherent. The plan-doc remains correctly HELD until #25 ships. Carry the 3 Minors into the plan-doc:
1. (Min-1) Cite the umbrella (`:4189`) and the component (`:352`) separately; re-grep all FOLLOWUPS anchors at plan time.
2. (Min-2) Frame the floor-7 hardened-taproot parity assertion as "refuse AND ≥1 advisory fires" (two advisories co-fire on the hardened∩taproot intersection — benign).
3. (Min-3) Pin the floor-1 golden as a hand-written divergent literal, explicitly NOT sourced from restore's own output (mirror #25's I1).
