# tech-manual-v1.0 final review — r1 (architect sign-off)

Date: 2026-05-12
Reviewer: feature-dev:code-reviewer (final r1, architect sign-off)

## Summary

- Cumulative cross-chapter consistency: 0C / 0I / 0L / 0N
- Acceptance criteria: all 11 PASS
- "Every aspect" architect sign-off: **PASS**
- Cycle-exit verification: confirmed
- Other: 0C / 0I / 1L / 0N

Total: 0C / 0I / 1L / 0N

## SPEC §7 acceptance criteria — per-criterion verdict

**A1** — every BIP-388-parseable shape walk-through across Parts II + III. PASS. §II.1 wire-encoding for single-sig + multisig + taproot-tree shapes; §III.2 all 11 derivation-shape buckets; §V.1 documents the v0.32 switch from the v0.14-era 5-shape allow-list to `rust-miniscript`-AST conversion covering every BIP-388-parseable shape.

**A2** — every public function referenced. PASS. 92/92 via `tests/api-surface-coverage.sh`; `make lint` 6/6 green. §V.1-§V.3 enumerate public items in `Item|Signature|Semantics|Source` tables. §V.4 correctly scopes to JSON envelope schema + ToolkitError taxonomy (binary-only crate).

**A3** — every Error variant has a chapter row. PASS. Independently verified: md-codec 43 + mk-codec 22 + ms-codec 10 + toolkit 26 = **101 variants**. All in §V.X.4 tables and troubleshooting §A-§D.

**A4** — glossary ≥80 with section pointers. PASS. **107 entries** at HEAD (96 v0.4 + 11 Phase 5.1-5.3 additions). All entries carry "First defined §X.Y" / "First cited §X.Y" pointers.

**A5** — index ≥250, bidirectional. PASS. **540 rows**. Bidirectional lint green.

**A6** — TOC auto-generated by pandoc. PASS by design. Makefile `--toc --toc-depth=3` invocation generates TOC fresh from headings on every build.

**A7** — BIP cross-reference ≥12 BIPs. PASS. **20 BIPs**.

**A8** — worked examples verified by `verify-examples.sh`. PASS. 15/15 transcripts pass with the absolute BIN paths from the handoff.

**A9** — both mirror invariants green. PASS. (1) CHANGELOG present through `[0.4.0]`; `[1.0.0]` will land in the tag commit. (2) `docs/technical-manual/FOLLOWUPS.md` has exactly 2 open `cross-repo` entries; both deferred to md1 work; neither blocks v1.0.

**A10** — PDF ≥200pp. PASS. **258 pages** (up from 242 at v0.4 close).

**A11** — reproducible build. PASS. SHA256 `2764b782aab34afc057170a7a73f946746ec148b53ab879b3fa62fe1d6a48463`, 879,001 bytes; byte-identical across two clean `SOURCE_DATE_EPOCH=1746921600` builds.

## Findings

### Low

**L-1 — Glossary preamble line 5 stale.**

`docs/technical-manual/src/60-back-matter/61-glossary.md:5` reads:

> Entries populate incrementally per cut; the tech-manual-v0.1 seed below tracks what Parts I + II introduce.

This was correct at v0.1 but the glossary now has 107 entries covering all five Parts. The preamble should reflect that.

**Fix:** replace line 5 with:

> Entries have accumulated across all five Parts (v0.1 through v1.0). Section pointers cite the section of first definitional use.

Per `zero_followups_from_release_cycles`: fold inline before tagging v1.0.

## Cycle-exit verification cross-check

- `cargo test --workspace --all-features`: 561 passed / 0 failed / 2 ignored.
- `make lint` 6/6 green.
- `make verify-examples` 15/15 transcripts pass.
- PDF: 258pp, 879,001 bytes, SHA256 `2764b782…48463`, byte-identical across two clean builds.
- api-surface-coverage helper: 92/92 symbols covered.

CHANGELOG correctly lacks `[1.0.0]` row (expected — tag commit adds it).

Two carry-over cross-repo FOLLOWUPs (`md1-wsh-multi-unsorted-integration-test`, `md1-bip49-integration-test`) still open; both block md1 work, neither blocks v1.0.

## Architect sign-off

**PASS**. All 11 SPEC §7 acceptance criteria independently verified. The "every aspect of the software" coverage claim is substantiated:

- Wire formats: Parts I (foundations) + II (md1 / mk1 / ms1)
- Address derivation: Part III (descriptor → miniscript → address; shape coverage; network + SLIP-0132)
- Bundle semantics: Part IV (anatomy / anti-collision / future shares)
- Rust API surface: Part V (4 crate chapters with full Error taxonomy + JSON envelope + engraving-card)
- CLI surface: intentionally delegated to the end-user manual (per SPEC §4.2)

The single Low finding (glossary preamble text) must be folded inline at the tag commit. After that fold, v1.0 is tag-ready.

## Verdict

- [ ] 0 C / 0 I / 0 L / 0 N — tag-ready
- [x] Fold L-1 inline → then tag-ready
