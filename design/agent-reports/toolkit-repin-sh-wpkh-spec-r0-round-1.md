# SPEC R0 — SPEC_toolkit_repin_sh_wpkh.md (round 1) — Opus, adversarial

**Persisted verbatim per CLAUDE.md** (+ opus fold adjudication at end). Reviewer verdict: **OPEN (1C / 1I + 3M)**. **Opus fold adjudication: C1 + M4 REJECTED as contamination false-positives (git-verified); I1 + M2 + M3 FOLDED; 1 cross-track finding folded into the partial-decode plan.**

## Reviewer's findings (verbatim summary)
**C1 (Critical, reviewer):** md-codec 0.41.0 adds TWO new Error variants (enum count 47→49): `MalformedPayloadPadding` (SPEC-covered) AND `EmptyOriginOverride{idx:u8}` (error.rs:341, "P0.3/I-1") — the latter absent from the SPEC → toolkit won't compile; both exhaustive matches (`md_codec_exit_code` error.rs:505-569, `friendly_md_codec` friendly.rs:216-405) need both arms; correct "three deltas"→"four".

**I1 (Important, reviewer):** `canonical_origin` consumer audit incomplete — 6 non-test consumers in toolkit `src/`, SPEC covered only 2 probes (bundle.rs:1418, verify_bundle.rs:1414) + the S4 mirror sites. Un-enumerated: `restore.rs:1645` (`canonical_origin(&d.tree).ok_or_else(...)` EXTRACTS origin as a derivation path in general/multisig `canonical_fallback` — pre-repin `None`→err, post-repin `Some(49')`→derives), `synthesize.rs:1201` (elides template origins), `bundle.rs:1199` (gates a "GENERAL POLICY" stderr warning). Reviewer traced all 3 SAFE for sh(wpkh) (n≥2 general path / no-`Sh`-arm mirror gates; sh(wpkh) is n==1) but the reachability args are load-bearing + unstated. Fold: 6-consumer audit table + per-site disposition + a restore.rs:1645 guard test.

**M2:** repair non-zero-pad cell not deterministically constructible (can't steer BCH onto a non-zero-pad codeword) → substitute a direct decode-path cell (hand-forged non-zero-pad → `reassemble` → `MalformedPayloadPadding`) + assert repair.rs:1710 wildcard degrades to `PostCorrectionDecodeFailed`.
**M3:** verify_bundle.rs:1408-1414 is the canonicity PROBE, not the fail-loud site; the real refusal is a downstream md1 BYTE-MISMATCH (post-repin re-derives an elided/49' md1 that can't match the stored explicit-48' card). Name the real mechanism.
**M4:** 0.41.0 "also adds unused-this-cycle public API" — DecodeOpts, decode_payload_with_opts, decode_md1_string_with_opts, reassemble_with_opts, unresolved_origin_indices → name as additive-unused.

Reviewer confirmed: exit 2 correct for the new decode-reject variant; grouped (not alpha) placement correct; sh(wpkh) flip CORRECT (m/49'/0'/0' is right BIP-49; old m/48'/0'/0'/1' came from `bip48_script_type_for_root_tag(Sh)→1'`, a BIP-48 multisig script-type, nonsensical for single-key sh(wpkh) — genuine fix); do-not-fix mirror intact; manual 5→6 real+mandatory; release ritual complete; SemVer MINOR correct.

## OPUS FOLD ADJUDICATION (2026-07-11)
**C1 + M4 REJECTED — contamination false-positives (git-verified).** The reviewer diffed vendored-0.40.0 vs the descriptor-mnemonic WORKING TREE, which carries Track A's UNCOMMITTED partial-decode P0 changes (same repo). Verified: `git show HEAD:crates/md-codec/src/error.rs` (committed 0.41.0, = the published `md-codec-v0.41.0` tier) has `MalformedPayloadPadding`=1, `EmptyOriginOverride`=**0**; the working tree has `EmptyOriginOverride`=1 (P0 added it, uncommitted, `git status` = ` M error.rs`). So `EmptyOriginOverride` + `DecodeOpts`/`*_with_opts`/`unresolved_origin_indices` are ALL Track A's 0.42.0-destined work, NOT in published 0.41.0. **Track B re-pins to published 0.41.0 → needs ONLY the `MalformedPayloadPadding` arm; the SPEC's "one new variant / three deltas" is CORRECT.** Fold: add an explicit SPEC note that the 0.40→0.41 delta is diffed against the **`md-codec-v0.41.0` git tag**, NOT the working tree (which carries the concurrent partial-decode P0).
**I1 FOLDED** (valid — toolkit `src/`, uncontaminated): add the 6-consumer `canonical_origin` audit table + restore.rs:1645 guard test.
**M2 + M3 FOLDED** (valid, toolkit-side).
**CROSS-TRACK FINDING FOLDED INTO PARTIAL-DECODE PLAN P2:** when partial-decode's P2 bumps the toolkit 0.41→0.42 (consuming Track A's md-codec with `EmptyOriginOverride`), the toolkit's two exhaustive `md_codec::Error` matches (error.rs:505-569, friendly.rs:216-405) WILL need an `EmptyOriginOverride` arm → added to `IMPLEMENTATION_PLAN_pathless_partial_decode.md` P2. (The contaminated diff usefully surfaced a real future gap.)
**LESSON (gotcha):** a SPEC-R0 that diffs "published vX vs vY" is contaminated if the repo working tree carries concurrent uncommitted work — diff against the git TAG / clean state, or run the two tracks in separate worktrees. [[feedback_track_inflight_agents_no_tangle]]

Re-dispatch a Track B convergence R0 (opus) — instructed to diff against the `md-codec-v0.41.0` TAG, verify the I1 audit fold + M2/M3, and confirm the C1/M4 rejection.
