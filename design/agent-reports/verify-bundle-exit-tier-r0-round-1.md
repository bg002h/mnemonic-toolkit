# R0 round 1 — SPEC_verify_bundle_descriptor_exit_tier.md

Reviewer: fable (independent). Date: 2026-08-03. Persisted verbatim.

**NOTE ON PERSISTENCE ORDER:** this report was persisted AFTER its fold, not
before, in violation of the project rule ("persist each reviewer's output
verbatim *before* folding it"). Caught by R0 round 2 (IMPORTANT-3). Recorded
here rather than silently corrected.

## VERDICT
NOT GREEN — 1 Critical, 2 Important: the diagnosis (7 sites, uniform provenance, manual/mechanism mismatch) is fully verified, but the SPEC's "keep `:1722` at exit 4" structure rests on a false claim — `:1722` is structurally unreachable, so the 6-vs-1 split, the §5 migration note, and T4 must be restructured.

## CRITICAL

**C-1. §3's characterization of `:1722` is false; the site is provably dead, so "6 sites to 2, 1 keeps 4" documents a contract arm that cannot fire.**
- Claim under test: SPEC §3 — "`:1722` is the one genuinely round-trip-shaped site: the same string already parsed clean at `:1468` with empty keys, so a failure here means the **completed keyed wallet** won't re-parse — key-binding-dependent, not a typo. Keeping it … gives it a live construction site."
- Evidence: `parse_descriptor(input, keys, fps)` (`crates/mnemonic-toolkit/src/parse_descriptor.rs:933-1012`) never uses `keys`/`fingerprints` in any fallible step. The parse pipeline (lex `:963`, resolve `:964`, `substitute_synthetic` `:975` — which substitutes **synthetic** xpubs, not the caller's keys — `MsDescriptor::from_str` `:976`, `walk_root` `:978`) is a deterministic function of `input` alone; the caller's keys are attached to the TLV afterward via infallible map/sort/`Some` plumbing (`:980-1003`). In `verify_bundle.rs`, `descriptor_str` is borrowed immutably at exactly two points after its last mutation (`:1412`): the probe `:1467` and the final parse `:1721` (awk over `:1460-1725` confirms no other occurrence). If `:1467` succeeded, `:1721` cannot fail. There is no "completed keyed wallet re-parse" — the bound keys never participate in parsing.
- Why it breaks the SPEC: (a) §3's "gives it a live construction site" is false — after the change `DescriptorReparseFailed` has zero reachable construction sites; (b) §5's migration note ("Exit 4 … now means only: cards mismatched, or the completed keyed wallet failed re-parse") would ship manual + CHANGELOG text advertising a dead mechanism — recreating the exact defect class the SPEC's own fact 3 condemns (documented rationale ≠ firing mechanism); (c) the claim that `error.rs:124-129`'s doc becomes "finally accurate for what actually reaches it" is false — nothing reaches it.
- Required change: restructure §3/§5/§8/T4 around the fact that **all seven** sites are input-stage. Either (a) re-tier all 7 to `DescriptorParse`/2 and retire the variant in place (precedent: `ExportWalletFormatStub`, `error.rs:141-145` — variant retained with zero construction sites, documented as such), or (b) keep `:1722` as an explicitly *defensive* arm with honest internal-invariant wording (precedent: `repair.rs:900-918`'s `PostCorrectionDecodeFailed` — "this never triggers …; a failure here would indicate a divergence"), and in either case fix the migration note per I-1. T4's escape hatch ("record in the test-module doc rather than forcing one") becomes the mandatory path, not a contingency.

## IMPORTANT

**I-1. The §5 migration note omits `Bip388VerifyDistinctness`, and §1's uniformity claim ignores the one *deliberate* verify=4/emit=2 asymmetry that already exists on this exact path.**
- `Bip388VerifyDistinctness` → exit 4 (`error.rs:21-24`, `:599`) fires **pre-card, on user input**, inside `descriptor_mode_verify_run` itself: concrete arm `verify_bundle.rs:1429-1431` (duplicate `(xpub, path)` in a user-supplied concrete descriptor) and @N arm `:1717-1719` (post-binding). The emit path maps the identical condition to `Bip388Distinctness` → exit 2 (`error.rs:598`) — a deliberate v0.19.0 SPEC §4.11.c per-surface split.
- Two consequences: (a) the §5 sentence "Exit 4 from descriptor-mode verify now means only: cards mismatched, or the completed keyed wallet failed re-parse" is wrong as shipping manual/CHANGELOG text — the live meaning set is {card mismatch, `Bip388VerifyDistinctness`} (and per C-1, *not* re-parse); (b) §1's "The same command already exits 2 for every OTHER malformed-descriptor shape" is unqualified — §4.11.c is prior art of an intentional verify-tier-4-for-input decision that the reversal case must distinguish (it can: §4.11.c targets legacy v0.2 self-multisig *artifacts* being re-verified, which is genuinely artifact-shaped — but the SPEC must say so, not stay silent).
- Required change: rewrite the §5 note's exit-4 meaning set; add a §7 paragraph distinguishing §4.11.c; state explicitly that `Bip388VerifyDistinctness` is out of scope and why.

**I-2. Test surface permits partial application to ship green — the exact "guard passes with protection removed" shape.**
- Of the 6 flipped sites, T1-T3 pin only `:1445` (lex) and `:1371` (file read). `:1449`, `:1468`, `:1678` are live and reachable — I proved each with a runtime repro (P4: resolve-gap `wsh(multi(2,@0/<0;1>/*,@2/<0;1>/*))` → exit 4; P3: probe-only failure `wsh(pk(@0/<0;1>/*),pk(@1/<0;1>/*))` → exit 4, "unrecognized name 'wsh'"; P6: `--slot @0.wif=…` catch-all → exit 4). An implementer who deletes the re-wraps at `:1445`/`:1371` but misses `:1449`/`:1468`/`:1678` leaves T1-T5 and the full suite GREEN while shipping a half-fixed contract. History shows the catch-all is a live drift site (`cli_verify_bundle_entropy_slot.rs:1-10`: the Entropy arm exists precisely because raw entropy used to fall into it).
- Required change: one RED-first cell per reachable flipped site — the three repros above are ready-made — plus a dead-code note (or defensive `DescriptorParse` + comment) for `:1514`.

## MINOR
- `cli_cycleA_phase2_funds_proof.rs:187` cites `verify_bundle.rs:1375` — stale (now `:1445`); refresh in the T1 flip.
- `cli_verify_bundle_entropy_slot.rs:6` module doc names the catch-all as "(`DescriptorReparseFailed`, exit 4)" — needs a lockstep doc-comment update; add to §4 blast radius.
- The SPEC header's "deliberately pinned exit 4" overstates I-B (see below); §7 should characterize I-B accurately — doing so *strengthens* the reversal case.
- §5's headline "a descriptor that fails lex/resolve/parse now exits 2" under-describes the change — `:1371` (fs error), `:1514`/`:1678` (slot-usage) are not lex/resolve/parse failures; enumerate the classes in the CHANGELOG text.
- Stderr text visible to users changes (prefix "descriptor re-parse failed during verify-bundle: " disappears). No consumer greps it except the flipped test (verified), but worth one line in §5.

## THE REVERSAL — INDEPENDENT JUDGMENT
Reverse it — and the SPEC is reversing less than it believes. I read cycleA plan-R0 round 1 verbatim (`design/agent-reports/cycleA-plan-r0-round-1.md:20-24`): finding I-B's substance was that the cycleA plan's Phase-3a test asserted the **wrong variant for the concrete fork** — the reviewer corrected that to `DescriptorParse`/2 and noted the @N-template fork's exit 4 as the incumbent behavior, marking the exit-4 pinning test "**(optional)**" and stating "only the assertion shape is wrong." I-B was an assertion-accuracy correction that pinned the status quo to keep cycleA's scope tight; it was never a de-novo ruling that 4 is the *right* tier for @N intake errors, and it demonstrably did not weigh the GUI's VERIFY-ME semantics or the emit/verify parity cost. There is no considered design decision here to defer to — only an incumbent-behavior pin whose stated rationale (the manual's "when the completed wallet is re-parsed") describes a mechanism I have now proven never fires. On the merits: exit 4's contract presupposes a verification *result* (verified at `app_window.rs:1180-1207`); a lex reject on flag intake produces none; the identical input exits 2 on `bundle` (proven live, P1 vs P2). The one legitimate verify=4-for-input precedent (§4.11.c) is distinguishable as legacy-artifact-shaped and actually supports the SPEC's stage principle: 4 speaks about an artifact/result, 2 rejects input. The pin should not stand — but the replacement must be the C-1-restructured version, not §3 as written.

## R-1 / R-2 / R-3
- **R-1 — RESOLVED (against the SPEC's structure): `:1722` is unreachable.** Not "no repro found" — structurally impossible. `parse_descriptor` is deterministic in its string argument; `keys`/`fps` feed only infallible TLV attachment (`parse_descriptor.rs:980-1003`); `descriptor_str` is byte-identical and unmutated between the always-preceding probe `:1467` and `:1721`. No key-dependent fallible step exists between them that maps to this site (distinctness is separately handled at `:1717` → `Bip388VerifyDistinctness`).
- **R-2 — RESOLVED: `:1514` is dead.** `validate_slot_set` (`slot_input.rs:263-272`) enforces contiguity from @0 at `:1454`; the shared coverage gate (`bundle.rs:2244-2259`) enforces `max_idx+1 == n` at `:1484` — both before the loop; together every `idx ∈ 0..n` has a slot entry. Live probe P5 (n=3, one slot) confirms: exit **2**, "descriptor has n=3 placeholders but --slot vec covers 1 slots" — the gate fires, `:1514` never does. (Empty slot-vec: `validate_slot_set` returns Ok on empty, gate catches `0 != n` → 2.)
- **R-3 — TRIGGERED.** The variant does become construction-site-dead. Ruling: do **not** retire the enum variant (needless API break; `ExportWalletFormatStub` precedent for retained dead variants). Either delete site `:1722`'s re-wrap along with the other six, or keep it as a defensive arm with `repair.rs:900-908`-style honest wording ("believed unreachable; a failure here would indicate a toolkit-internal parse-determinism break"). What is non-negotiable is that no user-facing doc (manual, CHANGELOG, troubleshooting `:179`, API table `:163`) may present "completed wallet failed re-parse" as a live exit-4 meaning.

## PROOF OF WORK
(Reviewer's full proof-of-work section retained in the session transcript; key results: exactly 7 construction sites at the cited lines and none elsewhere; zero `from_import|import_json` in verify_bundle.rs; `load_bundle_json_into_args` extracts only ms1/mk1/md1; zero exit-4 branching in scripts/.github/Makefile; GUI has no `code(4)`/`Some(4)` assertions; 9 live probes P1-P9 against a v0.96.0 build.)
