# Round 6 — C0/I2/M3

NOT GREEN — 0 Critical, 2 Important. The round-5 fold's six-site out-of-graph enumeration (R0-r5-C) is correctly and completely resolved (all six sites + the bleed mechanism + the authored isolation fix verified drift-free against live source), and every prior-round fold (vendor platform-completeness, repo-global scope, release re-home, matrix-scope, network-boundary, gzip, epoch, two-path P4, NO-BUMP, md-followup, man-pages.yml citations) is intact and accurate. HOWEVER the round-5 fold's OTHER finding (R0-r5-I-exit-gate-path-filter-blind) contains a material, demonstrable factual error: it claims the five install-bearing workflows are 'paths:-filtered away from a vendor-only commit' and do NOT fire — but each self-includes its own '.github/workflows/<self>.yml' in its paths filter (live-verified), and the P0 commit EDITS each of those files to add the isolation --config, so they SELF-TRIGGER on the P0 PR. This inverts the prescribed remediation (the 'add workflow_dispatch / push throwaway probe' instruction for the four doc workflows is unnecessary) and is a false load-bearing live-verified claim in a provenance plan. Separately, the 'ONLY rust.yml + fuzz-smoke.yml fire automatically' enumeration omits sibling-pin-check.yml (unfiltered on: push, live-verified), which is correctly not a bleed site but IS a pre-existing pin-drift gate that the isolation edits could false-RED by perturbing the '--git ... --tag ... <pkg>' token layout its regex anchors on, and which the exit-gate's 'pre-existing CI must re-GREEN' set never enumerates. Both are Important: they are wrong/incomplete facts in the very surfacing-mechanism the exit-gate depends on, and one risks an unhandled false-RED on the P0 commit. Fold both (rewrite R0-r5-I per the self-inclusion fact; add sibling-pin-check.yml to the exit-gate set with a token-preservation constraint), then re-dispatch the architect on the revised plan to converge 0C/0I before implementation, per the reviewer-loop discipline.

## [Important] R0-r5-I-exit-gate-path-filter-blind is FACTUALLY WRONG about which workflows fire on the P0 commit, and the error inverts the prescribed remediation. The fold's central live-verified claim — 'a vendor-only commit fires ONLY rust.yml + fuzz-smoke.yml automatically; cross-tool-differential.yml/manual.yml/quickstart.yml/technical-manual.yml/manual-gui.yml are paths:-filtered away from it' — is false. Each of those five workflows SELF-INCLUDES its own '.github/workflows/<self>.yml' in BOTH its push and pull_request paths: filters (live-verified: manual.yml:14/21, quickstart.yml:14/23, technical-manual.yml:30/41, manual-gui.yml:14/20, cross-tool-differential.yml:23/28). The P0 commit per the plan EDITS each of these five files (to add the per-line cargo-install isolation --config). Editing the file matches that file's own path filter → each of the five FIRES AUTOMATICALLY on the P0 push/PR. The fold's premise ('the P0 commit adds only CI-infra/vendor files' that don't touch the filtered paths) is wrong: the isolation edits live INSIDE the filtered workflow files. CONSEQUENCE: the entire prescribed remediation in P0 (lines 147-150) and the cross-phase invariant (line 294) — 'add workflow_dispatch to the four doc workflows OR push a throwaway path-poke probe commit' — is UNNECESSARY for the five workflows that carry isolation edits; they are self-triggering. This is a load-bearing live-verified claim in a provenance plan that is demonstrably false, and it mis-instructs the implementer to add four needless workflow_dispatch stanzas or throwaway probe commits. (The claim is only correct for a HYPOTHETICAL pure-vendor commit that touches none of these files — but that is not the P0 commit the plan describes.)

**fix:** Re-verify and rewrite the R0-r5-I analysis: (a) state that the five install-bearing workflows each self-include '.github/workflows/<self>.yml' in their paths filter, so the P0 commit's isolation edits to those files SELF-TRIGGER them on the P0 PR — no workflow_dispatch/probe needed for them; (b) drop the 'add workflow_dispatch to the four doc workflows OR push a throwaway probe' prescription for these five (keep only the genuine residual: any affected workflow the P0 commit does NOT edit and whose paths it does not touch — e.g. bitcoind-differential.yml, which the plan correctly notes has workflow_dispatch). Re-grep self-inclusion at implementation and record live line numbers.
## [Important] The 'ONLY rust.yml + fuzz-smoke.yml fire automatically' enumeration omits sibling-pin-check.yml, which has an UNFILTERED 'on: push:' (live-verified: .github/workflows/sibling-pin-check.yml fires on every push, every PR to master, + workflow_dispatch — no paths: filter). It therefore fires on the P0 vendor-only commit too, contradicting the 'ONLY two' claim. It is correctly NOT an out-of-graph bleed site (it runs actions/checkout@v6 + a pure-bash grep scan of workflow/prose files and runs NO cargo build/install/resolve — the grep matches at sibling-pin-check.yml:4 and :148 are a comment and the gate's own grep PATTERN, not cargo invocations), so it needs no isolation edit. BUT: it is a sibling-PIN drift gate that asserts every 'cargo install --git … --tag <pin>' line in the scanned workflow files matches scripts/install.sh's canonical pins. The P0/P3 isolation edits REWRITE the cargo-install lines in manual.yml/quickstart.yml/technical-manual.yml/manual-gui.yml/cross-tool-differential.yml (inserting --config or CARGO_HOME wrapping). If the isolation edit perturbs the '--git <url> --tag <tag> <pkg>' token layout the gate's regex 'grep -nE "cargo install --git +https?://[^ ]+ +--tag +[^ ]+"' keys on, sibling-pin-check could FALSE-RED on the P0 commit — a pre-existing gate the plan's exit-criteria never enumerates as part of the 'pre-existing CI must re-GREEN' set. This is exactly the silent-lagging-breakage class the plan claims to guard against, and it fires automatically (unfiltered), so it WILL surface on the P0 PR.

**fix:** Add sibling-pin-check.yml to the P0 (and per-repo P3) HARD EXIT-GATE 'pre-existing CI must re-GREEN' set, and add an implementation constraint: the chosen isolation edit MUST preserve the 'cargo install --git <url> --tag <tag> <pkg>' token sequence the sibling-pin-check regex matches (prefer appending '--config ...' AFTER the matched tokens, or front-loading CARGO_HOME=... env on the line, so the '--git/--tag' ordering the regex anchors on is unchanged). Note it fires automatically (unfiltered push) so it needs no active trigger — but it MUST be confirmed GREEN. Re-verify the regex against the post-edit lines.
## [Minor] man-pages.yml — the actual re-homed release build (the P1 R0-r1 load-bearing edit, the whole provenance claim) fires ONLY on 'on: push: tags: mnemonic-toolkit-v*' (live-verified: no branch push, no pull_request, no workflow_dispatch). This cycle is explicitly NO-BUMP / no-tag. So the P1 edits to man-pages.yml's musl-binaries job (the per-step docker-run re-home, host musl-tools removal, tar/gzip conversion) CANNOT be exercised on the P0/P1 branch or PR at all — the only trigger is a release tag, which this cycle does not cut. The repro-x86_64-musl gate in the NEW reproducible-musl-build.yml proves the RECIPE in-container, but the actual re-homed man-pages.yml release path rides untested until the next ordinary release tag. P4's 'rebuild == published SHA256SUMS' assertion is also un-exercisable until a real tagged release publishes an artifact built by the re-homed job. The plan does not flag this CI-vs-ship gap for the man-pages.yml edit specifically.

**fix:** Add a P1 note that man-pages.yml fires tag-only (no branch/PR/dispatch), so the re-home edit cannot be CI-exercised this NO-BUMP cycle; either (a) add a temporary workflow_dispatch (or a throwaway tag on the branch) to fire the re-homed musl-binaries job once and confirm it builds + uploads correctly before merge, or (b) explicitly accept that the re-home rides untested until the first post-merge release tag and state that the next release is the real validation point (and that P4 only becomes meaningful after that first tagged release). Currently the plan implies the re-home is gate-proven in-cycle, which it is not.
## [Minor] The plan asserts 'cross-tool-differential.yml + bitcoind-differential.yml HAVE workflow_dispatch (verified)' and uses that to prescribe manual dispatch. cross-tool-differential ALSO self-fires on the P0 commit (its paths self-include its own file — line 23/28 — and the P0 commit edits :53), so the manual-dispatch prescription for it is redundant with auto-firing. Only bitcoind-differential (which the P0 commit does NOT edit and whose paths it does not touch) genuinely needs the workflow_dispatch — and note bitcoind-differential is #[ignore]/env-gated offline (per project memory) so its dispatch may be a near-no-op for the resolution check anyway. The prescription over-counts the workflows needing manual triggering.

**fix:** Narrow the 'actively trigger' list to the genuine residual: bitcoind-differential.yml (workflow_dispatch, not edited by P0, paths not touched) — and verify it actually exercises cargo resolution through vendor/ rather than being a no-op offline-gated job. cross-tool-differential self-fires via its self-included path on the :53 edit.
## [Minor] Plan line 4 / line 5 / line 294 say bitcoind-differential.yml is 'paths:-filtered ... derive_address/address_render/parse_descriptor/restore/bundle.rs-filtered' and does NOT fire on a vendor-only commit. Verified correct that those source paths aren't touched by P0 — but bitcoind-differential.yml ALSO has a 'schedule: cron 17 5 * * *' daily trigger (live-verified) and self-includes '.github/workflows/bitcoind-differential.yml' in its paths. The P0 commit doesn't edit that file, so it won't self-fire, but the daily cron WILL run it against master after merge — so if its root-checkout cargo build/test resolves through a vendor/ gap, it surfaces on the next cron, not the PR. The plan treats it purely as a manual-dispatch item; the cron is an additional (post-merge, lagging) surfacing path worth noting for the in-graph confirmation.

**fix:** Note bitcoind-differential.yml's daily cron as a post-merge lagging surfacing path for its in-graph (vendor/-covered) root-checkout cargo build/test; confirm GREEN via the workflow_dispatch before merge so the cron doesn't surface a vendor/ gap a day later.