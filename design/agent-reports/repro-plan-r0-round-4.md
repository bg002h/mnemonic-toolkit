# Round 4 — C0/I1/M2

NOT-GREEN — 0 Critical, 1 Important, 2 Minor. The round-3 folds (REPO-GLOBAL [source] scope, vendor platform-completeness, sibling-prelude recon) are correctly and completely resolved against live source, and the round-1/2 folds (release re-home into the digest-pinned container, per-step docker-run-network=none matrix-scope/network boundary, three-stanza [source] block, SOURCE_DATE_EPOCH-off-commit-SHA, gzip -n at man-pages.yml:50+:133, --locked --offline, cc-under-musl validation, P4 distinct-path + remap-off negative) all verify faithful to the LOCKED brainstorm. But one NEW Important surfaced from the same repo-global hazard the round-3 fold opened: the hard exit-gate DETECTS the pre-existing-CI RED but sequences NO remediation for two workflow classes whose failing resolution is architecturally unfixable by making the toolkit's vendor/ platform-complete — fuzz-smoke.yml (separate fuzz/Cargo.lock workspace, in ALL 4 repos) and cross-tool-differential.yml's `cargo install --git` of an external crate (toolkit). The plan must converge to 0I before implementation.

## [Important] P0 HARD-EXIT-GATE + P3 per-codec equivalent detect-but-do-not-remediate two repo-global-[source] RED classes that platform-completeness CANNOT fix. The round-3 fold correctly establishes that committing `[source.crates-io] replace-with = "vendored-sources"` is REPO-GLOBAL (reaches every cargo invocation by directory-ancestry config discovery) and adds the hard exit-gate 'the FULL pre-existing CI must re-GREEN.' The fold's remediation model is exclusively 'make vendor/ platform-complete' (verified justified for rust.yml's own jobs: no [target.*] tables; single `cargo vendor --locked` vendors the whole 168-crate graph). BUT two pre-existing workflows the plan itself lists as affected (P0 §REPO-GLOBAL bullet + the preamble) will RED on the exact commit P0/P3 lands, with a fix OUTSIDE that model — because the failing resolution needs crates that are not in the repo's lock graph at all: (1) fuzz-smoke.yml — `cargo fuzz build/run` executes in `fuzz/`, a SEPARATE [workspace]-excluded crate with its own Cargo.lock (toolkit: 117 crates incl. libfuzzer-sys/arbitrary/jobserver NOT in the root's 168-crate lock/vendor; confirmed `fuzz/Cargo.lock` present in ALL 4 repos). Under ancestry config-discovery the root `.cargo/config.toml`'s source-replacement applies to the fuzz build → it resolves through the root vendor/, which lacks libfuzzer-sys/arbitrary/jobserver → RED, and no platform-completeness of the TOOLKIT's vendor fixes it (those crates aren't in the toolkit's lock). (2) cross-tool-differential.yml:53 (toolkit) — `cargo install --git https://github.com/bg002h/descriptor-mnemonic --tag descriptor-mnemonic-md-cli-v0.11.2 md-cli` resolves md-cli's OWN transitive dep graph through the cwd's `[source.crates-io]` redirect → toolkit vendor/ lacks them → RED. The plan treats the exit-gate as a pure go/no-go detector and provides NO authored remediation; the implementer hits a wall the plan didn't sequence a solution for, so P0 (and the P3 per-codec equivalents) are not shippable as written. This is a provenance plan for a custody tool — the gate must come with the fix.

**fix:** Add to P0 (and mirror to the P3 per-repo exit-gate) an explicit remediation for source-replacement bleed into out-of-graph cargo invocations, NOT just a detector: (a) FUZZ — add a `fuzz/.cargo/config.toml` that overrides/neutralizes the inherited `[source.crates-io]` replacement (or run cargo-fuzz with `CARGO_NET_OFFLINE=false` + a config that does not redirect, or commit a `fuzz/vendor/` + per-fuzz [source] block); state which, and gate that fuzz-smoke re-GREENs. Confirm cargo's config-merge semantics for the chosen override (a child `.cargo/config.toml` `[source.crates-io] replace-with` can re-point, but verify it can also DISABLE the parent replacement for this dep set). (b) cross-tool-differential `cargo install --git` — run the `cargo install` step from a directory OUTSIDE the repo tree (so it does not inherit the repo `.cargo/config.toml`), or pass `--config 'source.crates-io.replace-with=""'` / set `CARGO_HOME` to an isolated dir for that step, so md-cli resolves from crates.io normally. (c) re-state the P0/P3 exit-gate as 'pre-existing CI re-GREENs AFTER these isolation edits land in the SAME commit' rather than implying vendor-completeness alone suffices.
## [Minor] Factual drift in the round-3 fold preamble + P3-prelude: the plan repeatedly asserts the sibling repos are ABSENT from this checkout ('confirmed absent: ../descriptor-mnemonic, ../mnemonic-secret, ../mnemonic-key' — lines 6, 167) and that the sibling workflow filenames are 'UNCONFIRMED placeholders.' As of this review all three siblings ARE present at ../descriptor-mnemonic, ../mnemonic-secret, ../mnemonic-key, and the named filenames are in fact correct (md man-pages.yml musl-binaries combined-matrix job confirmed; ms man-release.yml; mk musl-binaries.yml all exist). The descriptor-mnemonic slug + .cargo/config.toml state the plan asserts (status 'phase 1 PARTIAL — status corrected 2026-06-24', remap omitted, codegen-units=1 + strip=symbols, no remap) all verify exactly. This does not change correctness — the P3-prelude recon is still the right discipline (citations decay) — but the 'absent / cannot confirm' framing is now false and would mislead the implementer into thinking recon requires fetching repos that are already on disk.

**fix:** Update the round-3 fold note + the P3-prelude step to reflect that the siblings are present in this checkout and their musl workflow filenames are confirmed (md man-pages.yml combined-matrix; ms man-release.yml; mk musl-binaries.yml), while KEEPING the mandate to re-grep each sibling's live line numbers + matrix structure against origin/master at P3 implementation time (citation-discipline still applies).
## [Minor] rust.yml was edited today (43298598, after the round-3 fold was authored) and the musl matrix now runs `cross test --release ... -- --skip mlock --skip g1_` (line 416) in addition to the `cargo test --target x86_64-musl` the plan cites at '369-405'. The cited line ranges still land correctly (test matrix 79-112, lib cross-platform check 211-265 covering windows-msvc/aarch64-gnu/freebsd, musl matrix 368-416), and `--offline` is confirmed absent everywhere, so the fold's substance holds. But the musl matrix now extends to ~416 (not 405) and includes an aarch64 `cross test` leg under QEMU that ALSO resolves through the repo-global vendor/ — a leg the exit-gate enumeration should name so the implementer confirms it re-GREENs too.

**fix:** Refresh the musl-matrix line citation to 368-416 and add the `cross test --release --target aarch64-unknown-linux-musl` QEMU leg (rust.yml:414-416) to the P0 HARD-EXIT-GATE enumeration of jobs that must re-GREEN through vendor/ (it pulls the cross container's view of the lock graph offline-free; confirm it resolves).