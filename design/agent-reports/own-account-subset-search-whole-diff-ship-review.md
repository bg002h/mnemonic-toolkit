> Reviewer: opus architect (Claude Opus 4.8, 1M context) — mandatory post-implementation WHOLE-DIFF adversarial execution review, immediately pre-tag toolkit v0.70.0.
> Branch `feature/own-account-subset-search` @ `b5594ef8`, clean-FF +28 commits ahead of `origin/master` (`5e7b9dec`). Reviewed delta: `git diff origin/master..HEAD`.
> Scope: cross-phase / integration view over P0–P5 (the per-phase R0 reviews were GREEN in isolation; this catches what they could not see across phase boundaries + ship-readiness).

**Verdict: GREEN — 0 Critical, 0 Important.** The toolkit may be tagged + shipped v0.70.0, subject to the one MINOR (run the TRUE CI rustfmt 1.95.0 gate pre-tag; the local toolchain is provably divergent and cannot certify it — evidence below).

---

## End-to-end funds-safety (whole-feature trace) — PASS

Traced a complete over-supply completion AND its verify-bundle parity end to end.

- **`realized_s` == the enumerated space (sizes the strong prefix).** `complete_multisig_template` (`restore.rs:1796–1922`) computes `realized_s` exactly: `s_opt(k_own, m_cosigners, n, sorted)` (opt-in, `:1831`), `s_own(k_own, j, m_cosigners, sorted)` with `j = n − m_cosigners` (own-only, `:1884`), or `perm_count_u128(n,n) = n!` (exact, `:1921`). The SAME value is passed to `validate_prefix_strength(prefix.len(), realized_s)` (`:1999`) AND drives the `Enumeration` the engine ranks over (`:1955–1972`). The engine's `cardinality()` (`permutation_search.rs:862–878`) returns exactly `s_own`/`s_opt`/`factorial(n)` for the matching variant, so the rank range `[0, S)` the shards scan IS `realized_s`. No cosigner-dropping: `own_anchored_unrank` appends ALL `m` cosigners to every selected own-subset (`:719–722`), and the bijection tests `own_anchored_bijects_s_own_{sorted,nonsorted}` + `opt_in_bijects_s_opt_{sorted,nonsorted}` (all GREEN) prove `unrank` is a bijection onto exactly the cardinality (no over/under-count, no dropped cosigner).
- **Own pool derived PUBLIC-ONLY on the over-supply path.** `restore.rs:1738–1760`: the `over_supply || opt_in` branch derives the `K_own` own candidates via `derive_accounts_xpub_only` (P0), which returns `Vec<(Xpub, Fingerprint)>` and never yields an `Xpriv` — the loop holds only `xpub_to_65(&account_xpub)`. The bare `derive_bip32_from_entropy_at_path` (owns an `Xpriv` in `DerivedAccount.account_xpriv`) is reached ONLY on the `else` branch (`:1764`) — the EXACT `--account`-list path (the tracked-separate 7-site gap, `derive-slot-account-xpriv-scrub-confinement`). Confirmed the over-supply path holds no owned `Xpriv`.
- **Per-slot origins built FRESH (C1).** `build_candidate` (`:1980–1993`) builds `TemplateSlotKey { key65, fingerprint, origin }` from the permuted pool triples and calls `build_keyed_template_descriptor` — the pre-existing fresh-build helper (synthesize.rs NOT in this cycle's diff). Origins come from `own_origin_for(acct)` (`:1714–1728`: `--origin` → cosigner-family-with-account-substituted → canonical fallback), never the carried `path_decl`. L8 coin-type freshness preserved in `canonical_fallback` (`:1664–1672`, substitutes `network.coin_type()` into the coin component).
- **`early_exit=true` ONLY for over-supply collision-free address-search.** `:2080` `let early_exit = over_supply || opt_in;` — set true ONLY in the address-search arm; id-search passes `false` hardcoded (`:2019`); the exact path computes `over_supply=false && opt_in=false` ⇒ `false`. The engine honors `stop_at = if early_exit {1} else {2}` (`permutation_search.rs:1006`), so prefix-id + exact keep the full-scan-with-2nd-match ambiguity certification. The §4.4 contract (collision-free full-scriptPubKey ⇒ a match is provably unique ⇒ first-match safe) holds because the whole-pool distinct-keys floor (`:1929–1930`) makes distinct subsets ⇒ distinct key SETS ⇒ distinct scriptPubKey.
- **All refusals exit ≠ 0, never silently complete.** Verified each refusal returns an `Err` before any output: wrong subset / over-supplied cosigners in own-only (`:1857–1869`, ModeViolation), wrong/unreachable account (NO-MATCH → `RestoreMismatch`, `:2104–2111`), §6 ceilings (`:1811–1823`, `:1843–1848`, `:1886–1891`; `OWN_ACCOUNT_MAX_CEILING`/`COSIGNER_SUBSET_MAX_CANDIDATES`=256, `REALIZED_S_MAX`=1e15, overflow `None`→refuse), weak prefix (`validate_prefix_strength`→`PrefixTooShort`), @N= ⊕ subset-search mutex (`:1612–1618`), `--own-account-max 0` (`:1481`), Ambiguous (`:2113–2120`). The property test `subset_search_unreachable_own_account_no_match_refuses` proves an own range that cannot reach the true account is a LOUD exit-4 NO-MATCH against the live binary.
- **verify-bundle parity.** `verify_multisig_template` (`verify_bundle.rs:833–1016`) threads `own_account_max` + `search_cosigner_subset` into the IDENTICAL `MultisigCompletionCtx` and calls the SAME `complete_multisig_template`. There is no second copy of the funds-safety logic — verify == restore by construction. The completed descriptor is recomposed via the IDENTICAL `candidate_descriptor_string`. `verify_bundle_own_account_max_completes_at_nonzero_account` GREEN.

**Independent-oracle gate (the make-or-break).** `prop_subset_search_roundtrip.rs` asserts completed first addresses == an INDEPENDENT rust-miniscript `derive_at_index` golden built from the ORIGINAL concrete descriptor (NOT md-codec reconstruction). The own account is RANDOM in `0..5` (not always 0), `K` is always strictly over-supplied (`own_acct+1+slack > 1`), so every case drives `S_own = C(K,1)·N! > N!`. Anti-vacuity cells are PERMANENT and discriminating: `oracle_own_account_change_changes_address_subset_axis` (different own account ⇒ different address — the subset axis), `opt_in_outsider_is_genuinely_distinct` (the dropped outsider is a real non-member), `cosigner_subset_search_drops_outsider_completes_to_golden` (golden has only real cosigners ⇒ a match proves the outsider was dropped). This is a non-vacuous independent oracle along exactly the axis the feature resolves. GREEN.

## Exact-path byte-invariance (v0.60.0/v0.69.1) — PASS

`pool.len()==n` (exact) keeps `realized_s = perm_count_u128(n,n) = n!` (`:1921`), `Enumeration::FullPermutation { n }` (`:1971`), `early_exit=false`, the bare `derive_bip32_from_entropy_at_path` (`:1764`), and the sorted-collapse evaluator-filter `apply_identity_filter = sorted_shape && !over_supply && !opt_in` (`:1975`). The shared `search()` wrapper drives `FullPermutation` (`permutation_search.rs:963–969`), whose `cardinality()=factorial(n)` and `unrank=unrank_permutation` — byte-identical to the pre-subset engine. Guarded by `exact_pool_address_search_byte_regression_guard` + `early_exit_false_reproduces_v060_full_scan_outcomes` (both GREEN). The 470-line restore.rs diff is additive around the exact path; the exact branch's behavior is unaltered.

## Hygiene (P0) consumed + scrub intact — PASS

`derive_account_xpub_only`/`derive_accounts_xpub_only` are now genuinely CONSUMED by the over-supply path (`restore.rs:1746`, P2/P3) and reached on verify-bundle via the shared engine (P4) — the `#[allow(dead_code)]` is correctly removed from `derive_accounts_xpub_only` (the still-additive single-account form + master/one helpers keep it). `ScrubbedXpriv` (`derive_slot.rs:195`) is move-only by construction: `impl Drop` ⇒ `Copy` is E0184-blocked; `Clone`-absence is pinned at COMPILE TIME by the `AmbiguousIfImpl<_>` `const _: fn()` static assertion (`:379–399`); no `into_inner`/`Deref`/escape hatch — only `&self` `xpub`/`fingerprint`. Drop scrubs `private_key.non_secure_erase()` + a VOLATILE `chain_code` zero-write (`:217–239`). The broader 7-site lift is correctly NOT done — `DerivedAccount.account_xpriv` is untouched; the FOLLOWUP slug is narrowed to that lift. TDD-3 byte-identical golden (`derive_account_xpub_only_byte_identical_to_bare_helper`) proves the scrub-confining helper did not perturb the keys. GREEN.

## SemVer / version-site / manual — PASS

- **MINOR justified.** Additive flags (`restore --search-cosigner-subset`, `verify-bundle --own-account-max`, `verify-bundle --search-cosigner-subset`) + the `restore --own-account-max` refuse→search behavior change is additive (a previously-REFUSED input now works; no existing-success path changed — confirmed by exact-path byte-invariance). 0.69.1 → 0.70.0 correct.
- **All 7 version sites at 0.70.0**, none at 0.69.1: `git grep '0\.69\.1' -- ':!design/' ':!*recon*' ':!CHANGELOG.md' ':!*.md'` → NONE. 0.70.0 confirmed at Cargo.toml:3, README.md:13, crates/.../README.md:9, Cargo.lock:727, fuzz/Cargo.lock:575, scripts/install.sh:32 (self-pin tag).
- **Manual flag-coverage lint RUN — `[lint] OK`** via `make -C docs/manual lint` with the 4 real binaries (mnemonic debug + md/ms/mk release). Step 4/6 flag-coverage passes: every new flag is documented in `41-mnemonic.md` (restore §727/§987–988 + the prose §240–293; verify-bundle §727–728). markdownlint + cspell + lychee + glossary + index also clean. CLI `--help` confirms all 3 new flag names are live on both subcommands.
- **CHANGELOG accurate**: MINOR, the 3 additive flags + the refuse→search Changed entry, the bounds (`K_own ≤ 256`, `S_opt ≤ 1e15`, time-cap), funds-safety (`realized_s` sizing, public-only derivation, all refusals exit≠0), NO-BUMP md/mk/GUI, and the lockstep debt note.

## FOLLOWUP flips — PASS

- `template-multisig-own-account-range-subset-search` → ✓ **RESOLVED (v0.70.0)**, with the accurate "shipped at 0.70.0 not the projected 0.61.0 — flipped in the shipping commit" note. The "Fix (deferred)" framing is superseded by the COMPLETE design record; `realized_s = S_own` refinement (vs the looser `P(pool,N)`) is documented.
- `derive-slot-account-xpriv-scrub-confinement` → `open` — **NARROWED (v0.70.0)**: P0 delivered the minimal additive helper; the stale "subset-search ... does NOT author this" Origin line is corrected to "P0 ... AUTHORED the minimal additive helper"; the remaining open scope is precisely the 7-site `DerivedAccount.account_xpriv → ScrubbedXpriv` lift. Both correct.

## Lockstep-debt recorded — PASS

3 new clap flag NAMES owe the GUI `schema_mirror`: `restore --search-cosigner-subset`, `verify-bundle --own-account-max`, `verify-bundle --search-cosigner-subset`. The manual is done (P6.1, lint GREEN). The CHANGELOG + manual both record the GUI schema-mirror as the NEXT (post-tag) paired-PR step owned by the orchestrator. No flag is unaccounted-for (the schema_mirror gate is a lagging indicator per CLAUDE.md; the leading paired-PR discipline is recorded).

## Regression / build (RAN) — PASS

- **`cargo test -p mnemonic-toolkit`: SUMMED 3482 passed / 0 failed / 16 ignored across 196 `test result:` lines** (full-log capture — the first partial grep-piped captures raced the buffering and undercounted; the authoritative complete-log sum is 3482, matching the expected ~3482). New suites green incl. the bijection tests, the exact-path byte regression guards, the independent-oracle property + anti-vacuity cells, restore + verify over-supply gate tests, and the bitcoind over-supply differential row (own@3, `--own-account-max 5`, vs Core `deriveaddresses`, env-gated).
- **`cargo clippy -p mnemonic-toolkit --tests --bins --lib -- -D warnings`: Finished, exit 0** (no warnings).
- `git diff --name-only origin/master..HEAD` EXCLUDES `mlock.rs` (confirmed).

## Panic-scan (funds-safety path) — PASS

The generators' internal `.expect()`s (`unrank_permutation:499`, `unrank_kperm:651`, `unrank_combination:674`, `own_anchored_unrank:714`, `opt_in_unrank:756/764–765`) all divide/consume the SAME cardinality that the engine validates up-front: `search_enumerated` calls `enumeration.cardinality().ok_or(SearchSpaceTooLarge)?` (`:1012–1014`) BEFORE any `unrank` — the M1/P1-M-1 discipline. So an overflowing/garbage cardinality REFUSES (typed error) and the `.expect()`s are unreachable on attacker-controlled overflow. Cardinality helpers are overflow-checked `Option<u128>` (`c_choose`/`p_count`/`factorial`/`s_own`/`s_opt`), and the restore-side computes them `Some`-first (`:1831`, `:1884`, `:1921`) before any consumer. `opt_in_unrank` returns `Vec::new()` (not panic) for an out-of-domain rank. No `.unwrap()`/`.expect()`/`panic!` on user/attacker data found on the completion path; CLI input parsing uses `?`-propagated `bad(...)`. The address-search evaluator swallows build/parse errors as `false` (no-match, fail-safe) rather than panicking.

---

## MINOR

- **M1 (fmt — verify against TRUE CI 1.95.0 pre-tag; NOT a blocker).** The CI fmt gate is `cargo +1.95.0 fmt --all -- --check` (`.github/workflows/rust.yml:50–76`, pinned 1.95.0, mlock.rs exempt). The locally-installed "1.95.0" toolchain is actually `rustfmt 1.9.0-stable (59807616e1 2026-04-14)` and is PROVABLY divergent from CI: a package-wide run flags ~30 already-green UNCHANGED files (error.rs, lib.rs, synthesize.rs, bundle.rs, convert.rs, …) that shipped on master, and master's own `restore.rs` shows 2 hunks under this same local toolchain — exactly the "local +1.95.0 churns already-green files" scenario. Single-file checks: `permutation_search.rs`, `verify_bundle.rs`, `derive_slot.rs` are CLEAN under both local 1.8.0 and local-1.95.0. `restore.rs` is flagged at 3 hunks: TWO (lines ~430/861, the pre-existing `passphrase` `Zeroizing::new(if …)` wraps, blame `db0bf583`) are present on CI-green master too ⇒ local divergence, NOT a finding; ONE (line ~1828, the cycle-new `ps::s_opt(...).ok_or_else(|| bad("…long string…"))?`, blame `d1fc4a35`) is a version-sensitive long-string closure-reflow — the structurally-identical sibling `ps::s_own(...).ok_or_else(...)` at `:1884` (same cycle) is NOT flagged, confirming a line-length boundary effect rather than a systematic mis-format. Because the local toolchain demonstrably mis-flags CI-green code, it cannot certify the new line either way. **Fix:** before tagging, run the genuine CI rustfmt 1.95.0 (`cargo +1.95.0 fmt --all -- --check` with the ACTUAL 1.95.0 toolchain, or let the CI fmt job adjudicate); if it flags `restore.rs:1828`, reformat that one hunk single-file and amend. This is a CI-cosmetic gate, touches no funds-safety, and the per-phase commits were R0-GREEN; it is a pre-tag verification item, not a ship blocker.

No Critical or Important findings. The funds-safety invariants — completed-addresses == independent oracle, exact-path byte-invariance, own-anchored bijection / `realized_s` == enumerated, public-only over-supply derivation, refuse-never-silent — all hold end-to-end. **GREEN: tag + ship v0.70.0** (resolve M1's CI-fmt verification as part of the tag mechanics).
