# Plan R0 refined-1 — C0/I0/M4 green=True

GREEN — 0 Critical / 0 Important. The refined job-scoped-[source] plan is internally consistent, faithful to the LOCKED brainstorm, and the repo-global-bleed apparatus is removed cleanly. Implementation may proceed per-phase (R0→TDD→single-implementer→persisted review). 4 Minors are polish, none gate the start of P0.

Verified against the six refinement criteria:

(1) JOB-SCOPED [source] CORRECTLY APPLIED. Every phase (P0 repro-substrate, P1 build, P2 cross, P3 per-repo) activates vendor/ via `cargo build --config 'source.crates-io.replace-with="vendored-sources"' --config 'source.vendored-sources.directory="vendor"'` — never a committed root [source] block. Isolated-$CARGO_HOME/config.toml recorded as the fallback for tools that can't take --config (and for the git-fork stanza if CLI quoting is awkward). I EMPIRICALLY CONFIRMED on this box's cargo 1.85.0 (the pin) that `cargo build --offline --config source.crates-io.replace-with=... --config source.vendored-sources.directory=...` resolves a vendored dep fully offline with an empty CARGO_HOME — the core mechanism is live-valid. The git-fork third-block requirement (R0-r5-C1, that a [patch.crates-io] git dep needs its own `source."git+...".{git,rev,replace-with}` block or --offline FAILS) is internally consistent with cargo source-replacement semantics; Cargo.toml:28-29 + Cargo.lock:700 confirm the exact rev/source-key the brainstorm cites.

(2) REPO-GLOBAL-BLEED APPARATUS FULLY REMOVED + NO INCONSISTENT LIVE REFERENCE. Scanning the active phase bodies (line 124+), every mention of the deleted apparatus (rust.yml workflow_dispatch self-trigger, fuzz/.cargo/config.toml neutralizer, the 6-site cargo-install isolation edits, sibling-pin-check token-order constraint, the HARD EXIT-GATE) appears ONLY inside an explicit REMOVED/moot callout that states WHY it's gone. The sole live `workflow_dispatch` in the active body (P4 negative-test gating) is a legitimately distinct use, not a survivor of the deleted rust.yml edit. The two cross-phase invariants that asserted repo-global bleed are struck (~~...~~) with supersession notes. The struck/historical R0 rounds 3→7 fold notes correctly retain the apparatus as audit-only. Removal is self-consistent.

(3) COMMITTED vendor/ INERTNESS — verifier present and logically sound. repro-substrate proves vendor completeness (positive: offline build resolves) AND activation-dependence (negative sub-check: drop the --config / remove vendor → --offline REDs). The inertness claim itself rests on the structural fact that committed source files cause no cargo behavior without a [source] block — correct, and the P0 LIGHT EXIT confirms pre-existing CI is unchanged rather than gating a full re-GREEN (justified: nothing redirects those jobs). Live-verified there is no existing root .cargo/config.toml to conflict with.

(4) REST INTACT + FAITHFUL. Re-home: per-STEP `docker run --network=none ...@sha256` on the x86_64 (musl_native) leg only — live-verified man-pages.yml is a single combined-matrix job (94-145) so job-level container: is correctly forbidden; build-native 120-124, build-cross 125-127, host apt 117, CC_x86_64 123 all match the plan's cited lines EXACTLY. Network boundary (build+package in --network=none; checkout/ensure-release/upload host-side with network, reading container-built artifacts off a bind-mounted path) faithful to brainstorm R0-r2-I-network-boundary. Two-path byte-identical (/build-a vs /build-b, R0-r3-I1) carried in P1/P2/P4. cc-under-musl validation (epoch-load-bearing probe + .comment + __DATE__/host-path residue grep) intact. SOURCE_DATE_EPOCH=$(git show -s --format=%ct <commit-SHA>) keyed off commit SHA not tag, faithful. gzip -n at BOTH tar sites — live-verified man-pages.yml:50 (mnemonic-man.tar.gz, correctly flagged hygiene-only/not provenance-hashed) and :133 (the SHA256SUMS-hashed binary tarball). P4 distinct-path + remap-off NEGATIVE test present with the closed-loop precondition (P4 meaningful only after P1/P3 release re-home lands). md FOLLOWUP correctly RE-SCOPED (do-not-re-edit-already-corrected-status a759c79; add the actual remap; flip slug RESOLVED in shipping commit). NO-BUMP justified. P3-prelude recon mandatory + correctly RELAXED (siblings only need the musl-workflow filename/matrix/re-home recon now, not the out-of-graph sweep). NO [target.*] tables — live-verified empty (grep exit 1).

(5) EACH PHASE EXECUTABLE + INDEPENDENTLY SHIPPABLE. P0 substrate / P1 x86_64+re-home / P2 aarch64 / P3 codecs / P4 drift-gate each carry their own GATE, EXIT CRITERIA, R0→TDD→review discipline, and NO-BUMP independence. The P4-needs-P1/P3-rehome ordering is a stated precondition, not a shippability violation.

(6) NEW FROM THE REFINEMENT: 0 Critical, 0 Important. The refinement strictly RELAXES burden (removes containment for a hazard it no longer creates; vendor/ platform-completeness now only covers the repro target set). No new funds/provenance risk introduced — the no-live-host-fetch provenance is preserved (vendor/ still committed, git-fork materialized), and the --offline mandate is preserved (three-block --config). 4 Minors below.

## [Minor] GIT-FORK THIRD-BLOCK PRESENTATION INCONSISTENCY IN THE PLAN. The brainstorm (post R0-r5-C1) is emphatic and explicit that the two-block --config form (source.crates-io.replace-with + source.vendored-sources.directory) FAILS --offline for the miniscript [patch.crates-io] git dep, and that ALL THREE cargo-vendor-emitted [source] blocks (including source."git+...rev=95fdd1c5...".{git,rev,replace-with}) are MANDATORY — it spells the full multi-flag command verbatim (§3.12/§4/§8/F4). The PLAN, by contrast, repeatedly writes the TWO-block form as the headline command (phase table P0 row; P0 repro-substrate gate cmd; P1 build cmd line 140; P2 cross cmd; P3 per-repo cmd) and relegates the mandatory git-fork block to a trailing parenthetical 'plus the third --config git-source override OR the isolated-$CARGO_HOME fallback ... record the chosen form in P1's R0.' Because R0-r5-C1 EMPIRICALLY PROVED the two-block form fails --offline (it reaches the live github host), an implementer who lifts the headline two-block command literally would author a repro-substrate/P1 build that either RED-fails --offline or silently fetches the fork from a live host — defeating the no-live-host provenance this whole cycle exists to establish. Not Important because the plan DOES carry the requirement in every parenthetical and defers the 'chosen form' to P1's R0, and the brainstorm (the locked design) is unambiguous — but the headline/parenthetical inversion is a foot-gun.

**fix:** Promote the git-fork block from a trailing parenthetical to part of the headline --config command everywhere it appears in the plan (P0 gate, P1, P2, P3), OR add a one-line bold note at first use: 'the two-block form shown is ABBREVIATED — the git-fork source."git+..." block is MANDATORY (R0-r5-C1: two-block FAILS --offline); see brainstorm §3.12 for the full verbatim command.' Make P1's R0 deliverable explicitly include 'record the full multi-block --config string actually used, copy-exact from brainstorm §4.'
## [Minor] P0 NEGATIVE SUB-CHECK '--offline with vendor/ absent' IS AWKWARD GIVEN vendor/ IS COMMITTED. The P0 repro-substrate negative sub-check is stated as 'a second step that DROPS the --config source.vendored-sources.directory override (or runs --offline with vendor/ absent) MUST RED.' The 'drop the --config override' arm is clean and executable in CI (the directory still exists; cargo just isn't pointed at it → --offline can't resolve crates.io → RED). But the 'vendor/ absent' alternative is harder to stage as a committed CI step since vendor/ is committed — it would need a transient rm or a separate fixture, which the plan doesn't sequence. The OR makes it look like either is trivially available.

**fix:** Drop the 'or runs --offline with vendor/ absent' alternative (or mark it as a local-only sanity check), and standardize the committed CI negative on the clean 'drop the --config source.vendored-sources.directory override → --offline RED' arm, which needs no filesystem mutation.
## [Minor] P1 BRAINSTORM-ONLY 'no-leak' ACCEPTANCE CHECK NOT MIRRORED INTO THE PLAN. Brainstorm §6/P1 (line 168) adds an 'Acceptance check (THE REFINEMENT): assert the repro [source] redirect does NOT leak into the non-repro cargo jobs — i.e. the test/check matrix + fuzz/ + the cargo install --git <sibling> doc-verify jobs still resolve their full dep graphs (they would RED if a committed root [source] block had been used instead of the job-scoped --config).' The PLAN's P0/P1 do not carry an equivalent explicit positive assertion; they rely on the structural argument ('inert ⇒ unaffected') plus a LIGHT confirmation. Given this is a provenance/custody tool and the entire refinement turns on 'job-scoped, not repo-global,' a concrete CI observation that one representative out-of-graph job (e.g. fuzz-smoke or a cargo-install-git doc job) still GREENs on the P0 PR is cheap insurance that no accidental committed [source]/`.cargo/config.toml` slipped in.

**fix:** Add to P0 EXIT (or P1 acceptance) the brainstorm-line-168 positive check: confirm at least one out-of-graph cargo job (fuzz-smoke or a cargo-install-git doc-verify) resolves normally on the P0/P1 PR — concretely proving the committed vendor/ introduced no global redirect, rather than only asserting inertness structurally.
## [Minor] P3 PER-REPO GIT-FORK STANZA SET IS REPO-SPECIFIC AND UNVERIFIED AT PLAN TIME. P3 correctly says each codec generates its own vendor/ and activates 'that repo's git-source --config override/fallback for whatever git-source stanzas its cargo vendor emits — generate per repo, record the keys.' Good. But the toolkit's mandatory-git-block lesson (R0-r5-C1) was learned BECAUSE the toolkit carries a [patch.crates-io] git dep; whether md/ms/mk each carry one (and thus each need the git-block treatment vs the two-block form sufficing) is unverified — the siblings aren't in this checkout (correctly noted). An implementer could under-apply (omit a needed git block → that repo's --offline fails/leaks) or over-apply.

**fix:** Fold into the P3-PRELUDE RECON (already mandatory) a per-repo step: grep each sibling's Cargo.toml for [patch.crates-io]/git= deps and its Cargo.lock for git+ source keys, run `cargo vendor` per repo, and RECORD the exact [source] block set each emits — so each repo's job-scoped --config string is derived from its own vendor output, not assumed from the toolkit's.