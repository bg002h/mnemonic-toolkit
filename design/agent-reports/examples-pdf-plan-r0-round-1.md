# Plan R0 — round 1 — `examples-pdf-un-ci-gated` (Examples.pdf modernize + gate)

- **Reviewer:** opus architect (adversarial plan-doc R0 gate; hard gate 0C/0I before any code).
- **Plan under review:** `design/IMPLEMENTATION_PLAN_examples_pdf_modernize_and_gate.md` (opus-authored, stamped `37472e25`).
- **Composes (not re-litigated):** R0-GREEN spec `SPEC_examples_pdf_modernize_and_gate.md` + spec-R0 `examples-pdf-spec-r0-round-1.md` (rulings BINDING) + the 6 locked user decisions (B-static; gate A WIDE+REQUIRED; Q1→(b) freeze §6.6; Q3 keep frozen depth2 transcript; Q4 keep committed PDF+attach; pin 0.55.3→0.75.0 source-built).
- **Source verified at:** `origin/master` = **`b0e7ce0b`** (the plan-doc's own commit; the plan stamps `37472e25` == HEAD-at-write-time). `git diff 37472e25..b0e7ce0b` = **only the plan file**; `git diff a98559c6..b0e7ce0b -- .examples-build/ scripts/install.sh .github/ Cargo.{toml,lock}` = **empty**. **Zero citation decay** — every `gen.sh`/`install.sh`/workflow/baseline line I re-grepped holds at `b0e7ce0b`.

---

## VERDICT: GREEN — 0 Critical / 0 Important. **Begin P1.**

The plan is a **faithful, complete realization** of the R0-GREEN spec + all 6 locked decisions. Every one of the 11 pin sites, both B-static freezes, all five determinism fixes, the four minor-folds, the gate wiring, and the STOP ledger check out against live source. The determinism proof is **adequate to guarantee the gate won't flap** — not merely asserted; the P2 born-green PR *executes* a real author≠runner cross-machine diff before the gate governs master, backstopping any unforeseen vector. The gate **would catch the drift class that caused this cycle** (a toolkit output-format change reddens step 6). Findings are **Minor only** (two off-by-one citation nits + two belt-and-suspenders notes) — none blocks the start of P1.

**USER decisions owed before their phase (the plan correctly surfaces all of them; encode before crossing the gate):**
- **OQ1 / S4 — branch protection on toolkit `master` (owed before P2). GENUINE USER DECISION — confirmed live-unprotected.** `gh api repos/bg002h/mnemonic-toolkit/branches/master/protection` → **HTTP 404 "Branch not protected."** Making `examples` a *required* check **establishes branch protection on toolkit master for the first time** — a repo-governance change affecting **all** pushes (direct-push has been the session norm), beyond this cycle's docs scope. **My recommendation: proceed with the plan's minimal form** — `contexts=[examples]` only, `enforce_admins` OFF (verbatim GUI `snapshots` precedent) — it is the whole anti-rot point and preserves the codec/toolkit direct-FF release flow (memory gotcha (d)). But the *act of first-time-enabling* protection needs an explicit user yes. The plan does **not** over-reach (explicitly: "do not over-scope to unrelated jobs without a separate user decision"; `rust.yml` etc. are NOT enrolled). Sound + sufficient.
- **Q2 (leading triggers + required-vs-advisory) — resolved by locked-decision 2 (WIDE + REQUIRED).** Already a user lock; the plan encodes it. The recurring per-release "re-pin+regen in the same PR or go red" cost is the accepted consequence — confirm the user still owns it (it changes the release ritual for all future toolkit work). Surfaced as S3.
- **Q5 (housekeeping, non-blocking):** flag stale `.docbins` (`current`=0.72.0, `gui-pinned`=0.70.0) to the user; the cycle already bypasses `.docbins` (CI source-builds).

---

## Faithful-realization ruling: **FAITHFUL + COMPLETE.**

**Pin sites (11) — all verified, none missed.** `grep -rn '0\.55\.3' .examples-build/` = 9 literal `gen.sh` sites (`:3,:22,:87,:104,:531,:696,:709,:716,:724`) + `.gitignore:5` = 10 version-string sites; **+ coupled `date:` `gen.sh:88`** = 11. The P1.a table addresses all 11: enforcement `:22`→`"mnemonic 0.75.0"` (crate `Cargo.toml` = **0.75.0** verified → source-built binary passes), header `:3`, subtitle `:87`, static date `:88` (never `$(date)`), intro `:104`, Appendix A `:696`, `.gitignore:5` (pin + framing reword), disambiguated history `:531`, `:709`→v0.75.0, obsolete same-version framing `:716-717` (verified the quote spans both lines), `:724` deleted by B-static. `gen.sh:666` (v0.49.1) correctly left as history. **No mixed-tier regen risk.**

**Determinism fixes — all five correct, verified against source:**
- Script-relative `REPO` (`gen.sh:15` — currently hardcoded `/scratch/...`). ✓
- Binary override `EXAMPLES_BIN_DIR` prepended (`gen.sh:18`). ✓
- Single-quote the two `install.sh` `run` calls (`gen.sh:179-180`) — **confirmed currently double-quoted**, leaking `/scratch/...` into the display (baseline `Examples.md:98,109`). Flip yields literal `sh "$REPO/scripts/install.sh"` while `eval` still expands. ✓
- Env scrub `unset XDG_DATA_HOME CARGO_INSTALL_ROOT; export HOME=/home/user; export LC_ALL=C LANG=C TZ=UTC` — **verified install.sh derives its path output from EXACTLY those three** (`:75` `MAN_DIR="${XDG_DATA_HOME:-$HOME/.local/share}/..."`, `:322`/`:336` `${CARGO_INSTALL_ROOT:-$HOME/.cargo/bin}`, dry-run mkdir/gen-man `:324,:327`); grep confirms **no fourth path-deriving env var**. Baseline leaks `/home/bcg/.cargo/bin`, `/home/bcg/.local/share/man/man1` (`Examples.md:111,116-136`) → normalize to `/home/user/...`. ✓
- Strict preflight (jq + python3), **bitcoind/bitcoin-cli DROPPED per Q1-b** — **verified safe**: `grep '^run .*bitcoin'` = zero executed bitcoind/bitcoin-cli (the §4 `bitcoin-cli` lines `:349-351` are `show` args, not run; the only executed §6.6 bitcoind block `:656-679` is deleted by P1.e). Executed tool set = `mnemonic` + python3(2) + jq(7) + sed(20). ✓

**B-static (P1.d) — correct.** Delete the `if command -v mnemonic-depth2 … else … fi` runtime branch (`:724`…else `:764`…**fi `:774`**); keep LIVE the mainline refusal `:749` (standing per-CI-run assertion the cap still holds — verified byte-identical at 0.75.0 by spec-R0), `cat taproot-4leaf.desc` `:735`, `bundle|jq>depth2.md1` `:742`, `cat depth2.md1` `:743`; **freeze** the `mnemonic-depth2 restore` transcript with a loud label. **Frozen-capture source verified faithful** against baseline `Examples.md:1596-1617`: `EXPERIMENTAL` advisory `:1598`, descriptor `tr([73c5da0a/84'/0'/4']…{{…}})#5trrgdg0` `:1601`, `first recv: bc1p6yc7kzttzsafprr6hwsaefuyqxvee4j48zdrqt4kl9ers68mhcestwvn66` `:1602`, 12-cosigner block `:1603-1614` (header `:1599` "12 cosigners"), `UNVERIFIED` `:1615`, `note: stdout is watch-only` `:1616`. Fold/drop the two `--version` runs `:729-730` + intro `:725-727`. Exemption is structural → zero CI carve-out. ✓

**P1.e §6.6 freeze (Q1-b) — the supersession is COHERENT, not a contradiction.** The spec's D1.4 chose (a) keep-live pinned-Core-in-CI *and explicitly surfaced the alternative as an open question needing sign-off (S2)*; spec-R0 ruled Q1 an architect call and **recommended (b) static-labelled capture**; the user then **locked Q1→(b)**. The plan implementing (b), marking D1.4-(a) "SUPERSEDED," and retiring S2 is the R0-recommended + user-locked resolution of a question the spec itself left open — fully coherent. Mechanically verified: delete the `if command -v bitcoind … else … fi` block `:656-679`; drop `RECV=$(…)` `:649` (**confirmed `$RECV` referenced ONLY at `:649,:660,:662,:673`, nowhere else**); keep lead-in `:651-655` + the "Byte-for-byte the same `bc1p…`" prose `:665-670` (which carries the kept v0.49.1 attribution `:666`); replace the executable middle with a labelled static heredoc of the `deriveaddresses` command + `["bc1p550…00n8c8"]` from baseline `Examples.md:1474-1475`. **Strengthening fact the plan under-sells:** the frozen address `bc1p550…00n8c8` is *also* re-derived LIVE every CI run by the kept `run 'mnemonic restore … taproot-multi.md1'` `:648` (baseline `first recv:` `Examples.md:1461` == the frozen `deriveaddresses` output `:1475`) — so a future 0.75.0 change to that address would drift the LIVE line and RED the gate (S1). The freeze is doubly-safe. P1(v) + P3 backstop the descriptor-string faithfulness. ✓

**Coupled prose covers ALL output-describing heredoc lines — VERIFIED COMPLETE.** `grep -niE 'twice|unbroken|grouped|hyphen'` = **exactly `gen.sh:209-210`** (the "printed twice" time-bomb). An extended sweep (`5-char|block|space|one line|per card|printed|separator|dash|subcommand|two rows|gen-man`) finds **no other prose describing card format/counts, mk1 line-wrapping, md1 separators, or `--help` row counts** — all other "block" hits are descriptor/miniscript/LaTeX. `:183` "list every subcommand" and `:103` "Every command below" stay true (no count claimed). The P1.c set is complete; the "printed twice→once" rewrite is the only coupled edit the 4 card-block changes demand.

## The 4 minor folds — each CORRECT.
- **M1 (P1→CI §6.6 handoff) MOOTED by Q1-b.** With §6.6 a static heredoc and zero bitcoind in CI, the "generate the golden under the same pinned Core" concern vanishes — the block is literal text, whole-file-diff-covered. Residual faithfulness = P1(v) author-time check + P3 whole-diff, S1-backstopped. Correct.
- **M2 (root `Cargo.toml` trigger) FOLDED.** Added to `examples.yml` triggers as belt-and-suspenders alongside `Cargo.lock` (a `[patch.crates-io]` miniscript-rev bump touches both). Correct.
- **M3 (SHA re-stamp) FOLDED.** Stamped `37472e25`; I independently confirmed no `.examples-build/`/`install.sh`/workflow citation moved between `a98559c6` and current HEAD. (The plan's "== local HEAD" now lags by exactly its own commit — an unavoidable self-reference, not decay.) Correct.
- **M4 (required-check) FOLDED.** `examples` becomes a required branch-protection context, `enforce_admins` off, GUI `snapshots` precedent. Correct — and correctly re-surfaced as OQ1/S4 because master is live-unprotected.

## Determinism-proof-adequacy ruling: **ADEQUATE — the gate will not flap. Proofs are EXECUTED, not asserted.**

The P1 gate runs six proofs in the worktree: (i) double-regen byte-identical; (ii) cross-env byte-identical under two **real** `$HOME`s; (iii) depth2-absent-vs-present byte-identical; (iv) diff ⊆ §3 enumeration (any address/xpub/descriptor/checksum/fingerprint delta = STOP S1); (v) §6.6-freeze faithfulness vs live 0.75.0; (vi) pandoc renders clean. I enumerated the residual vectors:

- **Path leaks** — `REPO` (display, single-quoted) + `HOME`/`XDG`/`CARGO` (scrubbed, verified as the *complete* derivation set). (ii) proves the scrub decouples output from author HOME.
- **Locale** — `LC_ALL=C` is *set inside gen.sh*, so the runner's ambient locale cannot leak; no need to vary it in the proof (it's pinned, not observed).
- **`--help` / `bundle` wrap-width** — spec-R0 proved **width-INSENSITIVE as a fixed property of the 0.75.0 binary** (byte-identical under `COLUMNS=40/200/unset`, captured non-TTY via `$( … 2>&1)`). Because captures are never a TTY and the binary is width-inert, an ambient `COLUMNS` on any runner cannot diverge output. Settled — no per-machine re-proof needed.
- **`$(date)`/`$USER`/`hostname`/`mktemp`** — spec-R0 full sweep = zero hits; the one `mktemp` was in the now-deleted §6.6 block. I re-confirmed the path-leak and executed-tool sets independently.

**The airtight backstop is P2's born-green PR** (the plan's explicit closer, GOTCHA #1 + P2 positive gate): the golden is generated on the **author** machine, committed, then CI on **ubuntu-latest** (a different machine) regenerates and diffs it. Any surviving machine-dependent vector reddens the *first* CI run **before merge**. This is a genuine author≠runner cross-machine execution proof, not an assertion. Combined with (i)-(iv), the gate-flap risk is closed. **Confirmed adequate.**

*(Belt-and-suspenders note, Minor-3 below: the scrub sets `HOME=/home/user`, likely nonexistent on the runner. This cannot cause a **flap** — author and CI both scrub to the same value — worst case a `mnemonic` "cannot write $HOME" warning would appear *identically* in both, and gate (iv) diff-⊆-§3 would surface it as an unenumerated hunk at P1. Layered gates cover it; flagged only for the implementer's awareness.)*

## Gate-would-catch-the-drift confirmation: **YES.**

The drift class this cycle exists to catch is a toolkit output-format change (the "printed twice→once" that rotted the committed PDF). Under `examples.yml`: step 5 regenerates `.examples-build/Examples.md` with the freshly source-built binary; step 6 `git diff --exit-code -- .examples-build/Examples.md` REDs the job when regen ≠ committed golden. Any format/`--help`/version/install-pin change → regen diverges → red, with the drift verbatim in the CI log. The WIDE triggers (`crates/**` + `Cargo.lock` + `Cargo.toml` + `install.sh`) make it a **leading** indicator — it fires *in the PR that changes the output*, not lazily. Spec §7's claim "the three-era patchwork could not have survived this gate" holds. The **negative red-CI proof is specified** (P2 gate: perturb one golden byte → observe RED at step 6 → drop the scratch commit; "do NOT ship without observing the red"). The `.gitignore` flip (remove line 9) + `git add` the golden + `examples.yml` land in **one atomic commit** → born-green, no transient red. CI builds `mnemonic` **from source** (`cargo build --bin mnemonic` + `EXAMPLES_BIN_DIR=target/debug`), not the stale `.docbins` — the 0.75.0 source binary satisfies the `gen.sh:22` pin. All confirmed.

## OQ1 branch-protection ruling: **USER DECISION owed before P2. Recommend the plan's minimal form.**

Toolkit `master` is **live-unprotected** (verified 404). Establishing protection is a governance change beyond this cycle's docs scope, so the *decision to enable it* is the user's. The plan's minimal shape — `contexts=[examples]` only, `enforce_admins` OFF, no enrollment of `rust.yml`/other jobs — is **sound and sufficient**: it gates the drift class while preserving admin direct-FF releases (the standing codec/toolkit release flow). Sequencing is correct (add the required context only *after* the PR's `examples` job is green once). **Recommendation: approve the minimal form.** S4 correctly gates the only divergence (if `enforce_admins` can't be set off, or if it would block direct-FF releases) back to the user.

## Findings by severity

**Critical:** none.
**Important:** none.

**Minor:**
- **M-1 (citation off-by-one — depth2 `fi`).** P1.d and the citation appendix cite the depth2 block as `724`…else `:764`…**`fi` at `:775`** / range `724-775`. Live: `gen.sh` is **774 lines**; `else` at `:764` (✓), else-fallback heredoc `:765-773`, **`fi` at `:774`** (the last line). The spec had this right (`724-774`); the plan drifted +1. Impact negligible (the deletion is bounded by matching `if`/`fi` tokens and `fi` is EOF), but correct the implementer's target to **`:774`** so the plan's "all line numbers hold verbatim" claim is true.
- **M-2 (citation off-by-one — §6.6 static pair).** P1.e cites the frozen `deriveaddresses` command+address at baseline `Examples.md:1474-1476`. Live: command `:1474`, `["bc1p550…"]` `:1475` (no `:1476` content). Trim to `1474-1475`. Cosmetic (baseline is an untracked artifact); does not affect the freeze content, which I verified faithful.
- **M-3 (scrubbed HOME is a nonexistent dir).** `export HOME=/home/user` points at a likely-absent path. Cannot flap the gate (author≡CI, deterministic) and is caught by gate (iv) if any tool warns — but the implementer could pre-empt by pointing HOME at a `mktemp -d` (still scrubbed/stable) OR simply confirm during P1 that no executed tool touches `$HOME`. Belt-and-suspenders; not required.
- **M-4 (optional proof strengthening).** P1 gate (ii) varies HOME but not `COLUMNS`. Width-insensitivity is already proven as a binary property by spec-R0 (binding) and captures are non-TTY, so this is settled — adding a `COLUMNS`-varied regen to (ii) would make the proof self-contained but is not needed to close the vector.

## Bottom line

**GREEN, 0C/0I — begin P1.** The plan faithfully realizes the spec and all six locked decisions; the Q1-b §6.6 supersession is a coherent resolution of a spec-surfaced open question, not a contradiction; the determinism proof is adequate and *executed* (P1 empirical proofs + P2 born-green author≠runner cross-machine diff); the gate demonstrably catches this cycle's drift class with a specified negative proof and an atomic born-green rollout; the STOP ledger (S1 live, S2 mooted, S3 live, S4 new) and the honesty ledger (gate A ≠ narration-truth; gate B deferred) are correct; NO-BUMP/toolkit-only scope holds. **USER decisions owed before P2:** OQ1/S4 (first-time branch protection on master — recommend the minimal `contexts=[examples]`, `enforce_admins` off form) and the Q2/locked-2 release-ritual acceptance; plus the Q5 `.docbins` housekeeping flag. Minors are two off-by-one citation nits (correct `fi` to `:774`, §6.6 to `1474-1475`) + two belt-and-suspenders notes — **none blocks the start of P1**. Per CLAUDE.md, re-dispatch this review after folding the Minors to confirm no drift.
