# Plan R0 refined-minors-confirm — C0/I0/M2 green=True

GREEN (0C/0I). The minor-fold is correctly applied and the plan is internally consistent and faithful to the LOCKED+refined brainstorm. The user-approved REFINEMENT (top-of-file, 2026-06-24) that moves the [source] redirect job-scoped (cargo build --config, off any committed root .cargo/config.toml) structurally dissolves the entire round-3→7 apparatus — and with it the round-7 Important (rust.yml must-fire HARD EXIT-GATE) plus both round-7 minors (man-pages line-drift, bitcoind cron), all of which lived inside the removed apparatus. The superseded rounds 3→7 fold notes are correctly walled off under explicit 'SUPERSEDED — retained for audit only' banners; no live body text contradicts the refinement. P0's active file set is consistent across the phase table, FILES, REMOVED-FILES (all struck-through with in-place WHY), EXIT CRITERIA, and the cross-phase invariants. Load-bearing surviving facts re-verified live: cargo 1.85.0 (the --config 'source.*' mechanism), the miniscript [patch.crates-io] git fork at rev 95fdd1c5… in Cargo.toml:28-29 + Cargo.lock:700 (grounds the MANDATORY three-block --config / R0-r5-C1), man-pages.yml's single combined-matrix musl-binaries job (host apt musl-tools :117, CC_* :123, tar :50/:133, sha256sum :135, gh upload :144-145), and fuzz/.cargo/config.toml appearing ONLY inside the audit fold-notes (not the live P0 file set). Brainstorm↔plan are mutually consistent on the refinement (no-leak check §6 L168, three-block --config, descriptor-mnemonic slug→RESOLVED in the shipping commit, the two-distinct-path P1/P2/P4 gate). P4's precondition correctly still depends on the P1/P3 release-rehome, which the refinement does not touch. No new drift, contradiction, or C/I introduced. Safe to start P0 implementation.

## [Minor] Brainstorm-SHA citation staleness. The plan cites the brainstorm basis at toolkit SHA 4329859899663a669624efd990d621bef61979a5 (plan L51), but the on-disk brainstorm now carries refinement supersession boxes (in F4 / §3.12 / §10) that were added AFTER that SHA was recorded. The plan and the on-disk brainstorm are mutually consistent on the refinement, so this is documentation-provenance only, not a correctness defect — it falls under CLAUDE.md's documented citation-decay class.

**fix:** At write/commit time, re-anchor the brainstorm-basis SHA to the commit that actually contains the on-disk refinement (re-cut after the brainstorm refinement is committed), and note the refinement's SHA alongside the original R0-GREEN SHA so future readers see the locked design == the on-disk design. Non-blocking.
## [Minor] Two carried-over round-7 minors are subsumed/mooted rather than literally edited. (a) The P1 bullet (L146) still cites the x86_64 build step as 'line 120-124'; the live run: line is :124 (the step name build-native is at :120, so the 120-124 span is accurate, not wrong). (b) The round-7 bitcoind-differential daily-cron note ('17 5 * * *') was never added — but bitcoind-differential is now entirely moot under the refinement (no live exit-gate references it), so the note is no longer applicable. Both round-7 minor fixes were self-described as deferred-to-impl ('at implementation re-grep' / 'optional one-line note').

**fix:** None required pre-implementation. The plan's standing re-grep-at-write-time discipline (CLAUDE.md) covers the man-pages line numbers; the bitcoind cron note is obviated by the refinement. The implementer should re-grep man-pages.yml for live build/upload line numbers when authoring the P1 edits (build run: is :124, gh upload commands :144-145).