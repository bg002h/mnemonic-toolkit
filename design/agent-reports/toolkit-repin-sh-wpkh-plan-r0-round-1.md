# PLAN R0 — IMPLEMENTATION_PLAN_toolkit_repin_sh_wpkh.md (round 1) — Fable, adversarial

**Persisted verbatim per CLAUDE.md** (+ opus fold at end). Verdict: **OPEN (1C / 1I / 4M).** Verified vs toolkit `7ffce786` (live binary runs) + `md-codec-v0.41.0` tag.

## CRITICAL
**C1 — flip-test #1's asserted value is WRONG: post-flip `bundle --descriptor "sh(wpkh(@0))"` with no origin does NOT default to `m/49'/0'/0'` — it emits an EMPTY-origin (master-key, `m/`) wallet.** (In plan B1.1(1) + SPEC S3.1 + Acceptance #2 + the B2.4 migration note; survived all 3 SPEC R0 rounds, which validated 49' as the md-codec TABLE value but never traced the toolkit's post-flip default path.) Trace (real code/binary): post-flip `is_non_canonical=false` (`bundle.rs:1417-1418`) → `bind_descriptor_mode_paths` EARLY-RETURNS before any origin fill (`bundle.rs:2262-2266` "Canonical: no default-inference") → `path_decl` keeps the descriptor's EMPTY annotation (`parse_descriptor.rs:294-303`, `to_origin_path(None)→components:[]`) → per-slot `anno_path`=empty (`bundle.rs:1512-1516`) → `master.derive_priv(empty)` = MASTER KEY (`bundle.rs:1562-1565`). Empirically pinned via the canonical analog: `bundle --descriptor "wpkh(@0)"` TODAY emits mk1 `origin_path: m/` + master xpub (not 84'), `restore` renders `wpkh([73c5da0a]xpub661My…/*)`. Post-flip sh(wpkh) takes the identical branch. **Real flip: `48'/0'/0'/1'` + notice → elided-origin master-key, NO notice** (49' materializes only in md-codec wallet-policy-id hashing + template-completion, never on the toolkit emit path). Failure modes: (a) the TDD-RED test can never GREEN — implementer weakens the assertion (undocumented funds decision) or injects 49' (unspecified new behavior / scope explosion); (b) the LOUD CHANGELOG note would tell sh(wpkh) users the new default derives at 49' → **pointing them where their coins are NOT.** Required fold: decide + document the post-flip semantics — recommend **(a) wpkh-parity**: assert empty-origin/master (mk1 `origin_path: m/`, master xpub, notice absent), migration note = "no-origin sh(wpkh) now emits an elided-origin card; to reproduce a pre-repin wallet pass explicit `[fp/48'/0'/0'/1']`" — and **errata-fold SPEC S3.1 + Acceptance #2 + re-converge** (the SPEC codifies the wrong value).

## IMPORTANT
**I1 — flip-test #3 (`--slot @N.path=`→refuse) over-generalizes.** The canonical-mode refusal (`bundle.rs:1429-1453`, §6.6 row 4) fires ONLY for `[Phrase|Seedqr|Ms1, Path]` sets. An `[Xpub, Path]` set does NOT refuse on canonical descriptors (empirically: `wpkh(@0)` + `@0.xpub=…@0.path=…` succeeds + honors the path; pre-flip sh(wpkh)+xpub+path = byte-equiv, NO pre/post delta). Fold: name the slot set `[Phrase, Path]` in B1.1(3); pin `[Xpub, Path]`-still-succeeds as a companion OR note out-of-scope.

## MINOR
- **M1** install.sh = `scripts/install.sh`; md sibling pin `descriptor-mnemonic-md-cli-v0.11.2` ~:36 in `component_info()`, self-pin ~:32.
- **M2** GUI: `classify_descriptor_canonicity` at `mnemonic-gui/src/form/conditional.rs` (not `src/schema/`); ~:58-101 range right.
- **M3** B0.3 repair cell — name the construction seam: a checksum-VALID non-zero-pad single md1 is buildable by doctoring data symbols + `md_codec::bch::bch_create_checksum_regular` (pattern at `prop_repair_never_wrong.rs:152-162`); fed to repair → `reject_non_zero_pad` → `repair.rs:1710` wildcard → `PostCorrectionDecodeFailed`. `decode_payload` pub (`lib.rs:50`) for the direct cell.
- **M4** flip-test #4 must be a HARDCODED fixture captured PRE-bump (post-bump the command can't emit it); notice site is `emit_default_path_notice` (~:2410/:2454), not the `:2262-2266` early-return. `friendly_md_codec` ends :406; `md_codec_exit_code` ends :570.

## 8-check summary
1. SPEC fidelity clean mapping EXCEPT C1 (faithful copy of a wrong SPEC claim). 2. re-vendor-from-tag present + correct + load-bearing (tag = 1 new variant `MalformedPayloadPadding`, 0 `EmptyOriginOverride`; working tree dirtied by Track-A P0). 3. B0.2 implementable + sufficient (both matches exhaustive; `From` at :1029 has wildcard; exit 2 correct; EmptyOriginOverride NOT needed at 0.41.0). 4. flips #2/#3(scoped)/#4 genuine; #1 wrong (C1); guard tests real (`restore.rs:344` n≥2 gate, `run_multisig:3084-3088` keyless refuse; verify mirror :389-392). 5. B1.3 6 comment sites + mirror-no-Sh-arm confirmed; new gui-schema cell (no pre-existing sh(wpkh) classify cell). 6. release ritual complete (M1 path nit; no sh(wpkh) in corpus/manual → no expected drift). 7. NO pre-existing test expectation flips (grep: sh(wpkh in tests only entropy-mode `--template bip49`, unaffected; `[Xpub,Path]`-canonical no delta). 8. citation drift minor (M1/M2/M4); every test constructible EXCEPT flip #1 as specified (C1).

## VERDICT: OPEN (1C / 1I / 4M) — fix C1 (+SPEC errata + re-converge), I1, M1-M4.

---
**FOLD STATUS (opus, 2026-07-11):** C1 = the funds-flip destination was mis-specified in BOTH the SPEC (S3.1/Acceptance #2/migration note) and the plan; the reviewer's trace (empty-origin/master, wpkh-parity, notice absent — NOT 49') is code+binary-verified. FOLDING: (i) SPEC S3.1 + Acceptance #2 + migration note errata → empty/master wpkh-parity; (ii) plan B1.1(1) assertion + migration note; (iii) I1 → scope `--slot` refuse to `[Phrase,Path]` + pin `[Xpub,Path]`-succeeds; (iv) M1-M4 citations + repair-cell seam + pre-bump fixture. Then re-dispatch a COMBINED convergence (opus — it includes the SPEC errata) over SPEC+plan. Every prior R0 validated 49' as the table value; only the plan-R0's toolkit-emit-path trace exposed that the emit path never uses it — the classic "verify the actual behavior, not the plausible table value."
