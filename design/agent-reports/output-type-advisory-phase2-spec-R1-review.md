# SPEC R1 review — output-type advisory Phase 2 (mk + md) + Tier-0

> Persisted verbatim from the opus architect R1 review (round R1), verifying the author's fold of the R0 RED (2C/3I/4M). Reviewer re-checked every R0 finding against LIVE source at toolkit `64943f2`, mk `e5620ce`, md `c599292`, ms-cli precedent, and hunted for fold-induced drift.

## Verdict: GREEN (0C/0I)

The fold correctly resolved all 2 Critical + 3 Important + 4 Minor findings. Every resolution was verified against live source (not just the spec's prose), and the new-drift scan surfaced no fold-introduced contradiction, miscount, or false citation. The spec is ready for a plan-doc.

## Resolution of R0 findings

- **[C1] dead-code mechanism — RESOLVED ✓.** §4 line 82 now mandates *"`#[allow(dead_code)]` is REQUIRED on the enum"* and correctly states *"`#[cfg(test)]` construction does NOT keep bin-target items live."* The false R0 claim ("the test keeps it live / no dead_code") is GONE — grep for `keeps it live` / `avoids dead_code` / `no dead_code` / `exercised by the .*test` returns **zero hits** spec-wide. §9's clippy footgun (line 133) is now consistent with §4: it explicitly says *"the test exercising a variant does NOT keep it live — the `OutputClass` enum **requires `#[allow(dead_code)]`**."* The mk-vs-md unconstructed-variant breakdown is ACCURATE and identical in both §4 (l.82) and §9 (l.133): *mk constructs ONLY WatchOnly (both Template + PrivateKeyMaterial unconstructed); md constructs Template+WatchOnly (PrivateKeyMaterial unconstructed)*. Verified against live source: all 6 mk emitting handlers are WatchOnly (§3); md emits Template ×6 + WatchOnly (address) ×1, so only PrivateKeyMaterial is never-constructed. Bin-only premise confirmed: `mk-cli/src/` = {cmd/, error.rs, main.rs, process_hardening.rs}, `md-cli/src/` = {cmd/, compile.rs, error.rs, format/, main.rs, parse/, process_hardening.rs} — NO `lib.rs` in either. Both CIs enforce the gate: mk `ci.yml:58` `cargo clippy --workspace --all-targets -- -D warnings`; md `ci.yml:47` same + global `RUSTFLAGS: "-D warnings"` (`:10`).

- **[C2] ms-cli precedent parity — RESOLVED ✓.** §4 line 79 now mandates the exact ms-cli enum: `#[allow(dead_code)] #[derive(Debug, Clone, Copy, PartialEq, Eq)] pub enum OutputClass { PrivateKeyMaterial, WatchOnly, Template }` — **no PartialOrd/Ord**, citing `advisory.rs:26-32`. Verified byte-for-byte against live `mnemonic-secret/crates/ms-cli/src/advisory.rs:26-32`: enum shape, derives, and reversed variant order all MATCH exactly. §4 line 81 explicitly says *"Do NOT port `worst_class_on_stdout`… Do NOT port `card_kind_class`"* — confirmed against the live precedent (ms-cli ports neither). §5 line 90 models the test on `byte_parity_advisory_lines` and says *"Do NOT assert a `.max()` lattice or `worst_class_on_stdout` — neither is ported. 'Byte-identical' is scoped to the three literal strings only."* — matches live `ms-cli/tests/cli_output_class.rs::byte_parity_advisory_lines` (asserts the 3 hard-coded literals via `contains`, no lattice). The `\u{2014}` escape form is mandated in both §4 l.80 and §5 l.90, matching ms-cli `advisory.rs:40-41` (the toolkit source uses a literal `—`; `cat -A` confirms both are the same U+2014 byte sequence `E2 80 94` → byte-parity holds).

- **[I1] multi-return emit — RESOLVED ✓.** §4 line 84 + §5 line 91 now enumerate every md `--json` early-return site, and I re-opened each handler to verify the line numbers:
  - `md encode` json early-return `:69`, text `:95` — spec says `(:69+:95)` ✓ EXACT.
  - `md decode` json `:24`, text `:30` — spec `(:24+:30)` ✓ EXACT.
  - `md inspect` json `:42`, text `:59` — spec `(:42+:59)` ✓ EXACT.
  - `md bytecode` json `:30`, text `:41` — spec `(:30+:41)` ✓ EXACT.
  - `md address` json `:57`, text `:65` — spec `(:57+text)` ✓ (text return at :65, spec uses "text").
  - `md compile` json `:21`, text `:25` — spec `(:21+:25, inside cli-compiler gate)` ✓ EXACT.
  - (a) line numbers accurate ✓. (b) no missed success return site — each handler's only success exits are the json early-return + the final text `Ok(0)`; all error paths are `?`-propagated or explicit `Err`. (c) `md repair` single-return + error-path-inert claim HOLDS: json/text both fall through to one `Ok(if any_correction {5} else {0})` at `:153` (spec says `:153` ✓); the error path is `return Ok(2)` at `:121` (spec says `:121` ✓); "single emit at `:152`" is the slot between the dispatch block (ends :151) and the `Ok(...)` (:153) — correct and skips the exit-2 path. (d) all mk handlers are genuinely single-trailing-return: encode `Ok(0)`@:97, decode @:38, inspect @:40, repair `Ok(5/0)`@:86 (error path is `?`-propagated `decode_string(original)?`@:67 → exit 2 via `CliError::Codec`), derive @:99 (no early return; both json/text branches inline), address @:126. CONFIRMED.

- **[I2] quickstart.yml / 5 bump sites — RESOLVED ✓.** §8.3 line 119 + §9 line 135 now list all 5 sites. I grepped `mk-cli-v` / `md-cli-v` / `descriptor-mnemonic-md-cli-v` across ALL `.github/workflows/*.yml` + `scripts/install.sh` — the result is EXACTLY the 5 cited lines and no others: `install.sh:35` (md `descriptor-mnemonic-md-cli-v0.6.1`), `install.sh:41` (mk `mk-cli-v0.6.0`), `manual.yml:77` (mk), `manual.yml:84` (md), `quickstart.yml:71` (mk). The 5-site set is COMPLETE — the two other workflows (`install-pin-check.yml`, `manual-gui.yml`) pin no mk/md tags. Cross-checked the gate mechanism in `sibling-pin-check.yml`: it scans every `*.yml` (except itself) for `cargo install --git … --tag <tag> <pkg>` and matches `<pkg>` (mk-cli / md-cli) against the canonical tag from install.sh's `component_info` table — confirming a missed quickstart.yml mk bump WOULD fire exit 1. The spec's note that `install.sh:38` ms-cli pin (`ms-cli-v0.5.0` vs reality v0.5.1) is out-of-scope and not a gate trigger is CORRECT: `manual.yml:88` also pins `ms-cli-v0.5.0`, so install.sh↔workflow match holds.

- **[I3] git-tag vs crates.io + md-codec 0.35 publish — RESOLVED ✓.** §8.1/§8.2 now reframe the gate-relevant deliverable as the **git tag** (`mk-cli-v0.6.1`, `descriptor-mnemonic-md-cli-v0.6.2`), with crates.io publish as a separate optional step — correct, since the toolkit consumes mk/md only via `cargo install --git … --tag`. §7 line 108 adds the crates.io pre-flight and asserts md-codec 0.35.0 is **CONFIRMED on crates.io** via the local registry cache. I VERIFIED this empirically: `~/.cargo/registry/cache/index.crates.io-…/md-codec-0.35.0.crate` exists (alongside 0.32.1/0.33.0/0.33.1/0.34.0) AND it is extracted at `~/.cargo/registry/src/index.crates.io-…/md-codec-0.35.0` — a crates.io-sourced crate, proving publication. §7's caret-resolution claim is correct: `Cargo.toml:22` is `md-codec = "0.34.0"` (caret `^0.34.0 = >=0.34.0,<0.35.0`), `Cargo.lock:644-645` resolves 0.34.0 from `registry+https://github.com/rust-lang/crates.io-index`, so `cargo update` will NOT pull 0.35.0 — the explicit `"0.35"` edit is required, exactly as §7 states.

- **[M1] missing_docs — RESOLVED ✓.** §4 line 83 + §9 line 133 call out `missing_docs = "warn"` + `-D warnings` requiring `///` on every pub item. Verified: both workspaces set `[workspace.lints.rust] missing_docs = "warn"` (mk `Cargo.toml:11-12`, md `Cargo.toml:11-12`), promoted to error by `-D warnings`.

- **[M2] mk-vectors inert basis — RESOLVED ✓.** §3 line 53 now reads *"machine test-corpus data → inert **by rule** (R0 M2 — NOT 'stdout empty'; contrast md `vectors` which writes files)."* Verified: mk `vectors.rs` writes corpus JSON to **stdout** by default (`print!("{VECTORS_V0_1_JSON}")` else-branch; `println!("{pretty}")` if-branch) — its inert basis IS "machine corpus by rule," not "stdout empty." md `vectors.rs` writes files (`write_lf(&out_dir.join(...))`) — stdout empty. Both correctly inert on distinct grounds, now correctly stated.

- **[M3] 24-recover-md1 idempotency + sole-affected — RESOLVED ✓.** §6 line 99 now names `24-recover-md1.{cmd,out}` as the confirmed sole affected set, flags it as pair-mode (`2>&1` merge, no `.err`) with the stdout/stderr interleave-ordering risk, and mandates re-capture AND re-run before committing. Verified: `transcripts/24-recover-md1.{cmd,out}` exist with NO `.err` (pair-mode); it is the ONLY transcript invoking `$MD_BIN` (its `.cmd` is `$MD_BIN decode md1… md1… md1…`); `$MK_BIN` is NEVER invoked in any transcript. `verify-examples.sh` pair-mode does `actual=$(… 2>&1 …)` and diffs against `.out` — confirming the interleave concern is real (captured stdout buffering vs unbuffered stderr advisory).

- **[M4] line-number caveat note — RESOLVED ✓.** §3 line 70 adds the explicit caveat: *"Known off-by-1/2 from R0 M4 (no class verdict changes): mk `derive` child_xpub at `:79`/`:91` (not `:80`=fingerprint); mk `verify` JSON shape `:118–126` (the `:127–133` cite is the text path); md `repair` emit-text tail `:197–199`."* Verified against live source: `mk derive` child_xpub JSON@:79 / text@:91, child_fingerprint@:80 ✓; `mk verify` emit_ok json-branch carries ok/chunks/policy_id_stubs (no xpub), text path "OK: mk1…" ✓; `md repair` corrected-chunk `println!("{chunk}")` tail in emit_text ✓. The caveat also reaffirms the source-of-truth (`secret_advisory.rs:80/:83/:97`) + 4 precedent sites are EXACT — re-confirmed (`:80` enum decl `{Template,WatchOnly,PrivateKeyMaterial}` Ord; `:83` worst_class_on_stdout; `:97` emit_output_class_advisory).

## Critical (new or unresolved)

None.

## Important (new or unresolved)

None.

## Minor

- **[m1] §2 source-of-truth section retains the toolkit's `Ord` / `worst_class_on_stdout` / `card_kind_class` descriptions — correct, but worth one defensive sentence in the plan-doc.** §2 (lines 26-27, 33) describes the toolkit's lib-internal Phase-1 design (which DOES derive Ord and DOES use worst_class_on_stdout/card_kind_class). This is appropriate — §2 is the byte-parity *reference*, not a port mandate — and §4 line 76 explicitly disclaims it ("NOT the toolkit's lib-internal `secret_advisory.rs` … different dead-code/lattice characteristics"). No contradiction. Defensive only: the plan-doc should restate up-front that the §2 toolkit shape is the *output reference* and the §4 ms-cli shape is the *implementation template*, so an implementer skimming §2 first doesn't accidentally re-derive the Ord enum. Not gating.

## New-drift scan (what I checked for fold-induced contradictions + result)

- **§4 vs §5 vs §9 on helper/test shape** — CONSISTENT. All three now mandate: ms-cli-shaped `#[allow(dead_code)]` non-Ord enum, no `worst_class_on_stdout`, no `card_kind_class`, byte-parity scoped to the 3 literal strings, test modeled on `byte_parity_advisory_lines`. No leftover mandate to port Ord/`.max()`/`worst_class_on_stdout`/`card_kind_class` (the only mentions are §2's correctly-scoped toolkit-reference description). grep-confirmed.
- **C1 contradiction residue** — NONE. The old "test keeps it live / avoids dead_code / no dead_code" phrasing returns zero grep hits. §4 and §9 now agree.
- **Handler counts §3 vs §8** — CONSISTENT. §8.1 mk = 6 (encode/decode/inspect/repair/derive/address) = §3 mk emitting rows. §8.2 md = 7 (6 Template + address WatchOnly) = §3 md emitting rows.
- **"5 sites" claim vs enumerated list vs live grep** — CONSISTENT and COMPLETE. The enumerated list (install.sh ×2 + manual.yml ×2 + quickstart.yml ×1) exactly equals the live grep across all workflows + install.sh; no other file pins mk/md.
- **Unconstructed-variant breakdown §4 l.82 vs §9 l.133** — IDENTICAL and accurate against live source.
- **§8 vs §7 Tier-0 ordering / framing** — CONSISTENT. §7 (caret bump + crates.io pre-flight + smoke test + FOLLOWUP correction) is folded into §8.3's toolkit unit; no ordering contradiction (3 independent units mk ⊥ md, toolkit consumes the git tags + the md-codec registry bump).
- **New file:line citations added by the fold** — all verified accurate: `md repair :153`/`:121`, `md encode :69/:95`, `md decode :24/:30`, `md inspect :42/:59`, `md bytecode :30/:41`, `md address :57`, `md compile :21/:25`, `ms-cli advisory.rs:26-32` + `:40-41`, the M4 caveat line numbers (`:79`/`:91`, `:118–126`, `:197–199`). No wrong citation introduced by the fold.
- **Scope creep / new ambiguity** — none detected. The fold tightened scope (dropped the Ord/worst_class port) rather than widening it.

## Notes

- Tier-0 remains genuinely low-risk and is now correctly framed (caret bump explicit; crates.io publication empirically confirmed via registry cache+src; smoke test must exercise the full `mnemonic repair --md1` path including the `parse_chunk` pre-gate at `repair.rs:~1196-1203`, not just the codec call; zero transcript drift since no transcript invokes `mnemonic repair`). The toolkit FOLLOWUPS.md:396 entry is verified false-resolved ("resolved v0.24.0 cycle" + "no toolkit code change required beyond the dep bump" while the pin is still 0.34.0) — §7's mandate to correct that Resolution prose to the actual bump SHA is sound.
- `md compile` emit is doubly-gated (`cli-compiler` module gate `cmd/mod.rs:3` + inner `json` early-return `:21`) and is NOT built by md-cli's own default-feature CI, but the toolkit's `manual.yml:84` installs md-cli `--features cli-compiler`, so it compiles there. §5's `#[cfg(feature="cli-compiler")]`-gated cell handles this. No transcript invokes `md compile`.
- The plan-doc must (per CLAUDE.md) re-grep every line citation against current origin/master at impl time — the §3/§4 line numbers are header-SHA snapshots and the spec correctly flags this (l.70).
- Cross-repo byte-parity (literal `—` in toolkit `secret_advisory.rs` == `\u{2014}` escape in ms-cli/the new mk+md ports) is empirically re-confirmed; the FOLLOWUP "byte-identical wording" discipline + identical literal constants are the only parity mechanism (no shared dep, no automated gate) — same model Phase 1 shipped with.
