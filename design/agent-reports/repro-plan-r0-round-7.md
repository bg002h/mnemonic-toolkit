# Round 7 — C0/I1/M2

NOT GREEN — 1 Important. The plan is exceptionally rigorous and the round-3→round-6 folds (repo-global [source] scope, vendor platform-completeness, the SIX-site out-of-graph-bleed enumeration, P3-prelude sibling recon, path-filter self-inclusion of the five install-bearing workflows, and sibling-pin-check.yml regex fragility) are ALL correctly and completely resolved — every load-bearing citation re-verified live this session against the working tree. The core recipe fidelity is intact: ENV-channel top-level --remap-path-prefix at /build/src (NOT committed config), the FULL three-stanza cargo-vendor [source] block, SOURCE_DATE_EPOCH=$(git show -s --format=%ct <commit-SHA>), gzip -n -9 at man-pages.yml:50+:133, --locked --offline, the cc-under-musl epoch-load-bearing/residue validation gate, the release-rehome via per-STEP docker run --network=none (matrix forbids job-level container:) with the build/package-only network boundary and copy-out for gh release upload, P4's distinct-path two-build + remap-off negative test, the md FOLLOWUP remap-add+RESOLVED-flip, and the NO-BUMP/no-manual-mirror/no-schema_mirror reasoning. I found ONE new Important: the SAME path-filter surfacing defect that rounds 5-6 chased through the install-bearing workflows was never applied to rust.yml — the single most load-bearing workflow in the P0 HARD EXIT-GATE (it carries the entire ubuntu+macos test matrix, the windows/aarch64-gnu/freebsd lib-cross-platform check, clippy, AND the musl matrix that must re-GREEN against the REPO-GLOBAL [source] block). The plan asserts twice (L154, L299) that rust.yml 'auto-fires on the P0 push (crates/**+Cargo.toml+Cargo.lock)', but the P0 commit's enumerated files (.cargo/config.toml, vendor/, Dockerfile.repro, fuzz/.cargo/config.toml, reproducible-musl-build.yml, + edits to the 5 install-bearing workflows) match NONE of rust.yml's paths filter (crates/**, Cargo.toml, Cargo.lock, .github/workflows/rust.yml), and cargo vendor --locked does NOT modify Cargo.lock. Live-verified: rust.yml has NO workflow_dispatch. So the plan's most important exit-gate can neither auto-fire nor be manually dispatched as written. Fold this (the fix is mechanical — the plan already contains the template), re-dispatch a convergence review per the reviewer-loop discipline, and it should reach GREEN in one round.

## [Important] rust.yml — the workflow carrying the ENTIRE P0 HARD EXIT-GATE matrix (ubuntu+macos test, the windows-msvc/aarch64-gnu/freebsd lib-cross-platform check, clippy, and the musl-build-test matrix that must re-GREEN against the committed REPO-GLOBAL [source] block) — does NOT fire on the P0 commit and cannot be manually dispatched, yet the plan asserts the opposite in two places. LIVE-VERIFIED this session: rust.yml's push+PR paths filter is exactly ['crates/**','Cargo.toml','Cargo.lock','.github/workflows/rust.yml'] (rust.yml:16-19,22-25) and rust.yml has NO workflow_dispatch (grep returns none). The P0 commit's enumerated file set (plan L75/L81-93/L157: .cargo/config.toml, vendor/, Dockerfile.repro, fuzz/.cargo/config.toml, .github/workflows/reproducible-musl-build.yml + in-place edits to manual/quickstart/technical-manual/manual-gui/cross-tool-differential.yml) matches NONE of those paths — and `cargo vendor --locked` reads but does NOT rewrite Cargo.lock, so Cargo.lock is not touched either. Plan L154 ('Also auto-firing on the P0 push: rust.yml (crates/**+Cargo.toml+Cargo.lock)') and L299 ('Also auto-firing: rust.yml, fuzz-smoke.yml, and sibling-pin-check.yml') are therefore FACTUALLY WRONG for the P0 file set — the identical path-filter-blind-spot defect class that rounds 5→6 corrected for the five install-bearing workflows (self-inclusion) and for bitcoind-differential (active dispatch), but never applied to rust.yml itself. CONSEQUENCE: the implementer following the plan's L155 ('Confirm GREEN for ALL of: ... rust.yml ...') and the L157/L298 exit criteria would either (a) see rust.yml produce NO run on the P0 PR and falsely treat its absence as 'nothing to confirm' (a CI-vs-plan gap that lets P0 FALSELY pass while the repo-global [source]/vendor regression in the test/cross-check/musl matrix goes entirely unmeasured — exactly the silent-lagging-breakage this provenance plan exists to gate against), or (b) be blocked with no dispatch path. This is load-bearing precisely because rust.yml IS the gate proving vendor/ platform-completeness across macos+windows+aarch64-gnu+freebsd, which is the whole point of the R0-r3-I-vendor-platform-completeness fold.

**fix:** Fold an ACTIVE trigger for rust.yml into P0's surfacing mechanism, exactly as round-6 did for the install-bearing workflows and bitcoind-differential. Cleanest: have the P0 commit EDIT rust.yml to add `workflow_dispatch:` — this both (a) makes the edit match rust.yml's own self-included path filter ('.github/workflows/rust.yml') so it AUTO-FIRES on the P0 PR (same edit-matches-self-path mechanism the round-6 fold relies on for the five install-bearing files), and (b) provides a manual dispatch handle. Alternatively, add a no-op path-poke to a path rust.yml filters on (e.g. a trivial whitespace/comment touch to Cargo.toml in the same commit) so it fires. Then CORRECT plan L154 and L299 to stop claiming rust.yml auto-fires on the unedited P0 file set, and move rust.yml from the 'auto-firing' set into the 'requires an active trigger (edit-to-self-path or path-poke), then confirm GREEN' set alongside bitcoind-differential. Re-state the L157/L298 exit criteria so 'confirm rust.yml GREEN' is gated on an actual run existing (a missing/skipped run is a hard blocker, not a pass). Re-dispatch the architect on the revision per the reviewer-loop discipline.
## [Minor] Several cited line numbers in man-pages.yml have drifted by a few lines vs the live file (non-load-bearing, but the citation-discipline standard prefers live numbers). LIVE: the x86_64 build step is man-pages.yml:124 (plan L177 cites 'line 120-124'); the gh-release upload is :144-145 (plan L178 cites 'line 140-145'); checkout is @v4 at :112 (plan correctly cites 112). The host apt musl-tools (:117) and CC_x86_64_unknown_linux_musl env (:123) and the tar czf sites (:50,:133) and sha256sum (:135) all match. None of these change the mechanism (single combined-matrix job, per-STEP docker run on the musl_native leg only, network boundary for ensure-release/upload) — all of which are verified correct.

**fix:** At implementation, re-grep man-pages.yml for the build-step / upload-step line numbers and use the live values; the plan's 'live-verified lines 94-145' framing is close enough that this need not block, but note the build step is :124 and upload is :144-145.
## [Minor] bitcoind-differential.yml also carries a daily `cron: '17 5 * * *'` trigger (bitcoind-differential.yml:53) in addition to the push-paths-filter + workflow_dispatch the plan cites. The plan correctly identifies it as the genuine residual needing ACTIVE dispatch and notes it HAS workflow_dispatch (verified at :54) — so the conclusion is sound — but the cron means it could ALSO fire on schedule independent of the P0 PR. This does not change the prescription (actively dispatch + confirm GREEN) but is worth a one-line note so the implementer isn't surprised by a scheduled run.

**fix:** Optional: add a parenthetical to the bitcoind-differential exit-gate bullet (L154/L299) noting it also has a daily cron (:53) — so a green scheduled run on the P0 branch's merge base is not mistaken for a P0-commit confirmation; the implementer should still actively workflow_dispatch it against the P0 ref.