# Round 5 — C1/I1/M2

NOT GREEN — 1 Critical + 1 Important. The round-2/3/4 folds are correctly and completely resolved in the body (re-home = per-step `docker run --network=none` on the x86_64 leg only; network boundary; single-host platform-complete `cargo vendor`; repo-global `[source]` hard exit-gate; P3-prelude recon; `fuzz/.cargo` neutralizer; `cross-tool-differential` isolation), and every load-bearing live citation re-verified against this checkout is ACCURATE (Cargo.toml workspace=["crates/mnemonic-toolkit"], fuzz [workspace]-excluded with own lock, libfuzzer-sys/arbitrary/jobserver root=0/fuzz=1, miniscript [patch.crates-io] at line 28-29 → Cargo.lock:700, no [target.*] tables, .cargo/Cross.toml absent, man-pages.yml tar@50/133 + sha256@135 + apt-musl@117 + CC@123 + build-native@120 + build-cross@125 + checkout@112 + upload@144-145, cross-tool-differential cargo-install@53, rust.yml metadata@95/build@105/test@112/os-matrix@84/cross-check@217-235/musl@369-405). Core recipe fidelity (ENV-channel top-level --remap at /build/src, three-stanza vendor block, SOURCE_DATE_EPOCH off commit SHA, gzip -n -9 at both tar sites, --locked --offline, cc-validation gate), P4 distinct-path + negative test, md-FOLLOWUP remap+flip, and NO-BUMP are all sound. BUT the out-of-graph-bleed enumeration the round-4 fold introduced is INCOMPLETE — it remediates 2 of (at least) 6 affected workflows and the P0 exit-gate's surfacing mechanism is itself defeated by GitHub path-filters, making the HARD EXIT-GATE falsely passable. Fix the Critical (+ the path-filter Important), re-dispatch per the reviewer-loop discipline.

## [Critical] INCOMPLETE out-of-graph-bleed enumeration — the REPO-GLOBAL `[source]` redirect REDs FOUR more workflows the plan never enumerates or remediates, defeating the very HARD EXIT-GATE the round-3/4 folds added. The round-4 fold (P0 lines 52-56/118, EXIT CRITERIA, invariant 263) authors out-of-graph isolation for EXACTLY TWO sites: `fuzz-smoke.yml` (via committed `fuzz/.cargo/config.toml`) and `cross-tool-differential.yml:53` (via `--config`/isolated-CARGO_HOME on the `cargo install --git md-cli` step). But a live sweep of `.github/workflows/` shows the IDENTICAL `cargo install --git <sibling>`-in-root-checkout hazard in FOUR more workflows that the plan never lists: `manual.yml:79,86,90` (mk/md/ms-cli), `quickstart.yml:77,83,87` (mk/md/ms-cli), `technical-manual.yml:109,114,117` (mk/md/ms-cli), and `manual-gui.yml:153,156,159,170` (md/ms/mk-cli + the toolkit itself; `--root` redirects only the install DESTINATION, not source resolution). Each runs in a root checkout (`actions/checkout@v6`, no cwd-escape on the install step) → inherits the committed root `.cargo/config.toml` `[source.crates-io] replace-with=vendored-sources` → resolves each sibling tool's OWN transitive dep graph through the toolkit `vendor/`, which lacks those crates (they are not in the toolkit's 168-crate root lock — exactly the class `vendor/`-platform-completeness STRUCTURALLY cannot fix, per the round-4 fold's own reasoning) → RED. The plan's REPO-GLOBAL hazard prose (P0 line 67, invariant 262) hand-waves 'the differential + fuzz-smoke workflows' and names only `bitcoind-differential.yml`/`cross-tool-differential.yml`/`fuzz-smoke.yml`, but the AUTHORED remediation + the EXIT-GATE surfacing list (line 119: 'confirm the existing rust.yml + fuzz-smoke.yml + cross-tool-differential.yml run ALL GREEN') silently drop the four doc workflows. Result: P0 ships GREEN, and the breakage detonates on the next unrelated docs PR — the precise silent-lagging-breakage anti-pattern (cf. the schema-mirror gui-schema-mirror-lockstep-discipline case study) this provenance plan claims to gate against. For a custody-tool provenance substrate this is not deferrable. (`bitcoind-differential.yml`'s root-checkout `cargo build`/`cargo test` at 121/131 are IN-graph so vendor-completeness covers them, but they too are absent from the exit-gate enumeration and should be confirmed.)

**fix:** Extend the P0 out-of-graph remediation (and the cross-phase invariant 263 + the HARD EXIT-GATE enumeration at lines 114-119/EXIT CRITERIA) to ALL six `cargo install --git`/root-checkout-cargo sites, not two. Author the SAME isolation edit (`--config 'source.crates-io.replace-with=""'` verified to empty the redirect, OR an isolated `CARGO_HOME` + cwd outside the repo tree) on every sibling `cargo install --git` line in `manual.yml`, `quickstart.yml`, `technical-manual.yml`, and `manual-gui.yml` (incl. the toolkit self-install at manual-gui.yml:170), landed in the SAME P0 commit as `.cargo/config.toml`+`vendor/`. Re-grep `.github/workflows/` for the canonical set (`grep -rnE 'cargo install (--git|--root)' .github/workflows/`) and list each affected file:line in the plan so the enumeration is exhaustive and verifiable, not exemplary. Mirror the same per-repo grep into the P3-prelude recon (it currently only directs siblings to look for a fuzz crate + a single `cargo install --git` differential step — a sibling's doc/manual workflows carry the same bleed). Restate the exit-gate as 'pre-existing CI re-GREENs after isolation edits on ALL out-of-graph cargo-install sites land,' not the two named today.
## [Important] The P0 HARD EXIT-GATE's surfacing mechanism is itself defeated by GitHub path-filters — it asserts a confirmation it cannot actually observe at P0 time. Line 119 says: 'push the P0 commit to the branch and confirm the existing rust.yml + fuzz-smoke.yml + cross-tool-differential.yml workflows run ALL GREEN against it — these are pre-existing jobs, so they fire automatically.' But the P0 commit adds only CI-infra/vendor files (`.cargo/config.toml`, `vendor/`, `Dockerfile.repro`, `fuzz/.cargo/config.toml`, the new `reproducible-musl-build.yml`). Verified live triggers: `cross-tool-differential.yml` is `paths:`-filtered to `parse_descriptor.rs`/`cli_cross_tool_differential.rs`/its own workflow file — NONE touched by the P0 commit → it does NOT fire, so the exit-gate cannot observe its GREEN/RED at all (the assertion that it 'fires automatically' is false for this commit). Same for `manual.yml`/`quickstart.yml`/`technical-manual.yml`/`manual-gui.yml`/`bitcoind-differential.yml` (all path-filtered away from a vendor-only commit). Only `rust.yml` (fires on any push) and `fuzz-smoke.yml` (the P0 commit DOES touch `fuzz/**` via `fuzz/.cargo/config.toml`) actually fire. So even the TWO sites the plan does remediate are only HALF-observable at P0 push time, and the four un-enumerated sites are wholly invisible. This makes the 'P0 not shippable until pre-existing CI re-GREENs' gate hollow for the path-filtered majority.

**fix:** Make the exit-gate ACTIVELY trigger each affected path-filtered workflow rather than rely on automatic firing: either (a) add `workflow_dispatch` and manually dispatch each affected workflow against the P0 branch as an explicit gate step (cross-tool-differential already has `workflow_dispatch`; bitcoind-differential too; the four doc workflows do NOT — they'd need it added or a throwaway path-touching probe), or (b) include a no-op touch of each path-filtered trigger path in the P0 verification (a throwaway commit on the branch that pokes `docs/manual/**` etc.) so the workflow fires and is observed GREEN, then drop it, or (c) gate via `act`/local re-run. State explicitly in the exit-gate that path-filtered workflows do NOT fire on a vendor-only commit and enumerate the dispatch/probe step for each (`cross-tool-differential`, `manual`, `quickstart`, `technical-manual`, `manual-gui`, `bitcoind-differential`). Carry the identical caveat into the P3 per-repo exit-gate.
## [Minor] `bitcoind-differential.yml` is named in the REPO-GLOBAL hazard prose (P0 line 67, invariant 262) but omitted from the HARD EXIT-GATE confirmation list (lines 114-119) and the EXIT CRITERIA. Its root-checkout `cargo build --bin mnemonic` (line 121) + `cargo test -p mnemonic-toolkit --test bitcoind_differential` (line 131) resolve through the committed `vendor/` (in-graph, so platform-completeness covers them) — but as a workflow that DOES resolve through the redirect it should be in the explicit confirm-GREEN set for completeness and to avoid an asymmetry where it is flagged as a hazard yet never gated.

**fix:** Add `bitcoind-differential.yml` to the exit-gate confirmation enumeration (and note it is in-graph so it needs no isolation edit, only a re-GREEN confirmation — distinguishing it from the out-of-graph `cargo install --git` set).
## [Minor] Citation drift (non-load-bearing): the plan repeatedly cites the man-pages.yml musl-binaries aarch64 leg as `cross: true` at 'lines 108-110' and the P1 build steps as 'build-native (line 120-124)' / 'build-cross (line 125-127)'. Live: `cross: true` is at line 109 (matrix entry block 108-110 OK), `build-native` at line 120, `build-cross` at line 125 — all within the cited ranges, so accurate. The `cross-platform check` matrix is cited as 'lines 212-235' but lives at 217-235 (the 212 lower bound is a few lines high). None alter the design; flagged only per the grep-verify-at-write-time discipline.

**fix:** Tighten the `lib cross-platform check` citation to lines 217-235 (matrix `include:` block) at next edit; the rest are within tolerance. Optional.