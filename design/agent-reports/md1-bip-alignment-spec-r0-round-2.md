# R0 review — `SPEC_md1_bip_alignment_and_code_honesty.md` (round 2, convergence)

**Reviewer:** Fable, adversarial, read-only. Persisted verbatim per CLAUDE.md. Ground truth re-verified against `descriptor-mnemonic` @ `origin/main ef1f3e71`, the toolkit working tree, `mnemonic-key`, and the BIP mediawiki source.
**Dispatched:** 2026-07-10 (MD SPEC, R0 round 2).
**NOTE (post-review):** GREEN verdict below is on the F-A1b-inclusive SPEC. User redirection 2026-07-10 (after this review) DROPS F-A1b (hard refusal) in favor of a separate "pathless-wallet backup" brainstorm (loud encode advisory + decoder partial-decode-with-placeholders). The SPEC will be re-cut and re-gated; the round-1-fold verifications below remain valid for the retained items (F-A1, F-A2, F-A3, F-A4, F-A5, F-A8, F-A9, I-1/I-2/I-3/I-4 mechanics, M-1..M-5, rulings).

## Round-1 fold verification — all 1C/4I/5M + 3 rulings

**C-1 → F-A1b: FOLDED, correct.**
- **Layer is right.** `encode_md1_string` (`encode.rs:170`) and `split` (`chunk.rs:236`) are the string-emit layer; `encode_payload` (`encode.rs:99`) stays the ungated primitive (SPEC line 33). Verified both md-cli encode branches — text and the `--json` early-return (`cmd/encode.rs:51-82`, `:84-100`) — route exclusively through those two functions, so the gate covers the whole CLI surface with no third path.
- **Class coverage is right.** Gate condition mirrors `validate_explicit_origin_required` (`validate.rs:221-244`): `canonical_origin(&tree)==None` ∧ any `@N` without explicit origin. sh(wpkh) exits the class via F-A1 (new `Some(49')` arm) and the SPEC pins the no-`--path` success test explicitly (line 36).
- **No mid-cycle breakage.** All 10 MANIFEST vectors (`test_vectors.rs:42-56`) are canonical shapes, and the vectors runner emits phrases through the gated `encode_md1_string`/`split` (`cmd/vectors.rs:50,59`) — so P2a's gate breaks neither the corpus nor the runner before P2e's `path` machinery lands. Additionally verified the toolkit cannot trip the gate on re-pin: mutation-3 KEEPS origins when `canonical_origin=None` (the C1 invariant, `synthesize.rs:1140-1150,1201`), so the toolkit never emits non-canonical-with-empty-origin.
- F-A4 interaction (advisory test must pass `--path`) captured twice (lines 35, 56); B-C2 carries the encoder-MUST (line 77) and Phase 1 notes it (line 124). ✓

**I-1: FOLDED.** Mechanism now correct — `identity.rs:215-217` is the L14 canonical-fill inside `compute_wallet_policy_id` (verified); `encode.rs:119` writes `path_decl` verbatim (verified); symmetry = decode-gate no-op. All three consumers enumerated and each verified at source: `validate.rs:222`, `canonicalize.rs:464-469`, `identity.rs:176-220`. A repo-wide grep confirms those are the ONLY md-codec runtime consumers of `canonical_origin`. Policy-id-equality (elided == explicit-49′), error→success flip, and explicit-form byte-identity tests all specified (line 29). ✓

**I-2: FOLDED.** `synthesize.rs:349-368` ("Mirrors the shape dispatch of `md_codec::canonical_origin::canonical_origin`", bip49 in the None class) and `template_admissible` `:1092-1127` (n==1 admits only `cli_template_from_tree=Some`, bip49 pinned REFUSED) verified verbatim. SPEC keeps the refusal pinned, forbids "fixing the mirror," files the FOLLOWUP in both repos (line 107, acceptance #5). "No runtime flip" claim re-verified: sh(wpkh) is structurally n==1, the admission gate fires before mutation-3, and no n≥2 shape flips. ✓ (Residual comment-tier gap → new M-6 below.)

**I-3: FOLDED.** `Vector.path: Option<&'static str>` + runner support specified (line 93; verified `Vector` has no path field today, `test_vectors.rs:13-32`, and the runner supplies none, `cmd/vectors.rs:36`); NUMS + tr_with_leaf vectors scheduled in F-V3 (line 97) and phased at P2e (line 125); rippled into B-I6 (line 85). ✓

**I-4: FOLDED.** Corpus regen in P2e → BIP §Test Vectors table/pins sync moved to Phase 3 (lines 91-99, 126); acceptance #4's independent-reader check runs against the FINAL corpus (lines 99, 116). ✓

**M-1** (md-cli flag = manual-only ripple, not schema_mirror — lines 46, 105-106) ✓ · **M-2** (F-A5 rewrites the WHOLE `bch.rs:19-31` block, both wrong claims named, mk byte-consistency — lines 64-65; block re-verified wrong at source) ✓ · **M-3** (advisory fires on both branches incl. `--json`; branch verified at `cmd/encode.rs:51-82` — lines 55-56) ✓ · **M-4** (F-A9 added; `error.rs:421-428` verified) ✓ · **M-5** ("sh_wpkh is an ADDITION"; verified absent from MANIFEST — line 98) ✓ · **Rulings:** A3 hard-error-keep-flag (lines 44-47) ✓, A4 pkh silent (lines 49, 53) ✓, B-I8 encoder-MUST + decoder-lenient + chunked-CSI note (line 87) ✓.

## Drift checks on the BIPs-first reorder

**(a) B-I4 placeholder — handled.** Phase 1 uses an explicit placeholder (line 124 "real error name TBD-in-P2, use a placeholder + finalize"); P2c finalizes it together with F-A8 (line 125), and B-I4 (line 83) takes the direction code→BIP ("cite the REAL error variant from F-A8"). The fictional cite verified present in the BIP (~line 250: `Error::InvalidBytecode { kind: BytecodeErrorKind::MalformedPayloadPadding }` — no such variant; `Error` is a flat insertion-ordered enum, `error.rs:20`ff). No dangling reference survives Phase 3.

**(b) No Phase-1 claim depends on a not-yet-existing Phase-2 artifact at write time.** The Goal paragraph (line 11) declares the target-stating model explicitly. Critically, Phase-1 normative content is *code-true today* where it must be: B-C1's chunking model matches `split()`/`reassemble()` exactly — fragments are whole bytes of the byte-padded assembled payload (`chunk.rs:241,262-269`), header/fragment boundary contiguous at bit 37 (`chunk_bit_count = 37 + 8N`, `chunk.rs:285` — the BIP's "3 bits of trailing slack" after the header is indeed fiction), reassembly concatenates fragment bytes (`chunk.rs:329-336`), sizing off the 320-bit budget (`chunk.rs:249-250`). The mk1 contrast claim verified (`mk-codec/src/consts.rs:39: CHUNKED_FRAGMENT_LONG_BYTES = 53`). BIP line-cites spot-checked (~246, ~330, ~306 — all present).

**(c) Recovery-safety invariant intact after all folds.** F-A1 strictly additive (gate no-ops on `Some`; encoder writes `path_decl` verbatim → no byte change to any existing card). F-A1b encode-only. F-A2 additive — re-verified at the bit level: single-payload first symbol is `[divergent][v3][v2][v1][v0]` (`header.rs:31`), so {4,8,12} all have v0=0; the chunk first symbol's LSB is the chunked flag =1 (`chunk.rs:4`) — the dispatch bit never diverts a currently-valid string. F-A8 rejects only non-conforming pads (rollback re-verified value-blind at `tlv.rs:292-298`). B-I8 keeps the decoder lenient. No previously-decodable card breaks.

## New findings (round 2)

**MINOR**

- **M-6. The toolkit `canonical_origin` consumer list in the ripple bullet is still incomplete — comment/diagnostic tier only, no runtime flip (verified), but the same "stale comment invites a bad simplification" hazard class as I-2.** A toolkit-wide grep finds two more runtime consumers and three more comments that equate `canonical_origin=Some` with the admissible-template set, an equivalence F-A1 breaks: (1) `crates/mnemonic-toolkit/src/cmd/gui_schema.rs:1318-1327` — `gui-schema --classify-descriptor 'sh(wpkh(@0/…))'` output flips `non-canonical`→`canonical` on re-pin. Semantically correct post-F-A1 and no test pins sh(wpkh) (verified `tests/cli_gui_schema_classify_descriptor.rs` cells 6-12), but the re-pin PR should ADD an sh(wpkh)→`canonical` cell. (2) `crates/mnemonic-toolkit/src/cmd/restore.rs:303` — the route-(1) comment lists 3 clauses while the code (`:317-320`) has 4; post-F-A1 the omitted `cli_template_from_tree` conjunct becomes the ONLY thing keeping an elided sh(wpkh) md1 out of the single-sig-template route, so the comment turns actively wrong. (3) `synthesize.rs:50-52` — Md1Form's doc "REQUIRES `descriptor.n == 1 && canonical_origin(&tree).is_some()`" becomes false. (4) `tests/cli_bundle_md1_template_form.rs:239` — refusal rationale goes stale (test stays green). **Fix:** generalize the ripple bullet to "update ALL toolkit comments equating `canonical_origin=Some` with the template-type set (synthesize.rs:50-52, restore.rs:303, cli_bundle_md1_template_form.rs:239)" + add the classify-descriptor sh(wpkh) test cell.
- **M-7. B-I6/P2e should state HOW the per-vector `path` surfaces in the emitted corpus files.** The four emitted files are `.template`/`.bytes.hex`/`.phrase.txt`/`.descriptor.json` (`cmd/vectors.rs:39-71`); a path-carrying vector is under-determined by its `.template` alone. `descriptor.json` carries it — arguably sufficient — but the BIP §Test Vectors table must pin template+path *pairs* for the NUMS/tr_with_leaf rows or acceptance #4's independent-reader reproduction is ambiguous.

## Verdict

Every round-1 finding is folded without drift: F-A1b sits at the right layer and closes the whole class; the corrected I-1 mechanism and consumer set are source-exact; the mirror trap is named with the refusal kept pinned; the vector machinery and phasing defects are resolved; the BIPs-first reorder introduces no dangling reference and its Phase-1 normative content is code-true where checkable. The recovery-safety invariant survives all folds. The two new Minors are checklist-tightening, not gate items.

**VERDICT: GREEN (0C/0I)** — 2 Minor (M-6, M-7) recommended for fold. [Superseded by user redirection — see NOTE at top; SPEC to be re-cut without F-A1b and re-gated.]
