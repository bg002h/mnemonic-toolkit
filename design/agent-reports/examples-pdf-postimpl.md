# Post-implementation whole-diff review — cycle `examples-pdf-un-ci-gated` (P1+P2+P3)

- **Reviewer:** opus architect, mandatory post-implementation adversarial whole-diff review (CLAUDE.md post-impl
  gate). Independent + adversarial; **every load-bearing claim independently re-run** — binary resolved, golden
  regenerated by the reviewer, determinism re-proved under varied HOME, both committed PDFs extracted and their
  entire funds surface diffed, the gate perturbed to observe red, the branch↔master merge dry-run executed.
- **Under review:** branch `feat/examples-pdf-modernize` tip `24bbad3b`, base master `b8067ebe`. Whole-cycle diff
  `git diff b8067ebe..24bbad3b` (3 commits: P1 `0b494660`, P2 `fd64f230`, P3 `24bbad3b`).
- **Authority composed (not re-litigated):** spec `SPEC_examples_pdf_modernize_and_gate.md` (R0-GREEN), plan
  `IMPLEMENTATION_PLAN_examples_pdf_modernize_and_gate.md` (R0-GREEN), spec-R0, plan-R0, OQ1 branch-protection
  ruling, and the two per-phase R0s (`examples-pdf-p1-r0-round-1.md`, `examples-pdf-p2-r0-round-1.md`, both GREEN
  0C/0I, persisted on master `9fbfad48`/`91b761a2`).

---

## VERDICT: GREEN — 0 Critical / 0 Important. **Ship cleared.**

Exact ship ordering (deadlock-free; ruling §5 + plan P3):

1. **Open the PR** (branch → master). The PR touches `.github/workflows/examples.yml` + `.examples-build/**` → the
   internal guard evaluates `relevant=true` → the **full** `examples` gate runs and must be **born green** (verified
   below: the committed golden regenerates byte-identically).
2. **After the first green `examples` run**, apply branch protection to toolkit `master` via `gh api -X PUT
   repos/bg002h/mnemonic-toolkit/branches/master/protection` with the ruling §3 JSON: `required_status_checks =
   {strict:false, contexts:["examples"]}`, `enforce_admins:false`, `required_pull_request_reviews:null`,
   `restrictions:null`, `required_linear_history:false`, `allow_force_pushes:false`, `allow_deletions:false`
   (byte-identical to the live GUI `snapshots` precedent, differing only in the context name).
3. **Merge the PR** → **verify master `examples` run green** → **tag `examples-v1.0.0`** → the tag run attaches the
   committed `docs/Examples.pdf` to the release (`gh release view examples-v1.0.0 --json assets`).

Deadlock-free because `enforce_admins:false` gives the admin author a bypass and preserves the codec/toolkit
`direct-FF + tag` release flow. Nothing tag/merge/protection-only is wrongly pre-baked in the branch: no
`examples-v*` tag exists yet, and branch protection is a post-first-green `gh api` action (not a commit).

---

## 1. Coherence — the 3 phases compose into a correct, shippable whole

The chain **gen.sh (P1) → committed golden `Examples.md` (P2) → `docs/Examples.pdf` (P3) → the `examples.yml`
regen+diff gate** is internally consistent. Reviewer-reproduced:

- **Born-green premise HOLDS.** Built/using `mnemonic 0.75.0` (`~/.cargo/bin/mnemonic`, crate `Cargo.toml`
  `version = "0.75.0"`), `EXAMPLES_BIN_DIR=$HOME/.cargo/bin bash .examples-build/gen.sh > regen.md`, then
  `diff` vs `24bbad3b:.examples-build/Examples.md` → **byte-identical (exit 0)**. The committed golden ==
  what gen.sh produces == the byte-source of the committed PDF's content.
- **Determinism re-proved.** Double-regen byte-identical (gate i); regen under a *different* real `$HOME` +
  `EXAMPLES_BIN_DIR` byte-identical (gate ii) — the D1.3 env-scrub (`unset XDG_DATA_HOME CARGO_INSTALL_ROOT; export
  HOME=/home/user; export LC_ALL=C LANG=C TZ=UTC`) fully decouples output from the author machine. The hardcoded
  `date: "2026-07-05"` literal (never `$(date)`) keeps the gate green across run dates.
- **The committed PDF is the modernized render** (not a stale one): its extracted text carries `v0.75.0`,
  printed-**once** cards, `--help` with `gen-man`/`word-card`, `mnemonic-toolkit-v0.75.0` install pins, and
  `/home/user` paths (0× `/home/bcg`, 0× `/scratch`).

## 2. NO-BUMP / scope

`git diff b8067ebe..24bbad3b --name-only` = **7 files**: `.examples-build/{gen.sh,.gitignore,Examples.md}`,
`.github/workflows/examples.yml`, `docs/Examples.pdf`, `design/FOLLOWUPS.md`, `docs/manual-gui/FOLLOWUPS.md`.
**ZERO `crates/**`, ZERO `src/`, ZERO `Cargo.toml`/`Cargo.lock`** change; no crate version bump; no
flag/subcommand/dropdown surface change (gen.sh only *re-captures* `--help`; `gen-man`+`word-card` already
shipped). → GUI `schema_mirror`, the manual flag-coverage lint, and all sibling-codec locksteps **do not fire**.
Docs/CI/build-script only, as specified. New tag family `examples-v1.0.0` (attach-only; crate not tagged).

## 3. Whole-cycle funds-stability (S1 backstop) — CONFIRMED byte-stable

`pdftotext -layout` on `b8067ebe:docs/Examples.pdf` (215270 B, 0.55.3-era) vs `24bbad3b:docs/Examples.pdf`
(214541 B, modernized). Tokens re-joined across the PDF's `↪` wrap-marker and compared as sets; long
tokens/descriptors compared whitespace-insensitively:

| Funds-critical class | Old | New | Result |
|---|---|---|---|
| xpubs (`xpub…`, ≥80ch) | 30 | 30 | **identical set** |
| addresses (`bc1…`) | 5 | 5 | **identical set** |
| descriptor checksums (`#xxxxxxxx`) | 15 | 15 | **identical set** |
| origin fingerprints (`[xxxxxxxx/`) | 3 | 3 | **identical set** |
| full descriptors (`tr/wsh/…#cs`) | 11 | 11 | **identical** (BCH checksum-verified: a 1-char change flips the checksum; both `#4ld0crxa` and `#jgulue7j` present in both) |
| m-format card payloads (ms1/mk1/md1) | — | 127 | **all 127** canonical payloads extracted from the NEW golden are **byte-present in the OLD PDF** |

Every apparent set-delta reduced to a **presentational artifact**, run to ground and dismissed:
- one descriptor "missing" from the greedy per-line regex → a pdftotext line-break placement artifact; the 160-char
  prefix + 60-char tail + BCH checksum are byte-identical.
- a `qtpl10cosigner` (old) vs `qtpl9cosigner` (new) glue → a **PDF page number** (`10`→`9`, the doc is one page
  shorter after cards went twice→once) glued to the address by whitespace-stripping; the `bc1q…qtpl` address is
  byte-identical at every occurrence.
- card set-diffs → the enumerated **twice→once** (second-copy glue) + **md1 hyphen→space** grouping + page-number
  glue; every differing card shares its full ≥45-char payload core with the other version, and all 127 new-golden
  payloads are exact substrings of the old PDF.

**No address, xpub, descriptor, checksum, or fingerprint changed anywhere.** The only deltas are exactly the spec §3
presentational/version classes: version strings, install pins (`v0.73.3→v0.75.0`), path normalization
(`/home/bcg`,`/scratch` → `/home/user`,`$REPO`), `--help` +`gen-man`/+`word-card`, card printed-twice→once, md1
grouping hyphen→space, and the §6.6 / Appendix-B static-capture restructure. **STOP S1 NOT tripped.** This matches
the FOLLOWUP resolution note's claim ("all 127 … card payloads byte-verified stable") — independently confirmed.

## 4. Gate soundness (end-to-end spot-check; per-phase P2 R0 did the deep dive)

- **Trigger shape correct (ruling §7):** `pull_request:` has **NO `paths:`** filter (line 36 → next non-blank is
  `jobs:`) — so the single job `examples` always runs + reports, safe as a REQUIRED check (no skipped-but-required
  wedge). `push: branches:[master,main]` **is** path-filtered over the wide set (`.examples-build/**`,
  `docs/Examples.pdf`, `scripts/install.sh`, `crates/**`, `Cargo.lock`, `Cargo.toml`,
  `.github/workflows/examples.yml`) — the load-bearing sole gate for direct-to-master pushes. `tags:['examples-v*']`
  (paths never filter tags).
- **Guard fail-safe:** the PR-only guard's grep path set matches the push `paths:` set (incl.
  `.github/workflows/examples.yml`, so the born-green PR self-exercises). 8 heavy steps carry
  `if: github.event_name != 'pull_request' || steps.guard.outputs.relevant == 'true'`. The only route to
  `relevant=false` is a clean fetch+diff with no gated match (genuine docs-only PR); a fetch/diff error hard-fails
  the job under the default `bash -eo pipefail` shell — **never a silent false-green** (P2 R0 empirically reproduced
  exit 128).
- **Born-green + catches-drift:** the gate step `git diff --exit-code -- .examples-build/Examples.md` (line 128).
  Reviewer-reproduced: committed golden matches fresh regen → exit 0; appending one byte → exit 1 (RED); restore →
  exit 0. Scoped to the one golden path, so ignored `preamble.tex`/`work/`/`*.err` can't false-positive.
- **No bitcoind** (the single "bitcoind" token is a comment: "NO bitcoind — §6.6 is a static capture"). **No
  chromium/mermaid.** **Injection-safe:** no `${{ }}` interpolated into any `run:` body; tag steps use quoted
  `$REF_NAME`/`$GH_TOKEN` env; `permissions: contents: write` scoped for the release upload only.

## 5. B-static integrity

- Both **STATIC CAPTURE labels** present in the golden: §6.6 (line 1285, "recorded from Bitcoin Core v27.0") and
  Appendix B (line 1400, "recorded 2026-06-15 from the experimental `mnemonic-depth2` build at v0.55.3 … NOT
  regenerated by gen.sh … not reproducible … in CI").
- **§6.6 freeze faithful:** the LIVE 0.75.0 `restore` first-recv `bc1p550zvnachy40z6hh8llka93mkm0c3635samp264ck6rfd0dcdc8s00n8c8`
  (golden line 1273, reproduced by the reviewer's from-binary regen) **equals** the frozen `deriveaddresses` static
  capture (line 1291) — the "Byte-for-byte the same `bc1p…`" prose is true. (P1 R0 additionally confirmed via an
  independent `bitcoin-cli deriveaddresses` run.)
- **Mainline STILL refuses depth-≥2:** the LIVE mainline refusal `error: taproot tree depth ≥2 (≥3 leaves) is not
  yet restorable …` (golden line 1393) is regenerated by live 0.75.0 on every run — the standing proof holds, and
  a future miniscript bump that silently lifts the cap reds the gate. The frozen depth-2 reconstruction (0.55.3-era
  experimental, not CI-reproducible by design) is loudly labelled and its `bc1p6yc7…`/`#5trrgdg0`/12-cosigner block
  is byte-present in the old PDF (unchanged experimental capture).
- Labels are carried in both the golden and (verified) the rendered PDF.

## 6. FOLLOWUP flips — accurate + lockstep

Both entries flipped to **RESOLVED 2026-07-05 (examples-v1.0.0)** in the P3 shipping commit, cross-citing each
other: toolkit `design/FOLLOWUPS.md::examples-pdf-un-ci-gated` (canonical, with the full resolution narrative) and
companion `docs/manual-gui/FOLLOWUPS.md::examples-pdf-un-ci-gated`. Both carry the **gate-B deferral note** (gate A
catches regen-drift, NOT narration-truth; per-command `.cmd`/`.out` + `include-transcript.lua` parity deferred to
v1.1+). The honesty scope (narration blind spot, committed-PDF byte-staleness convention-sync, sibling-CLI drift,
coverage gaps) is disclosed. Status/tier accurate (NO-BUMP, docs+CI-only).

## 7. Ship-readiness / ordering + M1-from-P2 reconciliation

Ordering above is deadlock-free. **M1-from-P2 confirmed clean:** master advanced 2 commits past the branch base
(`91b761a2`, `9fbfad48`) — the parallel-authored **P1/P2 per-phase R0 report files** only
(`design/agent-reports/examples-pdf-p{1,2}-r0-round-1.md`), which do **not** overlap any file the branch touches;
`git merge-tree` shows **no conflict markers**. Both R0s are GREEN 0C/0I. The branch is 2 commits behind master, but
with `strict:false` protection that does not block the merge, and the merge is conflict-free.

## 8. What's owed at ship

1. Open PR → confirm the first `examples` run is **born green** (validated: golden regens byte-identical; CI's
   from-source **debug** 0.75.0 binary produces byte-identical output — manual.yml precedent + P1/P2 R0 from-source
   regens).
2. Apply branch protection (ruling §3 JSON) **after** first green.
3. Merge → verify master `examples` green → tag `examples-v1.0.0` → verify `docs/Examples.pdf` asset attached (the
   workflow uploads the **committed** PDF — the reviewed artifact — not the freshly-built one).
4. Add the release-ritual line (re-pin gen.sh + regen golden + rebuild PDF every version bump) to
   `project_toolkit_release_ritual_version_sites`.
5. **Q5 housekeeping (non-blocking):** flag the stale `.docbins` tiers (`current`=0.72.0, `gui-pinned`=0.70.0) to
   the user so no future doc cycle regens against 0.72.0. (This cycle bypasses `.docbins` entirely.)

## Findings by severity

- **Critical:** none.
- **Important:** none.
- **Minor / observational (none block ship):**
  - **m1 (forward-reference, by-convention):** the FOLLOWUP status flips to "RESOLVED … via examples-v1.0.0" in the
    P3 commit before the tag is pushed — the standard "flip status in the shipping commit" discipline; the citation
    becomes true when the tag lands (owed-at-ship #3). No action.
  - **m2 (residual, disclosed):** the committed `docs/Examples.pdf` is **not** byte-gated (xelatex non-reproducible)
    — CI re-proves buildability only; PDF↔golden sync is convention (§7c / release ritual). The PDF path is in the
    push triggers so a PDF-only edit at least *runs* the gate. Accepted design.
  - **m3 (residual, disclosed):** gate A does not close the narration blind spot (gate B, v1.1+). Disclosed in both
    FOLLOWUPs + spec §7.

*Repo left clean: reviewer regens + extractions were written to scratch; gen.sh build byproducts
(`work/`, `preamble.tex`, `Examples.pdf`) removed; `git status` shows no tracked-file modification and the golden
byte-matches HEAD.*

*Review by opus architect, session 2026-07-05.*
