# Post-impl R0 — GUI FOLLOWUP-burndown batch (eval #4 + #6) — Fable, adversarial

**Persisted verbatim per CLAUDE.md before folding.** VERDICT: **OPEN (0C/1I + 2 Minor)**. The 3-file diff itself is correct, non-tautological, NO-BUMP-clean; the single Important is the missing sibling-side Companion filings.

**Oracles used (all confirmed at pinned versions):** `mnemonic 0.75.0` built fresh from tag `mnemonic-toolkit-v0.75.0` (`bb86eef7`) in a scratchpad worktree (`.docbins` are stale 0.70.0/0.72.0 — a false oracle, not used); `md 0.11.0`/`ms 0.13.0`/`mk 0.11.0` from `.wt-mp-{md,ms,mk}` release worktrees, each `git describe --exact-match` at the pinned tag.

## Critical
None.

## Important
**I-1 — Companion FOLLOWUP entries NOT filed in any of the three sibling repos (dangling `Companion:` wiring).** `mnemonic-gui/FOLLOWUPS.md`'s new entry cites `Companion: descriptor-mnemonic/design/FOLLOWUPS.md, mnemonic-secret/design/FOLLOWUPS.md, mnemonic-key/design/FOLLOWUPS.md → sibling-gui-schema-v5-default-value-emission`, but grep for that slug returns ZERO hits in all three sibling files (all exist; repos exist). GUI `CLAUDE.md:18` is explicit: mirror an entry in BOTH repos' FOLLOWUPS.md with cross-citing `Companion:` lines; precedent (F6 `gui-dropdown-none-opts0-materialization-audit`) follows it. Concrete failure: the producer-side action (md/ms/mk `gui-schema` v1→v5) lives in the sibling repos, and each sibling's own FOLLOWUPS.md is what its cycles scan — as filed, the producer half is invisible where it matters. Fix is outside the 3-file diff (three small sibling-side entries, filed in lockstep with the GUI PR).

## Minor
**M-1 — FOLLOWUPS.md phrasing: v1 JSON *omits* the `default_value` key; it does not "carry `"default_value": null` on every flag".** Verified live: `md gui-schema` → 0/35 flags carry the key at all (same ms/mk). Behaviorally identical (serde `#[serde(default)]` → None), no gate consequence — but the "R0-verified live" sentence is factually wrong as written.
**M-2 — `tests/schema_mirror_defaults_drift.rs:114` `resolve_bin` claims to mirror `tests/schema_mirror.rs:47` but drops that version's `.replace('-', '_')` env-var derivation.** Moot for md/ms/mk (no hyphens); citation-accuracy nit.

## Verified GREEN (adversarial evidence)
**S1 (#4):** fixture genuinely canonical at pinned 0.75.0 (`gui-schema --classify-descriptor "wpkh(@0[deadbeef/84h/0h/0h]/<0;1>/*)"` → `canonical`, exit 0); trailer arithmetic 18+4+4=26 (22 classify) confirmed; RED-proof is `h?`-only and ISOLATING — dropping `h?` (keeping `'?`) from exactly the 3 SUFFIX groups after `@\d+` (`src/form/conditional.rs:110/112/114`) REDs with the disagreement list containing EXACTLY the new fixture (`gui=NonCanonical toolkit=Canonical`); L12 apostrophe row and all others stay green; counter-check: full `'?h?` drop REDs BOTH the L12 row and the new row — confirming `h?`-only necessary for isolation (SPEC I1). Mutation reverted byte-clean. Blind-spot genuine (`canonicity_classifier.rs` has zero suffix-origin-bracket/h-notation fixtures).
**S2 (#6):** category-(i) real drift INDEPENDENTLY ruled out for all 16 gated flags (md 5/ms 8/mk 3) — mirror `choices` ≡ `gui-schema` choices ≡ `--help` possible-values on every one (incl. `ms combine --to` long-help); all 13 mirror `default_value: Some` entries match `--help [default:]`; total md/ms/mk `--help` defaults = 20; omission set = EXACTLY the 7 filed backfills → day-one is category (iii) as claimed. Choices gate non-tautological, REDs per-CLI (mutating `signetX`/`entropyX`/`changeX` → 5 drift rows attributed across all three CLIs; reverted byte-clean). One-sided guard honestly vacuous AND self-arming — md/ms/mk JSON verified v1 (zero `default_value`); code (`:198-206`) fires only on `Some(Some(_))` JSON side, no reverse assertion; proven self-arming with a shimmed v5-emitting `md` (divergent injected default → RED w/ FOLLOWUP-citing message; agreeing → GREEN). Allowlist 3-tuple `(cli, subcommand, flag)` (`:138`, lookup `:178`). Resolver in `tests/` only. Not skip-forever: `.github/workflows/schema-mirror.yml` installs all four pinned binaries + runs `cargo test --workspace` with `MD_BIN`/`MS_BIN`/`MK_BIN` set.
**Cross-cutting:** `git status --short` = exactly the 3 claimed files; no `src/`, no pin, no schema-source change (NO-BUMP holds); diff is pure additions + one trailer line (no stray fmt). Full `cargo test --workspace` with all four pinned bins: exit 0, ~688 tests, zero failures, no SKIPs of the new gates. `cargo clippy --all-targets -- -D warnings` clean; `cargo clippy --no-default-features -- -D warnings` clean.

## VERDICT: OPEN (0C/1I)
Single Important = missing sibling-side Companion filings (3 mechanical entries in descriptor-mnemonic/mnemonic-secret/mnemonic-key `design/FOLLOWUPS.md`), must land in lockstep with the GUI PR per `mnemonic-gui/CLAUDE.md:18`. Once filed → GREEN with the two Minors optionally folded.

---
**FOLD STATUS (opus, 2026-07-11):** All folded. I-1: the 3 sibling Companion entries filed — `mnemonic-secret/design/FOLLOWUPS.md`, `mnemonic-key/design/FOLLOWUPS.md`, `descriptor-mnemonic/design/FOLLOWUPS.md` (the md entry serialized AFTER the concurrent toolkit-v0.86.0 implementer released that file). M-1 (GUI FOLLOWUPS phrasing → "OMIT the key entirely, 0/35") + M-2 (resolver comment → "Adapted from … omits the `.replace('-','_')` normalization; no-op for md/ms/mk") folded. Scoped convergence R0 re-dispatched.
