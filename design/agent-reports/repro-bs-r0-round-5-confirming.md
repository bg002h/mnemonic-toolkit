# R0 Round 5 (confirming, post-minor-fold) — reproducible-builds brainstorm

**Verdict: C0 / I0 / M2 | GREEN: True**

R0-GREEN (round 5, confirming). All 4 round-4 Minors are folded correctly and completely, every load-bearing external citation re-verified against current origin/master, and the folds introduced NO new Critical or Important and no substantive contradiction. m1 (full three-stanza vendor config + F4-exception-extended-to-all-three + omission-breaks-offline): correctly and consistently stated at header L7, F4 L67, §3.12 L93-97, §4 L115, §8 L206, §10 L226 — all enumerate [source.crates-io] replace-with, [source."git+…rev=95fdd1c5…"] replace-with, and [source.vendored-sources] directory="vendor", all named verbatim/expansion-free so they do NOT reintroduce the C2 hazard; the RUSTFLAGS-via-ENV invariant (remap NEVER in committed config; only the [source] block is the committed-config exception) is preserved everywhere with no contradiction. m2 ([patch.crates-io] not plain git= dep): corrected at the header SHA line L5, §3.12 L97, §4 L115, §8 L206, and the r2-I3 fold-log L22, each adding "resolution unaffected — cargo vendor handles a patched dep identically, same [source] key"; live source CONFIRMS Cargo.toml:28=[patch.crates-io], :29=miniscript git rev, Cargo.lock:700 resolves to the live host — no downstream reference assumes a plain git dep. m3 (man-tarball hygiene-only): §3.13 L104 + header L7 correctly mark mnemonic-man.tar.gz as published-but-NOT-provenance-hashed vs the binary tarball at :133→:135; live source CONFIRMS no sha256sum covers the man tarball (it is release-uploaded at :69 but hashless), only the per-arch binary tarball is hashed — the spec's "not part of the provenance surface" claim holds (note: the man tarball is a release asset via :69, not merely a workflow artifact as the fold-report prose loosely said, but the spec BODY's claim is correct either way). m4 (gzip OS-byte explicit): §3.13 L102-103 + §5.6 L149 correctly state PRIMARY pin = same container-digest constraint as the compressor, SECONDARY = residue assertion OS byte (offset 9, observed 03/Unix) equals pinned value alongside mtime-zero; no stale "OS byte is implementation-fixed" phrasing survives. Two MINOR residues remain (below), neither blocking — fold at plan-authoring time. Spec is sound to proceed to plan-doc authoring + per-phase R0/TDD/whole-diff review.

## Residual minors
- **Minor** — Dangling §1.0 cross-reference in the m2 fold-summary at L7: the parenthetical folded-location list reads `(header/§3.12/§4/§1.0)`, but there is NO §1.0 section — the spec's section-1 anchors are §1 (Problem), §1.1, §1.2, §1.3. The fold-report explicitly acknowledged this ("there is no §1.0 section ... No §1.0 mischaracterization exists to fix") and chose to leave it, but the result is that L7 still literally points at a non-existent §1.0 as a fold target. The actual m2 content landed correctly in the existing sections (header L5, §3.12 L97, §4 L115), so this is a broken pointer in a summary parenthetical only — no provenance/funds claim is affected.
  - fix: In L7's m2 minor-summary, replace `(header/§3.12/§4/§1.0)` with `(header/§3.12/§4)` — or, if a §1-block pointer was intended, `(header/§1.1/§3.12/§4)` (§1.1 is the PROVEN-facts block the fold-report mentioned as the closest real anchor). Mechanical one-token edit; fold at plan-authoring time.
- **Minor** — Singular "stanza" shorthand survives at L162 (§6 P1 bullet): "If a repo keeps a `.cargo/config.toml`, it carries ONLY verbatim-safe profile flags + the ONE admitted `[source]`-replacement vendoring stanza (R0-r3-I2)". The m1 fold canonically redefined this as the FULL THREE-stanza block everywhere else (header L7, F4 L67, §3.12 L97, §4 L115, §3 L76, §10 L226). L162 was NOT in the fold-report's enumerated edit list and retains the pre-fold singular noun. This is loose back-reference shorthand pointing at the same [source] mechanism, not a substantive contradiction (the three-stanza canonical is stated unambiguously in five other places), so it does not mislead an implementer — but it is an internal-consistency residue the m1 fold left behind.
  - fix: At L162, change "the ONE admitted `[source]`-replacement vendoring stanza" to "the ONE admitted FULL three-stanza `cargo vendor` `[source]` block" to match the canonical phrasing used at L67/L76/L97/L115/L226. Mechanical; fold at plan-authoring time.
