# R0 Review — older() timelock mask gate — ROUND 3

**Source SHA:** `a7c1920` (verified == local HEAD) · **SPEC:** `design/SPEC_older_timelock_mask_gate.md` (folded, round 2 → 3) · **Round-2:** `design/agent-reports/older-timelock-mask-gate-r0-round2-review.md`
**Verdict:** 🟢 — **0 Critical / 0 Important / 0 Minor**

- **I-A: RESOLVED.** SPEC:63 now mandates honesty about the deferred window: *"The `--older` flag prose (`:3993`) sentence MUST be honest about the deferred M1 window (R0-r2 I-A): do NOT claim a blanket '>65535 rejected' — that is false for the accepted `0x400001..=0x40FFFF` 512-second-unit window. Write instead: 'interpreted as **blocks**; malformed BIP-68 encodings are rejected, but valid 512-second-unit encodings (`0x400001`–`0x40FFFF`) are currently accepted and reinterpreted as time units (tracked: `archetype-older-blocks-flag-accepts-time-units`)' — or drop the `--older`-row addition entirely and let the `:4009` domain line carry accuracy."* Exactly the directed fix, both options preserved; the false blanket-rejection claim is gone.
- **M-A: RESOLVED.** SPEC:53 now carries the asymmetry parenthetical: *"**RED-proof asymmetry (R0-r2 M-A):** the first three produce NO field diag before the fix (their RED is diag-existence). `older(0x80000090)` is ALREADY rejected pre-fix by the current `n >= 2^31` check — its RED component is the **wording assertion only** … the scratch-revert RED check must read the `0x80000090` cell's RED as the wording mismatch, not diag-absence."* Correct, and it additionally pins which wording each cell asserts (bit-31 no-op vs "effective value") — both PART A branches stay exercised.
- **No new drift.** Reject predicate at SPEC:19 unchanged (`(*n & !0x0040_FFFFu32) != 0 || (*n & 0x0000_FFFFu32) == 0`); edge table unchanged; PATCH v0.53.9 + no-lockstep + CHANGELOG/3-self-pin-sites unchanged (SPEC:60-62); the `:4009` domain-line instruction unchanged and consistent with the new `:3993` instruction (both describe the same masked domain); the new `:63` window text matches the M1 FOLLOWUP text at `:67` (`0x400001..=0x40FFFF`, tracked under the same slug); test cells, fixture discipline, and non-goals untouched. The two folds touched only the two directed sentences.

**GREEN — gate passed at 0C/0I. The SPEC is ready for implementation** (TDD RED-first per §Tests, then PART A/B, manual edits per :63, ritual per :65-70).
