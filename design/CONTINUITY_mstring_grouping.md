# CONTINUITY — mstring display-grouping cycle (resume guide)

**Last updated:** 2026-06-15. **Toolkit branch:** `feature/mstring-display-grouping` @ `0281886` (clean). Siblings unchanged: descriptor-mnemonic `main@eb9f368`, mnemonic-secret `master@b616530`, mnemonic-key `main@21786dc`, mnemonic-gui `master@c5e3434`.

## What this cycle is
Standardize mstring (`ms1`/`mk1`/`md1`) display output across all four CLIs (`mnemonic`/`md`/`ms`/`mk`): a uniform `--group-size <u16>` (default 5, `0`=unbroken) + `--separator` (set `{space,hyphen,comma}`, keyword+literal, default `space`); **print-once**, default **space/5 single line**; `--json` + verify-bundle forensics stay **unbroken**; `repair` output stays unbroken (no flags). Intake strips ALL whitespace + `-` + `,` so grouped/unbroken/any-separator forms re-ingest. `ms split`→shares one-per-line (labels→stderr); **`ms combine` gains `-`→stdin**. Drift control = **copy-with-checksum conformance vectors** (canonical TSV in toolkit; sibling copies + `.sha256`; CI `sha256sum -c`) — NO new crate. SemVer **MINOR per crate**. "chunk"=wire-splitting (reserved); "group"=this feature.

## Authoritative docs (read these first on resume)
- Spec (R0 GREEN ×3): `design/SPEC_mstring_display_grouping.md`
- P0 plan (plan-R0 GREEN): `design/IMPLEMENTATION_PLAN_mstring_grouping_p0_foundation.md`
- P1 plan (plan-R0 r1 folded, **re-dispatch pending**): `design/IMPLEMENTATION_PLAN_mstring_grouping_p1_md.md` — note its top **"R0-r1 corrections (MUST APPLY)"** block.
- Reviews (verbatim): `design/agent-reports/mstring-display-grouping-{r0-round1,2,3, plan-r0-p0-round1,2, plan-r0-p1-round1}-review.md`
- Memory: `project_mstring_display_grouping_cycle.md`

## Done
- **P0 foundation SHIPPED** on the toolkit branch (reversible; no release): `design/display-grouping-vectors.tsv` (22 vectors); `crates/mnemonic-toolkit/src/display_grouping.rs` (`render_grouped`/`strip_display_separators`/`is_display_separator` + 10 unit tests; `pub mod display_grouping;` added to `lib.rs` UNCONDITIONALLY — NOT format.rs, which is `#[cfg(fuzzing)]`-gated); `crates/mnemonic-toolkit/tests/display_grouping_conformance.rs`. Full toolkit suite green; fmt-clean (only mlock.rs diffs, g6-exempt).
- **P1 plan-R0 GREEN** (round 3, 0C/0I): reviews `…plan-r0-p1-round{1,2,3}-review.md`. r2 caught a Task-3 `git add` omission (smoke.rs+cli_repair.rs); folded.
- **P1 SHIPPED + RELEASED (2026-06-15).** `descriptor-mnemonic` `main@a3f9d8f` (ff-merged from `feature/mstring-display-grouping`, pushed). **md-codec 0.36.0 + md-cli 0.7.0 PUBLISHED to crates.io** (md-codec first; md-cli pins `=0.36.0`). Tags `md-codec-v0.36.0` + `descriptor-mnemonic-md-cli-v0.7.0` pushed. All local gates green (`test --workspace`+`--all-features`+`--doc`+`clippy --all-targets -D warnings`+`fmt --all --check`); CI on main green (bitcoind-differential ✓; CI + fuzz-smoke confirm). What shipped: md-codec `render_grouped`/`strip_display_separators`/`is_display_separator` (+ `render_codex32_grouped` now a hyphen wrapper); copy-with-checksum vectors (`design/display-grouping-vectors.tsv`+`.sha256`, CI `sha256sum -c` in fmt job) + `crates/md-codec/tests/display_grouping_conformance.rs`; `cmd::strip_md1_inputs` + intake strip on ALL six md1 surfaces (decode/bytecode/verify/inspect/address[in `build_descriptor`]/repair[stdin+positional]); `md encode --group-size`/`--separator` (default space/5 print-once; `--json`+`repair` unbroken; smoke.rs/after_long_help/cli_repair `encode_chunked` got `--group-size 0`); FOLLOWUP `display-grouping-render-strip-v1`. **NOTE: md-codec's codex32 decode ALREADY tolerates whitespace+hyphen (codex32.rs:134 "D11"); net-new md-cli strip coverage = comma + the repair decode path → conformance tests use COMMA-grouped fixtures to genuinely exercise the strip.** RELEASE MECHANICS THAT WORKED: workspace miniscript = crates.io 13.0.0 (no fork patch in manifests → md-codec publishes cleanly; CI's fork clone is vestigial for the published crate); md publish ritual = ff-merge branch→main + push, `cargo publish -p md-codec` then `-p md-cli` (dry-run each), then push both tags. No GH Release / no tag-triggered workflow (only ci/bitcoind-differential/fuzz-smoke run on push).

- **P2 SHIPPED + RELEASED (2026-06-15).** `mnemonic-secret` `master@3e23f59` (ff-merged + pushed); **ms-cli 0.8.0 PUBLISHED to crates.io** (ms-codec UNTOUCHED at 0.4.4 — fns kept ms-cli-local since ms-cli is bin-only; conformance = bin-crate `#[cfg(test)]`); tag `ms-cli-v0.8.0` pushed; CI on master green (rust workflow + fuzz-smoke). plan-R0×2→GREEN (r1=0C/4I, all missing breaking tests — `encode_no_engraving_card`/`encode_hex_input`/4×`encode_mnem_japanese` length tests/`cli_split`; reviews `…plan-r0-p2-round{1,2}-review.md`). 8 commits `55c92f6..3e23f59`. **KEY P2 SPECIFICS: EMIT-first/INTAKE-second ordering (OPPOSITE of P1 — removing the doubling-dedup heuristic while encode prints twice would break `encode|decode`); ms-codec decode does NOT tolerate separators (no md-style D11) → legacy `strip_whitespace` handled whitespace only → net-new strip = `-`/`,` → intake tests use COMMA fixtures; spec §13 note WRONG (24-word `starts_with` DOES break under grouping); `--separator bogus` exit = 64 (ms maps clap errors to 64, not 2); NO fmt CI gate in ms repo (clippy -D warnings + g6 mlock byte-equality are the gates); ms-cli IS a crates.io crate (has `documentation = docs.rs/ms-cli`).**

- **P3 SHIPPED + RELEASED (2026-06-15).** `mnemonic-key` `main@714905a` (ff-merged + pushed); **mk-cli 0.9.0 PUBLISHED to crates.io** (mk-codec UNTOUCHED at 0.4.0 — fns mk-cli-local, bin-only; conformance = bin-crate `#[cfg(test)]`); tag `mk-cli-v0.9.0` pushed; CI on main green. plan-R0×2→GREEN (r1=0C/1I — `cli_slip132.rs::run_encode_decode` missing breaking test, CLI `mk encode`→`mk_codec::decode` bypasses intake; reviews `…plan-r0-p3-round{1,2}-review.md`). 8 commits `7e1bd65..714905a`. **KEY P3 SPECIFICS: INTAKE-first ordering (like P1); mk encode was UNBROKEN→now space/5 (corrective); `read_mk1_strings` was edge-only `.trim()`→now strips ALL ws+`-`+`,` (interior), covering all 6 subcommands (decode/inspect/verify/repair/derive/address) + `-`→stdin; mk-codec tolerates NO separators → comma intake fixtures; NO mlock in mk-cli (only fmt-1.95.0 gate — run `cargo +1.95.0 fmt --all`); `--separator bogus`→exit 64; 2 breaking tests (round_trip::from_md1_derivation + cli_slip132::run_encode_decode) both pipe CLI mk encode→mk_codec::decode → need `--group-size 0`; mk-cli IS crates.io (documentation=docs.rs/mk-cli).**

- **P4 IN PROGRESS (Task 1+2 of 7 DONE; toolkit branch, NOT released).** plan-R0 GREEN ×3 (r1=4C/4I, r2=2C/2I — ms-shares coupling, convert exact-pins, ms1/mk1/md1 filter variants, verify_bundle equality strip; reviews `…plan-r0-p4-round{1,2,3}-review.md` — the r2 review has EXHAUSTIVE file:line lists). **Task 1** = `display_grouping::parse_separator` (`3bcdc02`). **Task 2** (`e12d519`, atomic 25 files) = bundle `--group-size`/`--separator` print-once + collapse `chunk_5char`/`chunk_mk1`/`chunk_md1` → `display_grouping::render_grouped` (also added `mod display_grouping;` to main.rs — it was lib-only from P0; toolkit is dual-homed lib+bin). **KEY DEVIATION (lower-risk, plan-R0-approved goal):** regenerated the 16 v0_1 + 1 v0_2 output-goldens UNBROKEN (`--group-size 0`) + added `--group-size 0` to their ~6 `assert_eq` bundle invocations → the ~12 input-fixture parsers (`!contains(' ')` filters) WORK UNCHANGED (they parse the unbroken card lines), AVOIDING the filter surgery the r2 review flagged as the top risk. New `bundle_default_text_is_space_grouped_print_once` test covers the default grouped output + `--separator`. cli_bundle_language_advisory length checks got `--group-size 0`. **FULL toolkit suite GREEN (168/168 binaries; mlock g4_a is a pre-existing ulimit-l env-flake, passed this run).**
  - **REMAINING P4 (Tasks 3-7, all R0-GREEN-mapped):** **Task 3** convert `--to ms1/mk1` grouping (NODE-TYPE-GATED — `convert.rs:1119` emits `{node}: {value}` for ALL node types; group ONLY when node∈{Ms1,Mk1}, NOT xpub/wif/descriptor) + ms-shares `run_split` text-branch grouping (`ms_shares.rs:298`, NOT before json `:283`) + ms-shares **combine** intake-strip (ATOMIC with split per r1-C1) + fix the 5 convert/bundle exact-pin/len tests (`cli_convert_happy_paths.rs:154`+`:326`, `cli_convert_round_trips.rs:69`, `cli_mnem_emit_preserve.rs:409`, `cli_convert_language_advisory.rs:150` — same `--group-size 0`/strip pattern). **Task 4** intake strip 5 sites: `slot_ms1.rs` ms1 decode, `verify_bundle.rs` — strip WHOLE `args.ms1`/`mk1`/`md1` vecs at run-fn TOP (so the `:1407` `supplied_ms1==expected_ms1` equality sees stripped; NOT `--bundle-json`), `repair.rs` resolve-groups, `convert.rs --from mk1=` strip BEFORE `value.split_whitespace()` (`:1560`) + `--from ms1=`. **Task 5** `cargo +1.95.0 fmt --all` THEN `git checkout crates/mnemonic-toolkit/src/mlock.rs` (g6 exempt) + clippy `-D warnings` + `cd fuzz && cargo build`. **Task 6** manuals (`docs/manual/src/40-cli-reference` common-flags + lint; `docs/technical-manual` remove `chunk_5char`/`chunk_md1` rows from `54-…api.md`, add `render_grouped`) + file toolkit-side `display-grouping-render-strip-v1` FOLLOWUP. **Task 7** bump 0.55.3→0.56.0 (MINOR) + md-codec pin "0.35"→"0.36" + lockstep (README:13, install.sh:32, manual.yml+quickstart.yml mk-cli `v0.8.0`→`v0.9.0`, `.examples-build/gen.sh` Nx version) + `grep render_codex32_grouped crates/` (confirm no display caller) + FULL re-verify + ff-merge master + git-tag `mnemonic-toolkit-v0.56.0` (NOT crates.io). Then **P5** (GUI schema_mirror).

## RESUME HERE — exact next actions
0. **P4 RESUME:** continue at Task 3 (above). Plan: `design/IMPLEMENTATION_PLAN_mstring_grouping_p4_toolkit.md` + the r2 review's exhaustive lists. Toolkit branch HEAD `e12d519`, suite green.
1. **P1 + P2 + P3 DONE (all 3 sibling CLIs released).** P4 = **integration crate, Task 1+2 done (above), Tasks 3-7 remain**. Scope: pin-bump siblings' libs (md-codec 0.36.0 — NOTE the toolkit consumes ms-codec/mk-codec/md-codec LIBS not the CLIs, and ms-codec/mk-codec were UNTOUCHED this cycle, only md-codec bumped → verify which pins actually move); collapse `format.rs` to ONE local `render_grouped` + DELETE `chunk_5char`/`chunk_mk1`/`chunk_md1` (P0 already added `display_grouping.rs` with the fns); add flags to `bundle`/`verify-bundle`/`convert`/`ms-shares` (NOT `repair`); intake strip sites; regen 20 goldens `tests/vectors/v0_1`+`v0_2`; update `docs/manual` (4 CLIs common-flags) + `docs/technical-manual` (remove dead `chunk_*` rows, add `render_grouped`); regen `docs/Examples.pdf`; **file the toolkit-side `display-grouping-render-strip-v1` companion** (P1/P2/P3 siblings already filed theirs pointing here). Then **P5** (mnemonic-gui schema_mirror flags + separator keyword dropdown, paired PR). Each: own plan → plan-R0 GREEN → TDD → release (autonomous — authorized).
2. **Toolkit release-ritual reminders (from v0.55.x):** rustfmt-1.95.0 gate + mlock.rs g6-exempt (NEVER fmt mlock.rs; run `cargo +1.95.0 fmt --all` THEN revert mlock.rs); fuzz crate = separate workspace (re-run fuzz build after version bump); README version-markers + scripts/install.sh self-pin + manual.yml/quickstart.yml sibling-pin locksteps; rust workflow doesn't run on tags (re-run full suite BEFORE tag). Toolkit ships git-tag (master+tag), historically NOT crates.io for the toolkit bin.

## Hard constraints / gotchas (do not relearn the hard way)
- **R0 gate is mandatory** at every level (spec, each plan, each phase): loop architect to 0C/0I, persist verbatim to `design/agent-reports/` BEFORE folding. It has caught real bugs in EVERY round this cycle.
- **NEVER `cargo fmt` mlock.rs** (g6 permanent exemption). Toolkit fmt gate = `cargo +1.95.0 fmt --all -- --check`; tolerate ONLY mlock.rs diffs. md/ms/mk fmt on stable (no exemption). Run the pinned fmt before any toolkit push.
- **md-cli is bin-only** (no lib.rs) → both pure fns live in **md-codec** (lib) for conformance-test reachability. Same `--lib`-runs-zero-tests trap applies in toolkit (use a real lib module; `--lib` only runs lib tests).
- **md-codec keeps `render_codex32_grouped`** as a thin wrapper (`render_grouped(s,n,'-')`) — public API + technical-manual entry; do not rename/remove.
- **Six md1-intake surfaces** in md-cli: decode/bytecode/verify/inspect/address/repair (shared `strip_md1_inputs`); address strips inside `build_descriptor`.
- **Release = outward-facing**: confirm with user before any tag/publish (per phase). Stage paths explicitly (no `git add -A`).
- Commit trailer: `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`.
- Lockstep (P4/P5): GUI `schema_mirror` (flags + separator keyword dropdown, paired PR), toolkit `docs/manual/src/40-cli-reference` (all 4 CLIs, common-flags section), sibling `FOLLOWUPS.md` companions (`display-grouping-render-strip-v1`).

## Kickoff prompt for the next session
> Resume the mstring display-grouping cycle. Read `design/CONTINUITY_mstring_grouping.md` (and the spec + P1 plan it points to), then continue P1: re-dispatch the P1 plan-R0 to GREEN (0C/0I, persist verbatim), then execute P1 inline in descriptor-mnemonic per the plan's corrections block, pausing for my authorization before the md-codec/md-cli tag+publish.
