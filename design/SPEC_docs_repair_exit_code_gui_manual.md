# SPEC — docs sync: GUI-manual repair exit codes (FOLLOWUP `gui-manual-repair-exit-code-lockstep`)

**Repo/book:** `docs/manual-gui/` (inside mnemonic-toolkit). Recon: `design/RECON_minors_docs_tail.md` §"Docs item". **NO-BUMP, docs-only.**
**Origin:** FOLLOWUP `gui-manual-repair-exit-code-lockstep` (FOLLOWUPS.md:71-73). The GUI-manual repair chapters carry a blanket "exit-5 `REPAIR_APPLIED` uniform across all four CLIs" claim. **R0 I1/I-NEW-1 — the manual-gui pins a whole constellation TIER** (`docs/manual-gui/pinned-upstream.toml`, via `mnemonic-gui-v0.57.0`/`v0.58.0`; even v0.58.0 pins toolkit-v0.75.0 + ms-cli-v0.13.0 + mk-cli-v0.11.0 + md-cli-v0.11.0 — ALL pre-demote; NO newer GUI tag has exit-4 semantics), so the blanket claim is TRUE for the pinned tier but becomes FALSE once each CLI demotes. Fix = **version-scoped, PER-BINARY prose** (option a): each chapter documents a SPECIFIC binary → scope to THAT binary's demote version + THAT binary's pin (per-binary mapping below), NOT a single toolkit version. Keep the pinned-tier accuracy + note the demotion; drop the fragile blanket claim (strictly-improving, NO-BUMP).

## Ground-truth exit-code table (DEFAULT substitution/ECC path, `--max-indel 0`) — R0 I2
| Surface | Substitution-correction outcome | Exit (current, latest per-CLI) | Exit (this manual's pinned tier) |
|---|---|---|---|
| `ms repair` / `mnemonic repair --ms1` | ANY subst correction (no set-level self-verify → VERIFY-ME candidate) | **4** | 5 |
| `mk repair` (standalone) | ANY correction | **5** | 5 |
| `mnemonic repair --mk1` | Bless (full-set decode-Ok) | **5** | 5 |
| `mnemonic repair --mk1` | Unverified candidate (incomplete/single-plate) | **4** | 5 |
| `md repair` / `mnemonic repair --md1` | ANY correction (sibling delegate returns Ok only on full decode success — `repair.rs:451-453`) | **5** | 5 |
| all | no correction | **0** | 0 |

**Indel-path tier (R0 I2 — toolkit `--max-indel` only; matters for 4i's worked example):** ms1 UNIQUE-indel recovery = exit **5** (indel re-validates the FULL checksum — a genuine self-verify, NOT a candidate; test `cli_ms1_repair_demote.rs:306`); mk1/md1 indel that consumed a substitution = exit **4** (`substitution_seen`, `repair.rs:1491`). So "ms1 ANY→4" is wrong for the indel case — scope any 4i row accordingly.
**mk asymmetry (R0 M3 — DELIBERATE, not a wart):** standalone `mk repair` = 5 for the unverified-candidate case while `mnemonic repair --mk1` = 4 — a designed Cycle-E difference (mk1 is watch-only; the toolkit surfaces the VERIFY-ME candidate tier, the standalone mk binary emits a stderr advisory + exit 5). Document as designed, cite Cycle E; no behavioral-alignment FOLLOWUP.
**Per-binary version mapping (R0 I-NEW-1 — each chapter documents ONE binary; the demote ships + is pinned PER-CLI, not per-toolkit):** `6a`/`ms repair` — ms1 demote ships **ms-cli v0.14.0**; book pins **ms-cli-v0.13.0** (`pinned-upstream.toml:87`, pre → exit 5). `4i`/`mnemonic repair` — toolkit demote ships **v0.80/v0.81**; pins **toolkit-v0.75.0** (pre → 5). `79`/`mk repair` — the standalone advisory ships **mk-cli v0.12.0**; pins **mk-cli-v0.11.0** (pre → exit 5, no advisory). `5A`/`md` — always exit 5 (no demote). Each chapter's prose cites ITS binary's version + tag (from `pinned-upstream.toml:85-88`), NOT toolkit's.

## Fix (docs-only) — **4 files** (R0 C1: `4i-repair.md` IS affected)
Adapt the already-shipped `docs/manual/src/40-cli-reference/43-ms.md:354-383` model prose (verbatim source: the "verified now / verifiable-by-reassembly later" framing + the indel-vs-subst nuance; the "VERIFY-ME Candidate" naming at :356-357 + the `blessed`/`candidate` `--json` verdict pair at :417 — R0 M1/M-NEW-1) into each chapter, replacing the blanket-uniformity claim with the **version-scoped, PER-BINARY** per-surface model:
- `docs/manual-gui/src/60-ms/6a-repair.md:47,:51-53,:75` — the `|5| REPAIR_APPLIED|` row + the "uniform across all four CLIs" sentence(s); `:75` worked-example "exit code `5`" for the ms1 subst repair → **4** (+ the `repair: correction UNVERIFIED` stderr advisory) as of **ms-cli v0.14.0**, with the note that THIS manual pins **ms-cli-v0.13.0** (pre-demote → exit 5). Do NOT cite toolkit versions here — `6a` documents the standalone `ms` binary.
- `docs/manual-gui/src/50-md/5A-repair.md:11-12` (intro claim — R0 I3, MISSED before) + `:63,:67-68` — md1 IS exit 5 (correct), but drop the "consistent across all four CLIs" blanket → the per-surface + version-scoped model.
- `docs/manual-gui/src/70-mk/79-repair.md:55,:59-61` — mk1 5(bless)/4(candidate) via `mnemonic repair --mk1` (toolkit) + standalone `mk repair` always 5 (the designed asymmetry above). If the rewrite quotes the standalone advisory, note it ships **mk-cli v0.12.0** (the pinned tier mk-cli-v0.11.0 predates it → exit 5, no advisory) and its wording differs from ms/toolkit ("…reassemble the full card (`mk decode`) to confirm…", `mk-cli cmd/repair.rs:141-143` — R0 M-NEW-2).
- **`docs/manual-gui/src/40-mnemonic/4i-repair.md:96-97,:100-124` (R0 C1 — NOT already-correct):** `:96` `|5| … incl. unique --max-indel |` is false for `--ms1` subst (→4) + `--mk1` partial (→4); `:97` exit-4 row omits the candidate tier; the `:100-124` worked example (`mnemonic repair --ms1` one subst at `--max-indel 0`) asserts "exit code `5`" (`:123-124`, line-broken) → live is exit **4** + the UNVERIFIED advisory. Rewrite the `:96-97` rows (fold the candidate tier into exit-4; KEEP the unique-indel→5 line at `:79/:85-87` which IS correct) + fix the `:123-124` assertion to 4 + the advisory + the version note.

## Acceptance
1. No blanket "exit-5 uniform/consistent across all four CLIs" claim remains in ANY of the 4 repair chapters (grep "uniform"/"consistent"); AND grep worked-example exit assertions (`` code `5` ``) — none may assert exit 5 for an ms1 substitution repair (must be 4 at each surface's OWN demote tier — 6a/standalone-`ms`: ms-cli v0.14.0, pinned ms-cli-v0.13.0 note; 4i/toolkit: v0.80/81, pinned v0.75.0 note; NEVER a toolkit-version citation in the standalone-`ms` 6a chapter — R0 I-R3-1).
2. Each chapter's exit-code table matches the version-scoped ground-truth table (current + pinned-tier columns) incl. the indel tier where a chapter documents `--max-indel` (4i).
3. The mk-standalone-vs-toolkit 4/5 asymmetry documented as a DESIGNED Cycle-E difference (mk1 watch-only), not a wart. md1 parenthetical uses "sibling delegate returns Ok only on full decode success" (R0 M2), NOT "chunk-set-hash".
4. Version-scoping present + PER-BINARY (R0 I-NEW-1): each chapter cites ITS binary's demote version + pinned tag (6a: ms1 demote @ ms-cli v0.14.0, pinned ms-cli-v0.13.0→5; 4i: toolkit demote @ v0.80/81, pinned v0.75.0→5; 79: mk advisory @ mk-cli v0.12.0, pinned v0.11.0→5; 5A md always 5) per `pinned-upstream.toml:85-88` — NOT a single toolkit version — so a reader on either tier isn't surprised.
5. `make -C docs/manual-gui html` + `make lint` pass (mermaid figures-cache, markdownlint, lychee, gui-schema-coverage — **preserve the `{#…-repair-…}` anchor IDs**); NO-BUMP (no code/schema/`docs/manual` non-gui change — 43-ms.md already correct). verify-examples/tutorial unaffected (zero repair transcripts gated).
6. Per-phase + post-impl R0 (docs prose-accuracy vs live exit codes + the version-scoping).

## Phasing
Single docs implementer; **4 files** (6a, 5A, 79, 4i). Independent of the M2/M3/M4 toolkit release. Per CLAUDE.md R0 gate; the R0 verifies documented exit codes vs live source + the pinned-tier framing. FOLLOWUP `gui-manual-repair-exit-code-lockstep` flips RESOLVED in the shipping commit (note the version-scoped resolution + the `4i`/`5A:11-12` additions the original entry missed).
